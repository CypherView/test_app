import {
  Toolbar,
  ToolbarModule
} from "./chunk-3D4PWSWN.js";
import {
  BreakpointObserver,
  Breakpoints
} from "./chunk-NVZPBD6C.js";
import {
  Drawer,
  DrawerModule
} from "./chunk-GQ7U7Y4R.js";
import {
  BulkSelectionService,
  GridConfigurationFactory,
  TreeTableConfigurationFactory
} from "./chunk-LXUP4XTY.js";
import {
  AdaptiveLayoutComponent
} from "./chunk-3XC7NKQU.js";
import {
  EntityDetailsService
} from "./chunk-CLLRPPPW.js";
import "./chunk-BSL4DLZ3.js";
import "./chunk-W3FU4YKA.js";
import {
  ENTITY_ADAPTER_REGISTRY
} from "./chunk-J3RAL2WO.js";
import {
  VersionUpdateService
} from "./chunk-GXVWRHN6.js";
import {
  AdminNavigationService,
  AlertDetailsSectionComponent,
  AlertIconStorageService,
  AlertsDataAdapter,
  AssetCategoriesDataAdapter,
  AssetCategoriesSectionComponent,
  AssetCategoryDetailsSectionComponent,
  AssetDetailsSectionComponent,
  AssetGroupDetailsSectionComponent,
  AssetGroupsDataAdapter,
  AssetRatingProfileDetailsSectionComponent,
  AssetRatingProfilesDataAdapter,
  AssetStateProfileDetailsSectionComponent,
  AssetStateProfilesDataAdapter,
  AssetTagDetailsSectionComponent,
  AssetTagsDataAdapter,
  AssetTreeBuilder,
  AssetsDataAdapter,
  CacheInvalidationService,
  ClientsDataAdapter,
  CompanyGroupDetailsSectionComponent,
  CompanyGroupsDataAdapter,
  ContactCreateWizardComponent,
  ContactsDataAdapter,
  CostCentreDetailsSectionComponent,
  CostCentresDataAdapter,
  CustomFieldDetailsSectionComponent,
  CustomFieldsDataAdapter,
  DetailsComponentFactory,
  DeviceConfigProfileDetailsSectionComponent,
  DeviceConfigProfilesDataAdapter,
  DeviceDetailsSectionComponent,
  DeviceProviderDetailsSectionComponent,
  DeviceProviderParametersSectionComponent,
  DeviceProvidersDataAdapter,
  DeviceTypesDataAdapter,
  DevicesDataAdapter,
  DistributorsDataAdapter,
  EmailProviderDetailsSectionComponent,
  EmailProviderParametersSectionComponent,
  EmailProvidersDataAdapter,
  EntityDetailsStateService,
  ExportTaskDetailsSectionComponent,
  ExportTaskSettingsSectionComponent,
  ExportTasksDataAdapter,
  GeoLockProfileDetailsSectionComponent,
  GeoLockProfilesDataAdapter,
  GeofenceDetailsSectionComponent,
  GeofenceGroupDetailsSectionComponent,
  GeofenceGroupsDataAdapter,
  GeofencesDataAdapter,
  InfoSectionComponent,
  IoTypeDetailsSectionComponent,
  IoTypesDataAdapter,
  MapSetDetailsSectionComponent,
  MapSetsDataAdapter,
  OverspeedProfileDetailsSectionComponent,
  OverspeedProfilesDataAdapter,
  RoadProfileDetailsSectionComponent,
  RoadProfilesDataAdapter,
  SimCardDetailsSectionComponent,
  SimCardsDataAdapter,
  SmsGatewayProviderDetailsSectionComponent,
  SmsGatewayProviderParametersSectionComponent,
  SmsGatewayProvidersDataAdapter,
  ThemeDetailsSectionComponent,
  ThemesDataAdapter,
  UserDetailsSectionComponent,
  UserRoleDetailsSectionComponent,
  UserRolesDataAdapter,
  UserRolesSectionComponent,
  UsersDataAdapter,
  VendorsDataAdapter,
  isAssetSharedIn
} from "./chunk-HAEK3MNB.js";
import {
  WizardComponent
} from "./chunk-NOYOHX5Z.js";
import "./chunk-DPIEMUOA.js";
import "./chunk-3PDKK7YX.js";
import "./chunk-227XKAZG.js";
import {
  ClientDetailsComponent,
  DeviceTypesInformationSectionComponent,
  DistributorDetailsComponent,
  FaIconComponent,
  FaIconLibrary,
  FontAwesomeModule,
  TypedEntitySectionBaseComponent,
  VendorDetailsComponent,
  faAngleLeft,
  faAngleRight,
  faBuildingUser,
  faCubes,
  faGauge,
  faMicrochip,
  faRoad,
  faSitemap,
  faSquareBinary,
  faUserSecret
} from "./chunk-LWZPKT57.js";
import "./chunk-AB3PBDIK.js";
import {
  LanguageService
} from "./chunk-7UON6CU4.js";
import "./chunk-UIOPXNY5.js";
import "./chunk-WLMUTOYA.js";
import {
  CardModule
} from "./chunk-HEDH66RH.js";
import "./chunk-RAHCTZWE.js";
import "./chunk-EDNONQMY.js";
import "./chunk-SV3SMBHH.js";
import {
  ConfirmDialog,
  ConfirmDialogModule
} from "./chunk-5ZKUO7BH.js";
import {
  GridComponent
} from "./chunk-VJ76VAAI.js";
import "./chunk-T2NLXXYI.js";
import "./chunk-DXDIX3ID.js";
import "./chunk-S4WNLXX2.js";
import {
  ApiSchemaService,
  DynamicFormComponent,
  Tree,
  TreeModule
} from "./chunk-3L22U4Q2.js";
import "./chunk-EVW4PTKX.js";
import {
  ADMIN_TREE,
  AssetGroupingService,
  Popover,
  PopoverModule,
  UtilityService
} from "./chunk-S5OZ6G2W.js";
import "./chunk-NT6TC2O6.js";
import {
  Chip,
  ChipModule
} from "./chunk-LHPCQEY3.js";
import "./chunk-2YBWOZSJ.js";
import "./chunk-BTNVOKO5.js";
import "./chunk-FYHRO7AA.js";
import {
  ProgressSpinner,
  ProgressSpinnerModule
} from "./chunk-6ZLKOSJY.js";
import "./chunk-47TCRRSK.js";
import "./chunk-5WWRPFVI.js";
import "./chunk-6RKFIQXA.js";
import {
  DialogService,
  DynamicDialogConfig,
  DynamicDialogRef
} from "./chunk-N3IYAUEM.js";
import "./chunk-6RNH5RNU.js";
import {
  IconField,
  InputIcon,
  InputText,
  InputTextModule,
  ScrollerModule
} from "./chunk-HAP3H3Y2.js";
import "./chunk-JE2L2HEP.js";
import "./chunk-KMNNT5Y5.js";
import "./chunk-437DZVOB.js";
import "./chunk-ZD2L3BUO.js";
import "./chunk-HUGEA54Y.js";
import "./chunk-233GVLOY.js";
import {
  FormsModule,
  ReactiveFormsModule,
  Tooltip,
  TooltipModule,
  Validators
} from "./chunk-7YJUDWRM.js";
import {
  TranslatePipe,
  TranslateService
} from "./chunk-RNIEZZVX.js";
import {
  AccountsService,
  ActivatedRoute,
  AdminStateService,
  AlertListItem,
  AlertsService,
  AssetRatingProfileListItem,
  AssetStateProfileListItem,
  AssetTagListItem,
  AssetsService,
  AuthDataService,
  BroadcastEventMessageService,
  ClientDataService,
  ClientListItem,
  Contact,
  DATE_FORMATS,
  DEFAULT_LIST_SIZE,
  DEFAULT_MEASUREMENT_UNITS,
  DEFAULT_PAGE_SIZE,
  DEFAULT_SYSTEM_ID,
  DeviceConfigProfileListItem,
  DeviceListItem,
  DeviceProviderListItem,
  DeviceTypesService,
  DevicesService,
  DistributorListItem,
  EmailProviderListItem,
  EntitiesService,
  ExportTaskListItem,
  GRID_CONFIGS,
  GeoLockProfileListItem,
  HttpErrorResponse,
  IoTypeListItem,
  LOCAL_STORAGE_KEY_SELECTED_CLIENT,
  LocalStorageService,
  MapSetListItem,
  MeasurementDateTimeService,
  MeasurementUnitType,
  MeasurementUnitsService,
  NavigationEnd,
  OverspeedProfileListItem,
  RequestCacheService,
  RoadProfileListItem,
  Router,
  RouterModule,
  RouterOutlet,
  SearchService,
  SimCardListItem,
  SimCardsService,
  SmsGatewayProviderListItem,
  SpatialUtils,
  TREE_TABLE_CONFIGS,
  ThemeResponse,
  UserPermissionLevel,
  VendorListItem,
  createDropdownField,
  createNumberField,
  enumToSelectOptionValues,
  getEntityIconClass,
  getThemeDictionary,
  hasProperty,
  isDefined,
  isEmpty,
  isEntityIconFontAwesome,
  isNullOrUndefined,
  isString,
  takeUntilDestroyed
} from "./chunk-6H7AOYCU.js";
import "./chunk-7BSAABDZ.js";
import {
  Button,
  ButtonDirective,
  ButtonModule,
  CommonModule,
  ConfirmationService,
  KeyValuePipe,
  MessageService,
  NgClass,
  NgComponentOutlet
} from "./chunk-T7ZENNNQ.js";
import {
  ChangeDetectionStrategy,
  ChangeDetectorRef,
  Component,
  DestroyRef,
  EMPTY,
  Injectable,
  Injector,
  Input,
  Output,
  Pipe,
  Subject,
  ViewChild,
  catchError,
  combineLatest,
  computed,
  debounceTime,
  distinctUntilChanged,
  effect,
  expand,
  filter,
  finalize,
  firstValueFrom,
  forkJoin,
  from,
  inject,
  input,
  map,
  of,
  output,
  reduce,
  setClassMetadata,
  signal,
  switchMap,
  take,
  tap,
  throwError,
  viewChild,
  ɵsetClassDebugInfo,
  ɵɵInheritDefinitionFeature,
  ɵɵProvidersFeature,
  ɵɵadvance,
  ɵɵattribute,
  ɵɵclassMap,
  ɵɵclassProp,
  ɵɵconditional,
  ɵɵconditionalCreate,
  ɵɵdefineComponent,
  ɵɵdefineInjectable,
  ɵɵdefinePipe,
  ɵɵelement,
  ɵɵelementContainer,
  ɵɵelementEnd,
  ɵɵelementStart,
  ɵɵgetCurrentView,
  ɵɵinterpolate1,
  ɵɵlistener,
  ɵɵloadQuery,
  ɵɵnamespaceSVG,
  ɵɵnextContext,
  ɵɵpipe,
  ɵɵpipeBind1,
  ɵɵpipeBind2,
  ɵɵproperty,
  ɵɵpureFunction0,
  ɵɵpureFunction1,
  ɵɵpureFunction2,
  ɵɵpureFunction3,
  ɵɵpureFunction4,
  ɵɵqueryAdvance,
  ɵɵqueryRefresh,
  ɵɵreference,
  ɵɵrepeater,
  ɵɵrepeaterCreate,
  ɵɵrepeaterTrackByIdentity,
  ɵɵrepeaterTrackByIndex,
  ɵɵresetView,
  ɵɵrestoreView,
  ɵɵsanitizeHtml,
  ɵɵtemplate,
  ɵɵtemplateRefExtractor,
  ɵɵtext,
  ɵɵtextInterpolate,
  ɵɵtextInterpolate1,
  ɵɵtextInterpolate2,
  ɵɵtwoWayBindingSet,
  ɵɵtwoWayListener,
  ɵɵtwoWayProperty,
  ɵɵviewQuery,
  ɵɵviewQuerySignal
} from "./chunk-JFHDN3YU.js";
import {
  cloneDeepWith_default
} from "./chunk-M4PBGCJ5.js";
import {
  __objRest,
  __spreadProps,
  __spreadValues
} from "./chunk-7WUTQBRG.js";

// src/app/features/administration/models/event-source.enum.ts
var EventSource;
(function(EventSource2) {
  EventSource2["USER"] = "user";
  EventSource2["SYSTEM"] = "system";
  EventSource2["ROUTE"] = "route";
  EventSource2["SEARCH"] = "search";
  EventSource2["BROADCAST"] = "broadcast";
  EventSource2["INITIAL_LOAD"] = "initial_load";
})(EventSource || (EventSource = {}));

// src/app/features/administration/services/tree/admin-tree-client.service.ts
var AdminTreeClientService = class _AdminTreeClientService {
  accountsService = inject(AccountsService);
  clientDataService = inject(ClientDataService);
  utilityService = inject(UtilityService);
  cacheService = inject(RequestCacheService);
  /**
   * Lists all clients for a given vendor and optional group
   *
   * This is the core method for loading paginated client data. It's used throughout
   * the tree system for:
   * - Initial vendor expansion (first 100 clients)
   * - "Load More" button clicks (next 100 clients)
   * - Automatic pagination during client search (loadClientsUntilFound)
   *
   * Features:
   * - Pagination support (offset/limit)
   * - Filtering by group (direct vendor clients or group clients)
   * - Optional search query for name filtering
   * - Excludes deleted clients (state != DELETED_STATE)
   * - Caching for performance (10 seconds)
   *
   * The returned hasMore flag is critical for pagination logic - it tells the
   * caller whether there are more clients to load beyond the current page.
   *
   * @param vendorId The ID of the vendor to fetch clients for
   * @param groupId Optional group ID to filter clients (undefined = direct vendor clients)
   * @param offset Starting offset for pagination (0-based)
   * @param limit Maximum number of results to return (default: 100)
   * @param searchQuery Optional search query to filter clients by name
   * @returns Promise resolving to {clients, total, hasMore}
   * @throws Error if vendorId is not provided
   */
  async listVendorClients(vendorId, groupId, offset, limit, searchQuery) {
    if (!vendorId) {
      throw new Error("Vendor ID is required");
    }
    const filters = {
      group: { value: groupId ?? ADMIN_TREE.DEFAULT_GROUP_ID, operator: "=" },
      state: { value: ADMIN_TREE.DELETED_STATE, operator: "!=" }
    };
    if (isDefined(searchQuery) && searchQuery.trim().length > 0) {
      filters["name"] = `*${searchQuery.trim()}*`;
    }
    const rqlFilter = this.utilityService.constructRqlFilter(filters);
    const searchSuffix = isDefined(searchQuery) ? `-search-${searchQuery}` : "";
    const cacheKey = `admin-tree-clients-${vendorId}-${groupId ?? "all"}-${offset ?? 0}-${limit ?? ADMIN_TREE.PAGINATION.DEFAULT_PAGE_SIZE}${searchSuffix}`;
    return this.cacheService.getOrFetchAsync(cacheKey, () => firstValueFrom(this.accountsService.listClients(vendorId, offset ?? ADMIN_TREE.PAGINATION.INITIAL_PAGE, limit ?? ADMIN_TREE.PAGINATION.DEFAULT_PAGE_SIZE, "name", rqlFilter).pipe(map((response) => {
      return {
        clients: response.items,
        total: response.count,
        hasMore: response.count > (offset ?? 0) + (response.items.length ?? 0)
      };
    }))), 1e4);
  }
  /**
   * Fetches a single client by ID
   * @param clientId - The ID of the client to fetch
   * @returns Promise resolving to ClientResponse or null if not found
   * @throws Error if clientId is not provided
   */
  async fetchClient(clientId) {
    if (!clientId) {
      throw new Error("Client ID is required");
    }
    return firstValueFrom(this.clientDataService.fetchClientWithCache(clientId).pipe(catchError(() => of(null))));
  }
  /**
   * Attempts to load a client using saved ID or default ID
   * @param savedClientId - Previously saved client ID
   * @param defaultClientId - Fallback client ID
   * @returns Promise resolving to ClientResponse or null if neither client exists
   */
  async tryLoadClient(savedClientId, defaultClientId) {
    if (isDefined(savedClientId)) {
      const client = await this.fetchClient(savedClientId);
      if (client)
        return client;
    }
    if (isDefined(defaultClientId)) {
      return this.fetchClient(defaultClientId);
    }
    return null;
  }
  static \u0275fac = function AdminTreeClientService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeClientService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeClientService, factory: _AdminTreeClientService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeClientService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-tree-group.service.ts
var AdminTreeGroupService = class _AdminTreeGroupService {
  accountsService = inject(AccountsService);
  cacheService = inject(RequestCacheService);
  utilityService = inject(UtilityService);
  /**
   * Lists all company groups for a given vendor
   * @param vendorId - The ID of the vendor to fetch groups for
   * @param searchQuery - Optional search query to filter groups by name
   * @returns Promise resolving to array of CompanyGroupResponse
   * @throws Error if vendorId is empty or undefined
   */
  async listCompanyGroups(vendorId, searchQuery) {
    if (!vendorId?.trim()) {
      throw new Error("Vendor ID is required");
    }
    let rqlFilter = "";
    if (isDefined(searchQuery) && searchQuery.trim().length > 0) {
      const filters = {
        name: `*${searchQuery.trim()}*`
      };
      rqlFilter = this.utilityService.constructRqlFilter(filters);
    }
    const searchSuffix = isDefined(searchQuery) ? `-search-${searchQuery}` : "";
    const cacheKey = `admin-tree-groups-${vendorId}${searchSuffix}`;
    return this.cacheService.getOrFetchAsync(cacheKey, () => firstValueFrom(this.accountsService.listCompanyGroups(
      vendorId,
      void 0,
      // offset
      void 0,
      // limit
      "name",
      // sort
      rqlFilter
    ).pipe(map((response) => response.items ?? []), catchError(() => of([])))), 3e4);
  }
  /**
   * Fetches a single company group by ID
   * @param groupId - The ID of the group to fetch
   * @returns Promise resolving to CompanyGroupResponse or null if not found
   */
  async fetchCompanyGroup(groupId) {
    if (!groupId?.trim()) {
      return null;
    }
    const cacheKey = `admin-tree-group-${groupId}`;
    return this.cacheService.getOrFetchAsync(cacheKey, () => firstValueFrom(this.accountsService.getCompanyGroup(groupId).pipe(catchError(() => of(null)))), 3e4);
  }
  static \u0275fac = function AdminTreeGroupService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeGroupService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeGroupService, factory: _AdminTreeGroupService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeGroupService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-tree-vendor.service.ts
var AdminTreeVendorService = class _AdminTreeVendorService {
  accountsService = inject(AccountsService);
  /**
   * Fetches a vendor associated with a client
   * @param vendorId - The ID of the vendor to fetch
   * @returns Promise resolving to VendorResponse or null if not found
   * @throws Error if vendorId is empty or undefined
   */
  async fetchClientVendor(vendorId) {
    if (!vendorId?.trim()) {
      throw new Error("Vendor ID is required");
    }
    return firstValueFrom(this.accountsService.getVendor(vendorId).pipe(catchError(() => {
      return of(null);
    })));
  }
  /**
   * Fetches a distributor associated with a vendor
   * @param distributorId - The ID of the distributor to fetch
   * @returns Promise resolving to VendorResponse or null if not found
   * @throws Error if distributorId is empty or undefined
   */
  async fetchVendorDistributor(distributorId) {
    if (!distributorId?.trim()) {
      throw new Error("Distributor ID is required");
    }
    return firstValueFrom(this.accountsService.getDistributor(distributorId).pipe(catchError(() => {
      return of(null);
    })));
  }
  /**
   * Lists all vendors associated with a distributor
   * @param distributorId - The ID of the distributor to fetch vendors for
   * @param filter - Optional filter string in RQL format (e.g., 'state!=deleted')
   * @returns Promise resolving to array of VendorListItem
   * @throws Error if distributorId is empty or undefined
   */
  async listDistributorVendors(distributorId, filter2) {
    if (!distributorId?.trim()) {
      throw new Error("Distributor ID is required");
    }
    const vendorFilter = filter2 ?? "state!=deleted";
    return firstValueFrom(this.accountsService.listVendors(distributorId, 0, DEFAULT_LIST_SIZE, "name:asc", vendorFilter).pipe(map((response) => response.items ?? []), catchError(() => {
      return of([]);
    })));
  }
  /**
   * Lists all distributors for a given owner
   * @param ownerId - The ID of the owner to fetch distributors for
   * @returns Promise resolving to array of DistributorListItem
   * @throws Error if ownerId is empty or undefined
   */
  async listDistributors(ownerId) {
    if (!ownerId?.trim()) {
      throw new Error("Owner ID is required");
    }
    return firstValueFrom(this.accountsService.listDistributors(ownerId, 0, DEFAULT_LIST_SIZE, "name:asc", "state=active").pipe(map((response) => response.items ?? []), catchError(() => {
      return of([]);
    })));
  }
  static \u0275fac = function AdminTreeVendorService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeVendorService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeVendorService, factory: _AdminTreeVendorService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeVendorService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-tree-node.service.ts
var AdminTreeNodeService = class _AdminTreeNodeService {
  /**
   * Finds the parent vendor node of a given target node in the tree
   * @param nodes - Array of tree nodes to search through
   * @param targetNode - The node whose parent vendor is being searched for
   * @returns The parent vendor TreeNode if found, null otherwise
   */
  findParentVendorNode(nodes, targetNode) {
    for (const node of nodes) {
      if (!node.children)
        continue;
      const found = this.searchChildNodes(node, targetNode);
      if (found)
        return node;
      for (const child of node.children) {
        if (!child.children)
          continue;
        const foundInChild = this.findParentVendorNode([child], targetNode);
        if (foundInChild)
          return foundInChild;
      }
    }
    return null;
  }
  searchChildNodes(node, targetNode) {
    return node.children?.includes(targetNode) ?? false;
  }
  /**
   * Finds a node in the tree that corresponds to the selected client
   * @param nodes - Array of tree nodes to search through
   * @param selectedClient - The client to search for
   * @returns The matching TreeNode if found, null otherwise
   */
  findSelectedNode(nodes, selectedClient) {
    if (!selectedClient)
      return null;
    return this.searchNode(nodes, selectedClient.id);
  }
  /**
   * Recursively searches for a node with matching client ID
   * @param nodes - Array of tree nodes to search through
   * @param clientId - The client ID to search for
   * @returns The matching TreeNode if found, null otherwise
   */
  searchNode(nodes, clientId) {
    for (const node of nodes) {
      if (node.key === clientId) {
        return node;
      }
      if (node.children) {
        const found = this.searchNode(node.children, clientId);
        if (found)
          return found;
      }
    }
    return null;
  }
  /**
   * Finds a node in the tree by key, optionally filtering by type
   * @param nodes - Array of tree nodes to search through
   * @param key - The node key to search for
   * @param type - Optional node type to match
   * @returns The matching TreeNode if found, null otherwise
   */
  findNodeByKey(nodes, key, type) {
    for (const node of nodes) {
      if (node.key === key && (type === void 0 || node.type === type)) {
        return node;
      }
      if (node.children) {
        const found = this.findNodeByKey(node.children, key, type);
        if (found)
          return found;
      }
    }
    return null;
  }
  static \u0275fac = function AdminTreeNodeService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeNodeService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeNodeService, factory: _AdminTreeNodeService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeNodeService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-tree.service.ts
var AdminTreeService = class _AdminTreeService {
  clientService = inject(AdminTreeClientService);
  vendorService = inject(AdminTreeVendorService);
  groupService = inject(AdminTreeGroupService);
  treeNodeService = inject(AdminTreeNodeService);
  /**
   * Lists all clients for a given vendor and optional group
   * @param vendorId - The ID of the vendor to fetch clients for
   * @param groupId - Optional group ID to filter clients
   * @param offset - Starting offset for pagination
   * @param limit - Maximum number of results to return
   * @param searchQuery - Optional search query to filter clients by name
   * @returns Promise resolving to array of ClientListItem
   * @throws Error if vendorId is not provided
   */
  listVendorClients(vendorId, groupId, offset, limit, searchQuery) {
    return this.clientService.listVendorClients(vendorId, groupId, offset, limit, searchQuery);
  }
  /**
   * Fetches a single client by ID
   * @param clientId - The ID of the client to fetch
   * @returns Promise resolving to ClientResponse or null if not found
   */
  fetchClient(clientId) {
    return this.clientService.fetchClient(clientId);
  }
  /**
   * Fetches a vendor associated with a client
   * @param vendorId - The ID of the vendor to fetch
   * @returns Promise resolving to ClientResponse or null if not found
   */
  fetchClientVendor(vendorId) {
    return this.vendorService.fetchClientVendor(vendorId);
  }
  /**
   * Fetches a distributor associated with a vendor
   * @param distributorId - The ID of the distributor to fetch
   * @returns Promise resolving to DistributorResponse or null if not found
   */
  fetchVendorDistributor(distributorId) {
    return this.vendorService.fetchVendorDistributor(distributorId);
  }
  /**
   * Lists all company groups for a given vendor
   * @param vendorId - The ID of the vendor to fetch groups for
   * @param searchQuery - Optional search query to filter groups by name
   * @returns Promise resolving to array of CompanyGroupResponse
   */
  listCompanyGroups(vendorId, searchQuery) {
    return this.groupService.listCompanyGroups(vendorId, searchQuery);
  }
  /**
   * Fetches a single company group by ID
   * @param groupId - The ID of the group to fetch
   * @returns Promise resolving to CompanyGroupResponse or null if not found
   */
  fetchCompanyGroup(groupId) {
    return this.groupService.fetchCompanyGroup(groupId);
  }
  /**
   * Finds the parent vendor node of a given target node in the tree
   * @param nodes - Array of tree nodes to search through
   * @param targetNode - The node whose parent vendor is being searched for
   * @returns The parent vendor TreeNode if found, null otherwise
   */
  findParentVendorNode(nodes, targetNode) {
    return this.treeNodeService.findParentVendorNode(nodes, targetNode);
  }
  /**
   * Finds a node in the tree that corresponds to the selected client
   * @param nodes - Array of tree nodes to search through
   * @param selectedClient - The client to search for
   * @returns The matching TreeNode if found, null otherwise
   */
  findSelectedNode(nodes, selectedClient) {
    return this.treeNodeService.findSelectedNode(nodes, selectedClient);
  }
  /**
   * Lists all vendors associated with a distributor
   * @param distributorId - The ID of the distributor to fetch vendors for
   * @param filter - Optional filter string in RQL format (e.g., 'state!=deleted')
   * @returns Promise resolving to array of VendorListItem
   */
  listDistributorVendors(distributorId, filter2) {
    return this.vendorService.listDistributorVendors(distributorId, filter2);
  }
  /**
   * Lists all distributors for a given owner
   * @param ownerId - The ID of the owner to fetch distributors for
   * @returns Promise resolving to array of DistributorListItem
   */
  listDistributors(ownerId) {
    return this.vendorService.listDistributors(ownerId);
  }
  /**
   * Attempts to load a client using saved ID or default ID
   * @param savedClientId - Previously saved client ID
   * @param defaultClientId - Fallback client ID
   * @returns Promise resolving to ClientResponse or null if neither client exists
   */
  tryLoadClient(savedClientId, defaultClientId) {
    return this.clientService.tryLoadClient(savedClientId, defaultClientId);
  }
  createClientNode(client, isSelected) {
    return {
      key: client.id,
      label: `${client.name ?? ""} ${ADMIN_TREE.LABELS.CLIENT}`,
      data: __spreadProps(__spreadValues({}, client), { type: ADMIN_TREE.NODE_TYPES.CLIENT }),
      leaf: true,
      icon: ADMIN_TREE.ICONS.CLIENT,
      checked: isSelected,
      selectable: true,
      type: ADMIN_TREE.NODE_TYPES.CLIENT
    };
  }
  createVendorNode(vendor, isExpanded, children, isSelected) {
    return {
      key: vendor.id,
      label: `${vendor.name ?? ""} ${ADMIN_TREE.LABELS.VENDOR}`,
      data: vendor,
      expanded: isExpanded,
      leaf: false,
      icon: ADMIN_TREE.ICONS.VENDOR,
      children,
      checked: isSelected,
      type: ADMIN_TREE.NODE_TYPES.VENDOR,
      selectable: true
    };
  }
  createDistributorNode(distributor, isExpanded, isSelected) {
    return {
      key: distributor.id,
      label: `${distributor.name ?? ""} ${ADMIN_TREE.LABELS.DISTRIBUTOR}`,
      data: distributor,
      expanded: isExpanded,
      leaf: false,
      icon: ADMIN_TREE.ICONS.DISTRIBUTOR,
      children: [],
      checked: isSelected,
      type: ADMIN_TREE.NODE_TYPES.DISTRIBUTOR,
      selectable: true
    };
  }
  createGroupNode(group, isExpanded, children, isSelected) {
    return {
      key: group.id,
      label: `${group.name ?? ""} ${ADMIN_TREE.LABELS.GROUP}`,
      data: group,
      expanded: isExpanded,
      leaf: false,
      icon: ADMIN_TREE.ICONS.GROUP,
      children,
      checked: isSelected,
      type: ADMIN_TREE.NODE_TYPES.GROUP
    };
  }
  createSuperAdminNode(isExpanded, children, isSelected) {
    return {
      key: "super-admin-root",
      label: `Super Admin ${ADMIN_TREE.LABELS.SUPER_ADMIN}`,
      data: {
        id: "super-admin-root",
        name: "Super Admin",
        type: ADMIN_TREE.NODE_TYPES.SUPER_ADMIN
      },
      expanded: isExpanded,
      leaf: false,
      icon: ADMIN_TREE.ICONS.SUPER_ADMIN,
      children,
      checked: isSelected,
      type: ADMIN_TREE.NODE_TYPES.SUPER_ADMIN,
      selectable: true
    };
  }
  static \u0275fac = function AdminTreeService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeService, factory: _AdminTreeService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/admin-search.service.ts
var AdminSearchService = class _AdminSearchService {
  adminTreeService = inject(AdminTreeService);
  adminState = inject(AdminStateService);
  cacheService = inject(RequestCacheService);
  searchService = inject(SearchService);
  searchState = signal({
    query: "",
    results: [],
    isSearching: false,
    expandedForSearch: /* @__PURE__ */ new Set(),
    highlightedNodes: /* @__PURE__ */ new Set()
  }, ...ngDevMode ? [{ debugName: "searchState" }] : (
    /* istanbul ignore next */
    []
  ));
  // Public computed values
  query = computed(() => this.searchState().query, ...ngDevMode ? [{ debugName: "query" }] : (
    /* istanbul ignore next */
    []
  ));
  results = computed(() => this.searchState().results, ...ngDevMode ? [{ debugName: "results" }] : (
    /* istanbul ignore next */
    []
  ));
  isSearching = computed(() => this.searchState().isSearching, ...ngDevMode ? [{ debugName: "isSearching" }] : (
    /* istanbul ignore next */
    []
  ));
  expandedForSearch = computed(() => this.searchState().expandedForSearch, ...ngDevMode ? [{ debugName: "expandedForSearch" }] : (
    /* istanbul ignore next */
    []
  ));
  highlightedNodes = computed(() => this.searchState().highlightedNodes, ...ngDevMode ? [{ debugName: "highlightedNodes" }] : (
    /* istanbul ignore next */
    []
  ));
  hasResults = computed(() => this.searchState().results.length > 0, ...ngDevMode ? [{ debugName: "hasResults" }] : (
    /* istanbul ignore next */
    []
  ));
  hasQuery = computed(() => this.searchState().query.trim().length > 0, ...ngDevMode ? [{ debugName: "hasQuery" }] : (
    /* istanbul ignore next */
    []
  ));
  searchTimeout;
  // #region ========== CORE SEARCH OPERATIONS ==========
  performSearch(query) {
    if (!query || query.length < 2) {
      return of([]);
    }
    return this.cacheService.getOrFetch(`admin-search-${query}`, () => this.searchService.searchEntities(query, false).pipe(switchMap((response) => {
      const results = this.mapEntitySearchToTreeResults(response.items);
      return of(results);
    })), 3e4);
  }
  updateQuery(query) {
    const trimmedQuery = query.trim();
    if (trimmedQuery !== this.query()) {
      this.searchState.update((state) => __spreadProps(__spreadValues({}, state), {
        query: trimmedQuery,
        isSearching: trimmedQuery.length > 0
      }));
      this.performDebouncedSearch(trimmedQuery);
    }
  }
  performDebouncedSearch(query) {
    if (isDefined(this.searchTimeout)) {
      clearTimeout(this.searchTimeout);
    }
    this.searchTimeout = setTimeout(() => {
      this.performSearch(query).subscribe((results) => {
        this.updateTreeSearchResults(results);
      });
    }, 300);
  }
  clearSearch() {
    this.searchState.set({
      query: "",
      results: [],
      isSearching: false,
      expandedForSearch: /* @__PURE__ */ new Set(),
      highlightedNodes: /* @__PURE__ */ new Set()
    });
    if (isDefined(this.searchTimeout)) {
      clearTimeout(this.searchTimeout);
    }
  }
  isNodeHighlighted(nodeId) {
    return this.highlightedNodes().has(nodeId);
  }
  shouldExpandForSearch(nodeId) {
    return this.expandedForSearch().has(nodeId);
  }
  // #endregion
  // #region ========== SEARCH RESULT MAPPING & HANDLING ==========
  /**
   * Maps EntitySearchResult[] to TreeSearchResult[]
   * Creates the tree structure path for each search result
   */
  mapEntitySearchToTreeResults(entities) {
    return entities.map((entity) => this.mapEntityToTreeResult(entity));
  }
  /**
   * Maps a single EntitySearchResult to TreeSearchResult
   * Determines the tree path based on entity type and owner hierarchy
   */
  mapEntityToTreeResult(entity) {
    const path = [];
    const parentIds = [];
    const distributor = this.adminState.vendorDistributor();
    if (distributor) {
      path.push(distributor.name ?? "Distributor");
      parentIds.push(distributor.id);
    }
    if (entity.owner?.id) {
      this.addOwnerHierarchy(entity, path, parentIds);
    }
    path.push(entity.name);
    const nodeType = this.getNodeType(entity.type);
    return {
      id: entity.id,
      name: entity.name,
      type: nodeType,
      path,
      parentIds,
      matched: true,
      owner: entity.owner,
      state: entity.state,
      fields: entity.fields
    };
  }
  /**
   * Adds owner hierarchy to the path and parentIds arrays
   */
  addOwnerHierarchy(entity, path, parentIds) {
    if (entity.type === "client" && entity.owner?.type === "vendor") {
      this.addVendorToPath(entity.owner.id, path, parentIds);
    } else if (entity.type === "client" && entity.owner?.type === "group") {
      this.addGroupHierarchy(entity.owner.id, entity.owner.name, path, parentIds);
    } else if (entity.type === "group" && entity.owner?.type === "vendor") {
      this.addVendorToPath(entity.owner.id, path, parentIds);
    }
  }
  /**
   * Adds vendor to path and parentIds
   */
  addVendorToPath(vendorId, path, parentIds) {
    const vendor = this.adminState.getVendorById(vendorId);
    if (vendor) {
      path.push(vendor.name ?? "Vendor");
      parentIds.push(vendor.id);
    }
  }
  /**
   * Adds group hierarchy (vendor + group) to path and parentIds
   */
  addGroupHierarchy(groupId, groupName, path, parentIds) {
    const vendorId = this.findVendorIdForGroup(groupId);
    if (isDefined(vendorId)) {
      this.addVendorToPath(vendorId, path, parentIds);
      path.push(groupName ?? "Group");
      parentIds.push(groupId);
    }
  }
  /**
   * Maps entity type string to tree node type
   */
  getNodeType(entityType) {
    switch (entityType) {
      case "vendor":
        return ADMIN_TREE.NODE_TYPES.VENDOR;
      case "group":
        return ADMIN_TREE.NODE_TYPES.GROUP;
      case "client":
      default:
        return entityType;
    }
  }
  /**
   * Updates the search state with results and handles UI updates
   */
  updateTreeSearchResults(results) {
    const highlightedNodes = new Set(results.map((r) => r.id));
    const expandedForSearch = /* @__PURE__ */ new Set();
    for (const result of results) {
      for (const parentId of result.parentIds) {
        expandedForSearch.add(parentId);
      }
    }
    this.searchState.update((state) => __spreadProps(__spreadValues({}, state), {
      results,
      isSearching: false,
      highlightedNodes,
      expandedForSearch
    }));
  }
  // #endregion
  // #region ========== CONTEXT & PERMISSION MANAGEMENT ==========
  /**
   * Gets an effective distributor context ID that works across all permission levels.
   * Uses the active context ID which handles both distributor and vendor-only scenarios.
   *
   * @returns A context key to use for state management, or null if no context available
   */
  getEffectiveDistributorId() {
    return this.adminState.getActiveContextId();
  }
  // #endregion
  // #region ========== NAVIGATION & PARENT NODE LOADING ==========
  async prepareResultForNavigation(result) {
    await this.ensureParentNodesLoaded(result);
    this.clearSearch();
  }
  async ensureParentNodesLoaded(result) {
    for (const parentId of result.parentIds) {
      const parentNode = this.findNodeInState(parentId);
      if (parentNode) {
        await this.loadParentNode(parentNode, parentId);
      } else {
        await this.handleUnknownParent(result.parentIds, parentId);
      }
    }
  }
  async loadParentNode(parentNode, parentId) {
    const effectiveDistributorId = this.getEffectiveDistributorId();
    if (isNullOrUndefined(effectiveDistributorId)) {
      console.warn("No context available for loading parent node");
      return;
    }
    switch (parentNode.type) {
      case ADMIN_TREE.NODE_TYPES.DISTRIBUTOR:
        await this.handleDistributorNode(parentId);
        break;
      case ADMIN_TREE.NODE_TYPES.VENDOR:
        await this.handleVendorNode(parentId, effectiveDistributorId);
        break;
      case ADMIN_TREE.NODE_TYPES.GROUP: {
        await this.handleGroupNode(parentId, effectiveDistributorId);
        break;
      }
    }
  }
  async handleDistributorNode(parentId) {
    if (!this.adminState.canAccessDistributorLevel()) {
      console.warn("User does not have distributor-level access");
      return;
    }
    if (this.adminState.hasDistributorData()) {
      this.adminState.setExpandedDistributor(parentId, true);
    }
  }
  async handleVendorNode(parentId, effectiveDistributorId) {
    if (!this.adminState.canAccessVendorLevel()) {
      console.warn("User does not have vendor-level access");
      return;
    }
    if (!this.adminState.isDistributorVendorLoaded(effectiveDistributorId, parentId)) {
      try {
        await this.loadVendorData(parentId, effectiveDistributorId);
      } catch (error) {
        if (error instanceof HttpErrorResponse && error.status === 403) {
          console.warn("Access denied when loading vendor parent node");
          return;
        }
        throw error;
      }
    }
    this.adminState.setDistributorNodeExpanded(effectiveDistributorId, parentId, true);
  }
  async handleGroupNode(parentId, effectiveDistributorId) {
    if (!this.adminState.canAccessVendorLevel()) {
      console.warn("User does not have vendor-level access for groups");
      return;
    }
    const vendorId = this.findVendorIdForGroup(parentId, effectiveDistributorId);
    if (isDefined(vendorId)) {
      try {
        await this.loadGroupData(vendorId, parentId, effectiveDistributorId);
        this.adminState.setDistributorExpandedGroup(effectiveDistributorId, parentId, true);
      } catch (error) {
        if (error instanceof HttpErrorResponse && error.status === 403) {
          console.warn("Access denied when loading group data");
          return;
        }
        throw error;
      }
    }
  }
  // #endregion
  // #region ========== DATA LOADING OPERATIONS ==========
  async loadVendorData(vendorId, distributorId) {
    const contextDistributorId = distributorId ?? this.getEffectiveDistributorId();
    if (isNullOrUndefined(contextDistributorId)) {
      console.warn("No context available for loading vendor data");
      return;
    }
    try {
      const [groups, clientResponse] = await Promise.all([
        this.adminTreeService.listCompanyGroups(vendorId),
        this.adminTreeService.listVendorClients(vendorId, void 0, 0, 100)
      ]);
      this.adminState.setDistributorVendorGroups(contextDistributorId, vendorId, groups);
      this.adminState.addDistributorVendorClients(contextDistributorId, vendorId, clientResponse.clients, {
        offset: 0,
        limit: 100,
        total: clientResponse.total,
        hasMore: clientResponse.hasMore
      });
      this.adminState.markDistributorVendorAsLoaded(contextDistributorId, vendorId);
    } catch (error) {
      if (error instanceof HttpErrorResponse && error.status === 403) {
        console.warn(`Access denied to vendor data for ${vendorId} (403)`);
        if (this.adminState.userPermissionLevel() === null) {
          this.adminState.setUserPermissionLevel(UserPermissionLevel.CLIENT_ONLY);
        }
        this.adminState.markDistributorVendorAsFailedToLoad(contextDistributorId, vendorId);
        throw error;
      }
      console.error(`Failed to load vendor data for ${vendorId}:`, error);
      this.adminState.markDistributorVendorAsFailedToLoad(contextDistributorId, vendorId);
      throw error;
    }
  }
  async loadGroupData(vendorId, groupId, distributorId) {
    const contextDistributorId = distributorId ?? this.getEffectiveDistributorId();
    if (isNullOrUndefined(contextDistributorId)) {
      console.warn("No context available for loading group data");
      return;
    }
    try {
      const vendorGroups = this.adminState.vendorGroups()[contextDistributorId]?.[vendorId] ?? [];
      let group = vendorGroups.find((g) => g.id === groupId);
      if (!isDefined(group)) {
        group = await this.adminTreeService.fetchCompanyGroup(groupId);
        if (isDefined(group)) {
          this.adminState.addDistributorVendorGroup(contextDistributorId, vendorId, group);
        } else {
          console.warn(`[Trace] loadGroupData: Failed to fetch group object for ${groupId}`);
        }
      }
      const paginationKey = this.adminState.getDistributorPaginationKey(contextDistributorId, vendorId, groupId);
      const distributorPagination = this.adminState.clientPagination()[contextDistributorId];
      if (isDefined(distributorPagination?.[paginationKey])) {
        return;
      }
      const clientResponse = await this.adminTreeService.listVendorClients(vendorId, groupId, 0, 100);
      this.adminState.addGroupClients(contextDistributorId, vendorId, groupId, clientResponse.clients, {
        offset: 0,
        limit: 100,
        total: clientResponse.total,
        hasMore: clientResponse.hasMore
      });
    } catch (error) {
      if (error instanceof HttpErrorResponse && error.status === 403) {
        console.warn(`Access denied to group data for ${vendorId}/${groupId} (403)`);
        if (this.adminState.userPermissionLevel() === null) {
          this.adminState.setUserPermissionLevel(UserPermissionLevel.CLIENT_ONLY);
        }
        throw error;
      }
      console.error(`Failed to load group data for ${vendorId}/${groupId}:`, error);
      throw error;
    }
  }
  // #endregion
  // #region ========== NODE FINDING & HIERARCHY ==========
  findNodeInState(nodeId) {
    const distributor = this.adminState.vendorDistributor();
    if (distributor?.id === nodeId) {
      return { id: distributor.id, type: ADMIN_TREE.NODE_TYPES.DISTRIBUTOR };
    }
    const effectiveDistributorId = this.getEffectiveDistributorId();
    if (isNullOrUndefined(effectiveDistributorId)) {
      return null;
    }
    const distributorVendors = this.adminState.distributorVendors()[effectiveDistributorId] ?? [];
    const vendor = distributorVendors.find((v) => v.id === nodeId);
    if (vendor) {
      return { id: vendor.id, type: ADMIN_TREE.NODE_TYPES.VENDOR };
    }
    const vendorGroups = this.adminState.vendorGroups()[effectiveDistributorId] ?? {};
    for (const groups of Object.values(vendorGroups)) {
      const group = groups.find((g) => g.id === nodeId);
      if (group) {
        return { id: group.id, type: ADMIN_TREE.NODE_TYPES.GROUP };
      }
    }
    return null;
  }
  async handleUnknownParent(parentIds, parentId) {
    const vendorInParents = this.findVendorInParents(parentIds);
    if (isDefined(vendorInParents)) {
      const effectiveDistributorId = this.getEffectiveDistributorId();
      if (isDefined(effectiveDistributorId)) {
        await this.loadGroupData(vendorInParents, parentId, effectiveDistributorId);
        this.adminState.setDistributorExpandedGroup(effectiveDistributorId, parentId, true);
      }
    }
  }
  findVendorInParents(parentIds) {
    const effectiveDistributorId = this.getEffectiveDistributorId();
    if (isNullOrUndefined(effectiveDistributorId)) {
      return null;
    }
    const distributorVendors = this.adminState.distributorVendors()[effectiveDistributorId] ?? [];
    return parentIds.find((id) => distributorVendors.some((v) => v.id === id)) ?? null;
  }
  findVendorIdForGroup(groupId, distributorId) {
    const contextDistributorId = distributorId ?? this.getEffectiveDistributorId();
    if (isNullOrUndefined(contextDistributorId)) {
      return null;
    }
    const vendorGroups = this.adminState.vendorGroups()[contextDistributorId] ?? {};
    for (const [vendorId, groups] of Object.entries(vendorGroups)) {
      if (groups.some((group) => group.id === groupId)) {
        return vendorId;
      }
    }
    return null;
  }
  /**
   * Extracts the hierarchy information (distributorId, vendorId, groupId) from a TreeSearchResult.
   * The parentIds array is ordered based on the entity type and hierarchy structure.
   *
   * For clients in distributor context:
   * - Vendor-owned: [distributorId, vendorId]
   * - Group-owned: [distributorId, vendorId, groupId]
   *
   * For clients without distributor:
   * - Vendor-owned: [vendorId]
   * - Group-owned: [vendorId, groupId]
   *
   * @param result The TreeSearchResult to extract hierarchy from
   * @returns Object containing distributorId, vendorId, and optional groupId
   */
  extractHierarchyFromResult(result) {
    const parentIds = result.parentIds;
    if (parentIds.length === 0) {
      return {};
    }
    const distributor = this.adminState.vendorDistributor();
    let vendorId;
    let groupId;
    let distributorId;
    if (isDefined(distributor) && parentIds[0] === distributor.id) {
      distributorId = parentIds[0];
      vendorId = parentIds[1];
      groupId = parentIds[2];
    } else {
      vendorId = parentIds[0];
      groupId = parentIds[1];
    }
    return { vendorId, groupId, distributorId };
  }
  findClientInDistributor(clientId, distributorId) {
    const vendorClients = this.adminState.vendorClients()[distributorId] ?? {};
    for (const [vendorId, clients] of Object.entries(vendorClients)) {
      if (clients.some((client) => client.id === clientId)) {
        return { distributorId, vendorId };
      }
    }
    const groupClients = this.adminState.groupClients()[distributorId] ?? {};
    for (const [vendorId, groups] of Object.entries(groupClients)) {
      for (const [groupId, clients] of Object.entries(groups)) {
        if (clients.some((client) => client.id === clientId)) {
          return { distributorId, vendorId, groupId };
        }
      }
    }
    return null;
  }
  /**
   * Finds the vendor and group IDs for a given client by searching through all loaded data.
   * Works across all permission levels (client-only, vendor, distributor, super admin).
   * @param clientId The ID of the client to find
   * @param distributorId Optional distributor ID to scope the search
   * @returns Object containing distributorId (or context key), vendorId and optional groupId, or null if not found
   */
  findClientHierarchy(clientId, distributorId) {
    if (isDefined(distributorId)) {
      return this.findClientInDistributor(clientId, distributorId);
    }
    const distributorVendorClients = this.adminState.vendorClients();
    for (const [contextId] of Object.entries(distributorVendorClients)) {
      const result = this.findClientInDistributor(clientId, contextId);
      if (result)
        return result;
    }
    const effectiveDistributorId = this.getEffectiveDistributorId();
    if (isDefined(effectiveDistributorId)) {
      return this.findClientInDistributor(clientId, effectiveDistributorId);
    }
    return null;
  }
  // #endregion
  // #region ========== TREE RESULT CREATION ==========
  /**
   * Helper methods for creating tree search results
   */
  createClientTreeSearchResult(client, vendorId) {
    const vendor = this.adminState.getVendorById(vendorId);
    const distributor = this.adminState.vendorDistributor();
    const path = [];
    const parentIds = [];
    if (distributor) {
      path.push(distributor.name ?? "Distributor");
      parentIds.push(distributor.id);
    }
    if (vendor) {
      path.push(vendor.name ?? "Vendor");
      parentIds.push(vendor.id);
    }
    path.push(client.name ?? "Client");
    return {
      id: client.id,
      name: client.name ?? "",
      type: ADMIN_TREE.NODE_TYPES.CLIENT,
      path,
      parentIds,
      matched: true
    };
  }
  createGroupClientTreeSearchResult(client, vendorId, groupId) {
    const vendor = this.adminState.getVendorById(vendorId);
    const distributor = this.adminState.vendorDistributor();
    const path = [];
    const parentIds = [];
    if (distributor) {
      path.push(distributor.name ?? "Distributor");
      parentIds.push(distributor.id);
    }
    if (vendor) {
      path.push(vendor.name ?? "Vendor");
      parentIds.push(vendor.id);
    }
    const effectiveDistributorId = this.getEffectiveDistributorId();
    let group = null;
    if (isDefined(effectiveDistributorId)) {
      const vendorGroups = this.adminState.vendorGroups()[effectiveDistributorId]?.[vendorId] ?? [];
      group = vendorGroups.find((g) => g.id === groupId);
    }
    if (group) {
      path.push(group.name ?? "Group");
      parentIds.push(group.id);
    } else {
      path.push("Group");
      parentIds.push(groupId);
    }
    path.push(client.name ?? "Client");
    return {
      id: client.id,
      name: client.name ?? "",
      type: ADMIN_TREE.NODE_TYPES.CLIENT,
      path,
      parentIds,
      matched: true
    };
  }
  createVendorTreeSearchResult(vendor) {
    const distributor = this.adminState.vendorDistributor();
    const path = [];
    const parentIds = [];
    if (distributor) {
      path.push(distributor.name ?? "Distributor");
      parentIds.push(distributor.id);
    }
    path.push(vendor.name ?? "Vendor");
    return {
      id: vendor.id,
      name: vendor.name ?? "",
      type: ADMIN_TREE.NODE_TYPES.VENDOR,
      path,
      parentIds,
      matched: true
    };
  }
  /**
   * Creates a TreeSearchResult object for a given client.
   * This is used to prepare for navigation from outside the search component,
   * e.g., from a 'View in Tree' action.
   * @param client The client list item.
   * @param vendorId The ID of the parent vendor.
   * @param groupId The optional ID of the parent group.
   * @returns A TreeSearchResult object.
   */
  createSearchResultForClient(client, vendorId, groupId) {
    if (isDefined(groupId)) {
      return this.createGroupClientTreeSearchResult(client, vendorId, groupId);
    }
    return this.createClientTreeSearchResult(client, vendorId);
  }
  /**
   * Creates a TreeSearchResult object for a given vendor.
   * @param vendor The vendor list item.
   * @returns A TreeSearchResult object.
   */
  createSearchResultForVendor(vendor) {
    return this.createVendorTreeSearchResult(vendor);
  }
  // #endregion
  // #region ========== PUBLIC API - TREE NAVIGATION ==========
  /**
   * Handles a client owned directly by a vendor
   */
  async handleVendorOwnedClient(client) {
    if (!isDefined(client.owner?.id) || !isDefined(client.id))
      return null;
    const vendorId = client.owner.id;
    await this.ensureVendorLoadedAndExpanded(vendorId);
    const foundClient = await this.loadClientsUntilFound(client.id, vendorId);
    if (foundClient) {
      const result = this.createSearchResultForClient(foundClient, vendorId);
      await this.ensureParentNodesLoaded(result);
      return result;
    }
    return null;
  }
  /**
   * Handles a client owned by a group
   */
  async handleGroupOwnedClient(client) {
    if (!isDefined(client.group) || !isDefined(client.id))
      return null;
    const groupId = client.group;
    let vendorId = this.findVendorIdForGroup(groupId);
    if (!isDefined(vendorId) && isDefined(client.owner?.id) && client.owner.id !== groupId) {
      vendorId = client.owner.id;
    }
    if (!isDefined(vendorId)) {
      return null;
    }
    await this.ensureVendorLoadedAndExpanded(vendorId);
    const effectiveDistributorId = this.getEffectiveDistributorId();
    if (isNullOrUndefined(effectiveDistributorId)) {
      return null;
    }
    this.adminState.setDistributorExpandedGroup(effectiveDistributorId, groupId, true);
    const foundClient = await this.loadClientsUntilFound(client.id, vendorId, groupId);
    if (foundClient) {
      const result = this.createSearchResultForClient(foundClient, vendorId, groupId);
      await this.ensureParentNodesLoaded(result);
      return result;
    }
    return null;
  }
  /**
   * Ensures a vendor is loaded and expanded
   */
  async ensureVendorLoadedAndExpanded(vendorId) {
    if (!this.adminState.canAccessVendorLevel()) {
      console.warn("User does not have vendor-level access");
      return;
    }
    const effectiveDistributorId = this.getEffectiveDistributorId();
    if (isNullOrUndefined(effectiveDistributorId)) {
      console.warn("No context available for loading vendor");
      return;
    }
    if (!this.adminState.isDistributorVendorLoaded(effectiveDistributorId, vendorId)) {
      try {
        await this.loadVendorData(vendorId, effectiveDistributorId);
      } catch (error) {
        if (error instanceof HttpErrorResponse && error.status === 403) {
          console.warn("Access denied to vendor data (403)");
          if (this.adminState.userPermissionLevel() === null) {
            this.adminState.setUserPermissionLevel(UserPermissionLevel.CLIENT_ONLY);
          }
          return;
        }
        throw error;
      }
    }
    this.adminState.setDistributorNodeExpanded(effectiveDistributorId, vendorId, true);
  }
  /**
   * Handles showing a client in the tree by preparing the tree and returning navigation info.
   * @param client The client entity to show.
   * @param vendorId The ID of the parent vendor.
   * @param groupId The optional ID of the parent group.
   * @returns Promise that resolves to the search result for navigation
   */
  async handleClientInTree(client, vendorId, groupId) {
    if (isDefined(groupId) && groupId !== DEFAULT_SYSTEM_ID) {
      const foundClient = await this.loadClientsUntilFound(client.id, vendorId, groupId);
      if (foundClient) {
        const result = this.createSearchResultForClient(foundClient, vendorId, groupId);
        await this.ensureParentNodesLoaded(result);
        return result;
      }
    } else {
      const foundClient = await this.loadClientsUntilFound(client.id, vendorId);
      if (foundClient) {
        const result = this.createSearchResultForClient(foundClient, vendorId);
        await this.ensureParentNodesLoaded(result);
        return result;
      }
    }
    throw new Error(`Client ${client.id} not found after pagination`);
  }
  /**
   * Handles showing a vendor in the tree by preparing the tree and returning navigation info.
   * @param vendor The vendor entity to show.
   * @returns Promise that resolves to the search result for navigation
   */
  async handleVendorInTree(vendor) {
    const result = this.createSearchResultForVendor(vendor);
    await this.ensureParentNodesLoaded(result);
    return result;
  }
  /**
   * Prepares a client for tree selection by ensuring its hierarchy is loaded and expanded.
   *
   * This method is used when you have just a client ID and need to make it visible and
   * selectable in the tree. It's typically called from deep links or when navigating
   * from another part of the application.
   *
   * Approach:
   * 1. First checks if client is already in loaded data (fast path)
   * 2. If not found, uses global search API to find the client
   * 3. Uses search result to determine parent hierarchy
   * 4. Ensures all parents are loaded and expanded
   * 5. Returns TreeSearchResult for navigation
   *
   * This method does NOT use pagination - it relies on global search which is more
   * efficient when you don't know where the client is in the hierarchy.
   *
   * @param clientId The ID of the client to select
   * @returns Promise that resolves with the TreeSearchResult if successful, null if client not found
   */
  async prepareClientForTreeSelection(clientId) {
    const hierarchy = this.findClientHierarchy(clientId);
    if (hierarchy) {
      const client = this.findClientInLoadedData(clientId, hierarchy.vendorId, hierarchy.groupId);
      if (client) {
        const result = this.createSearchResultForClient(client, hierarchy.vendorId, hierarchy.groupId);
        await this.ensureParentNodesLoaded(result);
        return result;
      }
    }
    try {
      const searchResponse = await firstValueFrom(this.searchService.searchEntities(clientId).pipe(catchError(() => of(null))));
      if (searchResponse?.items) {
        const clientEntity = searchResponse.items.find((item) => item.id === clientId);
        if (clientEntity) {
          const result = this.mapEntityToTreeResult(clientEntity);
          await this.ensureParentNodesLoaded(result);
          if (result.type === ADMIN_TREE.NODE_TYPES.CLIENT) {
            const hierarchy2 = this.extractHierarchyFromResult(result);
            if (isDefined(hierarchy2.vendorId)) {
              await this.loadClientsUntilFound(clientId, hierarchy2.vendorId, hierarchy2.groupId);
            }
          }
          return result;
        }
      }
    } catch (error) {
      console.error("Failed to search for client:", error);
    }
    console.warn(`Client ${clientId} not found in any vendor hierarchy`);
    return null;
  }
  /**
   * Prepares a client for tree selection using owner information from ClientDataService.
   *
   * This is the PREFERRED method for loading a client into the tree when you have
   * the full client object with owner information. It's more efficient than
   * prepareClientForTreeSelection because:
   *
   * 1. No global search API call needed (saves network request)
   * 2. Owner info tells us exactly where in hierarchy to look
   * 3. Can use targeted pagination instead of broad search
   *
   * This method handles two scenarios:
   * 1. Vendor-owned clients (no group, or group = DEFAULT_SYSTEM_ID)
   *    - Loads vendor's direct clients with pagination
   * 2. Group-owned clients (has group ID)
   *    - Loads group's clients with pagination
   *
   * Both scenarios use loadClientsUntilFound which has safety limits to prevent
   * infinite loops (max 50 pagination attempts, error handling, etc.)
   *
   * @param client The client with owner information from ClientDataService
   * @returns Promise that resolves with the TreeSearchResult if successful, null if not found
   */
  async prepareClientForTreeSelectionWithOwner(client) {
    if (!isDefined(client.id) || !isDefined(client.owner)) {
      console.warn("[Trace] prepareClientForTreeSelectionWithOwner: Client missing ID or owner information");
      return null;
    }
    try {
      if (client.group === DEFAULT_SYSTEM_ID || isNullOrUndefined(client.group)) {
        return await this.handleVendorOwnedClient(client);
      } else {
        return await this.handleGroupOwnedClient(client);
      }
    } catch (error) {
      console.error("[Trace] Failed to prepare client for tree selection:", error);
      return null;
    }
  }
  // #endregion
  // #region ========== PAGINATION-BASED CLIENT LOADING ==========
  /**
   * Recursively loads clients using the load more functionality until the target client is found
   *
   * This is a critical method that implements safe pagination to find a specific client
   * without causing infinite loops. It's used when:
   * - A deep link points to a client not in loaded data
   * - A search result selects a client that hasn't been loaded yet
   * - Navigation requires a specific client that may be deep in the pagination
   *
   * Safety Mechanisms (prevents infinite loops):
   * 1. MAX_PAGINATION_ATTEMPTS limit (50 attempts = 5000 clients max)
   * 2. Error handling that breaks the loop on API failures
   * 3. hasMore check from pagination state
   * 4. Updates pagination state to hasMore=false on errors
   *
   * Flow:
   * 1. Check if client is already in loaded data (quick return)
   * 2. Load initial page if no pagination exists (first 100 clients)
   * 3. Loop through remaining pages until client found or limits reached:
   *    - Load next 100 clients
   *    - Check if target client is in the new batch
   *    - Break on error to prevent infinite retries
   *    - Stop at MAX_PAGINATION_ATTEMPTS
   * 4. Return client if found, null otherwise
   *
   * @param targetClientId The ID of the client to find
   * @param vendorId The vendor ID that owns the client (or its group)
   * @param groupId Optional group ID if client is in a group
   * @returns Promise resolving to ClientListItem if found, null if not found after all attempts
   */
  async loadClientsUntilFound(targetClientId, vendorId, groupId) {
    const effectiveDistributorId = this.getEffectiveDistributorId();
    if (isNullOrUndefined(effectiveDistributorId)) {
      console.warn("[Trace] loadClientsUntilFound: No context available for loading clients");
      return null;
    }
    let foundClient = this.findClientInLoadedData(targetClientId, vendorId, groupId, effectiveDistributorId);
    if (foundClient) {
      return foundClient;
    }
    const paginationKey = this.adminState.getDistributorPaginationKey(effectiveDistributorId, vendorId, groupId);
    let currentPagination = this.adminState.clientPagination()[effectiveDistributorId]?.[paginationKey];
    if (isNullOrUndefined(currentPagination)) {
      await this.loadInitialClients(vendorId, groupId, effectiveDistributorId);
      foundClient = this.findClientInLoadedData(targetClientId, vendorId, groupId, effectiveDistributorId);
      if (foundClient) {
        return foundClient;
      }
      currentPagination = this.adminState.clientPagination()[effectiveDistributorId]?.[paginationKey];
    }
    const MAX_PAGINATION_ATTEMPTS = 50;
    let paginationAttempts = 0;
    while (isDefined(currentPagination) && currentPagination.hasMore && isNullOrUndefined(foundClient) && paginationAttempts < MAX_PAGINATION_ATTEMPTS) {
      paginationAttempts++;
      try {
        await this.loadMoreClientsPage(vendorId, groupId, effectiveDistributorId);
      } catch (error) {
        console.error(`[Trace] Error loading page ${paginationAttempts} while searching for client ${targetClientId}:`, error);
        break;
      }
      foundClient = this.findClientInLoadedData(targetClientId, vendorId, groupId, effectiveDistributorId);
      if (foundClient) {
        return foundClient;
      }
      currentPagination = this.adminState.clientPagination()[effectiveDistributorId]?.[paginationKey];
    }
    if (paginationAttempts >= MAX_PAGINATION_ATTEMPTS) {
      console.warn(`[Trace] Reached maximum pagination attempts (${MAX_PAGINATION_ATTEMPTS}) while searching for client ${targetClientId}`);
    }
    if (isNullOrUndefined(foundClient)) {
      console.warn(`[Trace] loadClientsUntilFound: Client ${targetClientId} NOT found after loading ${paginationAttempts} additional pages`);
    }
    return foundClient ?? null;
  }
  /**
   * Loads initial clients for a vendor or group within context
   */
  async loadInitialClients(vendorId, groupId, distributorId) {
    const contextDistributorId = distributorId ?? this.getEffectiveDistributorId();
    if (isNullOrUndefined(contextDistributorId)) {
      console.warn("No context available for loading initial clients");
      return;
    }
    try {
      const clientResponse = await this.adminTreeService.listVendorClients(vendorId, groupId, 0, 100);
      const paginationUpdate = {
        offset: 0,
        limit: 100,
        total: clientResponse.total,
        hasMore: clientResponse.hasMore
      };
      if (isDefined(groupId)) {
        this.adminState.addGroupClients(contextDistributorId, vendorId, groupId, clientResponse.clients, paginationUpdate, false);
      } else {
        this.adminState.addDistributorVendorClients(contextDistributorId, vendorId, clientResponse.clients, paginationUpdate, false);
      }
    } catch (error) {
      if (error instanceof HttpErrorResponse && error.status === 403) {
        console.warn("Access denied when loading initial clients (403)");
        if (this.adminState.userPermissionLevel() === null) {
          this.adminState.setUserPermissionLevel(UserPermissionLevel.CLIENT_ONLY);
        }
        throw error;
      }
      console.error("Failed to load initial clients:", error);
      throw error;
    }
  }
  /**
   * Loads the next page of clients using the existing load more functionality
   *
   * This method loads the next 100 clients from the API and updates the state.
   * It includes critical error handling to prevent infinite loops:
   *
   * Error Handling:
   * - On error, sets hasMore=false to stop future pagination attempts
   * - Uses empty array to update just the pagination state
   * - Re-throws error so caller knows the load failed
   *
   * The error handling is crucial because without it, a persistent API error
   * (network issue, auth problem, etc.) would cause the caller to retry
   * indefinitely, causing an infinite loop and browser hang.
   *
   * @param vendorId The vendor ID to load clients for
   * @param groupId Optional group ID if loading group clients
   * @param distributorId Optional distributor ID (uses current if not provided)
   * @throws Error if the API request fails (after updating state to prevent retries)
   */
  async loadMoreClientsPage(vendorId, groupId, distributorId) {
    const contextDistributorId = distributorId ?? this.getEffectiveDistributorId();
    if (isNullOrUndefined(contextDistributorId)) {
      console.warn("No context available for loading more clients");
      return;
    }
    const paginationKey = this.adminState.getDistributorPaginationKey(contextDistributorId, vendorId, groupId);
    const currentPagination = this.adminState.clientPagination()[contextDistributorId]?.[paginationKey];
    if (!currentPagination?.hasMore || currentPagination.loadingMore) {
      return;
    }
    const nextOffset = currentPagination.offset + currentPagination.limit;
    try {
      this.adminState.setDistributorLoadingMore(contextDistributorId, vendorId, groupId, true);
      const clientResponse = await this.adminTreeService.listVendorClients(vendorId, groupId, nextOffset, 100);
      const paginationUpdate = {
        offset: nextOffset,
        limit: 100,
        total: clientResponse.total,
        hasMore: clientResponse.hasMore
      };
      if (isDefined(groupId)) {
        this.adminState.addGroupClients(contextDistributorId, vendorId, groupId, clientResponse.clients, paginationUpdate, true);
      } else {
        this.adminState.addDistributorVendorClients(contextDistributorId, vendorId, clientResponse.clients, paginationUpdate, true);
      }
    } catch (error) {
      if (error instanceof HttpErrorResponse && error.status === 403) {
        console.warn("Access denied when loading more clients (403)");
        if (this.adminState.userPermissionLevel() === null) {
          this.adminState.setUserPermissionLevel(UserPermissionLevel.CLIENT_ONLY);
        }
      }
      console.error("Failed to load more clients:", error);
      this.adminState.setDistributorLoadingMore(contextDistributorId, vendorId, groupId, false);
      const errorPaginationUpdate = {
        offset: nextOffset,
        limit: 100,
        total: currentPagination.total,
        hasMore: false
        // Stop trying to load more after an error
      };
      if (isDefined(groupId)) {
        this.adminState.addGroupClients(
          contextDistributorId,
          vendorId,
          groupId,
          [],
          // Empty array - just updating pagination
          errorPaginationUpdate,
          true
        );
      } else {
        this.adminState.addDistributorVendorClients(
          contextDistributorId,
          vendorId,
          [],
          // Empty array - just updating pagination
          errorPaginationUpdate,
          true
        );
      }
      throw error;
    }
  }
  /**
   * Finds a client in the currently loaded data within distributor context
   */
  findClientInLoadedData(clientId, vendorId, groupId, distributorId) {
    const contextDistributorId = distributorId ?? this.getEffectiveDistributorId();
    if (isNullOrUndefined(contextDistributorId)) {
      return void 0;
    }
    if (isDefined(groupId)) {
      const groupClients = this.adminState.groupClients()[contextDistributorId]?.[vendorId]?.[groupId];
      return groupClients?.find((client) => client.id === clientId);
    } else {
      const vendorClients = this.adminState.vendorClients()[contextDistributorId]?.[vendorId];
      return vendorClients?.find((client) => client.id === clientId);
    }
  }
  /**
   * Searches for and loads a client using the global search API
   * @param clientId The ID of the client to find
   * @param _searchContext Optional context about where to look (vendor/group) - not used in global search
   * @returns Promise that resolves to true if client was found and loaded
   */
  async searchAndLoadClient(clientId, _searchContext) {
    try {
      const searchResponse = await firstValueFrom(this.cacheService.getOrFetch(`admin-search-${clientId}`, () => this.searchService.searchEntities(clientId).pipe(catchError(() => of(null))), 3e4));
      if (searchResponse?.items) {
        const clientEntity = searchResponse.items.find((item) => item.id === clientId);
        if (clientEntity) {
          const result = this.mapEntityToTreeResult(clientEntity);
          await this.ensureParentNodesLoaded(result);
          if (result.type === ADMIN_TREE.NODE_TYPES.CLIENT) {
            const hierarchy = this.extractHierarchyFromResult(result);
            if (isDefined(hierarchy.vendorId)) {
              await this.loadClientsUntilFound(clientId, hierarchy.vendorId, hierarchy.groupId);
            }
          }
          return true;
        }
      }
    } catch (error) {
      console.error("Failed to search and load client:", error);
    }
    return false;
  }
  // #endregion
  // #region ========== UTILITIES & PRELOADING SEARCH DATA ==========
  /**
   * Preloads search data for better performance
   * This method is called from AdminDataService for background preloading
   */
  async preloadSearchData() {
    const effectiveDistributorId = this.getEffectiveDistributorId();
    if (isNullOrUndefined(effectiveDistributorId)) {
      return;
    }
    try {
      const distributorVendors = this.adminState.distributorVendors()[effectiveDistributorId] ?? [];
      const preloadPromises = distributorVendors.map(async (vendor) => {
        try {
          if (!this.adminState.isDistributorVendorLoaded(effectiveDistributorId, vendor.id)) {
            await this.loadVendorData(vendor.id, effectiveDistributorId);
          }
        } catch (error) {
          console.warn(`Failed to preload vendor data for ${vendor.id}:`, error);
        }
      });
      await Promise.all(preloadPromises);
    } catch (error) {
      console.warn("Failed to preload search data:", error);
    }
  }
  static \u0275fac = function AdminSearchService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminSearchService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminSearchService, factory: _AdminSearchService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminSearchService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-tree-context-extractor.service.ts
var AdminTreeContextExtractorService = class _AdminTreeContextExtractorService {
  /**
   * Extracts search context by looking at the current tree structure
   * to determine which vendor/group might contain the missing client
   */
  extractContext(_nodeKey, nodes) {
    const context = {};
    const extractFromNodes = (treeNodes, currentVendorId, currentGroupId) => {
      for (const node of treeNodes) {
        if (node.type === "vendor") {
          currentVendorId = node.key;
          currentGroupId = void 0;
        } else if (node.type === "group") {
          currentGroupId = node.key;
        }
        if (node.type === "client" && isDefined(currentVendorId) && currentVendorId !== "") {
          context.vendorId = currentVendorId;
          if (isDefined(currentGroupId) && currentGroupId !== "") {
            context.groupId = currentGroupId;
          }
        }
        if (node.children && node.children.length > 0) {
          extractFromNodes(node.children, currentVendorId, currentGroupId);
        }
      }
    };
    extractFromNodes(nodes);
    return isDefined(context.vendorId) && context.vendorId !== "" ? context : void 0;
  }
  static \u0275fac = function AdminTreeContextExtractorService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeContextExtractorService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeContextExtractorService, factory: _AdminTreeContextExtractorService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeContextExtractorService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-tree-management.service.ts
var AdminTreeManagementService = class _AdminTreeManagementService {
  adminState = inject(AdminStateService);
  adminTreeService = inject(AdminTreeService);
  apiSchemaService = inject(ApiSchemaService);
  adminSearchService = inject(AdminSearchService);
  hierarchyTree = computed(() => {
    const distributor = this.adminState.vendorDistributor();
    const vendor = this.adminState.clientVendor();
    const selectedClient = this.adminState.selectedClient();
    const usesModernSchema = this.apiSchemaService.usesModernSchema();
    const permissionLevel = this.adminState.userPermissionLevel();
    const _selKey = this.adminState.selectedNodeKey();
    const _selType = this.adminState.selectedNodeType();
    const searchExpandedNodes = this.adminSearchService.expandedForSearch();
    if (permissionLevel === UserPermissionLevel.CLIENT_ONLY) {
      return [];
    }
    if (permissionLevel === UserPermissionLevel.SUPER_ADMIN && usesModernSchema) {
      const tree = this.buildSuperAdminHierarchy(selectedClient);
      return this.applySearchEnhancements(tree, searchExpandedNodes);
    }
    if (permissionLevel === UserPermissionLevel.DISTRIBUTOR && distributor) {
      const tree = this.buildDistributorHierarchy(distributor, selectedClient);
      return this.applySearchEnhancements(tree, searchExpandedNodes);
    }
    if (permissionLevel === UserPermissionLevel.VENDOR && vendor) {
      const tree = this.buildVendorOnlyHierarchy(vendor, selectedClient);
      return this.applySearchEnhancements(tree, searchExpandedNodes);
    }
    return [];
  }, ...ngDevMode ? [{ debugName: "hierarchyTree" }] : (
    /* istanbul ignore next */
    []
  ));
  buildSuperAdminHierarchy(selectedClient) {
    const superAdminExpandedNodes = this.adminState.expandedNodes()["super-admin"] ?? /* @__PURE__ */ new Set();
    const usesModernSchema = this.apiSchemaService.usesModernSchema();
    const allDistributors = this.adminState.distributors();
    const isSuperAdminExpanded = usesModernSchema || superAdminExpandedNodes.has("super-admin-root");
    const superAdminNode = this.adminTreeService.createSuperAdminNode(isSuperAdminExpanded, [], this.isNodeSelected("super-admin-root", ADMIN_TREE.NODE_TYPES.SUPER_ADMIN));
    if (isSuperAdminExpanded && allDistributors.length > 0) {
      superAdminNode.children = allDistributors.map((dist) => {
        return this.buildDistributorNodeWithContext(dist, selectedClient);
      });
    } else {
      superAdminNode.children = [];
    }
    return [superAdminNode];
  }
  /**
   * Builds a distributor node with its own vendor hierarchy using distributor-scoped data
   */
  buildDistributorNodeWithContext(distributor, selectedClient) {
    const distributorId = distributor.id;
    const expandedDistributors = this.adminState.expandedDistributors();
    const isDistributorExpanded = expandedDistributors.has(distributorId);
    const distributorNode = this.adminTreeService.createDistributorNode(__spreadProps(__spreadValues({}, distributor), {
      entity: {
        id: distributor.id,
        name: distributor.name,
        creationDate: (/* @__PURE__ */ new Date()).toISOString(),
        modifiedDate: (/* @__PURE__ */ new Date()).toISOString()
      },
      owner: { id: "", name: "" },
      tags: null
    }), isDistributorExpanded, this.isNodeSelected(distributor.id, ADMIN_TREE.NODE_TYPES.DISTRIBUTOR));
    if (isDistributorExpanded) {
      const distributorVendors = this.adminState.getDistributorVendors(distributorId);
      distributorNode.children = distributorVendors.map((vendor) => this.buildVendorNodeWithDistributorContext(vendor, selectedClient, distributorId));
    } else {
      distributorNode.children = [];
    }
    return distributorNode;
  }
  buildDistributorHierarchy(distributor, selectedClient) {
    const distributorId = distributor.id;
    const expandedDistributors = this.adminState.expandedDistributors();
    const isDistributorExpanded = expandedDistributors.has(distributorId);
    const distributorNode = this.adminTreeService.createDistributorNode(distributor, isDistributorExpanded, this.isNodeSelected(distributorId, ADMIN_TREE.NODE_TYPES.DISTRIBUTOR));
    if (isDistributorExpanded) {
      const distributorVendors = this.adminState.getDistributorVendors(distributorId);
      distributorNode.children = distributorVendors.map((vendor) => this.buildVendorNodeWithDistributorContext(vendor, selectedClient, distributorId));
    } else {
      distributorNode.children = [];
    }
    return [distributorNode];
  }
  buildVendorOnlyHierarchy(vendor, selectedClient) {
    const distributorId = vendor.owner?.id ?? this.adminState.getActiveContextId();
    if (isNullOrUndefined(distributorId)) {
      const vendorNode2 = this.adminTreeService.createVendorNode(vendor, false, [], this.isNodeSelected(vendor.id, ADMIN_TREE.NODE_TYPES.VENDOR));
      return [vendorNode2];
    }
    const vendorNode = this.buildVendorNodeWithDistributorContext(vendor, selectedClient, distributorId);
    return [vendorNode];
  }
  createLoadMoreNode(vendorId, groupId, loading) {
    const key = `loadmore_${vendorId}${isDefined(groupId) ? "_" + groupId : ""}`;
    return {
      key,
      label: "Load More...",
      type: ADMIN_TREE.NODE_TYPES.LOAD_MORE,
      leaf: true,
      icon: loading ? "pi pi-spin pi-spinner" : "pi pi-plus",
      data: {
        type: ADMIN_TREE.NODE_TYPES.LOAD_MORE,
        vendorId,
        groupId,
        loading
      },
      styleClass: "load-more-node"
    };
  }
  /**
   * Builds a vendor node with distributor context, using distributor-scoped data
   */
  buildVendorNodeWithDistributorContext(vendor, selectedClient, distributorId) {
    const vendorClients = this.adminState.getDistributorVendorClients(distributorId, vendor.id);
    const vendorGroups = this.adminState.getDistributorVendorGroups(distributorId, vendor.id);
    const isVendorExpanded = this.adminState.isDistributorNodeExpanded(distributorId, vendor.id);
    const children = [];
    const vendorNode = this.adminTreeService.createVendorNode(vendor, isVendorExpanded, children, this.isNodeSelected(vendor.id, ADMIN_TREE.NODE_TYPES.VENDOR));
    if (isVendorExpanded) {
      if (vendorGroups.length > 0) {
        children.push(...vendorGroups.map((group) => {
          const isGroupExpanded = this.adminState.isDistributorGroupExpanded(distributorId, group.id);
          const groupChildren = [];
          const groupNode = this.adminTreeService.createGroupNode(group, isGroupExpanded, groupChildren, this.isNodeSelected(group.id, ADMIN_TREE.NODE_TYPES.GROUP));
          if (isGroupExpanded) {
            const clientsInGroup = this.getGroupClientsForDistributor(distributorId, vendor.id, group.id);
            groupChildren.push(...clientsInGroup.map((client) => this.adminTreeService.createClientNode(client, client.id === selectedClient?.id)));
            const groupPagination = this.adminState.getDistributorPaginationState(distributorId, vendor.id, group.id);
            if (groupPagination?.hasMore === true) {
              groupChildren.push(this.createLoadMoreNode(vendor.id, group.id, groupPagination?.loadingMore ?? false));
            }
          }
          return groupNode;
        }));
      }
      children.push(...vendorClients.map((client) => this.adminTreeService.createClientNode(client, client.id === selectedClient?.id)));
      const vendorPagination = this.adminState.getDistributorPaginationState(distributorId, vendor.id);
      if (vendorPagination?.hasMore === true) {
        children.push(this.createLoadMoreNode(vendor.id, void 0, vendorPagination?.loadingMore ?? false));
      }
    }
    return vendorNode;
  }
  /**
   * Helper method to get group clients for a specific distributor
   */
  getGroupClientsForDistributor(distributorId, vendorId, groupId) {
    return this.adminState.getGroupClients(distributorId, vendorId, groupId);
  }
  applySearchEnhancements(nodes, searchExpandedNodes) {
    return nodes.map((node) => this.enhanceNodeForSearch(node, searchExpandedNodes));
  }
  enhanceNodeForSearch(node, searchExpandedNodes) {
    const enhanced = __spreadValues({}, node);
    if (this.adminSearchService.isNodeHighlighted(node.key)) {
      enhanced.styleClass = [enhanced.styleClass, "search-highlighted"].filter(Boolean).join(" ");
    }
    if (searchExpandedNodes.has(node.key)) {
      enhanced.expanded = true;
    }
    if (enhanced.children) {
      enhanced.children = enhanced.children.map((child) => this.enhanceNodeForSearch(child, searchExpandedNodes));
    }
    return enhanced;
  }
  /**
   * Helper method to check if a node is currently selected
   * @param key - The node key to check
   * @param type - The node type to check
   * @returns boolean indicating if node is selected
   */
  isNodeSelected(key, type) {
    return this.adminState.selectedNodeKey() === key && this.adminState.selectedNodeType() === type;
  }
  static \u0275fac = function AdminTreeManagementService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeManagementService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeManagementService, factory: _AdminTreeManagementService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeManagementService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/admin-data.service.ts
var AdminDataService = class _AdminDataService {
  adminTreeService = inject(AdminTreeService);
  adminState = inject(AdminStateService);
  adminSearchService = inject(AdminSearchService);
  messageService = inject(MessageService);
  localStorageService = inject(LocalStorageService);
  authDataService = inject(AuthDataService);
  apiSchemaService = inject(ApiSchemaService);
  async loadClient(routeClientId) {
    const savedClientId = this.localStorageService.getItem(LOCAL_STORAGE_KEY_SELECTED_CLIENT, true)?.id;
    const defaultClientId = this.authDataService.getUserProfile()?.defaultClient?.id;
    const clientIdToLoad = routeClientId ?? savedClientId ?? defaultClientId;
    if (isNullOrUndefined(clientIdToLoad)) {
      const permissionLevel = this.adminState.userPermissionLevel();
      if (permissionLevel === UserPermissionLevel.CLIENT_ONLY) {
        this.messageService.add({
          severity: "error",
          summary: "Configuration Error",
          detail: "CLIENT_ONLY user has no default client assigned. Contact your administrator."
        });
      } else {
        this.messageService.add({
          severity: "info",
          summary: "No Client Selected",
          detail: "Displaying data at your permission level."
        });
      }
      return;
    }
    try {
      const client = await this.adminTreeService.tryLoadClient(clientIdToLoad, defaultClientId);
      this.adminState.setSelectedClient(client);
      if (isNullOrUndefined(client)) {
        this.messageService.add({
          severity: "warn",
          summary: "Warning",
          detail: "No client found for the user"
        });
      }
    } catch (error) {
      this.messageService.add({
        severity: "error",
        summary: "Error",
        detail: `An error occurred while initializing the client:  ${error instanceof Error ? error.message : "Unknown error"}`
      });
    }
  }
  /**
   * Loads vendor data for the given owner ID.
   */
  async loadVendorDataById(vendorId) {
    try {
      const vendor = await this.adminTreeService.fetchClientVendor(vendorId);
      if (isDefined(vendor?.id)) {
        this.adminState.setClientVendor(vendor);
        return vendor;
      }
      return null;
    } catch (error) {
      if (error instanceof HttpErrorResponse && (error.status === 404 || error.status === 403)) {
        console.warn(`Vendor ${vendorId} not found or access denied (${error.status})`);
        return null;
      }
      console.error(`Unexpected error loading vendor ${vendorId}:`, error);
      throw error;
    }
  }
  /**
   * Loads distributor data for the given vendor.
   */
  async loadDistributorDataById(distributorId) {
    try {
      const distributor = await this.adminTreeService.fetchVendorDistributor(distributorId);
      if (isDefined(distributor)) {
        this.adminState.setVendorDistributor(distributor);
        this.adminState.setExpandedDistributor(distributor.id, true);
        const vendors = await this.adminTreeService.listDistributorVendors(distributor.id);
        this.adminState.setDistributorVendors(distributor.id, vendors);
        return distributor;
      }
      return null;
    } catch (error) {
      if (error instanceof HttpErrorResponse && (error.status === 404 || error.status === 403)) {
        console.warn(`Distributor ${distributorId} not found or access denied (${error.status})`);
        return null;
      }
      console.error(`Unexpected error loading distributor ${distributorId}:`, error);
      throw error;
    }
  }
  /**
   * Loads super admin data (all distributors).
   */
  async loadSuperAdminData() {
    const usesModernSchema = this.apiSchemaService.usesModernSchema();
    if (!usesModernSchema)
      return;
    try {
      const distributors = await this.adminTreeService.listDistributors(DEFAULT_SYSTEM_ID);
      this.adminState.setDistributors(distributors);
    } catch (error) {
      console.error("Error loading distributors for super admin:", error);
    }
  }
  /**
   * Loads vendor clients and groups for initial data load.
   */
  async loadInitialVendorClientsAndGroups(vendor) {
    const [vendorClientResponse, groups] = await Promise.all([
      this.adminTreeService.listVendorClients(vendor.id, void 0, 0, DEFAULT_LIST_SIZE),
      this.adminTreeService.listCompanyGroups(vendor.id).catch((error) => {
        if (error instanceof HttpErrorResponse && error.status === 403) {
          console.warn("Access denied to vendor groups (403) - continuing with direct clients only");
        } else {
          console.error("Error loading groups:", error);
        }
        return [];
      })
    ]);
    this.adminState.addDistributorVendorClients(vendor.owner.id, vendor.id, vendorClientResponse.clients, {
      offset: 0,
      limit: DEFAULT_LIST_SIZE,
      total: vendorClientResponse.total,
      hasMore: vendorClientResponse.hasMore
    }, false);
    this.adminState.setDistributorVendorGroups(vendor.owner.id, vendor.id, groups);
  }
  /**
   * Loads group clients if the selected client belongs to a group.
   */
  async loadInitialGroupClients(vendor, selectedClient) {
    if (!isDefined(selectedClient.group)) {
      return;
    }
    try {
      const groupClientResponse = await this.adminTreeService.listVendorClients(vendor.id, selectedClient.group, 0, DEFAULT_LIST_SIZE);
      this.adminState.addGroupClients(vendor.owner.id, vendor.id, selectedClient.group, groupClientResponse.clients, {
        offset: 0,
        limit: DEFAULT_LIST_SIZE,
        total: groupClientResponse.total,
        hasMore: groupClientResponse.hasMore
      }, false);
      this.adminState.setDistributorExpandedGroup(vendor.owner.id, selectedClient.group, true);
    } catch (error) {
      console.error("Error loading group clients:", error);
    }
  }
  /**
   * Maps owner type to permission level.
   */
  getPermissionLevelFromOwnerType(ownerType) {
    switch (ownerType) {
      case "system":
        return UserPermissionLevel.SUPER_ADMIN;
      case "distributor":
        return UserPermissionLevel.DISTRIBUTOR;
      case "vendor":
        return UserPermissionLevel.VENDOR;
      case "client":
        return UserPermissionLevel.CLIENT_ONLY;
      default:
        return UserPermissionLevel.CLIENT_ONLY;
    }
  }
  /**
   * Loads the initial hierarchical data for the administration tree.
   * Uses ownerType from UserProfile to determine access level and permission boundaries.
   *
   * **Permission Level Guarantees:**
   * - SUPER_ADMIN: Loads all distributors; doesn't require a specific client to be set
   * - DISTRIBUTOR: Loads self and vendors; fallback to permission-level-only if no client selected
   * - VENDOR: Loads self and clients; fallback to permission-level-only if no client selected
   * - CLIENT_ONLY: Must load a specific client; if none available, displays warning and shows empty tree
   *
   * If loadClient fails to retrieve a client (and one is expected), loadDataByPermissionLevelOnly
   * is called as a graceful fallback.
   *
   * @param routeClientId Optional client ID from the route parameters
   * @returns Promise<void> Resolves when all accessible data is loaded
   */
  async loadInitialData(routeClientId) {
    this.adminState.setInitialLoading(true);
    try {
      const ownerType = this.authDataService.getUserOwnerType();
      const permissionLevel = this.getPermissionLevelFromOwnerType(ownerType);
      this.adminState.setUserPermissionLevel(permissionLevel);
      await this.loadClient(routeClientId);
      const selectedClient = this.adminState.selectedClient();
      if (isNullOrUndefined(selectedClient)) {
        await this.loadDataByPermissionLevelOnly(permissionLevel);
        return;
      }
      await this.loadHierarchyFromClient(selectedClient, permissionLevel);
    } catch (error) {
      this.handleInitialDataError(error);
    } finally {
      this.adminState.setInitialLoading(false);
      this.preloadSearchDataInBackground();
    }
  }
  /**
   * Loads data when no specific client is selected, based only on permission level.
   * Note: For CLIENT_ONLY users, this scenario indicates the client should have been
   * loaded in loadClient(). If we reach here, it's a configuration issue.
   */
  async loadDataByPermissionLevelOnly(permissionLevel) {
    const ownerId = this.authDataService.getUserOwnerId();
    if (isNullOrUndefined(ownerId) && permissionLevel !== UserPermissionLevel.SUPER_ADMIN)
      return;
    switch (permissionLevel) {
      case UserPermissionLevel.SUPER_ADMIN:
        await this.loadSuperAdminData();
        break;
      case UserPermissionLevel.DISTRIBUTOR:
        if (isDefined(ownerId))
          await this.loadDistributorDataById(ownerId);
        break;
      case UserPermissionLevel.VENDOR:
        if (isDefined(ownerId))
          await this.loadVendorDataById(ownerId);
        break;
      case UserPermissionLevel.CLIENT_ONLY:
        this.messageService.add({
          severity: "warn",
          summary: "No Client Available",
          detail: "Unable to load a client. Contact your administrator."
        });
        break;
    }
  }
  /**
   * Loads the full hierarchy path for a chosen client based on permission level
   */
  async loadHierarchyFromClient(client, permissionLevel) {
    const vendorId = client.owner?.id;
    if (permissionLevel === UserPermissionLevel.CLIENT_ONLY || isNullOrUndefined(vendorId)) {
      this.adminState.setFallbackToClient(client);
      return;
    }
    try {
      const vendor = await this.loadVendorDataById(vendorId);
      if (!vendor) {
        this.adminState.setFallbackToClient(client);
        return;
      }
      await this.setupVendorLevelContext(vendor, client);
      if (permissionLevel === UserPermissionLevel.VENDOR) {
        this.adminState.setFallbackToVendor(vendor);
        return;
      }
      const distributorId = vendor.owner?.id;
      if (!distributorId)
        return;
      const distributor = await this.loadDistributorDataById(distributorId);
      if (!distributor && permissionLevel !== UserPermissionLevel.SUPER_ADMIN) {
        this.adminState.setFallbackToVendor(vendor);
        return;
      }
      if (permissionLevel === UserPermissionLevel.SUPER_ADMIN) {
        await this.loadSuperAdminData();
      }
    } catch (error) {
      this.messageService.add({
        severity: "error",
        summary: "Hierarchy Load Failed",
        detail: `Unable to load administrative hierarchy: ${error instanceof Error ? error.message : "Unknown error"}`
      });
      this.adminState.setFallbackToClient(client);
    }
  }
  /**
   * Sets up vendor-level data including clients, groups, and initial expansion
   */
  async setupVendorLevelContext(vendor, selectedClient) {
    const distributorId = vendor.owner?.id ?? "none";
    this.adminState.markDistributorVendorAsLoaded(distributorId, vendor.id);
    this.adminState.setDistributorNodeExpanded(distributorId, vendor.id, true);
    await this.loadInitialVendorClientsAndGroups(vendor);
    await this.loadInitialGroupClients(vendor, selectedClient);
  }
  /**
   * Handles errors during initial data loading
   */
  handleInitialDataError(error) {
    if (!(error instanceof HttpErrorResponse && error?.status === 403)) {
      this.messageService.add({
        severity: "error",
        summary: "Error",
        detail: `Failed to load initial data:  ${error instanceof Error ? error.message : "Unknown error"}`
      });
    }
  }
  /**
   * Preloads search data in the background to improve search performance
   * This method runs asynchronously and doesn't block the UI
   */
  preloadSearchDataInBackground() {
    this.adminSearchService.preloadSearchData().catch((error) => {
      console.error("Error preloading search data:", error);
    });
  }
  /**
   * Checks if the user has permission to access vendor-level data.
   * This is a guard method that can be used before attempting to fetch vendor data.
   *
   * @returns boolean indicating if vendor-level access is likely available
   */
  canAccessVendorLevel() {
    const permissionLevel = this.adminState.userPermissionLevel();
    return permissionLevel === UserPermissionLevel.VENDOR || permissionLevel === UserPermissionLevel.DISTRIBUTOR || permissionLevel === UserPermissionLevel.SUPER_ADMIN;
  }
  /**
   * Checks if the user has permission to access distributor-level data.
   * This is a guard method that can be used before attempting to fetch distributor data.
   *
   * @returns boolean indicating if distributor-level access is likely available
   */
  canAccessDistributorLevel() {
    const permissionLevel = this.adminState.userPermissionLevel();
    return permissionLevel === UserPermissionLevel.DISTRIBUTOR || permissionLevel === UserPermissionLevel.SUPER_ADMIN;
  }
  /**
   * Checks if the user has permission to access super admin data.
   * This is a guard method that can be used before attempting to fetch super admin data.
   *
   * @returns boolean indicating if super admin access is likely available
   */
  canAccessSuperAdminLevel() {
    return this.adminState.userPermissionLevel() === UserPermissionLevel.SUPER_ADMIN && this.authDataService.isSuperAdmin() && this.apiSchemaService.usesModernSchema();
  }
  /**
   * Expands a tree node and loads its children data.
   * For vendor nodes, loads company groups and clients.
   * For group nodes, loads the associated clients.
   *
   * @param node - The TreeNode to expand
   * @returns Promise<void> Resolves when child data is loaded
   * @throws Displays error message via MessageService if loading fails
   */
  async expandVendorNode(node) {
    if (isNullOrUndefined(node.data?.id) || node.type !== ADMIN_TREE.NODE_TYPES.VENDOR)
      return;
    const vendorId = node.data.id;
    const distributorId = this.getDistributorContextFromNode(node);
    if (isNullOrUndefined(distributorId)) {
      console.error("No distributor context found for vendor node");
      return;
    }
    const distributorLoadedVendors = this.adminState.loadedVendors()[distributorId];
    const isAlreadyLoaded = isDefined(distributorLoadedVendors) && distributorLoadedVendors.has(vendorId) === true;
    const isAlreadyExpanded = this.adminState.isDistributorNodeExpanded(distributorId, vendorId);
    if (isAlreadyLoaded && isAlreadyExpanded) {
      return;
    }
    if (isAlreadyLoaded && !isAlreadyExpanded) {
      this.adminState.setDistributorNodeExpanded(distributorId, vendorId, true);
      return;
    }
    try {
      this.adminState.setDistributorNodeLoading(distributorId, vendorId);
      this.adminState.markDistributorVendorAsLoaded(distributorId, vendorId);
      this.adminState.setDistributorNodeExpanded(distributorId, vendorId, true);
      const [groups, clients] = await Promise.all([
        this.adminTreeService.listCompanyGroups(vendorId),
        this.adminTreeService.listVendorClients(vendorId, void 0, 0, DEFAULT_LIST_SIZE)
      ]);
      this.adminState.setDistributorVendorGroups(distributorId, vendorId, groups);
      this.adminState.addDistributorVendorClients(distributorId, vendorId, clients.clients, {
        offset: 0,
        limit: DEFAULT_LIST_SIZE,
        total: clients.total,
        hasMore: clients.hasMore
      });
    } catch (error) {
      this.adminState.removeDistributorLoadedVendor(distributorId, vendorId);
      this.adminState.markDistributorVendorAsFailedToLoad(distributorId, vendorId);
      this.messageService.add({
        severity: "error",
        summary: "Error",
        detail: `Failed to load vendor data: ${error instanceof Error ? error.message : "Unknown error"}`
      });
    } finally {
      this.adminState.clearDistributorNodeLoading(distributorId, vendorId);
    }
  }
  /**
   * Helper method to get distributor context from a tree node by walking up the tree
   * @param node - The tree node to get context from
   * @returns The distributor ID or null if not found
   */
  getDistributorContextFromNode(node) {
    if (node.type === ADMIN_TREE.NODE_TYPES.VENDOR && isDefined(node.data?.owner?.id)) {
      return node.data.owner.id;
    }
    let current = node.parent;
    while (current) {
      if (current.type === ADMIN_TREE.NODE_TYPES.DISTRIBUTOR) {
        return current.data?.id ?? null;
      }
      current = current.parent;
    }
    return this.adminState.getActiveContextId();
  }
  /**
   * Expands a group node and loads its associated clients.
   *
   * @param groupNode - The group TreeNode to expand
   * @returns Promise<void> Resolves when client data is loaded
   * @throws Displays error message via MessageService if loading fails
   */
  async expandGroupNode(groupNode) {
    if (isNullOrUndefined(groupNode.data?.id) || isNullOrUndefined(groupNode.parent?.data?.id))
      return;
    const groupId = groupNode.data.id;
    const vendorId = groupNode.parent.data.id;
    const distributorId = this.getDistributorContextFromNode(groupNode);
    if (isNullOrUndefined(distributorId)) {
      console.error("No distributor context found for group node");
      return;
    }
    const paginationKey = this.adminState.getDistributorPaginationKey(distributorId, vendorId, groupId);
    const distributorPagination = this.adminState.clientPagination()[distributorId];
    if (isDefined(distributorPagination?.[paginationKey])) {
      this.adminState.setDistributorExpandedGroup(distributorId, groupId, true);
      return;
    }
    try {
      const clients = await this.adminTreeService.listVendorClients(vendorId, groupId, 0, DEFAULT_LIST_SIZE);
      this.adminState.addGroupClients(distributorId, vendorId, groupId, clients.clients, {
        offset: 0,
        limit: DEFAULT_LIST_SIZE,
        total: clients.total,
        hasMore: clients.hasMore
      });
      this.adminState.setDistributorExpandedGroup(distributorId, groupId, true);
    } catch (error) {
      this.messageService.add({
        severity: "error",
        summary: "Error",
        detail: `Failed to load group clients: ${error instanceof Error ? error.message : "Unknown error"}`
      });
      this.adminState.setDistributorExpandedGroup(distributorId, groupId, false);
    }
  }
  async expandDistributorNode(distributorNode) {
    const distributorId = distributorNode.data.id;
    if (isNullOrUndefined(distributorId))
      return;
    try {
      const vendors = await this.adminTreeService.listDistributorVendors(distributorId);
      this.adminState.setDistributorVendors(distributorId, vendors);
      this.adminState.setExpandedDistributor(distributorId, true);
    } catch (error) {
      this.messageService.add({
        severity: "error",
        summary: "Error",
        detail: `Failed to load distributor vendors: ${error instanceof Error ? error.message : "Unknown error"}`
      });
    }
  }
  collapseVendorNode(node) {
    const contextId = this.adminState.getActiveContextId();
    if (isNullOrUndefined(contextId)) {
      return;
    }
    this.adminState.setDistributorNodeExpanded(contextId, node.data.id, false);
  }
  collapseGroupNode(groupNode) {
    const distributorId = this.getDistributorContextFromNode(groupNode);
    if (isDefined(distributorId)) {
      this.adminState.setDistributorExpandedGroup(distributorId, groupNode.data.id, false);
    }
  }
  collapseDistributorNode(distributorNode) {
    this.adminState.setExpandedDistributor(distributorNode.data.id, false);
  }
  /**
   * Loads the next page of clients for a given vendor or group within a distributor context.
   * @param vendorId - The ID of the vendor.
   * @param groupId - Optional group ID.
   * @param distributorId - Optional distributor ID. If not provided, will try to get from current context.
   */
  async loadMoreClients(vendorId, groupId, distributorId) {
    const contextDistributorId = distributorId ?? this.adminState.getActiveContextId();
    if (isNullOrUndefined(contextDistributorId)) {
      return;
    }
    const paginationKey = this.adminState.getDistributorPaginationKey(contextDistributorId, vendorId, groupId);
    const currentPagination = this.adminState.clientPagination()[contextDistributorId]?.[paginationKey];
    if (isNullOrUndefined(currentPagination) || isNullOrUndefined(currentPagination.hasMore) || currentPagination.loadingMore === true) {
      return;
    }
    const nextOffset = currentPagination.offset + currentPagination.limit;
    this.adminState.setLoadingMoreClients(contextDistributorId, vendorId, groupId, true);
    try {
      const clientResponse = await this.adminTreeService.listVendorClients(vendorId, groupId, nextOffset, DEFAULT_LIST_SIZE);
      if (isDefined(groupId)) {
        this.adminState.addGroupClients(contextDistributorId, vendorId, groupId, clientResponse.clients, {
          offset: nextOffset,
          limit: DEFAULT_LIST_SIZE,
          total: clientResponse.total,
          hasMore: clientResponse.hasMore
        }, true);
      } else {
        this.adminState.addDistributorVendorClients(contextDistributorId, vendorId, clientResponse.clients, {
          offset: nextOffset,
          limit: DEFAULT_LIST_SIZE,
          total: clientResponse.total,
          hasMore: clientResponse.hasMore
        }, true);
      }
    } catch (error) {
      this.messageService.add({
        severity: "error",
        summary: "Error",
        detail: `Failed to load more clients: ${error instanceof Error ? error.message : "Unknown error"}`
      });
    } finally {
      this.adminState.setLoadingMoreClients(contextDistributorId, vendorId, groupId, false);
    }
  }
  /**
   * Expands a super admin node and loads distributor data
   * @param _superAdminNode - The super admin node to expand
   */
  expandSuperAdminNode(_superAdminNode) {
  }
  /**
   * Collapses a super admin node
   * @param _superAdminNode - The super admin node to collapse
   */
  collapseSuperAdminNode(_superAdminNode) {
  }
  async expandNode(node) {
    switch (node.type) {
      case ADMIN_TREE.NODE_TYPES.VENDOR:
        await this.expandVendorNode(node);
        break;
      case ADMIN_TREE.NODE_TYPES.GROUP:
        await this.expandGroupNode(node);
        break;
      case ADMIN_TREE.NODE_TYPES.DISTRIBUTOR:
        await this.expandDistributorNode(node);
        break;
      case ADMIN_TREE.NODE_TYPES.SUPER_ADMIN:
        this.expandSuperAdminNode(node);
        break;
    }
  }
  collapseNode(node) {
    switch (node.type) {
      case ADMIN_TREE.NODE_TYPES.VENDOR:
        this.collapseVendorNode(node);
        break;
      case ADMIN_TREE.NODE_TYPES.GROUP:
        this.collapseGroupNode(node);
        break;
      case ADMIN_TREE.NODE_TYPES.DISTRIBUTOR:
        this.collapseDistributorNode(node);
        break;
      case ADMIN_TREE.NODE_TYPES.SUPER_ADMIN:
        this.collapseSuperAdminNode(node);
        break;
    }
  }
  static \u0275fac = function AdminDataService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminDataService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminDataService, factory: _AdminDataService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminDataService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/state/admin-container-state.service.ts
var AdminContainerStateService = class _AdminContainerStateService {
  localStorage = inject(LocalStorageService);
  // Container-specific state signals
  treeCollapsed = signal(true, ...ngDevMode ? [{ debugName: "treeCollapsed" }] : (
    /* istanbul ignore next */
    []
  ));
  nestedContentScrollable = signal(false, ...ngDevMode ? [{ debugName: "nestedContentScrollable" }] : (
    /* istanbul ignore next */
    []
  ));
  // UI state
  selectedItem = signal(null, ...ngDevMode ? [{ debugName: "selectedItem" }] : (
    /* istanbul ignore next */
    []
  ));
  detailsLoading = signal(false, ...ngDevMode ? [{ debugName: "detailsLoading" }] : (
    /* istanbul ignore next */
    []
  ));
  hasMoreData = signal(false, ...ngDevMode ? [{ debugName: "hasMoreData" }] : (
    /* istanbul ignore next */
    []
  ));
  loadingMore = signal(false, ...ngDevMode ? [{ debugName: "loadingMore" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    const storedTreeState = this.localStorage.getItem("admin-tree-collapsed");
    if (storedTreeState !== null) {
      this.treeCollapsed.set(storedTreeState);
    }
  }
  /**
   * Toggles the tree panel state and persists to local storage
   */
  toggleTreePanel() {
    const newState = !this.treeCollapsed();
    this.treeCollapsed.set(newState);
    this.localStorage.setItem("admin-tree-collapsed", newState);
  }
  /**
   * Sets the nested content scrollable state
   */
  setNestedContentScrollable(scrollable) {
    if (this.nestedContentScrollable() !== scrollable) {
      this.nestedContentScrollable.set(scrollable);
    }
  }
  /**
   * Resets all state to initial values
   */
  reset() {
    this.selectedItem.set(null);
    this.detailsLoading.set(false);
    this.hasMoreData.set(false);
    this.loadingMore.set(false);
  }
  static \u0275fac = function AdminContainerStateService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminContainerStateService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminContainerStateService, factory: _AdminContainerStateService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminContainerStateService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();

// src/app/features/administration/services/tree/admin-tree-selection.service.ts
var AdminTreeSelectionService = class _AdminTreeSelectionService {
  adminState = inject(AdminStateService);
  vendorService = inject(AdminTreeVendorService);
  clientDataService = inject(ClientDataService);
  /**
   * Selects a node in the tree and updates the admin state accordingly.
   * Handles different node types: SUPER_ADMIN, DISTRIBUTOR, VENDOR, GROUP, CLIENT
   *
   * @param node - The TreeNode to select
   */
  selectNode(node) {
    if (isNullOrUndefined(node.data?.id) || node.type === ADMIN_TREE.NODE_TYPES.LOAD_MORE) {
      return;
    }
    this.adminState.setSelectedNode(node.key ?? node.data?.id, node.type ?? null);
    switch (node.type) {
      case ADMIN_TREE.NODE_TYPES.SUPER_ADMIN:
        this.selectSuperAdmin(node);
        break;
      case ADMIN_TREE.NODE_TYPES.CLIENT:
        this.selectClient(node);
        break;
      case ADMIN_TREE.NODE_TYPES.VENDOR:
        this.selectVendor(node);
        break;
      case ADMIN_TREE.NODE_TYPES.GROUP:
        this.selectGroup(node);
        break;
      case ADMIN_TREE.NODE_TYPES.DISTRIBUTOR:
        this.selectDistributor(node);
        break;
      default:
        console.warn("Unknown node type for selection:", node.type);
    }
  }
  /**
   * Selects the super admin root node
   */
  selectSuperAdmin(_node) {
    this.adminState.setSelectedClient(null);
    this.adminState.setClientVendor(null);
    this.adminState.setVendorDistributor(null);
  }
  /**
   * Selects a client node
   */
  selectClient(node) {
    const clientData = node.data;
    const fullClient = this.clientDataService.getSelectedClient();
    if (fullClient?.id === clientData?.id) {
      this.adminState.setSelectedClient(fullClient);
    } else {
      this.adminState.setSelectedClient(clientData);
    }
    this.updateLineage(node);
  }
  /**
   * Selects a vendor node and sets distributor context
   */
  selectVendor(node) {
    const vendor = node.data;
    this.adminState.setClientVendor(vendor);
    this.adminState.setSelectedClient(null);
    this.updateLineage(node);
    if (!this.adminState.vendorDistributor() && isDefined(vendor.owner?.id)) {
      void this.setVendorDistributorFromOwnerId(vendor.owner.id);
    }
  }
  /**
   * Selects a group node
   */
  selectGroup(node) {
    this.adminState.setSelectedClient(null);
    this.updateLineage(node);
    const distributorId = this.getDistributorContextFromNode(node);
    if (isDefined(distributorId)) {
      this.adminState.setDistributorExpandedGroup(distributorId, node.data.id, true);
    }
  }
  /**
   * Selects a distributor node
   */
  selectDistributor(node) {
    this.adminState.setVendorDistributor(node.data);
    this.adminState.setSelectedClient(null);
    this.adminState.setClientVendor(null);
  }
  /**
   * Sets the vendor's distributor in state by fetching distributor details from the API.
   * @param distributorId - The ID of the distributor (from vendor.owner.id)
   */
  async setVendorDistributorFromOwnerId(distributorId) {
    try {
      const distributor = await this.vendorService.fetchVendorDistributor(distributorId);
      if (isDefined(distributor)) {
        this.adminState.setVendorDistributor(distributor);
      }
    } catch (error) {
      console.error("Failed to fetch vendor distributor:", error);
    }
  }
  /**
   * Helper to ensure the parent distributor/vendor signals stay populated
   * even when a specific child (like a Group or Client) is selected.
   * This maintains the active context required for API calls.
   */
  updateLineage(node) {
    let current = node.parent;
    while (current) {
      if (current.type === ADMIN_TREE.NODE_TYPES.VENDOR) {
        if (this.adminState.clientVendor()?.id !== current.data?.id) {
          this.adminState.setClientVendor(current.data);
        }
      } else if (current.type === ADMIN_TREE.NODE_TYPES.DISTRIBUTOR) {
        if (this.adminState.vendorDistributor()?.id !== current.data?.id) {
          this.adminState.setVendorDistributor(current.data);
        }
      } else if (current.type === ADMIN_TREE.NODE_TYPES.GROUP) {
        const distributorId = this.getDistributorContextFromNode(current);
        if (isDefined(distributorId)) {
          this.adminState.setDistributorExpandedGroup(distributorId, current.data.id, true);
        }
      }
      current = current.parent;
    }
  }
  /**
   * Helper method to get distributor context from a tree node by walking up the tree
   */
  getDistributorContextFromNode(node) {
    let current = node.parent;
    while (current) {
      if (current.type === ADMIN_TREE.NODE_TYPES.DISTRIBUTOR) {
        return current.data?.id ?? current.key;
      }
      current = current.parent;
    }
    return null;
  }
  static \u0275fac = function AdminTreeSelectionService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeSelectionService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeSelectionService, factory: _AdminTreeSelectionService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeSelectionService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-scroll-management.service.ts
var AdminScrollManagementService = class _AdminScrollManagementService {
  adminState = inject(AdminStateService);
  containerState = inject(AdminContainerStateService);
  treeManagementService = inject(AdminTreeManagementService);
  treeNodeService = inject(AdminTreeNodeService);
  selectionService = inject(AdminTreeSelectionService);
  // Track if the service has been initialized
  initialized = false;
  // Dependencies for scroll effects
  selectedTreeNode;
  setSelectedTreeNode;
  scrollToNodeKey = signal(null, ...ngDevMode ? [{ debugName: "scrollToNodeKey" }] : (
    /* istanbul ignore next */
    []
  ));
  suppressScrollToClient = signal(false, ...ngDevMode ? [{ debugName: "suppressScrollToClient" }] : (
    /* istanbul ignore next */
    []
  ));
  scrollToCenterNodeKey = signal(null, ...ngDevMode ? [{ debugName: "scrollToCenterNodeKey" }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * Initializes the scroll management service with required dependencies
   */
  initialize(dependencies) {
    if (this.initialized) {
      return;
    }
    this.selectedTreeNode = dependencies.selectedTreeNode;
    this.setSelectedTreeNode = dependencies.setSelectedTreeNode;
    this.setupScrollEffects();
    this.initialized = true;
  }
  /**
   * Sets up the reactive effects for scroll management
   */
  setupScrollEffects() {
    effect(() => {
      const currentSelectedClient = this.adminState.selectedClient();
      const currentTreeNode = this.selectedTreeNode();
      const suppressScroll = this.suppressScrollToClient();
      if (isDefined(currentSelectedClient) && (currentTreeNode?.key !== currentSelectedClient.id || currentTreeNode?.data?.type !== ADMIN_TREE.NODE_TYPES.CLIENT)) {
        const treeNodes = this.treeManagementService.hierarchyTree();
        if (treeNodes.length > 0) {
          const nodeToSelect = this.treeNodeService.findNodeByKey(treeNodes, currentSelectedClient.id, ADMIN_TREE.NODE_TYPES.CLIENT);
          if (nodeToSelect) {
            this.setSelectedTreeNode(nodeToSelect);
            this.selectionService.selectNode(nodeToSelect);
            if (!suppressScroll) {
              this.scrollToClient(currentSelectedClient.id);
            }
          }
        }
      }
    });
    effect(() => {
      const treeNodes = this.treeManagementService.hierarchyTree();
      const selectedClient = this.adminState.selectedClient();
      const currentTreeNode = this.selectedTreeNode();
      const suppressScroll = this.suppressScrollToClient();
      if (treeNodes.length > 0 && isDefined(selectedClient) && currentTreeNode?.key === selectedClient.id && !this.containerState.treeCollapsed() && !suppressScroll) {
        const nodeExists = this.treeNodeService.findNodeByKey(treeNodes, selectedClient.id, ADMIN_TREE.NODE_TYPES.CLIENT);
        if (nodeExists) {
          setTimeout(() => {
            if (!this.suppressScrollToClient()) {
              this.scrollToClient(selectedClient.id);
            }
          }, 200);
        }
      }
    });
  }
  /**
   * Scrolls the tree to center on the specified client node
   * @param clientId The ID of the client to scroll to
   */
  scrollToClient(clientId) {
    if (this.suppressScrollToClient()) {
      return;
    }
    if (this.scrollToNodeKey() === clientId) {
      return;
    }
    this.scrollToNodeKey.set(null);
    setTimeout(() => {
      if (!this.suppressScrollToClient()) {
        this.scrollToNodeKey.set(clientId);
        setTimeout(() => {
          if (this.scrollToNodeKey() === clientId) {
            this.scrollToNodeKey.set(null);
          }
        }, 200);
      }
    }, 100);
  }
  /**
   * Scrolls to center a specific node (for expansion visibility)
   * This is different from scrollToClient which is for selection
   */
  scrollToCenterNode(nodeKey) {
    this.scrollToCenterNodeKey.set(nodeKey);
    setTimeout(() => this.scrollToCenterNodeKey.set(null), 200);
  }
  /**
   * Temporarily suppresses automatic scrolling to selected client during user interactions
   * @param durationMs Duration in milliseconds to suppress scrolling (default: 2000ms)
   */
  temporarilySuppressScrollToClient(durationMs = 2e3) {
    this.suppressScrollToClient.set(true);
    if (durationMs === 0) {
      return;
    }
    setTimeout(() => this.suppressScrollToClient.set(false), durationMs);
  }
  /**
   * Handles scroll behavior for search result navigation
   * @param entityId The ID of the entity to scroll to
   */
  handleSearchResultScroll(entityId) {
    this.scrollToClient(entityId);
  }
  /**
   * Handles scroll behavior for node selection with optional delay
   * @param nodeKey The key of the node to scroll to
   * @param delay Optional delay in milliseconds
   */
  handleNodeSelectionScroll(nodeKey, delay = 200) {
    if (!this.containerState.treeCollapsed()) {
      setTimeout(() => {
        this.scrollToClient(nodeKey);
      }, delay);
    }
  }
  /**
   * Handles scroll behavior for client navigation with delay
   * @param clientId The ID of the client to scroll to
   * @param delay Delay in milliseconds (default: 300ms)
   */
  handleClientNavigationScroll(clientId, delay = 300) {
    setTimeout(() => {
      this.scrollToClient(clientId);
    }, delay);
  }
  static \u0275fac = function AdminScrollManagementService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminScrollManagementService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminScrollManagementService, factory: _AdminScrollManagementService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminScrollManagementService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-tree-client-loader.service.ts
var ClientLoadFailureError = class extends Error {
  constructor(message) {
    super(message);
    this.name = "ClientLoadFailureError";
  }
};
var AdminTreeClientLoaderService = class _AdminTreeClientLoaderService {
  adminSearchService = inject(AdminSearchService);
  clientService = inject(AdminTreeClientService);
  treeNodeService = inject(AdminTreeNodeService);
  scrollManagement = inject(AdminScrollManagementService);
  // Dependencies injected from event handler
  hierarchyTreeGetter;
  /**
   * Initializes the service with required dependencies
   */
  initialize(dependencies) {
    this.hierarchyTreeGetter = dependencies.hierarchyTreeGetter;
  }
  /**
   * Loads client data for tree selection
   */
  async loadClientForTree(clientId) {
    await this.fetchClientForTreeSelection(clientId);
  }
  /**
   * Waits for node to appear in tree and attempts selection
   */
  async waitAndSelect(clientId) {
    const found = await this.waitForNodeInTree(clientId);
    if (found) {
      const tree = this.hierarchyTreeGetter?.() ?? [];
      const node = this.treeNodeService.findNodeByKey(tree, clientId);
      return { found: true, node: node ?? void 0 };
    }
    return { found: false };
  }
  /**
   * Clears scroll requests for a specific client
   */
  clearScrollRequests(clientId) {
    const currentScrollClient = this.scrollManagement.scrollToNodeKey();
    if (currentScrollClient === clientId) {
      this.scrollManagement.scrollToNodeKey.set(null);
    }
  }
  /**
   * Loads a client using the existing AdminSearchService methods
   */
  async fetchClientForTreeSelection(clientId) {
    try {
      const client = await this.getClientWithOwnerInfo(clientId);
      if (client) {
        const result = await this.adminSearchService.prepareClientForTreeSelectionWithOwner(client);
        if (result === null) {
          throw new ClientLoadFailureError(`Failed to prepare client ${clientId} for tree selection`);
        }
      } else {
        const success = await this.adminSearchService.searchAndLoadClient(clientId);
        if (!success) {
          throw new ClientLoadFailureError(`Failed to search and load client ${clientId}`);
        }
      }
    } catch (error) {
      if (error instanceof ClientLoadFailureError) {
        throw error;
      }
      console.error(`Failed to load client ${clientId} using existing methods:`, error);
      throw new ClientLoadFailureError(`Failed to load client ${clientId} using existing methods`);
    }
  }
  /**
   * Gets client information with owner details
   */
  async getClientWithOwnerInfo(clientId) {
    try {
      return await this.clientService.fetchClient(clientId);
    } catch {
      return null;
    }
  }
  /**
   * Waits for a node to appear in the tree after data loading
   */
  async waitForNodeInTree(nodeId) {
    const maxAttempts = 30;
    const delayMs = 100;
    for (let i = 0; i < maxAttempts; i++) {
      const tree = this.hierarchyTreeGetter?.() ?? [];
      const node = this.treeNodeService.findNodeByKey(tree, nodeId);
      if (node) {
        return true;
      }
      await new Promise((resolve) => setTimeout(resolve, delayMs));
    }
    return false;
  }
  static \u0275fac = function AdminTreeClientLoaderService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeClientLoaderService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeClientLoaderService, factory: _AdminTreeClientLoaderService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeClientLoaderService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-tree-entity-sync.service.ts
var TIMING = {
  NODE_SELECTION_DELAY: 100
};
var AdminTreeEntitySyncService = class _AdminTreeEntitySyncService {
  broadcastService = inject(BroadcastEventMessageService);
  adminState = inject(AdminStateService);
  adminDataService = inject(AdminDataService);
  adminSearchService = inject(AdminSearchService);
  detailsService = inject(EntityDetailsService);
  accountsService = inject(AccountsService);
  navigationService = inject(AdminNavigationService);
  scrollManagement = inject(AdminScrollManagementService);
  clientLoader = inject(AdminTreeClientLoaderService);
  destroyRef = inject(DestroyRef);
  selectionService = inject(AdminTreeSelectionService);
  adminTreeService = inject(AdminTreeService);
  clientService = inject(AdminTreeClientService);
  messageService = inject(MessageService);
  // Callbacks to access tree data (set during initialization)
  hierarchyTreeGetter;
  findNodeByKeyRecursiveCallback;
  // Callbacks for parent component actions (set during initialization)
  treeCollapsed;
  toggleTreePanel;
  setEventSource;
  resetEventSource;
  selectedTreeNode;
  handleError;
  /**
   * Initialize the service with required dependencies
   */
  initialize(dependencies) {
    this.hierarchyTreeGetter = dependencies.getHierarchyTree;
    this.findNodeByKeyRecursiveCallback = dependencies.findNodeByKeyRecursive;
    this.treeCollapsed = dependencies.treeCollapsed;
    this.toggleTreePanel = dependencies.toggleTreePanel;
    this.setEventSource = dependencies.setEventSource;
    this.resetEventSource = dependencies.resetEventSource;
    this.selectedTreeNode = dependencies.selectedTreeNode;
    this.handleError = dependencies.handleError;
    this.setupBroadcastSubscriptions();
  }
  /**
   * Gets client information with owner details from ClientService
   */
  async getClientWithOwnerInfo(clientId) {
    try {
      return await this.clientService.fetchClient(clientId);
    } catch {
      return null;
    }
  }
  /**
   * Common method to navigate to a search result
   */
  async navigateToSearchResult(result) {
    const { type, id } = result;
    if (type === ADMIN_TREE.NODE_TYPES.CLIENT || type === ADMIN_TREE.NODE_TYPES.VENDOR || type === ADMIN_TREE.NODE_TYPES.DISTRIBUTOR) {
      await this.navigationService.navigateToNode(type, id);
    }
  }
  /**
   * Tries to select a node by ID in the current tree
   */
  trySelectPendingNode(nodeId) {
    const finalTree = this.hierarchyTreeGetter?.() ?? [];
    const nodeToSelect = this.findNodeByKeyRecursiveCallback?.(finalTree, nodeId);
    if (nodeToSelect) {
      this.selectedTreeNode.set(nodeToSelect);
      this.selectionService.selectNode(nodeToSelect);
      return true;
    }
    return false;
  }
  /**
   * Setup broadcast event subscriptions
   */
  setupBroadcastSubscriptions() {
    this.broadcastService.on("view-entity-in-tree").pipe(takeUntilDestroyed(this.destroyRef)).subscribe((data) => {
      const item = data?.entity;
      if (isDefined(item?.id) && item.entityType === "client") {
        this.setEventSource(EventSource.BROADCAST);
        this.handleClientInTree(item, item.owner?.id, item["group"]).catch((err) => this.handleError(err, "Error showing item in tree"));
      }
      if (isDefined(item?.id) && item.entityType === "vendor") {
        this.setEventSource(EventSource.BROADCAST);
        this.handleVendorInTree(item).catch((err) => this.handleError(err, "Error showing vendor in tree"));
      }
    });
    this.broadcastService.on("entity-updated").pipe(takeUntilDestroyed(this.destroyRef)).subscribe((data) => {
      if (isDefined(data) && typeof data === "object" && data !== null && "entityType" in data && "operation" in data && "entityId" in data) {
        const updateData = data;
        if (updateData.entityType === "client" && updateData.operation === "create" && updateData.entityId) {
          this.setEventSource(EventSource.SYSTEM);
          this.handleClientCreated(updateData.entityId).catch((err) => this.handleError(err, "Error refreshing tree after client creation"));
        }
      }
    });
  }
  /**
   * Handles the 'view-entity-in-tree' event for a client
   */
  async handleClientInTree(client, vendorId, groupId) {
    try {
      this.detailsService.closeDetailsPanel();
      if (this.treeCollapsed()) {
        this.toggleTreePanel.set(true);
      }
      await this.setDistributorContextForClient(client.id);
      const result = await this.adminSearchService.handleClientInTree(client, vendorId, groupId);
      await new Promise((resolve) => setTimeout(resolve, 200));
      const nodeFound = await this.clientLoader.waitForNodeInTree(client.id);
      if (nodeFound) {
        const tree = this.hierarchyTreeGetter?.() ?? [];
        const vendorNode = this.findNodeByKeyRecursiveCallback?.(tree, vendorId);
        if (vendorNode && vendorNode.expanded !== true) {
          await this.adminDataService.expandNode(vendorNode);
          await new Promise((resolve) => setTimeout(resolve, 150));
        }
        await this.navigateToSearchResult(result);
        const selectionSuccess = this.trySelectPendingNode(client.id);
        if (selectionSuccess) {
          this.scrollManagement.scrollToClient(client.id);
        }
      }
    } catch (err) {
      this.handleError(err, "Error showing item in tree");
    } finally {
      this.resetEventSource();
    }
  }
  /**
   * Handles the 'view-entity-in-tree' event for a vendor
   */
  async handleVendorInTree(vendor) {
    try {
      this.detailsService.closeDetailsPanel();
      if (this.treeCollapsed()) {
        this.toggleTreePanel.set(true);
      }
      const result = await this.adminSearchService.handleVendorInTree(vendor);
      await new Promise((resolve) => setTimeout(resolve, TIMING.NODE_SELECTION_DELAY));
      await this.navigateToSearchResult(result);
    } catch (err) {
      this.handleError(err, "Error showing vendor in tree");
    } finally {
      this.resetEventSource();
    }
  }
  /**
   * Ensures the correct distributor is set in admin state before loading clients.
   * This prevents loading clients from the wrong distributor context.
   *
   * Strategy:
   * 1. Check if client is already in loaded tree data (has hierarchy info)
   * 2. If not, fetch client from API to get full details including distributor
   * 3. Set the correct distributor context before pagination begins
   *
   * @param clientId The ID of the client we're trying to load
   */
  async setDistributorContextForClient(clientId) {
    const hierarchy = this.adminSearchService.findClientHierarchy(clientId);
    if (hierarchy) {
      await this.switchDistributor(hierarchy.distributorId);
      return;
    }
    try {
      const clientDetails = await this.getClientWithOwnerInfo(clientId);
      if (clientDetails && isDefined(clientDetails["distributor"]?.["id"])) {
        await this.switchDistributor(clientDetails["distributor"]["id"]);
        return;
      }
      if (isDefined(clientDetails?.owner?.id)) {
        const vendorId = clientDetails.owner.id;
        try {
          const vendorDetails = await firstValueFrom(this.accountsService.getVendor(vendorId));
          if (isDefined(vendorDetails?.owner?.id)) {
            await this.switchDistributor(vendorDetails.owner.id);
            return;
          }
        } catch (error) {
          console.warn(`Failed to fetch vendor ${vendorId} to determine distributor:`, error);
        }
      }
    } catch (error) {
      console.warn(`Could not determine distributor for client ${clientId}:`, error);
    }
  }
  /**
   * Sets the distributor in admin state if it's different from current
   */
  async switchDistributor(distributorId) {
    const currentDistributorId = this.adminState.getActiveContextId();
    if (currentDistributorId !== distributorId) {
      try {
        const distributor = await firstValueFrom(this.accountsService.getDistributor(distributorId));
        this.adminState.setVendorDistributor(distributor);
      } catch (error) {
        console.error(`Failed to fetch distributor ${distributorId}:`, error);
        throw error;
      }
    }
  }
  /**
   * Handles client creation event to refresh the tree state.
   * This method is called when a client is created, and it refreshes the relevant parts of the tree.
   */
  async handleClientCreated(clientId) {
    try {
      const client = await this.adminTreeService.tryLoadClient(clientId);
      if (!client) {
        console.warn("Could not load newly created client:", clientId);
        return null;
      }
      const vendorId = client.owner?.id;
      if (!vendorId) {
        console.warn("Client does not have a vendor ID:", clientId);
        return null;
      }
      return client;
    } catch (_error) {
      this.messageService.add({
        severity: "error",
        summary: "Error",
        detail: "Failed to refresh vendor data after client creation"
      });
      throw _error;
    }
  }
  /**
   * Manually trigger view client in tree (for programmatic use)
   */
  async viewClientInTree(client, vendorId, groupId) {
    this.setEventSource(EventSource.BROADCAST);
    await this.handleClientInTree(client, vendorId, groupId);
  }
  /**
   * Manually trigger view vendor in tree
   */
  async viewVendorInTree(vendor) {
    this.setEventSource(EventSource.BROADCAST);
    await this.handleVendorInTree(vendor);
  }
  static \u0275fac = function AdminTreeEntitySyncService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeEntitySyncService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeEntitySyncService, factory: _AdminTreeEntitySyncService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeEntitySyncService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-tree-search-navigation.service.ts
var TIMING2 = {
  NODE_SELECTION_DELAY: 100,
  NODE_MONITORING_INTERVAL: 150,
  NODE_SELECTION_TIMEOUT: 1e4
};
var AdminTreeSearchNavigationService = class _AdminTreeSearchNavigationService {
  adminState = inject(AdminStateService);
  adminSearchService = inject(AdminSearchService);
  navigationService = inject(AdminNavigationService);
  scrollManagement = inject(AdminScrollManagementService);
  selectionService = inject(AdminTreeSelectionService);
  entitySync = inject(AdminTreeEntitySyncService);
  accountsService = inject(AccountsService);
  assetsService = inject(AssetsService);
  devicesService = inject(DevicesService);
  simCardsService = inject(SimCardsService);
  messageService = inject(MessageService);
  destroyRef = inject(DestroyRef);
  // Signals for tracking search state
  searchingForNode = signal(false, ...ngDevMode ? [{ debugName: "searchingForNode" }] : (
    /* istanbul ignore next */
    []
  ));
  searchingNodeKey = signal(null, ...ngDevMode ? [{ debugName: "searchingNodeKey" }] : (
    /* istanbul ignore next */
    []
  ));
  // Pending node selection tracking
  pendingNodeSelection = null;
  activeMonitorId = null;
  constructor() {
    this.destroyRef.onDestroy(() => {
      if (this.activeMonitorId !== null) {
        clearInterval(this.activeMonitorId);
        this.activeMonitorId = null;
      }
    });
  }
  // Callbacks to access tree data (set during initialization)
  hierarchyTreeGetter;
  findNodeByKeyRecursiveCallback;
  selectedTreeNode;
  /**
   * Initialize the service with required dependencies
   */
  initialize(dependencies) {
    this.hierarchyTreeGetter = dependencies.getHierarchyTree;
    this.findNodeByKeyRecursiveCallback = dependencies.findNodeByKeyRecursive;
    this.selectedTreeNode = dependencies.selectedTreeNode;
  }
  /**
   * Navigate to a search result
   */
  async navigateToResult(result) {
    const { type, id, name, owner } = result;
    switch (type) {
      case ADMIN_TREE.NODE_TYPES.CLIENT:
      case ADMIN_TREE.NODE_TYPES.VENDOR:
      case ADMIN_TREE.NODE_TYPES.DISTRIBUTOR:
      case ADMIN_TREE.NODE_TYPES.GROUP:
      case ADMIN_TREE.NODE_TYPES.SUPER_ADMIN:
        await this.navigationService.navigateToNode(result.type, result.id);
        await this.updateAdminStateForSearchResult(result);
        await this.scheduleSelection(result.id, result.type);
        break;
      default: {
        const resolvedOwner = isDefined(owner) ? owner : await this.resolveOwner(type, id);
        if (isDefined(resolvedOwner) && isDefined(name)) {
          await this.updateAdminStateForSearchResult(resolvedOwner);
          await this.scheduleSelection(resolvedOwner.id, resolvedOwner.type);
          await this.navigateAndSelectInGrid(type, id, name, resolvedOwner);
        } else {
          console.warn(`Cannot navigate to ${type}: could not resolve owner for id ${id}.`);
          this.messageService.add({
            severity: "warn",
            summary: `Unable to navigate to ${type}`,
            detail: `Cannot navigate to ${type}: could not resolve owner for id ${id}.`
          });
        }
      }
    }
  }
  /**
   * Schedule node selection when node becomes available
   */
  async scheduleSelection(nodeId, nodeType) {
    const immediateResult = this.trySelectNode(nodeId);
    if (immediateResult) {
      return;
    }
    if (nodeType === ADMIN_TREE.NODE_TYPES.CLIENT) {
      this.searchingForNode.set(true);
      this.searchingNodeKey.set(nodeId);
      try {
        await this.adminSearchService.searchAndLoadClient(nodeId);
        setTimeout(() => {
          this.trySelectNode(nodeId);
          this.searchingForNode.set(false);
          this.searchingNodeKey.set(null);
        }, TIMING2.NODE_SELECTION_DELAY);
      } catch (error) {
        console.error(`Failed to search and load client ${nodeId}:`, error);
        this.searchingForNode.set(false);
        this.searchingNodeKey.set(null);
        try {
          const client = await this.entitySync.handleClientCreated(nodeId);
          if (isDefined(client)) {
            this.adminState.setSelectedClient(client);
          }
        } catch (loadError) {
          console.error(`Failed to load client ${nodeId} directly:`, loadError);
        }
      }
    } else {
      this.scheduleSelectionWithMonitoring(nodeId, nodeType);
    }
  }
  /**
   * Schedule node selection with periodic monitoring
   */
  scheduleSelectionWithMonitoring(nodeId, nodeType) {
    this.pendingNodeSelection = { nodeId, nodeType, timestamp: Date.now() };
    this.monitorForNodeAvailability();
  }
  /**
   * Monitor for node availability with periodic checks
   */
  monitorForNodeAvailability() {
    if (this.activeMonitorId !== null) {
      clearInterval(this.activeMonitorId);
    }
    const monitorId = setInterval(() => {
      if (!this.pendingNodeSelection) {
        clearInterval(monitorId);
        this.activeMonitorId = null;
        return;
      }
      const { nodeId } = this.pendingNodeSelection;
      const found = this.trySelectNode(nodeId);
      if (found) {
        this.pendingNodeSelection = null;
        clearInterval(monitorId);
        this.activeMonitorId = null;
      }
    }, TIMING2.NODE_MONITORING_INTERVAL);
    this.activeMonitorId = monitorId;
    setTimeout(() => {
      if (this.pendingNodeSelection) {
        const { nodeId } = this.pendingNodeSelection;
        console.warn(`Timeout: Could not find tree node for ${nodeId} after extended wait`);
        this.pendingNodeSelection = null;
      }
      if (this.activeMonitorId !== null) {
        clearInterval(this.activeMonitorId);
        this.activeMonitorId = null;
      }
    }, TIMING2.NODE_SELECTION_TIMEOUT);
  }
  /**
   * Try to select a node by ID in the current tree
   */
  trySelectNode(nodeId) {
    const finalTree = this.hierarchyTreeGetter?.() ?? [];
    const nodeToSelect = this.findNodeByKeyRecursiveCallback?.(finalTree, nodeId);
    if (nodeToSelect) {
      this.selectedTreeNode.set(nodeToSelect);
      this.selectionService.selectNode(nodeToSelect);
      this.scrollManagement.scrollToClient(nodeId);
      return true;
    }
    return false;
  }
  /**
   * Update admin state based on search result
   */
  async updateAdminStateForSearchResult(result) {
    try {
      if (result.type === ADMIN_TREE.NODE_TYPES.CLIENT) {
        const hierarchy = this.adminSearchService.findClientHierarchy(result.id);
        if (hierarchy) {
          const client = this.findClientInState(result.id, hierarchy.vendorId, hierarchy.groupId, hierarchy.distributorId);
          if (client) {
            const clientResponse = __spreadProps(__spreadValues({}, client), {
              entity: isDefined(client["entity"]) ? client["entity"] : { id: client.id, name: client.name }
            });
            this.adminState.setSelectedClient(clientResponse);
          }
        }
      } else if (result.type === ADMIN_TREE.NODE_TYPES.VENDOR) {
        const contextId = this.adminState.getActiveContextId();
        const vendor = this.adminState.getVendorById(result.id, contextId ?? void 0);
        if (isDefined(vendor)) {
          this.adminState.setSelectedClient(null);
        }
      }
    } catch (error) {
      console.error("Failed to update admin state for search result:", error);
    }
  }
  /**
   * Find a client in the admin state using distributor-scoped data
   */
  findClientInState(clientId, vendorId, groupId, distributorId) {
    const contextDistributorId = distributorId ?? this.adminState.getActiveContextId();
    if (isNullOrUndefined(contextDistributorId)) {
      return void 0;
    }
    if (isDefined(groupId)) {
      const groupClients = this.adminState.groupClients()[contextDistributorId]?.[vendorId]?.[groupId];
      return groupClients?.find((client) => client.id === clientId);
    } else {
      const vendorClients = this.adminState.vendorClients()[contextDistributorId]?.[vendorId];
      return vendorClients?.find((client) => client.id === clientId);
    }
  }
  /**
   * Navigate to entity details and select in grid
   */
  async navigateAndSelectInGrid(entityType, entityId, name, owner) {
    await this.navigationService.navigateToEntityDetails(entityType, entityId, owner);
  }
  /**
   * Fetches the owner of an entity from the API when it is absent from the search result.
   */
  async resolveOwner(type, id) {
    try {
      let entity = null;
      switch (type) {
        case "user":
          entity = await firstValueFrom(this.accountsService.getUser(id));
          break;
        case "asset":
          entity = await firstValueFrom(this.assetsService.getAsset(id));
          break;
        case "device":
          entity = await firstValueFrom(this.devicesService.getDevice(id));
          break;
        case "simcard":
          entity = await firstValueFrom(this.simCardsService.getSimCard(id));
          break;
        default:
          console.warn(`No owner fetch strategy for entity type: ${type}`);
          return null;
      }
      return entity?.owner ?? null;
    } catch (error) {
      console.error(`Failed to resolve owner for ${type} ${id}:`, error);
      return null;
    }
  }
  /**
   * Clear any pending node selections
   */
  cleanup() {
    this.pendingNodeSelection = null;
    this.searchingForNode.set(false);
    this.searchingNodeKey.set(null);
  }
  static \u0275fac = function AdminTreeSearchNavigationService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeSearchNavigationService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeSearchNavigationService, factory: _AdminTreeSearchNavigationService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeSearchNavigationService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();

// src/app/features/administration/services/admin-tree-error-handler.service.ts
var AdminTreeErrorHandlerService = class _AdminTreeErrorHandlerService {
  versionUpdateService = inject(VersionUpdateService);
  /**
   * Handles errors from admin tree operations
   * Returns true if error was a version mismatch (reload triggered)
   * Returns false if error should be handled locally
   *
   * IMPORTANT: If version mismatch detected, this method handles the reload.
   * If not a version mismatch, the error should be re-thrown or handled locally.
   */
  handleAdminTreeError(error, context) {
    console.error(`Admin tree error (${context}):`, error);
    if (this.versionUpdateService.isVersionMismatchError(error)) {
      console.log("Version mismatch detected in admin tree context:", context);
      this.versionUpdateService.handleVersionMismatch(`admin tree: ${context}`);
      return true;
    }
    console.log("Not a version mismatch, returning for local handling");
    return false;
  }
  static \u0275fac = function AdminTreeErrorHandlerService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeErrorHandlerService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeErrorHandlerService, factory: _AdminTreeErrorHandlerService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeErrorHandlerService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-tree-event-handler.service.ts
var TIMING3 = {
  SCROLL_SUPPRESSION: 2e3,
  NODE_SELECTION_DELAY: 100,
  NODE_MONITORING_INTERVAL: 150,
  NODE_SELECTION_TIMEOUT: 1e4
};
var AdminTreeEventHandlerService = class _AdminTreeEventHandlerService {
  adminDataService = inject(AdminDataService);
  adminState = inject(AdminStateService);
  adminSearchService = inject(AdminSearchService);
  navigationService = inject(AdminNavigationService);
  containerState = inject(AdminContainerStateService);
  clientLoader = inject(AdminTreeClientLoaderService);
  scrollManagement = inject(AdminScrollManagementService);
  selectionService = inject(AdminTreeSelectionService);
  entitySync = inject(AdminTreeEntitySyncService);
  searchNavigation = inject(AdminTreeSearchNavigationService);
  adminTreeErrorHandler = inject(AdminTreeErrorHandlerService);
  // Expose signals for component to use
  selectedTreeNode = signal(null, ...ngDevMode ? [{ debugName: "selectedTreeNode" }] : (
    /* istanbul ignore next */
    []
  ));
  // Tree panel state (delegated to container state)
  treeCollapsed = this.containerState.treeCollapsed;
  // Signal-based communication instead of callbacks
  toggleTreePanel = signal(false, ...ngDevMode ? [{ debugName: "toggleTreePanel" }] : (
    /* istanbul ignore next */
    []
  ));
  errorMessage = signal(null, ...ngDevMode ? [{ debugName: "errorMessage" }] : (
    /* istanbul ignore next */
    []
  ));
  // Track current event source
  currentEventSource = EventSource.USER;
  // Required dependencies that will be injected via initialization
  hierarchyTreeGetter;
  /**
   * Sets the event source for the next operation
   */
  setEventSource(source) {
    this.currentEventSource = source;
  }
  /**
   * Resets event source back to user after operation
   */
  resetEventSource() {
    this.currentEventSource = EventSource.USER;
  }
  /**
   * Checks if current event allows auto-scrolling
   */
  shouldAllowAutoScroll() {
    return this.currentEventSource !== EventSource.USER;
  }
  /**
   * Initialize the service with required dependencies
   */
  initialize(dependencies) {
    this.hierarchyTreeGetter = dependencies.getHierarchyTree;
    this.entitySync.initialize({
      getHierarchyTree: dependencies.getHierarchyTree,
      findNodeByKeyRecursive: dependencies.findNodeByKeyRecursive,
      treeCollapsed: () => this.treeCollapsed(),
      toggleTreePanel: this.toggleTreePanel,
      setEventSource: (source) => this.setEventSource(source),
      resetEventSource: () => this.resetEventSource(),
      selectedTreeNode: this.selectedTreeNode,
      handleError: (err, msg) => this.handleError(err, msg)
    });
    this.searchNavigation.initialize({
      getHierarchyTree: dependencies.getHierarchyTree,
      findNodeByKeyRecursive: dependencies.findNodeByKeyRecursive,
      selectedTreeNode: this.selectedTreeNode
    });
    this.clientLoader.initialize({
      hierarchyTreeGetter: this.hierarchyTreeGetter
    });
  }
  /**
   * Cleanup method to clear any pending operations
   */
  cleanup() {
    this.searchNavigation.cleanup();
  }
  /**
   * Handles node expansion
   */
  async onNodeExpand(node) {
    try {
      this.handleScrollSuppressionForExpansion();
      await this.adminDataService.expandNode(node);
    } catch (error) {
      this.handleError(error, "Failed to expand node");
    } finally {
      this.resetEventSource();
    }
  }
  /**
   * Handles scroll suppression logic for node expansion
   */
  handleScrollSuppressionForExpansion() {
    if (this.shouldAllowAutoScroll()) {
      this.scrollManagement.temporarilySuppressScrollToClient(TIMING3.SCROLL_SUPPRESSION);
    } else {
      this.scrollManagement.temporarilySuppressScrollToClient(0);
    }
  }
  /**
   * Handles node collapse
   */
  onNodeCollapse(node) {
    if (!isDefined(node.data?.id))
      return;
    if (this.shouldAllowAutoScroll()) {
      this.scrollManagement.temporarilySuppressScrollToClient(TIMING3.SCROLL_SUPPRESSION);
    } else {
      this.scrollManagement.temporarilySuppressScrollToClient(0);
    }
    this.adminDataService.collapseNode(node);
    this.resetEventSource();
  }
  /**
   * Handles node selection
   */
  async onNodeSelect(node) {
    this.selectedTreeNode.set(node);
    if (node.type === ADMIN_TREE.NODE_TYPES.LOAD_MORE) {
      this.scrollManagement.temporarilySuppressScrollToClient(0);
      const loadData = node.data;
      if (isDefined(loadData.vendorId)) {
        await this.adminDataService.loadMoreClients(loadData.vendorId, loadData.groupId);
      }
    } else if (isDefined(node.key) && isDefined(node.type)) {
      this.selectionService.selectNode(node);
      await this.navigationService.navigateToNode(node.type, node.key);
      if (node.type === ADMIN_TREE.NODE_TYPES.CLIENT && this.shouldAllowAutoScroll()) {
        this.scrollManagement.scrollToClient(node.key);
      }
    }
    this.resetEventSource();
  }
  /**
   * Updates tree selection programmatically without triggering navigation
   * Use this for syncing tree state with route changes or other non-user actions
   */
  setTreeSelection(node) {
    this.selectedTreeNode.set(node);
    if (node) {
      this.selectionService.selectNode(node);
    }
  }
  /**
   * Handles node unselection
   */
  onNodeUnselect(node) {
    if (this.selectedTreeNode()?.key === node.key) {
      this.selectedTreeNode.set(null);
      this.adminState.setSelectedClient(null);
    }
  }
  /**
   * Handles when a node is not found in the current tree data
   *
   * This is a critical method for handling deep links and search results where the target
   * client may not be in the currently loaded tree data. It implements several safeguards
   * to prevent infinite loops:
   *
   * 1. Duplicate Search Prevention: Checks if already searching for this node
   * 2. Delegates to AdminSearchService which has:
   *    - Maximum pagination attempts (50 attempts = 5000 clients max)
   *    - Error handling that breaks pagination loops
   *    - Fallback to global search API
   * 3. Clears scroll requests on failure to prevent retry loops
   *
   * Flow:
   * 1. Check if already searching (prevent duplicates)
   * 2. Set searching flags
   * 3. Fetch client using API with owner info
   * 4. Load parent hierarchy and paginate until client found
   * 5. Wait for node to appear in tree
   * 6. Select and scroll to node
   * 7. Clear searching flags and scroll requests
   *
   * @param event Contains the nodeKey (client ID) and optional context
   */
  onNodeNotFoundInData(event) {
    const clientId = event.nodeKey;
    if (this.searchNavigation.searchingForNode() && this.searchNavigation.searchingNodeKey() === clientId) {
      return;
    }
    this.searchNavigation.searchingForNode.set(true);
    this.searchNavigation.searchingNodeKey.set(clientId);
    this.clientLoader.loadClientForTree(clientId).then(async () => {
      const result = await this.clientLoader.waitAndSelect(clientId);
      if (result.found && result.node) {
        this.setEventSource(EventSource.SYSTEM);
        this.selectedTreeNode.set(result.node);
        this.searchNavigation.trySelectNode(clientId);
      } else {
        this.clientLoader.clearScrollRequests(clientId);
      }
    }).catch((error) => {
      this.clientLoader.clearScrollRequests(clientId);
      if (!(error instanceof Error && error.message.includes("not found"))) {
        this.handleError(error, "Failed to load client for tree");
      }
    }).finally(() => {
      this.searchNavigation.searchingForNode.set(false);
      this.searchNavigation.searchingNodeKey.set(null);
      this.resetEventSource();
    });
  }
  /**
   * Handles search query changes
   */
  onSearchQueryChange(query) {
    this.adminSearchService.updateQuery(query);
  }
  /**
   * Handles search result selection
   */
  async onSearchResultSelect(result) {
    try {
      this.setEventSource(EventSource.SEARCH);
      await this.adminSearchService.prepareResultForNavigation(result);
      await new Promise((resolve) => setTimeout(resolve, 100));
      await this.searchNavigation.navigateToResult(result);
    } catch (error) {
      this.handleError(error, "Failed to navigate to search result");
    } finally {
      this.resetEventSource();
    }
  }
  /**
   * Handles search clear
   */
  onSearchClear() {
    this.adminSearchService.clearSearch();
  }
  handleError(error, message) {
    console.error(message, error);
    const isVersionMismatch = this.adminTreeErrorHandler.handleAdminTreeError(error, message);
    if (!isVersionMismatch) {
      this.errorMessage.set({ error, message });
    }
  }
  static \u0275fac = function AdminTreeEventHandlerService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeEventHandlerService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeEventHandlerService, factory: _AdminTreeEventHandlerService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeEventHandlerService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/shared/components/tree-panel/tree-panel.component.ts
var _c0 = ["treeComponent"];
var _c1 = ["treeWrapper"];
function TreePanelComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 3)(1, "div", 5);
    \u0275\u0275element(2, "i", 6);
    \u0275\u0275elementStart(3, "span", 7);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const ctx_r0 = \u0275\u0275nextContext();
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate(ctx_r0.loadingMessage);
  }
}
var TreePanelComponent = class _TreePanelComponent {
  treeComponent;
  treeWrapper;
  pendingTimeouts = /* @__PURE__ */ new Set();
  nodes = input([], ...ngDevMode ? [{ debugName: "nodes" }] : (
    /* istanbul ignore next */
    []
  ));
  selectionMode = input("single", ...ngDevMode ? [{ debugName: "selectionMode" }] : (
    /* istanbul ignore next */
    []
  ));
  loading = input(false, ...ngDevMode ? [{ debugName: "loading" }] : (
    /* istanbul ignore next */
    []
  ));
  searchingForNode = input(false, ...ngDevMode ? [{ debugName: "searchingForNode" }] : (
    /* istanbul ignore next */
    []
  ));
  searchingNodeKey = input(null, ...ngDevMode ? [{ debugName: "searchingNodeKey" }] : (
    /* istanbul ignore next */
    []
  ));
  emptyMessage = input("No data available", ...ngDevMode ? [{ debugName: "emptyMessage" }] : (
    /* istanbul ignore next */
    []
  ));
  styleClass = input("", ...ngDevMode ? [{ debugName: "styleClass" }] : (
    /* istanbul ignore next */
    []
  ));
  selection = input(null, ...ngDevMode ? [{ debugName: "selection" }] : (
    /* istanbul ignore next */
    []
  ));
  scrollToNodeKey = input(null, ...ngDevMode ? [{ debugName: "scrollToNodeKey" }] : (
    /* istanbul ignore next */
    []
  ));
  scrollToCenterNodeKey = input(null, ...ngDevMode ? [{ debugName: "scrollToCenterNodeKey" }] : (
    /* istanbul ignore next */
    []
  ));
  contextExtractor = input(null, ...ngDevMode ? [{ debugName: "contextExtractor" }] : (
    /* istanbul ignore next */
    []
  ));
  nodeSelect = output();
  nodeUnselect = output();
  nodeExpand = output();
  nodeCollapse = output();
  nodeNotFoundInData = output();
  loadingMessage = "Loading...";
  constructor() {
    effect(() => {
      const nodeKey = this.scrollToNodeKey();
      if (nodeKey !== null && nodeKey !== "") {
        this.scheduleTimeout(() => this.scrollToNode(nodeKey), 150);
      }
    });
    effect(() => {
      const nodeKey = this.scrollToCenterNodeKey();
      if (nodeKey !== null && nodeKey !== "") {
        this.scheduleTimeout(() => this.scrollToCenterNode(nodeKey), 150);
      }
    });
  }
  ngOnDestroy() {
    for (const id of this.pendingTimeouts) {
      clearTimeout(id);
    }
    this.pendingTimeouts.clear();
  }
  scheduleTimeout(fn, delay) {
    const id = setTimeout(() => {
      this.pendingTimeouts.delete(id);
      fn();
    }, delay);
    this.pendingTimeouts.add(id);
  }
  onNodeSelect(event) {
    this.nodeSelect.emit(event.node);
  }
  onNodeUnselect(event) {
    this.nodeUnselect.emit(event.node);
  }
  onNodeExpand(event) {
    this.nodeExpand.emit(event.node);
  }
  onNodeCollapse(event) {
    this.nodeCollapse.emit(event.node);
  }
  /**
   * Scrolls to a specific node in the tree and centers it in the viewport
   * @param nodeKey The key of the node to scroll to
   */
  scrollToNode(nodeKey) {
    const targetNode = this.findNodeInData(nodeKey);
    if (targetNode === null) {
      const extractor = this.contextExtractor();
      const context = extractor?.extractContext(nodeKey, this.nodes());
      this.nodeNotFoundInData.emit({
        nodeKey,
        context
      });
      return;
    }
    this.expandPathToNode(targetNode);
    this.performOptimizedScroll(nodeKey);
  }
  /**
   * Performs optimized scrolling using PrimeNG's native methods with fallbacks
   */
  performOptimizedScroll(nodeKey) {
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        const success = this.scrollToNodeInDOM(nodeKey);
        if (!success) {
          this.scheduleTimeout(() => this.scrollToNodeInDOM(nodeKey), 300);
        }
      });
    });
  }
  /**
   * Scrolls to center a specific node (for expanded nodes visibility)
   * This is different from scrollToNode - it only centers without selecting
   * @param nodeKey The key of the node to center in the viewport
   */
  scrollToCenterNode(nodeKey) {
    const targetNode = this.findNodeInData(nodeKey);
    if (targetNode === null) {
      const extractor = this.contextExtractor();
      const context = extractor?.extractContext(nodeKey, this.nodes());
      this.nodeNotFoundInData.emit({
        nodeKey,
        context
      });
      return;
    }
    const nodeElement = this.findNodeElementInDOM(nodeKey);
    if (nodeElement) {
      this.scrollToShowExpandedChildren(nodeElement, targetNode);
    }
  }
  /**
   * Scrolls to position the expanded node and its children optimally in the viewport
   * @param nodeElement The DOM element of the parent node
   * @param targetNode The tree node data
   */
  scrollToShowExpandedChildren(nodeElement, targetNode) {
    const treeWrapper = this.treeWrapper?.nativeElement;
    if (isNullOrUndefined(treeWrapper)) {
      return;
    }
    if (isNullOrUndefined(targetNode.expanded) || !targetNode.children || targetNode.children.length === 0) {
      this.scrollWithoutVirtualScroll(nodeElement);
      return;
    }
    const elementRect = nodeElement.getBoundingClientRect();
    const wrapperRect = treeWrapper.getBoundingClientRect();
    const elementTopRelativeToWrapper = elementRect.top - wrapperRect.top;
    const wrapperHeight = treeWrapper.clientHeight;
    const offsetFromTop = wrapperHeight / 3;
    const targetScrollTop = treeWrapper.scrollTop + elementTopRelativeToWrapper - offsetFromTop;
    if (typeof treeWrapper.scrollTo === "function") {
      treeWrapper.scrollTo({
        top: Math.max(0, targetScrollTop),
        behavior: "smooth"
      });
    } else {
      treeWrapper.scrollTop = Math.max(0, targetScrollTop);
    }
    this.scheduleTimeout(() => this.highlightElement(nodeElement), 300);
  }
  /**
   * Recursively searches for a node in the tree data
   */
  findNodeInData(nodeKey) {
    const searchInNodes = (nodes) => {
      for (const node of nodes) {
        if (node.key === nodeKey || node.data?.id === nodeKey || node.label?.includes(nodeKey) === true) {
          return node;
        }
        if (node.children && node.children.length > 0) {
          const found = searchInNodes(node.children);
          if (found)
            return found;
        }
      }
      return null;
    };
    return searchInNodes(this.nodes());
  }
  /**
   * Expands all parent nodes to make the target node visible
   */
  expandPathToNode(targetNode) {
    const pathToExpand = [];
    const findPath = (nodes, target, path) => {
      for (const node of nodes) {
        const currentPath = [...path, node];
        if (node === target) {
          pathToExpand.push(...path);
          return true;
        }
        if (node.children && node.children.length > 0) {
          if (findPath(node.children, target, currentPath)) {
            return true;
          }
        }
      }
      return false;
    };
    findPath(this.nodes(), targetNode, []);
    for (const nodeToExpand of pathToExpand) {
      if (nodeToExpand.expanded !== true) {
        nodeToExpand.expanded = true;
        this.nodeExpand.emit(nodeToExpand);
      }
    }
  }
  /**
   * Scrolls to a node using the appropriate method based on virtual scroll setting
   * @returns true if successful, false otherwise
   */
  scrollToNodeInDOM(nodeKey) {
    const treeComponent = this.treeComponent;
    if (isNullOrUndefined(treeComponent)) {
      return false;
    }
    try {
      const nodeData = this.findNodeInData(nodeKey);
      if (isNullOrUndefined(nodeData)) {
        return false;
      }
      const nodeElement = this.findNodeElementInDOM(nodeKey);
      if (isNullOrUndefined(nodeElement)) {
        return this.tryAlternativeScrollMethods(nodeKey);
      }
      if (treeComponent.virtualScroll === true) {
        return this.scrollWithVirtualScroll(treeComponent, nodeElement, nodeKey);
      } else {
        return this.scrollWithoutVirtualScroll(nodeElement);
      }
    } catch (error) {
      console.error("Error scrolling to tree node:", error);
      return false;
    }
  }
  /**
   * Scrolls using virtual scroll method
   */
  scrollWithVirtualScroll(treeComponent, nodeElement, _nodeKey) {
    const treeContainer = treeComponent.el?.nativeElement;
    if (isNullOrUndefined(treeContainer)) {
      return false;
    }
    const elementRect = nodeElement.getBoundingClientRect();
    const containerRect = treeContainer.getBoundingClientRect();
    const elementTopRelativeToContainer = elementRect.top - containerRect.top;
    const containerHeight = treeContainer.clientHeight;
    const elementHeight = elementRect.height;
    const centerOffset = (containerHeight - elementHeight) / 2;
    const targetScrollTop = treeContainer.scrollTop + elementTopRelativeToContainer - centerOffset;
    treeComponent.scrollTo({
      top: Math.max(0, targetScrollTop),
      behavior: "smooth"
    });
    this.scheduleTimeout(() => this.highlightElement(nodeElement), 300);
    return true;
  }
  /**
   * Scrolls without virtual scroll using wrapper element
   */
  scrollWithoutVirtualScroll(nodeElement) {
    const treeWrapper = this.treeWrapper?.nativeElement;
    if (isNullOrUndefined(treeWrapper)) {
      return false;
    }
    const elementRect = nodeElement.getBoundingClientRect();
    const wrapperRect = treeWrapper.getBoundingClientRect();
    const elementTopRelativeToWrapper = elementRect.top - wrapperRect.top;
    const wrapperHeight = treeWrapper.clientHeight;
    const elementHeight = elementRect.height;
    const centerOffset = (wrapperHeight - elementHeight) / 2;
    const targetScrollTop = treeWrapper.scrollTop + elementTopRelativeToWrapper - centerOffset;
    if (typeof treeWrapper.scrollTo === "function") {
      treeWrapper.scrollTo({
        top: Math.max(0, targetScrollTop),
        behavior: "smooth"
      });
    } else {
      treeWrapper.scrollTop = Math.max(0, targetScrollTop);
    }
    this.scheduleTimeout(() => this.highlightElement(nodeElement), 300);
    return true;
  }
  /**
   * Try alternative scroll methods when primary methods fail
   */
  tryAlternativeScrollMethods(nodeKey) {
    const treeComponent = this.treeComponent;
    if (treeComponent?.virtualScroll === true) {
      const nodeIndex = this.findNodeIndex(nodeKey);
      if (nodeIndex !== -1) {
        treeComponent.scrollToVirtualIndex(nodeIndex);
        return true;
      }
    }
    return this.fallbackDOMScroll(nodeKey);
  }
  /**
   * Finds the DOM element for a specific node key
   */
  findNodeElementInDOM(nodeKey) {
    const treeWrapper = this.treeWrapper?.nativeElement;
    if (isNullOrUndefined(treeWrapper)) {
      return null;
    }
    return this.findNodeElement(treeWrapper, nodeKey);
  }
  /**
   * Finds the index of a node in the flattened tree structure
   */
  findNodeIndex(nodeKey) {
    let index = 0;
    const searchInNodes = (nodes) => {
      for (const node of nodes) {
        if (node.key === nodeKey || node.data?.id === nodeKey || node.label?.includes(nodeKey) === true) {
          return index;
        }
        index++;
        if (node.children && node.children.length > 0 && node.expanded === true) {
          const foundIndex = searchInNodes(node.children);
          if (foundIndex !== -1) {
            return foundIndex;
          }
        }
      }
      return -1;
    };
    return searchInNodes(this.nodes());
  }
  /**
   * Fallback DOM-based scrolling method
   */
  fallbackDOMScroll(nodeKey) {
    const treeWrapper = this.treeWrapper?.nativeElement;
    if (isNullOrUndefined(treeWrapper)) {
      return false;
    }
    try {
      const nodeElement = this.findNodeElement(treeWrapper, nodeKey);
      if (nodeElement === null) {
        return false;
      }
      const elementRect = nodeElement.getBoundingClientRect();
      const wrapperRect = treeWrapper.getBoundingClientRect();
      const elementTopRelativeToWrapper = elementRect.top - wrapperRect.top;
      const wrapperHeight = treeWrapper.clientHeight;
      const elementHeight = elementRect.height;
      const centerOffset = (wrapperHeight - elementHeight) / 2;
      const targetScrollTop = treeWrapper.scrollTop + elementTopRelativeToWrapper - centerOffset;
      if (typeof treeWrapper.scrollTo === "function") {
        treeWrapper.scrollTo({
          top: Math.max(0, targetScrollTop),
          behavior: "smooth"
        });
      } else {
        treeWrapper.scrollTop = Math.max(0, targetScrollTop);
      }
      this.highlightElement(nodeElement);
      return true;
    } catch (error) {
      console.error("Error in fallback DOM scroll:", error);
      return false;
    }
  }
  findNodeElement(treeElement, nodeKey) {
    let nodeElement = this.findBySelectors(treeElement, nodeKey);
    nodeElement ??= this.findByNodeSearch(treeElement, nodeKey);
    nodeElement ??= this.findByContentSearch(treeElement, nodeKey);
    return nodeElement;
  }
  findBySelectors(treeElement, nodeKey) {
    const selectors = [
      `[data-pc-section="node"][aria-label*="${nodeKey}"]`,
      `.p-treenode[data-key="${nodeKey}"]`,
      `[data-id="${nodeKey}"]`,
      `[aria-label*="${nodeKey}"]`
    ];
    for (const selector of selectors) {
      const element = treeElement.querySelector(selector);
      if (element !== null) {
        return element;
      }
    }
    return null;
  }
  findByNodeSearch(treeElement, nodeKey) {
    const allNodes = treeElement.querySelectorAll('.p-treenode, .p-treenode-content, [role="treeitem"]');
    for (const node of Array.from(allNodes)) {
      const htmlNode = node;
      if (this.isMatchingNode(htmlNode, nodeKey)) {
        return htmlNode;
      }
    }
    return null;
  }
  findByContentSearch(treeElement, nodeKey) {
    const contentNodes = treeElement.querySelectorAll(".p-treenode-label, .p-treenode-content");
    for (const node of Array.from(contentNodes)) {
      const htmlNode = node;
      const textContent = htmlNode.textContent;
      if (textContent?.includes(nodeKey) === true) {
        const treeNode = htmlNode.closest(".p-treenode");
        if (treeNode !== null) {
          return treeNode;
        }
      }
    }
    return null;
  }
  isMatchingNode(htmlNode, nodeKey) {
    const dataKey = htmlNode.getAttribute("data-key");
    const ariaLabel = htmlNode.getAttribute("aria-label");
    const textContent = htmlNode.textContent?.trim();
    return dataKey === nodeKey || ariaLabel?.includes(nodeKey) === true || textContent?.includes(nodeKey) === true || htmlNode.querySelector(`[data-key="${nodeKey}"]`) !== null;
  }
  scrollToElement(treeElement, nodeElement) {
    const treeRect = treeElement.getBoundingClientRect();
    const nodeRect = nodeElement.getBoundingClientRect();
    const treeScrollTop = treeElement.scrollTop;
    const nodeOffsetTop = nodeRect.top - treeRect.top + treeScrollTop;
    const centerPosition = nodeOffsetTop - treeRect.height / 2 + nodeRect.height / 2;
    treeElement.scrollTo({
      top: Math.max(0, centerPosition),
      behavior: "smooth"
    });
  }
  highlightElement(nodeElement) {
    nodeElement.style.transition = "background-color 0.3s ease";
    nodeElement.style.backgroundColor = "rgba(59, 130, 246, 0.1)";
    this.scheduleTimeout(() => {
      nodeElement.style.backgroundColor = "";
      this.scheduleTimeout(() => {
        nodeElement.style.transition = "";
      }, 300);
    }, 1500);
  }
  /**
   * Alternative method to find nodes using PrimeNG Tree component properties
   */
  findNodeUsingPrimeNGTree(nodeKey) {
    if (isNullOrUndefined(this.treeComponent)) {
      return null;
    }
    try {
      const treeEl = this.treeComponent.el?.nativeElement;
      if (isNullOrUndefined(treeEl)) {
        return null;
      }
      const strategies = [
        () => treeEl.querySelector(`[data-key="${nodeKey}"]`),
        () => treeEl.querySelector(`[aria-label*="${nodeKey}"]`),
        () => {
          const allItems = treeEl.querySelectorAll('[role="treeitem"], .p-treenode');
          for (const item of Array.from(allItems)) {
            const element = item;
            if (element.textContent?.includes(nodeKey) === true) {
              return element;
            }
          }
          return null;
        }
      ];
      for (const element of strategies) {
        const result = element();
        if (result !== null) {
          return result;
        }
      }
      return null;
    } catch (error) {
      console.error("Error in findNodeUsingPrimeNGTree:", error);
      return null;
    }
  }
  static \u0275fac = function TreePanelComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _TreePanelComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _TreePanelComponent, selectors: [["app-tree-panel"]], viewQuery: function TreePanelComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuery(_c0, 5)(_c1, 5);
    }
    if (rf & 2) {
      let _t;
      \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.treeComponent = _t.first);
      \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.treeWrapper = _t.first);
    }
  }, inputs: { nodes: [1, "nodes"], selectionMode: [1, "selectionMode"], loading: [1, "loading"], searchingForNode: [1, "searchingForNode"], searchingNodeKey: [1, "searchingNodeKey"], emptyMessage: [1, "emptyMessage"], styleClass: [1, "styleClass"], selection: [1, "selection"], scrollToNodeKey: [1, "scrollToNodeKey"], scrollToCenterNodeKey: [1, "scrollToCenterNodeKey"], contextExtractor: [1, "contextExtractor"] }, outputs: { nodeSelect: "nodeSelect", nodeUnselect: "nodeUnselect", nodeExpand: "nodeExpand", nodeCollapse: "nodeCollapse", nodeNotFoundInData: "nodeNotFoundInData" }, decls: 5, vars: 7, consts: [["treeWrapper", ""], ["treeComponent", ""], [1, "tree-panel-wrapper", "h-full", "overflow-auto", "relative"], [1, "node-search-overlay", "absolute", "inset-0", "bg-white", "bg-opacity-75", "flex", "items-center", "justify-center", "z-10"], [3, "onNodeSelect", "onNodeUnselect", "onNodeExpand", "onNodeCollapse", "value", "selectionMode", "selection", "loading", "emptyMessage", "styleClass"], [1, "node-search-message", "flex", "items-center", "space-x-2", "bg-white", "px-4", "py-2", "rounded-lg", "border"], [1, "pi", "pi-spin", "pi-spinner", "text-blue-500"], [1, "text-sm", "text-gray-700"]], template: function TreePanelComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "div", 2, 0);
      \u0275\u0275conditionalCreate(2, TreePanelComponent_Conditional_2_Template, 5, 1, "div", 3);
      \u0275\u0275elementStart(3, "p-tree", 4, 1);
      \u0275\u0275listener("onNodeSelect", function TreePanelComponent_Template_p_tree_onNodeSelect_3_listener($event) {
        return ctx.onNodeSelect($event);
      })("onNodeUnselect", function TreePanelComponent_Template_p_tree_onNodeUnselect_3_listener($event) {
        return ctx.onNodeUnselect($event);
      })("onNodeExpand", function TreePanelComponent_Template_p_tree_onNodeExpand_3_listener($event) {
        return ctx.onNodeExpand($event);
      })("onNodeCollapse", function TreePanelComponent_Template_p_tree_onNodeCollapse_3_listener($event) {
        return ctx.onNodeCollapse($event);
      });
      \u0275\u0275elementEnd()();
    }
    if (rf & 2) {
      \u0275\u0275advance(2);
      \u0275\u0275conditional(ctx.searchingForNode() ? 2 : -1);
      \u0275\u0275advance();
      \u0275\u0275property("value", ctx.nodes())("selectionMode", ctx.selectionMode())("selection", ctx.selection())("loading", ctx.loading() && !ctx.searchingForNode())("emptyMessage", ctx.emptyMessage())("styleClass", ctx.styleClass());
    }
  }, dependencies: [TreeModule, Tree], styles: ["\n.tree-panel-wrapper[_ngcontent-%COMP%] {\n  scroll-behavior: smooth;\n}\n.tree-panel-wrapper   [_nghost-%COMP%]     .p-tree .p-treenode {\n  transition: background-color 0.2s ease;\n}\n.tree-panel-wrapper[_ngcontent-%COMP%]   .node-search-overlay[_ngcontent-%COMP%] {\n  -webkit-backdrop-filter: blur(1px);\n  backdrop-filter: blur(1px);\n  animation: _ngcontent-%COMP%_fadeIn 0.2s ease-in;\n}\n.tree-panel-wrapper[_ngcontent-%COMP%]   .node-search-message[_ngcontent-%COMP%] {\n  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);\n  animation: _ngcontent-%COMP%_slideIn 0.3s ease-out;\n}\n@keyframes _ngcontent-%COMP%_fadeIn {\n  from {\n    opacity: 0;\n  }\n  to {\n    opacity: 1;\n  }\n}\n@keyframes _ngcontent-%COMP%_slideIn {\n  from {\n    transform: translateY(-10px);\n    opacity: 0;\n  }\n  to {\n    transform: translateY(0);\n    opacity: 1;\n  }\n}\n/*# sourceMappingURL=tree-panel.component.css.map */"] });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TreePanelComponent, [{
    type: Component,
    args: [{ selector: "app-tree-panel", imports: [TreeModule], template: '<div #treeWrapper class="tree-panel-wrapper h-full overflow-auto relative">\n  <!-- Custom loading overlay for node search -->\n  @if (searchingForNode()) {\n    <div\n      class="node-search-overlay absolute inset-0 bg-white bg-opacity-75 flex items-center justify-center z-10"\n    >\n      <div\n        class="node-search-message flex items-center space-x-2 bg-white px-4 py-2 rounded-lg border"\n      >\n        <i class="pi pi-spin pi-spinner text-blue-500"></i>\n        <span class="text-sm text-gray-700">{{ loadingMessage }}</span>\n      </div>\n    </div>\n  }\n\n  <p-tree\n    #treeComponent\n    [value]="nodes()"\n    [selectionMode]="selectionMode()"\n    [selection]="selection()"\n    [loading]="loading() && !searchingForNode()"\n    [emptyMessage]="emptyMessage()"\n    [styleClass]="styleClass()"\n    (onNodeSelect)="onNodeSelect($event)"\n    (onNodeUnselect)="onNodeUnselect($event)"\n    (onNodeExpand)="onNodeExpand($event)"\n    (onNodeCollapse)="onNodeCollapse($event)"\n  />\n</div>\n', styles: ["/* src/app/shared/components/tree-panel/tree-panel.component.scss */\n.tree-panel-wrapper {\n  scroll-behavior: smooth;\n}\n.tree-panel-wrapper :host ::ng-deep .p-tree .p-treenode {\n  transition: background-color 0.2s ease;\n}\n.tree-panel-wrapper .node-search-overlay {\n  -webkit-backdrop-filter: blur(1px);\n  backdrop-filter: blur(1px);\n  animation: fadeIn 0.2s ease-in;\n}\n.tree-panel-wrapper .node-search-message {\n  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);\n  animation: slideIn 0.3s ease-out;\n}\n@keyframes fadeIn {\n  from {\n    opacity: 0;\n  }\n  to {\n    opacity: 1;\n  }\n}\n@keyframes slideIn {\n  from {\n    transform: translateY(-10px);\n    opacity: 0;\n  }\n  to {\n    transform: translateY(0);\n    opacity: 1;\n  }\n}\n/*# sourceMappingURL=tree-panel.component.css.map */\n"] }]
  }], () => [], { treeComponent: [{
    type: ViewChild,
    args: ["treeComponent"]
  }], treeWrapper: [{
    type: ViewChild,
    args: ["treeWrapper"]
  }], nodes: [{ type: Input, args: [{ isSignal: true, alias: "nodes", required: false }] }], selectionMode: [{ type: Input, args: [{ isSignal: true, alias: "selectionMode", required: false }] }], loading: [{ type: Input, args: [{ isSignal: true, alias: "loading", required: false }] }], searchingForNode: [{ type: Input, args: [{ isSignal: true, alias: "searchingForNode", required: false }] }], searchingNodeKey: [{ type: Input, args: [{ isSignal: true, alias: "searchingNodeKey", required: false }] }], emptyMessage: [{ type: Input, args: [{ isSignal: true, alias: "emptyMessage", required: false }] }], styleClass: [{ type: Input, args: [{ isSignal: true, alias: "styleClass", required: false }] }], selection: [{ type: Input, args: [{ isSignal: true, alias: "selection", required: false }] }], scrollToNodeKey: [{ type: Input, args: [{ isSignal: true, alias: "scrollToNodeKey", required: false }] }], scrollToCenterNodeKey: [{ type: Input, args: [{ isSignal: true, alias: "scrollToCenterNodeKey", required: false }] }], contextExtractor: [{ type: Input, args: [{ isSignal: true, alias: "contextExtractor", required: false }] }], nodeSelect: [{ type: Output, args: ["nodeSelect"] }], nodeUnselect: [{ type: Output, args: ["nodeUnselect"] }], nodeExpand: [{ type: Output, args: ["nodeExpand"] }], nodeCollapse: [{ type: Output, args: ["nodeCollapse"] }], nodeNotFoundInData: [{ type: Output, args: ["nodeNotFoundInData"] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(TreePanelComponent, { className: "TreePanelComponent", filePath: "src/app/shared/components/tree-panel/tree-panel.component.ts", lineNumber: 38 });
})();

// src/app/shared/components/tree-search/tree-search.component.ts
var _c02 = ["searchInput"];
var _c12 = ["searchContainer"];
var _c2 = (a0) => ["fas", a0];
var _forTrack0 = ($index, $item) => $item.id;
function TreeSearchComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "p-inputicon", 8);
    \u0275\u0275listener("click", function TreeSearchComponent_Conditional_6_Template_p_inputicon_click_0_listener() {
      \u0275\u0275restoreView(_r3);
      const ctx_r3 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r3.clearSearch());
    })("keydown.enter", function TreeSearchComponent_Conditional_6_Template_p_inputicon_keydown_enter_0_listener() {
      \u0275\u0275restoreView(_r3);
      const ctx_r3 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r3.clearSearch());
    })("keydown.space", function TreeSearchComponent_Conditional_6_Template_p_inputicon_keydown_space_0_listener() {
      \u0275\u0275restoreView(_r3);
      const ctx_r3 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r3.clearSearch());
    });
    \u0275\u0275elementEnd();
  }
}
function TreeSearchComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "p-inputicon", 6);
  }
}
function TreeSearchComponent_Conditional_8_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 9);
    \u0275\u0275element(1, "i", 12);
    \u0275\u0275elementStart(2, "span");
    \u0275\u0275text(3, "Searching...");
    \u0275\u0275elementEnd()();
  }
}
function TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "fa-icon", 18);
  }
  if (rf & 2) {
    const result_r6 = \u0275\u0275nextContext().$implicit;
    const ctx_r3 = \u0275\u0275nextContext(3);
    \u0275\u0275property("icon", \u0275\u0275pureFunction1(1, _c2, ctx_r3.getFaIconName(result_r6.type)));
  }
}
function TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "i");
  }
  if (rf & 2) {
    const result_r6 = \u0275\u0275nextContext().$implicit;
    const ctx_r3 = \u0275\u0275nextContext(3);
    \u0275\u0275classMap(ctx_r3.getResultTypeIcon(result_r6.type) + " text-primary !text-2xl opacity-50 transition-opacity duration-200");
  }
}
function TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 24);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const result_r6 = \u0275\u0275nextContext().$implicit;
    const ctx_r3 = \u0275\u0275nextContext(3);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate1(" ", ctx_r3.formatPath(result_r6.path), " ");
  }
}
function TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_12_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 26)(1, "span", 27);
    \u0275\u0275text(2, "State");
    \u0275\u0275elementEnd();
    \u0275\u0275element(3, "span", 28);
    \u0275\u0275elementStart(4, "span", 29);
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const result_r6 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275advance(5);
    \u0275\u0275textInterpolate(result_r6.state);
  }
}
function TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_12_Conditional_2_For_6_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 31);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const field_r7 = ctx.$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(field_r7);
  }
}
function TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_12_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 26)(1, "span", 27);
    \u0275\u0275text(2, "Tags");
    \u0275\u0275elementEnd();
    \u0275\u0275element(3, "span", 28);
    \u0275\u0275elementStart(4, "div", 30);
    \u0275\u0275repeaterCreate(5, TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_12_Conditional_2_For_6_Template, 2, 1, "span", 31, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const result_r6 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275advance(5);
    \u0275\u0275repeater(result_r6.fields);
  }
}
function TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 25);
    \u0275\u0275conditionalCreate(1, TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_12_Conditional_1_Template, 6, 1, "div", 26);
    \u0275\u0275conditionalCreate(2, TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_12_Conditional_2_Template, 7, 0, "div", 26);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const result_r6 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275advance();
    \u0275\u0275conditional(result_r6.state ? 1 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(result_r6.fields && result_r6.fields.length > 0 ? 2 : -1);
  }
}
function TreeSearchComponent_Conditional_8_Conditional_2_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 15);
    \u0275\u0275listener("click", function TreeSearchComponent_Conditional_8_Conditional_2_For_2_Template_div_click_0_listener() {
      const result_r6 = \u0275\u0275restoreView(_r5).$implicit;
      const ctx_r3 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r3.selectResult(result_r6));
    })("keydown.enter", function TreeSearchComponent_Conditional_8_Conditional_2_For_2_Template_div_keydown_enter_0_listener() {
      const result_r6 = \u0275\u0275restoreView(_r5).$implicit;
      const ctx_r3 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r3.selectResult(result_r6));
    });
    \u0275\u0275elementStart(1, "div", 16)(2, "div", 17);
    \u0275\u0275conditionalCreate(3, TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_3_Template, 1, 3, "fa-icon", 18)(4, TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_4_Template, 1, 2, "i", 19);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(5, "div", 20)(6, "div", 21)(7, "span", 22);
    \u0275\u0275text(8);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "span", 23);
    \u0275\u0275text(10);
    \u0275\u0275elementEnd()();
    \u0275\u0275conditionalCreate(11, TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_11_Template, 2, 1, "div", 24);
    \u0275\u0275conditionalCreate(12, TreeSearchComponent_Conditional_8_Conditional_2_For_2_Conditional_12_Template, 3, 2, "div", 25);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const result_r6 = ctx.$implicit;
    const \u0275$index_29_r8 = ctx.$index;
    const ctx_r3 = \u0275\u0275nextContext(3);
    \u0275\u0275classProp("selected", ctx_r3.selectedIndex() === \u0275$index_29_r8);
    \u0275\u0275advance(3);
    \u0275\u0275conditional(ctx_r3.isIconFontAwesome(result_r6.type) ? 3 : 4);
    \u0275\u0275advance(5);
    \u0275\u0275textInterpolate1(" ", result_r6.name, " ");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", ctx_r3.getResultTypeLabel(result_r6.type), " ");
    \u0275\u0275advance();
    \u0275\u0275conditional(result_r6.path.length > 1 ? 11 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional((result_r6.owner == null ? null : result_r6.owner.name) || result_r6.state || result_r6.fields && result_r6.fields.length > 0 ? 12 : -1);
  }
}
function TreeSearchComponent_Conditional_8_Conditional_2_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 14)(1, "p-button", 32);
    \u0275\u0275listener("onClick", function TreeSearchComponent_Conditional_8_Conditional_2_Conditional_3_Template_p_button_onClick_1_listener() {
      \u0275\u0275restoreView(_r9);
      const ctx_r3 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r3.showAllResults());
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r3 = \u0275\u0275nextContext(3);
    \u0275\u0275advance();
    \u0275\u0275property("label", "+" + (ctx_r3.results().length - ctx_r3.displayResults().length) + " more results");
  }
}
function TreeSearchComponent_Conditional_8_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 10);
    \u0275\u0275repeaterCreate(1, TreeSearchComponent_Conditional_8_Conditional_2_For_2_Template, 13, 7, "div", 13, _forTrack0);
    \u0275\u0275conditionalCreate(3, TreeSearchComponent_Conditional_8_Conditional_2_Conditional_3_Template, 2, 1, "div", 14);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = \u0275\u0275nextContext(2);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r3.displayResults());
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r3.hasMoreResults() ? 3 : -1);
  }
}
function TreeSearchComponent_Conditional_8_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 11);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = \u0275\u0275nextContext(2);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate1(' No results found for "', ctx_r3.searchQuery(), '" ');
  }
}
function TreeSearchComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 7);
    \u0275\u0275conditionalCreate(1, TreeSearchComponent_Conditional_8_Conditional_1_Template, 4, 0, "div", 9)(2, TreeSearchComponent_Conditional_8_Conditional_2_Template, 4, 1, "div", 10)(3, TreeSearchComponent_Conditional_8_Conditional_3_Template, 2, 1, "div", 11);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r3.isSearching() ? 1 : ctx_r3.hasResults() ? 2 : ctx_r3.hasQuery() ? 3 : -1);
  }
}
var TreeSearchComponent = class _TreeSearchComponent {
  searchInput;
  searchContainer;
  // Inputs
  placeholder = input("Search company structure...", ...ngDevMode ? [{ debugName: "placeholder" }] : (
    /* istanbul ignore next */
    []
  ));
  disabled = input(false, ...ngDevMode ? [{ debugName: "disabled" }] : (
    /* istanbul ignore next */
    []
  ));
  results = input([], ...ngDevMode ? [{ debugName: "results" }] : (
    /* istanbul ignore next */
    []
  ));
  isSearching = input(false, ...ngDevMode ? [{ debugName: "isSearching" }] : (
    /* istanbul ignore next */
    []
  ));
  maxDisplayResults = input(10, ...ngDevMode ? [{ debugName: "maxDisplayResults" }] : (
    /* istanbul ignore next */
    []
  ));
  // Outputs
  queryChange = output();
  resultSelect = output();
  searchClear = output();
  // Internal state
  searchQuery = signal("", ...ngDevMode ? [{ debugName: "searchQuery" }] : (
    /* istanbul ignore next */
    []
  ));
  showResults = signal(false, ...ngDevMode ? [{ debugName: "showResults" }] : (
    /* istanbul ignore next */
    []
  ));
  selectedIndex = signal(-1, ...ngDevMode ? [{ debugName: "selectedIndex" }] : (
    /* istanbul ignore next */
    []
  ));
  showAll = signal(false, ...ngDevMode ? [{ debugName: "showAll" }] : (
    /* istanbul ignore next */
    []
  ));
  // Computed values
  hasQuery = computed(() => this.searchQuery().trim().length > 0, ...ngDevMode ? [{ debugName: "hasQuery" }] : (
    /* istanbul ignore next */
    []
  ));
  hasResults = computed(() => this.results().length > 0, ...ngDevMode ? [{ debugName: "hasResults" }] : (
    /* istanbul ignore next */
    []
  ));
  displayResults = computed(() => {
    if (this.showAll()) {
      return this.results();
    }
    const maxResults = this.maxDisplayResults();
    return this.results().slice(0, maxResults);
  }, ...ngDevMode ? [{ debugName: "displayResults" }] : (
    /* istanbul ignore next */
    []
  ));
  hasMoreResults = computed(() => !this.showAll() && this.results().length > this.maxDisplayResults(), ...ngDevMode ? [{ debugName: "hasMoreResults" }] : (
    /* istanbul ignore next */
    []
  ));
  clickListener;
  library = inject(FaIconLibrary);
  labelCache = /* @__PURE__ */ new Map();
  constructor() {
    this.library.addIcons(faMicrochip, faCubes, faSquareBinary, faGauge, faSitemap, faRoad, faUserSecret, faBuildingUser);
    this.clickListener = (event) => {
      const container = this.searchContainer?.nativeElement;
      if (this.showResults() && isDefined(container) && !container.contains(event.target)) {
        this.hideResults();
      }
    };
    document.addEventListener("click", this.clickListener);
  }
  ngOnDestroy() {
    if (this.clickListener) {
      document.removeEventListener("click", this.clickListener);
    }
  }
  onSearchInput(value) {
    this.searchQuery.set(value);
    this.queryChange.emit(value);
    this.showAll.set(false);
    if (value.trim().length > 0) {
      this.showResults.set(true);
      this.selectedIndex.set(-1);
    } else {
      this.hideResults();
    }
  }
  onSearchKeydown(event) {
    const results = this.displayResults();
    switch (event.key) {
      case "ArrowDown":
        event.preventDefault();
        this.selectedIndex.update((index) => Math.min(index + 1, results.length - 1));
        break;
      case "ArrowUp":
        event.preventDefault();
        this.selectedIndex.update((index) => Math.max(index - 1, -1));
        break;
      case "Enter": {
        event.preventDefault();
        const selectedIdx = this.selectedIndex();
        if (selectedIdx >= 0 && selectedIdx < results.length) {
          this.selectResult(results[selectedIdx]);
        } else if (results.length > 0) {
          this.selectResult(results[0]);
        }
        break;
      }
      case "Escape":
        event.preventDefault();
        this.clearSearch();
        break;
    }
  }
  selectResult(result) {
    this.resultSelect.emit(result);
    this.hideResults();
    this.searchInput.nativeElement.blur();
  }
  clearSearch() {
    this.searchQuery.set("");
    this.hideResults();
    this.searchClear.emit();
    this.searchInput.nativeElement.focus();
  }
  hideResults() {
    this.showResults.set(false);
    this.selectedIndex.set(-1);
    this.showAll.set(false);
  }
  onInputFocus() {
    if (this.hasQuery() && this.hasResults()) {
      this.showResults.set(true);
    }
  }
  getResultTypeIcon(type) {
    return getEntityIconClass(type);
  }
  isIconFontAwesome(type) {
    return isEntityIconFontAwesome(type);
  }
  getResultTypeLabel(type) {
    if (!type) {
      return "";
    }
    if (this.labelCache.has(type)) {
      return this.labelCache.get(type);
    }
    const formatted = type.replaceAll(/([A-Z])/g, " $1").trim().split(" ").map((word) => word.charAt(0).toUpperCase() + word.slice(1).toLowerCase()).join(" ");
    this.labelCache.set(type, formatted);
    return formatted;
  }
  formatPath(path) {
    if (isNullOrUndefined(path) || path.length === 0) {
      return "";
    }
    return path.slice(0, -1).join(" > ");
  }
  showAllResults() {
    this.showAll.set(true);
  }
  getFaIconName(type) {
    const iconClass = getEntityIconClass(type);
    return iconClass.replace("fas fa-", "");
  }
  static \u0275fac = function TreeSearchComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _TreeSearchComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _TreeSearchComponent, selectors: [["app-tree-search"]], viewQuery: function TreeSearchComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuery(_c02, 5)(_c12, 5);
    }
    if (rf & 2) {
      let _t;
      \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.searchInput = _t.first);
      \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.searchContainer = _t.first);
    }
  }, inputs: { placeholder: [1, "placeholder"], disabled: [1, "disabled"], results: [1, "results"], isSearching: [1, "isSearching"], maxDisplayResults: [1, "maxDisplayResults"] }, outputs: { queryChange: "queryChange", resultSelect: "resultSelect", searchClear: "searchClear" }, decls: 9, vars: 5, consts: [["searchContainer", ""], ["searchInput", ""], [1, "tree-search-container", "relative"], [1, "p-inputgroup"], ["type", "text", "pInputText", "", "autocomplete", "off", 1, "tree-search-input", "h-10", 3, "input", "keydown", "focus", "placeholder", "disabled", "value"], ["pTooltip", "Clear search", "tooltipPosition", "top", "tabindex", "0", 1, "pi", "pi-times", "cursor-pointer", "hover:text-gray-600", "transition-colors", "clear-search-icon"], [1, "pi", "pi-search"], [1, "search-results-panel", "absolute", "top-full", "left-0", "right-0", "z-50", "mt-1", "bg-white", "border", "border-gray-300", "rounded", "shadow-lg", "max-h-80", "overflow-hidden"], ["pTooltip", "Clear search", "tooltipPosition", "top", "tabindex", "0", 1, "pi", "pi-times", "cursor-pointer", "hover:text-gray-600", "transition-colors", "clear-search-icon", 3, "click", "keydown.enter", "keydown.space"], [1, "p-4", "text-center"], [1, "search-results-list"], [1, "p-4", "text-center", "text-gray-500"], [1, "pi", "pi-spin", "pi-spinner", "mr-2"], [1, "search-result-item", "p-3", "cursor-pointer", "border-b", "border-gray-200", "last:border-b-0", "hover:bg-blue-50", "transition-colors", 3, "selected"], [1, "p-2", "text-center", "text-sm", "bg-gray-50"], [1, "search-result-item", "p-3", "cursor-pointer", "border-b", "border-gray-200", "last:border-b-0", "hover:bg-blue-50", "transition-colors", 3, "click", "keydown.enter"], [1, "flex", "items-start", "gap-3"], [1, "flex-shrink-0", "mt-0.5"], [1, "text-primary", "!text-2xl", "opacity-50", "transition-opacity", "duration-200", 3, "icon"], [3, "class"], [1, "flex-1", "min-w-0"], [1, "flex", "items-center", "gap-2", "mb-1"], [1, "font-semibold", "truncate"], [1, "text-sm", "uppercase", "tracking-wide", "text-gray-500", "font-medium", "px-1.5", "py-0.5", "bg-gray-50", "rounded", "flex-shrink-0", "opacity-70"], [1, "text-sm", "text-gray-500", "truncate", "mb-2"], [1, "metadata-table", "text-sm"], [1, "metadata-row"], [1, "metadata-label"], [1, "metadata-divider"], [1, "metadata-value", "capitalize"], [1, "metadata-value", "flex", "flex-wrap", "gap-1"], [1, "field-badge"], [1, "p-button-text", "p-button-sm", 3, "onClick", "label"]], template: function TreeSearchComponent_Template(rf, ctx) {
    if (rf & 1) {
      const _r1 = \u0275\u0275getCurrentView();
      \u0275\u0275elementStart(0, "div", 2, 0)(2, "div", 3)(3, "p-iconfield")(4, "input", 4, 1);
      \u0275\u0275listener("input", function TreeSearchComponent_Template_input_input_4_listener() {
        \u0275\u0275restoreView(_r1);
        const searchInput_r2 = \u0275\u0275reference(5);
        return \u0275\u0275resetView(ctx.onSearchInput(searchInput_r2.value));
      })("keydown", function TreeSearchComponent_Template_input_keydown_4_listener($event) {
        return ctx.onSearchKeydown($event);
      })("focus", function TreeSearchComponent_Template_input_focus_4_listener() {
        return ctx.onInputFocus();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275conditionalCreate(6, TreeSearchComponent_Conditional_6_Template, 1, 0, "p-inputicon", 5)(7, TreeSearchComponent_Conditional_7_Template, 1, 0, "p-inputicon", 6);
      \u0275\u0275elementEnd()();
      \u0275\u0275conditionalCreate(8, TreeSearchComponent_Conditional_8_Template, 4, 1, "div", 7);
      \u0275\u0275elementEnd();
    }
    if (rf & 2) {
      \u0275\u0275advance(4);
      \u0275\u0275property("placeholder", ctx.placeholder())("disabled", ctx.disabled())("value", ctx.searchQuery());
      \u0275\u0275advance(2);
      \u0275\u0275conditional(ctx.hasQuery() ? 6 : 7);
      \u0275\u0275advance(2);
      \u0275\u0275conditional(ctx.showResults() ? 8 : -1);
    }
  }, dependencies: [
    CommonModule,
    FormsModule,
    InputTextModule,
    InputText,
    ButtonModule,
    Button,
    ProgressSpinnerModule,
    ScrollerModule,
    InputIcon,
    IconField,
    TooltipModule,
    Tooltip,
    FontAwesomeModule,
    FaIconComponent
  ], styles: ["\n.tree-search-container[_ngcontent-%COMP%] {\n  width: 100%;\n}\n.tree-search-input[_ngcontent-%COMP%] {\n  width: 100%;\n}\n.clear-search-icon[_ngcontent-%COMP%] {\n  right: 8px !important;\n  left: auto !important;\n  z-index: 10;\n}\n.clear-search-icon[_ngcontent-%COMP%]:focus {\n  outline: 2px solid var(--primary-color, #3b82f6);\n  outline-offset: 2px;\n  border-radius: 2px;\n}\n.search-results-panel[_ngcontent-%COMP%] {\n  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);\n  max-height: 320px;\n  z-index: 1050;\n  background: var(--input-background-color) !important;\n  border-color: var(--input-border-color) !important;\n}\n.search-results-list[_ngcontent-%COMP%] {\n  max-height: 280px;\n  overflow-y: auto;\n}\n.search-result-item[_ngcontent-%COMP%] {\n  transition: all 0.15s ease-in-out;\n}\n.search-result-item.selected[_ngcontent-%COMP%] {\n  background-color: var(--primary-color, #3b82f6) !important;\n  color: white;\n}\n.search-result-item.selected[_ngcontent-%COMP%]   .text-gray-900[_ngcontent-%COMP%], \n.search-result-item.selected[_ngcontent-%COMP%]   .text-gray-600[_ngcontent-%COMP%], \n.search-result-item.selected[_ngcontent-%COMP%]   .text-gray-500[_ngcontent-%COMP%], \n.search-result-item.selected[_ngcontent-%COMP%]   .text-gray-400[_ngcontent-%COMP%] {\n  color: rgba(255, 255, 255, 0.95) !important;\n}\n.search-result-item.selected[_ngcontent-%COMP%]   .bg-gray-50[_ngcontent-%COMP%], \n.search-result-item.selected[_ngcontent-%COMP%]   .bg-gray-100[_ngcontent-%COMP%] {\n  background-color: rgba(255, 255, 255, 0.25) !important;\n  color: rgba(255, 255, 255, 0.95) !important;\n}\n.search-result-item.selected[_ngcontent-%COMP%]   i[_ngcontent-%COMP%] {\n  color: rgba(255, 255, 255, 0.9) !important;\n  opacity: 1 !important;\n}\n.search-result-item.selected[_ngcontent-%COMP%]   span[_ngcontent-%COMP%] {\n  color: rgba(255, 255, 255, 0.95) !important;\n  opacity: 1 !important;\n}\n.search-result-item.selected[_ngcontent-%COMP%]   .metadata-label[_ngcontent-%COMP%] {\n  color: rgba(255, 255, 255, 0.9);\n}\n.search-result-item.selected[_ngcontent-%COMP%]   .metadata-divider[_ngcontent-%COMP%] {\n  background-color: rgba(255, 255, 255, 0.4);\n}\n.search-result-item.selected[_ngcontent-%COMP%]   .metadata-value[_ngcontent-%COMP%] {\n  color: rgba(255, 255, 255, 0.95);\n}\n.search-result-item.selected[_ngcontent-%COMP%]   .field-badge[_ngcontent-%COMP%] {\n  background-color: rgba(255, 255, 255, 0.25);\n  color: rgba(255, 255, 255, 0.95);\n}\n.search-result-item[_ngcontent-%COMP%]:hover:not(.selected) {\n  background-color: #eff6ff;\n}\n.search-result-item[_ngcontent-%COMP%]:hover:not(.selected)   i[_ngcontent-%COMP%] {\n  opacity: 0.7;\n}\n.search-result-item[_ngcontent-%COMP%]:focus {\n  outline: none;\n}\n.search-result-item[_ngcontent-%COMP%]   i[_ngcontent-%COMP%] {\n  flex-shrink: 0;\n}\n.metadata-table[_ngcontent-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  gap: 0.375rem;\n  margin-top: 0.5rem;\n}\n.metadata-row[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: center;\n  gap: 0.5rem;\n  min-height: 1.25rem;\n}\n.metadata-label[_ngcontent-%COMP%] {\n  font-weight: 600;\n  color: #9ca3af;\n  min-width: 3.5rem;\n  flex-shrink: 0;\n  opacity: 0.8;\n}\n.metadata-divider[_ngcontent-%COMP%] {\n  width: 1px;\n  height: 1rem;\n  background-color: #e5e7eb;\n  flex-shrink: 0;\n  opacity: 0.6;\n}\n.metadata-value[_ngcontent-%COMP%] {\n  color: #374151;\n  flex: 1;\n  min-width: 0;\n}\n.field-badge[_ngcontent-%COMP%] {\n  display: inline-block;\n  background-color: #f3f4f6;\n  color: #6b7280;\n  padding: 0.125rem 0.5rem;\n  border-radius: 0.25rem;\n  font-size: 0.6875rem;\n  font-weight: 500;\n}\n/*# sourceMappingURL=tree-search.component.css.map */"], changeDetection: 0 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TreeSearchComponent, [{
    type: Component,
    args: [{ selector: "app-tree-search", imports: [
      CommonModule,
      FormsModule,
      InputTextModule,
      ButtonModule,
      ProgressSpinnerModule,
      ScrollerModule,
      InputIcon,
      IconField,
      TooltipModule,
      FontAwesomeModule
    ], changeDetection: ChangeDetectionStrategy.OnPush, template: `<div #searchContainer class="tree-search-container relative">
  <div class="p-inputgroup">
    <p-iconfield>
      <input
        #searchInput
        type="text"
        pInputText
        [placeholder]="placeholder()"
        [disabled]="disabled()"
        [value]="searchQuery()"
        (input)="onSearchInput(searchInput.value)"
        (keydown)="onSearchKeydown($event)"
        (focus)="onInputFocus()"
        class="tree-search-input h-10"
        autocomplete="off"
      />

      @if (hasQuery()) {
        <p-inputicon
          class="pi pi-times cursor-pointer hover:text-gray-600 transition-colors clear-search-icon"
          (click)="clearSearch()"
          (keydown.enter)="clearSearch()"
          (keydown.space)="clearSearch()"
          pTooltip="Clear search"
          tooltipPosition="top"
          tabindex="0"
        />
      } @else {
        <p-inputicon class="pi pi-search" />
      }
    </p-iconfield>
  </div>

  @if (showResults()) {
    <div
      class="search-results-panel absolute top-full left-0 right-0 z-50 mt-1 bg-white border border-gray-300 rounded shadow-lg max-h-80 overflow-hidden"
    >
      @if (isSearching()) {
        <div class="p-4 text-center">
          <i class="pi pi-spin pi-spinner mr-2"></i>
          <span>Searching...</span>
        </div>
      } @else if (hasResults()) {
        <div class="search-results-list">
          @for (result of displayResults(); track result.id; let i = $index) {
            <div
              class="search-result-item p-3 cursor-pointer border-b border-gray-200 last:border-b-0 hover:bg-blue-50 transition-colors"
              [class.selected]="selectedIndex() === i"
              (click)="selectResult(result)"
              (keydown.enter)="selectResult(result)"
            >
              <div class="flex items-start gap-3">
                <div class="flex-shrink-0 mt-0.5">
                  @if (isIconFontAwesome(result.type)) {
                    <fa-icon
                      [icon]="['fas', getFaIconName(result.type)]"
                      class="text-primary !text-2xl opacity-50 transition-opacity duration-200"
                    ></fa-icon>
                  } @else {
                    <i
                      [class]="
                        getResultTypeIcon(result.type) +
                        ' text-primary !text-2xl opacity-50 transition-opacity duration-200'
                      "
                    ></i>
                  }
                </div>
                <div class="flex-1 min-w-0">
                  <div class="flex items-center gap-2 mb-1">
                    <span class="font-semibold truncate">
                      {{ result.name }}
                    </span>
                    <span
                      class="text-sm uppercase tracking-wide text-gray-500 font-medium px-1.5 py-0.5 bg-gray-50 rounded flex-shrink-0 opacity-70"
                    >
                      {{ getResultTypeLabel(result.type) }}
                    </span>
                  </div>

                  @if (result.path.length > 1) {
                    <div class="text-sm text-gray-500 truncate mb-2">
                      {{ formatPath(result.path) }}
                    </div>
                  }

                  @if (
                    result.owner?.name ||
                    result.state ||
                    (result.fields && result.fields.length > 0)
                  ) {
                    <div class="metadata-table text-sm">
                      @if (result.state) {
                        <div class="metadata-row">
                          <span class="metadata-label">State</span>
                          <span class="metadata-divider"></span>
                          <span class="metadata-value capitalize">{{
                            result.state
                          }}</span>
                        </div>
                      }
                      @if (result.fields && result.fields.length > 0) {
                        <div class="metadata-row">
                          <span class="metadata-label">Tags</span>
                          <span class="metadata-divider"></span>
                          <div class="metadata-value flex flex-wrap gap-1">
                            @for (field of result.fields; track field) {
                              <span class="field-badge">{{ field }}</span>
                            }
                          </div>
                        </div>
                      }
                    </div>
                  }
                </div>
              </div>
            </div>
          }
          @if (hasMoreResults()) {
            <div class="p-2 text-center text-sm bg-gray-50">
              <p-button
                [label]="
                  '+' +
                  (results().length - displayResults().length) +
                  ' more results'
                "
                class="p-button-text p-button-sm"
                (onClick)="showAllResults()"
              />
            </div>
          }
        </div>
      } @else if (hasQuery()) {
        <div class="p-4 text-center text-gray-500">
          No results found for "{{ searchQuery() }}"
        </div>
      }
    </div>
  }
</div>
`, styles: ["/* src/app/shared/components/tree-search/tree-search.component.scss */\n.tree-search-container {\n  width: 100%;\n}\n.tree-search-input {\n  width: 100%;\n}\n.clear-search-icon {\n  right: 8px !important;\n  left: auto !important;\n  z-index: 10;\n}\n.clear-search-icon:focus {\n  outline: 2px solid var(--primary-color, #3b82f6);\n  outline-offset: 2px;\n  border-radius: 2px;\n}\n.search-results-panel {\n  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);\n  max-height: 320px;\n  z-index: 1050;\n  background: var(--input-background-color) !important;\n  border-color: var(--input-border-color) !important;\n}\n.search-results-list {\n  max-height: 280px;\n  overflow-y: auto;\n}\n.search-result-item {\n  transition: all 0.15s ease-in-out;\n}\n.search-result-item.selected {\n  background-color: var(--primary-color, #3b82f6) !important;\n  color: white;\n}\n.search-result-item.selected .text-gray-900,\n.search-result-item.selected .text-gray-600,\n.search-result-item.selected .text-gray-500,\n.search-result-item.selected .text-gray-400 {\n  color: rgba(255, 255, 255, 0.95) !important;\n}\n.search-result-item.selected .bg-gray-50,\n.search-result-item.selected .bg-gray-100 {\n  background-color: rgba(255, 255, 255, 0.25) !important;\n  color: rgba(255, 255, 255, 0.95) !important;\n}\n.search-result-item.selected i {\n  color: rgba(255, 255, 255, 0.9) !important;\n  opacity: 1 !important;\n}\n.search-result-item.selected span {\n  color: rgba(255, 255, 255, 0.95) !important;\n  opacity: 1 !important;\n}\n.search-result-item.selected .metadata-label {\n  color: rgba(255, 255, 255, 0.9);\n}\n.search-result-item.selected .metadata-divider {\n  background-color: rgba(255, 255, 255, 0.4);\n}\n.search-result-item.selected .metadata-value {\n  color: rgba(255, 255, 255, 0.95);\n}\n.search-result-item.selected .field-badge {\n  background-color: rgba(255, 255, 255, 0.25);\n  color: rgba(255, 255, 255, 0.95);\n}\n.search-result-item:hover:not(.selected) {\n  background-color: #eff6ff;\n}\n.search-result-item:hover:not(.selected) i {\n  opacity: 0.7;\n}\n.search-result-item:focus {\n  outline: none;\n}\n.search-result-item i {\n  flex-shrink: 0;\n}\n.metadata-table {\n  display: flex;\n  flex-direction: column;\n  gap: 0.375rem;\n  margin-top: 0.5rem;\n}\n.metadata-row {\n  display: flex;\n  align-items: center;\n  gap: 0.5rem;\n  min-height: 1.25rem;\n}\n.metadata-label {\n  font-weight: 600;\n  color: #9ca3af;\n  min-width: 3.5rem;\n  flex-shrink: 0;\n  opacity: 0.8;\n}\n.metadata-divider {\n  width: 1px;\n  height: 1rem;\n  background-color: #e5e7eb;\n  flex-shrink: 0;\n  opacity: 0.6;\n}\n.metadata-value {\n  color: #374151;\n  flex: 1;\n  min-width: 0;\n}\n.field-badge {\n  display: inline-block;\n  background-color: #f3f4f6;\n  color: #6b7280;\n  padding: 0.125rem 0.5rem;\n  border-radius: 0.25rem;\n  font-size: 0.6875rem;\n  font-weight: 500;\n}\n/*# sourceMappingURL=tree-search.component.css.map */\n"] }]
  }], () => [], { searchInput: [{
    type: ViewChild,
    args: ["searchInput"]
  }], searchContainer: [{
    type: ViewChild,
    args: ["searchContainer"]
  }], placeholder: [{ type: Input, args: [{ isSignal: true, alias: "placeholder", required: false }] }], disabled: [{ type: Input, args: [{ isSignal: true, alias: "disabled", required: false }] }], results: [{ type: Input, args: [{ isSignal: true, alias: "results", required: false }] }], isSearching: [{ type: Input, args: [{ isSignal: true, alias: "isSearching", required: false }] }], maxDisplayResults: [{ type: Input, args: [{ isSignal: true, alias: "maxDisplayResults", required: false }] }], queryChange: [{ type: Output, args: ["queryChange"] }], resultSelect: [{ type: Output, args: ["resultSelect"] }], searchClear: [{ type: Output, args: ["searchClear"] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(TreeSearchComponent, { className: "TreeSearchComponent", filePath: "src/app/shared/components/tree-search/tree-search.component.ts", lineNumber: 62 });
})();

// src/app/features/administration/components/admin-container/tree-navigation-panel/tree-navigation-panel.component.ts
var _c03 = (a0) => ({ "absolute right-0 inset-y-0": a0 });
function TreeNavigationPanelComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 1)(1, "div", 10)(2, "app-tree-search", 11);
    \u0275\u0275listener("queryChange", function TreeNavigationPanelComponent_Conditional_1_Template_app_tree_search_queryChange_2_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onSearchQueryChange($event));
    })("resultSelect", function TreeNavigationPanelComponent_Conditional_1_Template_app_tree_search_resultSelect_2_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onSearchResultSelect($event));
    })("searchClear", function TreeNavigationPanelComponent_Conditional_1_Template_app_tree_search_searchClear_2_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onSearchClear());
    });
    \u0275\u0275elementEnd()();
    \u0275\u0275element(3, "hr");
    \u0275\u0275elementStart(4, "div", 12)(5, "app-tree-panel", 13);
    \u0275\u0275listener("nodeExpand", function TreeNavigationPanelComponent_Conditional_1_Template_app_tree_panel_nodeExpand_5_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onNodeExpand($event));
    })("nodeCollapse", function TreeNavigationPanelComponent_Conditional_1_Template_app_tree_panel_nodeCollapse_5_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onNodeCollapse($event));
    })("nodeSelect", function TreeNavigationPanelComponent_Conditional_1_Template_app_tree_panel_nodeSelect_5_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onNodeSelect($event));
    })("nodeNotFoundInData", function TreeNavigationPanelComponent_Conditional_1_Template_app_tree_panel_nodeNotFoundInData_5_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onNodeNotFoundInData($event));
    });
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275property("results", ctx_r1.searchResults()())("isSearching", ctx_r1.isSearching()());
    \u0275\u0275advance(3);
    \u0275\u0275property("nodes", ctx_r1.hierarchyTree()())("loading", ctx_r1.isLoading()())("searchingForNode", ctx_r1.searchingForNode()())("searchingNodeKey", ctx_r1.searchingNodeKey()())("selection", ctx_r1.findSelectedNode())("scrollToNodeKey", ctx_r1.scrollToNodeKey()())("scrollToCenterNodeKey", ctx_r1.scrollToCenterNodeKey()())("contextExtractor", ctx_r1.contextExtractor);
  }
}
var TreeNavigationPanelComponent = class _TreeNavigationPanelComponent {
  adminState = inject(AdminStateService);
  contextExtractor = inject(AdminTreeContextExtractorService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  // Input properties
  treeCollapsed = input.required(...ngDevMode ? [{ debugName: "treeCollapsed" }] : (
    /* istanbul ignore next */
    []
  ));
  hierarchyTree = input.required(...ngDevMode ? [{ debugName: "hierarchyTree" }] : (
    /* istanbul ignore next */
    []
  ));
  isLoading = input.required(...ngDevMode ? [{ debugName: "isLoading" }] : (
    /* istanbul ignore next */
    []
  ));
  searchingForNode = input.required(...ngDevMode ? [{ debugName: "searchingForNode" }] : (
    /* istanbul ignore next */
    []
  ));
  searchingNodeKey = input.required(...ngDevMode ? [{ debugName: "searchingNodeKey" }] : (
    /* istanbul ignore next */
    []
  ));
  selectedTreeNode = input.required(...ngDevMode ? [{ debugName: "selectedTreeNode" }] : (
    /* istanbul ignore next */
    []
  ));
  scrollToNodeKey = input.required(...ngDevMode ? [{ debugName: "scrollToNodeKey" }] : (
    /* istanbul ignore next */
    []
  ));
  scrollToCenterNodeKey = input.required(...ngDevMode ? [{ debugName: "scrollToCenterNodeKey" }] : (
    /* istanbul ignore next */
    []
  ));
  searchResults = input.required(...ngDevMode ? [{ debugName: "searchResults" }] : (
    /* istanbul ignore next */
    []
  ));
  isSearching = input.required(...ngDevMode ? [{ debugName: "isSearching" }] : (
    /* istanbul ignore next */
    []
  ));
  // Output events
  toggleTreePanel = output();
  navigateToSelectedClient = output();
  nodeExpand = output();
  nodeCollapse = output();
  nodeSelect = output();
  nodeNotFoundInData = output();
  searchQueryChange = output();
  searchResultSelect = output();
  searchClear = output();
  // Font Awesome icons
  faAngleLeft = faAngleLeft;
  faAngleRight = faAngleRight;
  /**
   * Computed property for the toggle button text
   */
  toggleButtonText = computed(() => {
    this.languageService.currentLanguage();
    return this.adminState.selectedClient()?.name ?? this.adminState.clientVendor()?.name ?? this.adminState.vendorDistributor()?.name ?? this.translate.instant("ADMIN.SHARED.NAVIGATION_PANEL.TOGGLE_BUTTON_TEXT");
  }, ...ngDevMode ? [{ debugName: "toggleButtonText" }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * Finds the currently selected node for the tree panel
   */
  findSelectedNode() {
    return this.selectedTreeNode()();
  }
  // Event handlers - these simply emit to parent
  onToggleTreePanel() {
    this.toggleTreePanel.emit();
  }
  onNavigateToSelectedClient() {
    this.navigateToSelectedClient.emit();
  }
  onNodeExpand(node) {
    this.nodeExpand.emit(node);
  }
  onNodeCollapse(node) {
    this.nodeCollapse.emit(node);
  }
  onNodeSelect(node) {
    this.nodeSelect.emit(node);
  }
  onNodeNotFoundInData(event) {
    this.nodeNotFoundInData.emit(event);
  }
  onSearchQueryChange(query) {
    this.searchQueryChange.emit(query);
  }
  onSearchResultSelect(result) {
    this.searchResultSelect.emit(result);
  }
  onSearchClear() {
    this.searchClear.emit();
  }
  static \u0275fac = function TreeNavigationPanelComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _TreeNavigationPanelComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _TreeNavigationPanelComponent, selectors: [["app-tree-navigation-panel"]], inputs: { treeCollapsed: [1, "treeCollapsed"], hierarchyTree: [1, "hierarchyTree"], isLoading: [1, "isLoading"], searchingForNode: [1, "searchingForNode"], searchingNodeKey: [1, "searchingNodeKey"], selectedTreeNode: [1, "selectedTreeNode"], scrollToNodeKey: [1, "scrollToNodeKey"], scrollToCenterNodeKey: [1, "scrollToCenterNodeKey"], searchResults: [1, "searchResults"], isSearching: [1, "isSearching"] }, outputs: { toggleTreePanel: "toggleTreePanel", navigateToSelectedClient: "navigateToSelectedClient", nodeExpand: "nodeExpand", nodeCollapse: "nodeCollapse", nodeSelect: "nodeSelect", nodeNotFoundInData: "nodeNotFoundInData", searchQueryChange: "searchQueryChange", searchResultSelect: "searchResultSelect", searchClear: "searchClear" }, decls: 15, vars: 17, consts: [[1, "h-full", "flex", "relative"], [1, "h-full", "flex-grow", "flex", "flex-col", "pr-8"], [1, "vertical-toggle-panel", "flex", "h-full", "z-10", "flex-col", 3, "ngClass"], [1, "group", "flex", "items-center", "justify-center", "p-1.5", "md:p-2", "bg-gray-50", "hover:bg-gray-100", "rounded", "transition", "cursor-pointer", "border", "border-gray-300", "shadow-sm", "my-2", "md:my-4", 3, "click", "title"], [1, "pi", "pi-home", "text-gray-700", "group-hover:text-primary", "transition", "text-xs", "md:text-sm"], [1, "flex-grow", "flex", "items-center", "justify-center"], [1, "group", "flex", "flex-col", "items-center", "justify-center", "px-0.5", "md:px-1", "py-1.5", "md:py-2", "bg-gray-100", "hover:bg-gray-200", "rounded", "transition", "cursor-pointer", "border", "border-gray-300", "shadow-sm", 3, "click"], [1, "text-xs", "md:text-sm", "font-semibold", "text-gray-700", "tracking-tight", "select-none", "vertical-toggle-button"], [1, "mt-1", "md:mt-2"], [1, "text-gray-500", "group-hover:text-primary", "transition", "text-sm", "md:text-base", 3, "icon"], [1, "flex", "items-center", "px-4", "m-2"], [1, "w-full", 3, "queryChange", "resultSelect", "searchClear", "results", "isSearching"], [1, "flex-grow", "flex", "overflow-hidden"], [1, "w-full", "overflow-auto", 3, "nodeExpand", "nodeCollapse", "nodeSelect", "nodeNotFoundInData", "nodes", "loading", "searchingForNode", "searchingNodeKey", "selection", "scrollToNodeKey", "scrollToCenterNodeKey", "contextExtractor"]], template: function TreeNavigationPanelComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "div", 0);
      \u0275\u0275conditionalCreate(1, TreeNavigationPanelComponent_Conditional_1_Template, 6, 10, "div", 1);
      \u0275\u0275elementStart(2, "div", 2)(3, "button", 3);
      \u0275\u0275pipe(4, "translate");
      \u0275\u0275pipe(5, "translate");
      \u0275\u0275listener("click", function TreeNavigationPanelComponent_Template_button_click_3_listener() {
        return ctx.onNavigateToSelectedClient();
      });
      \u0275\u0275element(6, "span", 4);
      \u0275\u0275elementEnd();
      \u0275\u0275elementStart(7, "div", 5)(8, "button", 6);
      \u0275\u0275pipe(9, "translate");
      \u0275\u0275pipe(10, "translate");
      \u0275\u0275listener("click", function TreeNavigationPanelComponent_Template_button_click_8_listener() {
        return ctx.onToggleTreePanel();
      });
      \u0275\u0275elementStart(11, "span", 7);
      \u0275\u0275text(12);
      \u0275\u0275elementEnd();
      \u0275\u0275elementStart(13, "span", 8);
      \u0275\u0275element(14, "fa-icon", 9);
      \u0275\u0275elementEnd()()()()();
    }
    if (rf & 2) {
      \u0275\u0275advance();
      \u0275\u0275conditional(!ctx.treeCollapsed()() ? 1 : -1);
      \u0275\u0275advance();
      \u0275\u0275property("ngClass", \u0275\u0275pureFunction1(15, _c03, !ctx.treeCollapsed()()));
      \u0275\u0275advance();
      \u0275\u0275property("title", \u0275\u0275pipeBind1(4, 7, "ADMIN.SHARED.NAVIGATION_PANEL.GO_TO_CLIENT"));
      \u0275\u0275attribute("aria-label", \u0275\u0275pipeBind1(5, 9, "ADMIN.SHARED.NAVIGATION_PANEL.NAVIGATE_TO_CLIENT"));
      \u0275\u0275advance(5);
      \u0275\u0275attribute("aria-label", ctx.treeCollapsed()() ? \u0275\u0275pipeBind1(9, 11, "ADMIN.SHARED.NAVIGATION_PANEL.EXPAND_COMPANY_STRUCTURE") : \u0275\u0275pipeBind1(10, 13, "ADMIN.SHARED.NAVIGATION_PANEL.COLLAPSE_COMPANY_STRUCTURE"));
      \u0275\u0275advance(4);
      \u0275\u0275textInterpolate1(" ", ctx.toggleButtonText(), " ");
      \u0275\u0275advance(2);
      \u0275\u0275property("icon", ctx.treeCollapsed()() ? ctx.faAngleRight : ctx.faAngleLeft);
    }
  }, dependencies: [
    CommonModule,
    NgClass,
    TreePanelComponent,
    TreeSearchComponent,
    ToolbarModule,
    FontAwesomeModule,
    FaIconComponent,
    TranslatePipe
  ], styles: ["\n.vertical-toggle-panel[_ngcontent-%COMP%] {\n  position: absolute;\n  right: 0;\n  top: 0;\n  bottom: 0;\n  width: 2rem;\n  min-width: 2rem;\n  max-width: 2rem;\n  overflow: visible;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  background-color: #f3f4f6;\n  transition: all 0.3s;\n  z-index: 10;\n}\n@media (min-width: 640px) {\n  .vertical-toggle-panel[_ngcontent-%COMP%] {\n    width: 2.5rem;\n    min-width: 2.5rem;\n    max-width: 2.5rem;\n  }\n}\n.vertical-toggle-panel[_ngcontent-%COMP%]   button[_ngcontent-%COMP%] {\n  outline: none;\n  box-shadow: 0 0 0 2px var(--navigation-font-color);\n}\n.vertical-toggle-button[_ngcontent-%COMP%] {\n  writing-mode: vertical-lr;\n  transform: rotate(180deg);\n  letter-spacing: 0.1em;\n}\n/*# sourceMappingURL=tree-navigation-panel.component.css.map */"], changeDetection: 0 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TreeNavigationPanelComponent, [{
    type: Component,
    args: [{ selector: "app-tree-navigation-panel", imports: [
      CommonModule,
      TreePanelComponent,
      TreeSearchComponent,
      ToolbarModule,
      FontAwesomeModule,
      TranslatePipe
    ], changeDetection: ChangeDetectionStrategy.OnPush, template: `<div class="h-full flex relative">
  <!-- Tree Panel - conditionally shown -->
  @if (!treeCollapsed()()) {
    <div class="h-full flex-grow flex flex-col pr-8">
      <div class="flex items-center px-4 m-2">
        <app-tree-search
          [results]="searchResults()()"
          [isSearching]="isSearching()()"
          (queryChange)="onSearchQueryChange($event)"
          (resultSelect)="onSearchResultSelect($event)"
          (searchClear)="onSearchClear()"
          class="w-full"
        ></app-tree-search>
      </div>
      <hr />
      <!-- Tree container with flex and overflow handling -->
      <div class="flex-grow flex overflow-hidden">
        <app-tree-panel
          [nodes]="hierarchyTree()()"
          [loading]="isLoading()()"
          [searchingForNode]="searchingForNode()()"
          [searchingNodeKey]="searchingNodeKey()()"
          [selection]="findSelectedNode()"
          [scrollToNodeKey]="scrollToNodeKey()()"
          [scrollToCenterNodeKey]="scrollToCenterNodeKey()()"
          [contextExtractor]="contextExtractor"
          (nodeExpand)="onNodeExpand($event)"
          (nodeCollapse)="onNodeCollapse($event)"
          (nodeSelect)="onNodeSelect($event)"
          (nodeNotFoundInData)="onNodeNotFoundInData($event)"
          class="w-full overflow-auto"
        ></app-tree-panel>
      </div>
    </div>
  }

  <!-- Toggle Button - always visible -->
  <div
    class="vertical-toggle-panel flex h-full z-10 flex-col"
    [ngClass]="{ 'absolute right-0 inset-y-0': !treeCollapsed()() }"
  >
    <!-- Home Icon Button - positioned at top -->
    <button
      class="group flex items-center justify-center p-1.5 md:p-2 bg-gray-50 hover:bg-gray-100 rounded transition cursor-pointer border border-gray-300 shadow-sm my-2 md:my-4"
      (click)="onNavigateToSelectedClient()"
      [attr.aria-label]="
        'ADMIN.SHARED.NAVIGATION_PANEL.NAVIGATE_TO_CLIENT' | translate
      "
      [title]="'ADMIN.SHARED.NAVIGATION_PANEL.GO_TO_CLIENT' | translate"
    >
      <span
        class="pi pi-home text-gray-700 group-hover:text-primary transition text-xs md:text-sm"
      ></span>
    </button>

    <!-- Spacer to push the toggle button to center -->
    <div class="flex-grow flex items-center justify-center">
      <!-- Tree Toggle Button - centered -->
      <button
        class="group flex flex-col items-center justify-center px-0.5 md:px-1 py-1.5 md:py-2 bg-gray-100 hover:bg-gray-200 rounded transition cursor-pointer border border-gray-300 shadow-sm"
        (click)="onToggleTreePanel()"
        [attr.aria-label]="
          treeCollapsed()()
            ? ('ADMIN.SHARED.NAVIGATION_PANEL.EXPAND_COMPANY_STRUCTURE'
              | translate)
            : ('ADMIN.SHARED.NAVIGATION_PANEL.COLLAPSE_COMPANY_STRUCTURE'
              | translate)
        "
      >
        <span
          class="text-xs md:text-sm font-semibold text-gray-700 tracking-tight select-none vertical-toggle-button"
        >
          {{ toggleButtonText() }}
        </span>
        <span class="mt-1 md:mt-2">
          <fa-icon
            [icon]="treeCollapsed()() ? faAngleRight : faAngleLeft"
            class="text-gray-500 group-hover:text-primary transition text-sm md:text-base"
          ></fa-icon>
        </span>
      </button>
    </div>
  </div>
</div>
`, styles: ["/* src/app/features/administration/components/admin-container/tree-navigation-panel/tree-navigation-panel.component.scss */\n.vertical-toggle-panel {\n  position: absolute;\n  right: 0;\n  top: 0;\n  bottom: 0;\n  width: 2rem;\n  min-width: 2rem;\n  max-width: 2rem;\n  overflow: visible;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  background-color: #f3f4f6;\n  transition: all 0.3s;\n  z-index: 10;\n}\n@media (min-width: 640px) {\n  .vertical-toggle-panel {\n    width: 2.5rem;\n    min-width: 2.5rem;\n    max-width: 2.5rem;\n  }\n}\n.vertical-toggle-panel button {\n  outline: none;\n  box-shadow: 0 0 0 2px var(--navigation-font-color);\n}\n.vertical-toggle-button {\n  writing-mode: vertical-lr;\n  transform: rotate(180deg);\n  letter-spacing: 0.1em;\n}\n/*# sourceMappingURL=tree-navigation-panel.component.css.map */\n"] }]
  }], null, { treeCollapsed: [{ type: Input, args: [{ isSignal: true, alias: "treeCollapsed", required: true }] }], hierarchyTree: [{ type: Input, args: [{ isSignal: true, alias: "hierarchyTree", required: true }] }], isLoading: [{ type: Input, args: [{ isSignal: true, alias: "isLoading", required: true }] }], searchingForNode: [{ type: Input, args: [{ isSignal: true, alias: "searchingForNode", required: true }] }], searchingNodeKey: [{ type: Input, args: [{ isSignal: true, alias: "searchingNodeKey", required: true }] }], selectedTreeNode: [{ type: Input, args: [{ isSignal: true, alias: "selectedTreeNode", required: true }] }], scrollToNodeKey: [{ type: Input, args: [{ isSignal: true, alias: "scrollToNodeKey", required: true }] }], scrollToCenterNodeKey: [{ type: Input, args: [{ isSignal: true, alias: "scrollToCenterNodeKey", required: true }] }], searchResults: [{ type: Input, args: [{ isSignal: true, alias: "searchResults", required: true }] }], isSearching: [{ type: Input, args: [{ isSignal: true, alias: "isSearching", required: true }] }], toggleTreePanel: [{ type: Output, args: ["toggleTreePanel"] }], navigateToSelectedClient: [{ type: Output, args: ["navigateToSelectedClient"] }], nodeExpand: [{ type: Output, args: ["nodeExpand"] }], nodeCollapse: [{ type: Output, args: ["nodeCollapse"] }], nodeSelect: [{ type: Output, args: ["nodeSelect"] }], nodeNotFoundInData: [{ type: Output, args: ["nodeNotFoundInData"] }], searchQueryChange: [{ type: Output, args: ["searchQueryChange"] }], searchResultSelect: [{ type: Output, args: ["searchResultSelect"] }], searchClear: [{ type: Output, args: ["searchClear"] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(TreeNavigationPanelComponent, { className: "TreeNavigationPanelComponent", filePath: "src/app/features/administration/components/admin-container/tree-navigation-panel/tree-navigation-panel.component.ts", lineNumber: 40 });
})();

// src/app/features/administration/services/entity/entity-details-configuration.service.ts
var DetailsConfigurationService = class _DetailsConfigurationService {
  entityStateService = inject(EntityDetailsStateService);
  confirmationService = inject(ConfirmationService);
  broadcastService = inject(BroadcastEventMessageService);
  injector = inject(Injector);
  adapters = inject(ENTITY_ADAPTER_REGISTRY);
  bulkSelectionService = inject(BulkSelectionService);
  // Track which popover is active
  // Track which popover is active
  activePopover = this.entityStateService.activePopover;
  // Registry of entity action handlers
  actionHandlers = {};
  // Registry of entity action providers
  actionProviders = {};
  // State options for different entity types
  stateOptions = {};
  constructor() {
    this.initializeDefaultHandlers();
  }
  /**
   * Initialize default operation handlers and state options
   */
  initializeDefaultHandlers() {
    this.initializeRegisteredAdapters();
  }
  initializeRegisteredAdapters() {
    const registrations = this.adapters;
    for (const reg of registrations) {
      const adapter = this.injector.get(reg.adapter);
      this.registerAdapterStateOptions(adapter, reg.types);
      this.registerAdapterActionIcons(adapter, reg.types);
      this.registerAdapterUpdateState(adapter, reg.types);
      this.registerAdapterDeleteHandler(adapter, reg.types);
    }
  }
  registerAdapterStateOptions(adapter, types) {
    if (typeof adapter.getStateOptions !== "function")
      return;
    const options = adapter.getStateOptions();
    for (const type of types) {
      this.registerStateOptions(type, options);
    }
  }
  registerAdapterActionIcons(adapter, types) {
    if (typeof adapter.getActionIcons !== "function")
      return;
    const getActionIcons = adapter.getActionIcons.bind(adapter);
    for (const type of types) {
      this.registerActionProvider(type, (id) => getActionIcons(id));
    }
  }
  registerAdapterUpdateState(adapter, types) {
    if (typeof adapter.updateState !== "function")
      return;
    const updateState = adapter.updateState.bind(adapter);
    for (const type of types) {
      this.registerOperationHandler(type, "updateState", (id, params) => {
        if (isNullOrUndefined(params.state)) {
          return throwError(() => new Error("State parameter is required"));
        }
        return updateState(id, params.state);
      });
    }
  }
  registerAdapterDeleteHandler(adapter, types) {
    if (typeof adapter.delete !== "function")
      return;
    const deleteMethod = adapter.delete.bind(adapter);
    for (const type of types) {
      this.registerOperationHandler(type, "delete", (id) => deleteMethod(id));
    }
  }
  /**
   * Gets action icons for the currently selected entity, or bulk-mode icons when multiple are selected
   */
  getActionIcons() {
    if (this.bulkSelectionService.isBulkMode()) {
      const entityType2 = String(this.bulkSelectionService.checkedItems()[0]?.entityType ?? "");
      if (!entityType2)
        return [];
      return this.getEntitySpecificActions(entityType2, "bulk").filter((icon) => icon.bulkAware === true);
    }
    const entityType = this.entityStateService.getSelectedItemType();
    const entityId = this.entityStateService.getSelectedItemId();
    if (!entityType || !entityId) {
      return [];
    }
    const specificActions = this.getEntitySpecificActions(entityType, entityId);
    return specificActions;
  }
  /**
   * Get entity-specific action icons
   */
  getEntitySpecificActions(entityType, entityId) {
    const provider = this.actionProviders[entityType];
    return isDefined(provider) ? provider(entityId) : [];
  }
  /**
   * Toggle popover visibility
   */
  togglePopover(id) {
    this.entityStateService.togglePopover(id);
  }
  /**
   * Explicitly set the active popover id
   */
  setActivePopover(id) {
    this.activePopover.set(id);
  }
  /**
   * Update entity state — applies to all bulk-checked items or falls back to the single entityId
   */
  updateEntityState(entityType, ids, state) {
    this.setActivePopover(null);
    const targetIds = ids;
    const itemCount = targetIds.length;
    const resultSubject = new Subject();
    const message = `
    <div class="flex flex-col gap-2">
      <div>You are about to apply the following changes to ${itemCount} item${itemCount > 1 ? "s" : ""}:</div>
      <div class="p-2">
        <div class="flex items-center">
          <span class="font-medium capitalize">${state}</span>
          <span class="mx-2">\u2192</span>
          <span>Change state to <span class="capitalize">'${state}'</span></span>
        </div>
      </div>
      <div class="mt-2">Are you sure you want to continue?</div>
    </div>
  `;
    this.confirmationService.confirm({
      message,
      header: "Bulk Update",
      icon: "pi pi-exclamation-triangle",
      accept: () => {
        forkJoin(targetIds.map((id) => this.executeOperation(entityType, "updateState", String(id), {
          state
        }))).subscribe({
          next: (results) => resultSubject.next(results),
          error: (error) => resultSubject.error(error),
          complete: () => resultSubject.complete()
        });
      },
      reject: () => {
        resultSubject.complete();
      },
      acceptButtonProps: { label: "Apply" },
      rejectButtonProps: { label: "Cancel", severity: "secondary" }
    });
    return resultSubject.asObservable();
  }
  /**
   * Execute an operation on an entity
   * @returns Observable of the operation result
   */
  executeOperation(entityType, operationType, entityId, params) {
    if (isNullOrUndefined(this.actionHandlers[entityType]?.[operationType])) {
      const errorMsg = `No handler found for operation ${operationType} on entity type ${entityType}`;
      return throwError(() => new Error(errorMsg));
    }
    const handler = this.actionHandlers[entityType][operationType];
    return handler(entityId, params ?? {}).pipe(tap((res) => {
      const eventType = operationType === "delete" ? "entity-deleted" : "entity-updated";
      this.broadcastService.broadcast(eventType, {
        entityType,
        operation: operationType,
        entityId,
        data: res
      });
    }), catchError((error) => {
      return throwError(() => error);
    }));
  }
  /**
   * Get state options for an entity type
   */
  getStateOptions(entityType) {
    return this.stateOptions[entityType] ?? [];
  }
  /**
   * Register a new operation handler
   */
  registerOperationHandler(entityType, operationType, handler) {
    if (isNullOrUndefined(this.actionHandlers[entityType])) {
      this.actionHandlers[entityType] = {};
    }
    this.actionHandlers[entityType][operationType] = handler;
  }
  /**
   * Register state options for an entity type
   */
  registerStateOptions(entityType, options) {
    this.stateOptions[entityType] = options;
  }
  /**
   * Register action provider for an entity type
   */
  registerActionProvider(entityType, provider) {
    this.actionProviders[entityType] = provider;
  }
  static \u0275fac = function DetailsConfigurationService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DetailsConfigurationService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _DetailsConfigurationService, factory: _DetailsConfigurationService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DetailsConfigurationService, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], () => [], null);
})();

// src/app/shared/pipes/truncate.pipe.ts
var TruncatePipe = class _TruncatePipe {
  transform(value, ...args) {
    if (!value)
      return value;
    const limit = args[0] ? parseInt(args[0], 10) : 20;
    const trail = args[1] ? args[1] : "...";
    return value.length > limit ? value.substring(0, limit) + trail : value;
  }
  static \u0275fac = function TruncatePipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _TruncatePipe)();
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({ name: "truncate", type: _TruncatePipe, pure: true });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TruncatePipe, [{
    type: Pipe,
    args: [{
      name: "truncate"
    }]
  }], null, null);
})();

// src/app/features/administration/components/details-panel/details-panel.component.ts
var _c04 = ["statePopover"];
var _forTrack02 = ($index, $item) => $item.value;
var _forTrack1 = ($index, $item) => $item.id;
function DetailsPanelComponent_ng_template_2_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "h2", 16);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = \u0275\u0275nextContext(2);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate1(" ", ctx_r0.bulkLabel, " ");
  }
}
function DetailsPanelComponent_ng_template_2_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "h2", 17);
    \u0275\u0275text(1);
    \u0275\u0275pipe(2, "truncate");
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = \u0275\u0275nextContext(2);
    \u0275\u0275property("pTooltip", ctx_r0.detailsService.getSelectedItemTitle());
    \u0275\u0275advance();
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(2, 2, ctx_r0.detailsService.getSelectedItemTitle(), "35"), " ");
  }
}
function DetailsPanelComponent_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "span", 15);
    \u0275\u0275pipe(1, "translate");
    \u0275\u0275conditionalCreate(2, DetailsPanelComponent_ng_template_2_Conditional_2_Template, 2, 1, "h2", 16)(3, DetailsPanelComponent_ng_template_2_Conditional_3_Template, 3, 5, "h2", 17);
  }
  if (rf & 2) {
    const ctx_r0 = \u0275\u0275nextContext();
    \u0275\u0275attribute("aria-label", \u0275\u0275pipeBind1(1, 2, "ADMIN.DETAILS_PANEL.ARIA.BOX_ICON"));
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r0.bulkSelectionService.isBulkMode() ? 2 : 3);
  }
}
function DetailsPanelComponent_ng_template_4_Template(rf, ctx) {
}
function DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "p-button", 24);
    \u0275\u0275listener("onClick", function DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Conditional_1_Template_p_button_onClick_0_listener() {
      \u0275\u0275restoreView(_r3);
      const icon_r4 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r0 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r0.executeAction(icon_r4));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const icon_r4 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275property("label", icon_r4.label ?? "")("disabled", icon_r4.disabled ?? false)("pTooltip", icon_r4.disabled ? icon_r4.tooltip : "");
  }
}
function DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 22);
    \u0275\u0275namespaceSVG();
    \u0275\u0275elementStart(1, "svg", 25);
    \u0275\u0275listener("click", function DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Conditional_2_Template_svg_click_1_listener($event) {
      \u0275\u0275restoreView(_r5);
      const icon_r4 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r0 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(!icon_r4.disabled && ctx_r0.executeAction(icon_r4, $event, icon_r4.id === "changeState"));
    })("keydown.enter", function DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Conditional_2_Template_svg_keydown_enter_1_listener($event) {
      \u0275\u0275restoreView(_r5);
      const icon_r4 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r0 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(!icon_r4.disabled && ctx_r0.executeAction(icon_r4, $event, icon_r4.id === "changeState"));
    });
    \u0275\u0275element(2, "use");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const icon_r4 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r0 = \u0275\u0275nextContext(2);
    \u0275\u0275property("pTooltip", ctx_r0.resolveTooltip(icon_r4));
    \u0275\u0275advance();
    \u0275\u0275classProp("opacity-50", icon_r4.disabled)("pointer-events-none", icon_r4.disabled);
    \u0275\u0275attribute("aria-label", icon_r4.tooltip);
    \u0275\u0275advance();
    \u0275\u0275attribute("href", icon_r4.icon);
  }
}
function DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "span", 26);
    \u0275\u0275listener("click", function DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Conditional_3_Template_span_click_0_listener($event) {
      \u0275\u0275restoreView(_r6);
      const icon_r4 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r0 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(!icon_r4.disabled && ctx_r0.executeAction(icon_r4, $event, icon_r4.id === "changeState"));
    })("keydown.enter", function DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Conditional_3_Template_span_keydown_enter_0_listener($event) {
      \u0275\u0275restoreView(_r6);
      const icon_r4 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r0 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(!icon_r4.disabled && ctx_r0.executeAction(icon_r4, $event, icon_r4.id === "changeState"));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const icon_r4 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r0 = \u0275\u0275nextContext(2);
    \u0275\u0275classMap(\u0275\u0275interpolate1("", icon_r4.icon, " action hover !text-xl cursor-pointer"));
    \u0275\u0275classProp("opacity-50", icon_r4.disabled);
    \u0275\u0275property("pTooltip", ctx_r0.resolveTooltip(icon_r4));
    \u0275\u0275attribute("aria-label", icon_r4.tooltip);
  }
}
function DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 20);
    \u0275\u0275conditionalCreate(1, DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Conditional_1_Template, 1, 3, "p-button", 21)(2, DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Conditional_2_Template, 3, 7, "div", 22)(3, DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Conditional_3_Template, 1, 7, "span", 23);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const icon_r4 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275advance();
    \u0275\u0275conditional(icon_r4.isButton ? 1 : icon_r4.isSvg ? 2 : 3);
  }
}
function DetailsPanelComponent_ng_template_6_For_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275conditionalCreate(0, DetailsPanelComponent_ng_template_6_For_2_Conditional_0_Template, 4, 1, "div", 20);
  }
  if (rf & 2) {
    const icon_r4 = ctx.$implicit;
    \u0275\u0275conditional(icon_r4.visible ? 0 : -1);
  }
}
function DetailsPanelComponent_ng_template_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 18);
    \u0275\u0275repeaterCreate(1, DetailsPanelComponent_ng_template_6_For_2_Template, 1, 1, null, null, _forTrack1);
    \u0275\u0275elementStart(3, "button", 19);
    \u0275\u0275pipe(4, "translate");
    \u0275\u0275listener("click", function DetailsPanelComponent_ng_template_6_Template_button_click_3_listener() {
      \u0275\u0275restoreView(_r2);
      const ctx_r0 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r0.onClosePanel());
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r0.actionIcons);
    \u0275\u0275advance(2);
    \u0275\u0275attribute("aria-label", \u0275\u0275pipeBind1(4, 1, "ADMIN.DETAILS_PANEL.ARIA.CLOSE"));
  }
}
function DetailsPanelComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 8);
    \u0275\u0275element(1, "p-progressSpinner", 27);
    \u0275\u0275elementEnd();
  }
}
function DetailsPanelComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementContainer(0, 9);
  }
  if (rf & 2) {
    const ctx_r0 = \u0275\u0275nextContext();
    \u0275\u0275property("ngComponentOutlet", ctx_r0.detailsService.detailsComponent())("ngComponentOutletInputs", ctx_r0.detailsService.detailsInputs());
  }
}
function DetailsPanelComponent_For_19_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "span", 30);
  }
}
function DetailsPanelComponent_For_19_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 28);
    \u0275\u0275listener("click", function DetailsPanelComponent_For_19_Template_button_click_0_listener() {
      const state_r8 = \u0275\u0275restoreView(_r7).$implicit;
      const ctx_r0 = \u0275\u0275nextContext();
      const statePopover_r9 = \u0275\u0275reference(12);
      ctx_r0.updateState(state_r8.value);
      return \u0275\u0275resetView(statePopover_r9.hide());
    });
    \u0275\u0275elementStart(1, "span", 29);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(3, DetailsPanelComponent_For_19_Conditional_3_Template, 1, 0, "span", 30);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const state_r8 = ctx.$implicit;
    const ctx_r0 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(state_r8.label);
    \u0275\u0275advance();
    \u0275\u0275conditional(state_r8.value === ctx_r0.currentState ? 3 : -1);
  }
}
function DetailsPanelComponent_ng_template_21_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 31);
    \u0275\u0275element(1, "div", 32);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const message_r10 = ctx.$implicit;
    \u0275\u0275advance();
    \u0275\u0275property("innerHTML", message_r10.message, \u0275\u0275sanitizeHtml);
  }
}
var DetailsPanelComponent = class _DetailsPanelComponent {
  detailsService = inject(EntityDetailsService);
  stateService = inject(EntityDetailsStateService);
  bulkSelectionService = inject(BulkSelectionService);
  detailsConfigService = inject(DetailsConfigurationService);
  destroyRef = inject(DestroyRef);
  activatedRoute = inject(ActivatedRoute);
  detailsFactory = inject(DetailsComponentFactory);
  closePanel = output();
  statePopover;
  actionIcons = [];
  // Get currently active popover
  get activePopover() {
    return this.detailsConfigService.activePopover();
  }
  // Get state options for current entity (or bulk entity type)
  get stateOptions() {
    const entityType = this.stateService.getSelectedItemType() || (this.bulkSelectionService.isBulkMode() === true ? this.bulkSelectionService.checkedItems()[0]?.entityType ?? "" : null);
    return isDefined(entityType) ? this.detailsConfigService.getStateOptions(entityType) : [];
  }
  // Get current state of the entity
  get currentState() {
    const entity = this.stateService.selectedItem();
    return entity?.["state"] ?? "";
  }
  // Human-readable label for the bulk selection header
  get bulkLabel() {
    const items = this.bulkSelectionService.checkedItems();
    const rawType = items[0]?.entityType ?? "item";
    const label = rawType.charAt(0).toUpperCase() + rawType.slice(1);
    return `${items.length} ${label}s selected`;
  }
  constructor() {
    effect(() => {
      this.stateService.selectedItem();
      this.bulkSelectionService.checkedItems();
      this.updateActionIcons();
    });
  }
  ngOnInit() {
    this.updateActionIcons();
  }
  updateActionIcons() {
    this.actionIcons = this.detailsConfigService.getActionIcons();
  }
  executeAction(icon, event, usePopover) {
    const isBulk = this.bulkSelectionService.isBulkMode() === true;
    const dispatch = () => {
      if (isBulk && icon.bulkAction) {
        const ids = this.bulkSelectionService.checkedItems().map((i) => String(i.id));
        icon.bulkAction(ids);
      } else {
        icon.action();
      }
    };
    if (usePopover === true && event) {
      dispatch();
      if (this.statePopover?.overlayVisible) {
        this.statePopover.hide();
      } else {
        this.statePopover.show(event);
      }
    } else {
      dispatch();
    }
  }
  resolveTooltip(icon) {
    const isBulk = this.bulkSelectionService.isBulkMode() === true;
    if (isBulk && isDefined(icon.bulkTooltip)) {
      if (typeof icon.bulkTooltip === "function") {
        const ids = this.bulkSelectionService.checkedItems().map((i) => String(i.id));
        return icon.bulkTooltip(ids);
      }
      return icon.bulkTooltip;
    }
    return icon.tooltip;
  }
  togglePopover(id) {
    this.detailsConfigService.togglePopover(id);
  }
  // OverlayPanel handles its own positioning; no-op placeholder for future hooks
  setActivePopover(id) {
    this.detailsConfigService.setActivePopover(id);
  }
  updateState(state) {
    if (state === this.currentState) {
      this.togglePopover("changeState");
      return;
    }
    const routeEntityType = this.detailsFactory.getEntityTypeFromRoute(this.activatedRoute, "type");
    const isBulk = this.bulkSelectionService.isBulkMode() === true;
    const checkedItem = this.bulkSelectionService.checkedItems()[0];
    const entityType = this.stateService.getSelectedItemType() || String(checkedItem.entityType) || routeEntityType;
    const ids = isBulk ? this.bulkSelectionService.checkedItems().map((i) => String(i.id)) : [String(this.stateService.getSelectedItemId() ?? "")].filter(Boolean);
    if (isDefined(entityType) && ids.length > 0) {
      this.detailsConfigService.updateEntityState(entityType, ids, state).pipe(takeUntilDestroyed(this.destroyRef)).subscribe();
    }
  }
  onClosePanel() {
    this.detailsService.closeDetailsPanel();
    this.closePanel.emit();
  }
  static \u0275fac = function DetailsPanelComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DetailsPanelComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _DetailsPanelComponent, selectors: [["app-details-panel"]], viewQuery: function DetailsPanelComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuery(_c04, 5);
    }
    if (rf & 2) {
      let _t;
      \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.statePopover = _t.first);
    }
  }, outputs: { closePanel: "closePanel" }, decls: 23, vars: 6, consts: [["start", ""], ["center", ""], ["end", ""], ["statePopover", ""], ["message", ""], [1, "h-full", "flex", "flex-col", "overflow-hidden"], [1, "!rounded-none", "!border-x-0", "h-header"], [1, "flex-1", "p-0", "overflow-y-auto", "overflow-x-hidden", "min-w-0"], [1, "flex", "flex-col", "justify-content-center", "items-center", "h-full"], [3, "ngComponentOutlet", "ngComponentOutletInputs"], ["appendTo", "body", "styleClass", "state-overlay-panel", 3, "onShow", "onHide", "dismissable"], [1, "px-3", "py-2", "border-b"], [1, "text-sm", "font-medium"], [1, "max-h-72", "overflow-auto", "py-1"], [1, "w-full", "text-left", "py-2", "px-3", "text-sm", "flex", "items-center", "hover:bg-emphasis", "cursor-pointer"], [1, "pi", "pi-box", "pr-2", "!text-xl"], [1, "font-semibold"], [1, "font-semibold", "cursor-pointer", 3, "pTooltip"], [1, "flex", "row", "gap-2", "items-center", "overflow-x-auto", "toolbar-actions-container"], ["pButton", "", "icon", "pi pi-times", 1, "p-button-rounded", 3, "click"], [1, "relative"], ["size", "small", "tooltipPosition", "left", "appendTo", "body", 3, "label", "disabled", "pTooltip"], ["tooltipPosition", "left", "appendTo", "body", 3, "pTooltip"], ["tooltipPosition", "left", "appendTo", "body", 3, "class", "opacity-50", "pTooltip"], ["size", "small", "tooltipPosition", "left", "appendTo", "body", 3, "onClick", "label", "disabled", "pTooltip"], [1, "custom-icon-svg", "w-[1.25rem]", "h-[1.25rem]", "cursor-pointer", "action", "hover", 3, "click", "keydown.enter"], ["tooltipPosition", "left", "appendTo", "body", 3, "click", "keydown.enter", "pTooltip"], [1, "w-8", "h-8"], [1, "w-full", "text-left", "py-2", "px-3", "text-sm", "flex", "items-center", "hover:bg-emphasis", "cursor-pointer", 3, "click"], [1, "flex-1"], [1, "pi", "pi-check", "text-primary", "ml-2"], [1, "py-2", "px-1"], [3, "innerHTML"]], template: function DetailsPanelComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "div", 5)(1, "p-toolbar", 6);
      \u0275\u0275template(2, DetailsPanelComponent_ng_template_2_Template, 4, 4, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(4, DetailsPanelComponent_ng_template_4_Template, 0, 0, "ng-template", null, 1, \u0275\u0275templateRefExtractor)(6, DetailsPanelComponent_ng_template_6_Template, 5, 3, "ng-template", null, 2, \u0275\u0275templateRefExtractor);
      \u0275\u0275elementEnd();
      \u0275\u0275elementStart(8, "div", 7);
      \u0275\u0275conditionalCreate(9, DetailsPanelComponent_Conditional_9_Template, 2, 0, "div", 8);
      \u0275\u0275conditionalCreate(10, DetailsPanelComponent_Conditional_10_Template, 1, 2, "ng-container", 9);
      \u0275\u0275elementEnd()();
      \u0275\u0275elementStart(11, "p-popover", 10, 3);
      \u0275\u0275listener("onShow", function DetailsPanelComponent_Template_p_popover_onShow_11_listener() {
        return ctx.setActivePopover("changeState");
      })("onHide", function DetailsPanelComponent_Template_p_popover_onHide_11_listener() {
        return ctx.setActivePopover(null);
      });
      \u0275\u0275elementStart(13, "div", 11)(14, "h3", 12);
      \u0275\u0275text(15);
      \u0275\u0275pipe(16, "translate");
      \u0275\u0275elementEnd()();
      \u0275\u0275elementStart(17, "div", 13);
      \u0275\u0275repeaterCreate(18, DetailsPanelComponent_For_19_Template, 4, 2, "button", 14, _forTrack02);
      \u0275\u0275elementEnd()();
      \u0275\u0275elementStart(20, "p-confirmDialog");
      \u0275\u0275template(21, DetailsPanelComponent_ng_template_21_Template, 2, 1, "ng-template", null, 4, \u0275\u0275templateRefExtractor);
      \u0275\u0275elementEnd();
    }
    if (rf & 2) {
      \u0275\u0275advance(9);
      \u0275\u0275conditional(ctx.detailsService.isLoading() && !ctx.detailsService.isSoftRefreshing() ? 9 : -1);
      \u0275\u0275advance();
      \u0275\u0275conditional((!ctx.detailsService.isLoading() || ctx.detailsService.isSoftRefreshing()) && ctx.detailsService.detailsComponent() ? 10 : -1);
      \u0275\u0275advance();
      \u0275\u0275property("dismissable", true);
      \u0275\u0275advance(4);
      \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(16, 4, "ADMIN.DETAILS_PANEL.CHANGE_STATE_TITLE"), " ");
      \u0275\u0275advance(3);
      \u0275\u0275repeater(ctx.stateOptions);
    }
  }, dependencies: [
    CommonModule,
    NgComponentOutlet,
    ButtonModule,
    ButtonDirective,
    Button,
    ProgressSpinnerModule,
    ProgressSpinner,
    CardModule,
    RouterModule,
    ToolbarModule,
    Toolbar,
    TooltipModule,
    Tooltip,
    ConfirmDialogModule,
    ConfirmDialog,
    PopoverModule,
    Popover,
    TruncatePipe,
    TranslatePipe
  ], styles: ['\n[_nghost-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  height: 100%;\n  width: 100%;\n  overflow: hidden;\n  min-width: 0;\n}\n[_nghost-%COMP%]    > div[_ngcontent-%COMP%] {\n  min-width: 0;\n  max-width: 100%;\n}\n.flex-1[_ngcontent-%COMP%] {\n  flex: 1 1 auto;\n  min-height: 0;\n}\n  .p-component-outlet {\n  height: 100%;\n}\n  .p-component-outlet > * {\n  height: 100%;\n  display: block;\n}\n  .state-popover {\n  background: var(--input-background-color);\n  animation: _ngcontent-%COMP%_fadeIn 0.2s ease-in-out;\n}\n  .state-overlay-panel {\n  border-radius: 0.5rem;\n  min-width: 220px;\n  max-width: min(320px, 90vw);\n  padding: 0;\n}\n  .state-overlay-panel .p-popover-content {\n  padding: 0;\n}\n  .p-toolbar {\n  min-width: 0;\n  flex-shrink: 1;\n}\n  .p-toolbar .p-toolbar-group-start {\n  flex-shrink: 1;\n  min-width: 0;\n  overflow: hidden;\n}\n  .p-toolbar .p-toolbar-group-start h2 {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n  .p-toolbar .p-toolbar-group-end {\n  flex-shrink: 0;\n  min-width: 0;\n  display: flex !important;\n}\n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container {\n  overflow-x: auto;\n  overflow-y: hidden;\n  display: flex;\n  gap: 0.5rem;\n  align-items: center;\n  max-width: 100%;\n  flex-wrap: nowrap;\n  position: relative;\n  scroll-behavior: smooth;\n  -webkit-overflow-scrolling: touch;\n  scrollbar-width: thin;\n  scrollbar-color: rgba(0, 0, 0, 0.2) transparent;\n}\n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container::-webkit-scrollbar {\n  height: 4px;\n}\n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container::-webkit-scrollbar-track {\n  background: transparent;\n}\n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container::-webkit-scrollbar-thumb {\n  background-color: rgba(0, 0, 0, 0.2);\n  border-radius: 2px;\n}\n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container::-webkit-scrollbar-thumb:hover {\n  background-color: rgba(0, 0, 0, 0.3);\n}\n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container::after {\n  content: "";\n  position: absolute;\n  right: 0;\n  top: 0;\n  bottom: 4px;\n  width: 30px;\n  background:\n    linear-gradient(\n      to right,\n      transparent,\n      var(--surface-0, #ffffff) 70%);\n  pointer-events: none;\n  opacity: 0;\n  transition: opacity 0.2s ease;\n}\n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container:not(:hover)::after {\n  opacity: 1;\n}\n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container > * {\n  flex-shrink: 0;\n}\n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container button, \n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container span, \n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container svg, \n  .p-toolbar .p-toolbar-group-end .toolbar-actions-container div {\n  flex-shrink: 0;\n}\n.toolbar-actions-container[_ngcontent-%COMP%] {\n  flex-wrap: nowrap !important;\n  -webkit-overflow-scrolling: touch;\n  max-width: 100%;\n  min-width: 0;\n}\n.toolbar-actions-container[_ngcontent-%COMP%]    > *[_ngcontent-%COMP%] {\n  flex-shrink: 0 !important;\n}\n[_nghost-%COMP%]     app-details-panel {\n  min-width: 0;\n  max-width: 100%;\n}\n@media (max-width: 1024px) {\n    .p-toolbar .p-toolbar-group-start h2 {\n    font-size: 0.95rem;\n  }\n    .p-toolbar .p-toolbar-group-end .toolbar-actions-container {\n    gap: 0.375rem;\n  }\n}\n@keyframes _ngcontent-%COMP%_fadeIn {\n  from {\n    opacity: 0;\n    transform: translateY(-10px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n.hover[_ngcontent-%COMP%]:hover {\n  opacity: 1 !important;\n  color: var(--color-primary-color);\n}\n/*# sourceMappingURL=details-panel.component.css.map */'] });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DetailsPanelComponent, [{
    type: Component,
    args: [{ selector: "app-details-panel", imports: [
      CommonModule,
      ButtonModule,
      ProgressSpinnerModule,
      CardModule,
      RouterModule,
      ToolbarModule,
      TooltipModule,
      ConfirmDialogModule,
      TruncatePipe,
      TranslatePipe,
      PopoverModule
    ], template: `<div class="h-full flex flex-col overflow-hidden">
  <p-toolbar class="!rounded-none !border-x-0 h-header">
    <ng-template #start>
      <span
        class="pi pi-box pr-2 !text-xl"
        [attr.aria-label]="'ADMIN.DETAILS_PANEL.ARIA.BOX_ICON' | translate"
      ></span>
      @if (bulkSelectionService.isBulkMode()) {
        <h2 class="font-semibold">
          {{ bulkLabel }}
        </h2>
      } @else {
        <h2
          class="font-semibold cursor-pointer"
          [pTooltip]="detailsService.getSelectedItemTitle()"
        >
          {{ detailsService.getSelectedItemTitle() | truncate: '35' }}
        </h2>
      }
    </ng-template>
    <ng-template #center> </ng-template>
    <ng-template #end>
      <div
        class="flex row gap-2 items-center overflow-x-auto toolbar-actions-container"
      >
        <!-- Dynamic action icons -->
        @for (icon of actionIcons; track icon.id) {
          @if (icon.visible) {
            <div class="relative">
              <!-- Button variant -->
              @if (icon.isButton) {
                <p-button
                  [label]="icon.label ?? ''"
                  size="small"
                  [disabled]="icon.disabled ?? false"
                  [pTooltip]="icon.disabled ? icon.tooltip : ''"
                  tooltipPosition="left"
                  appendTo="body"
                  (onClick)="executeAction(icon)"
                ></p-button>
              }
              <!-- SVG icon -->
              @else if (icon.isSvg) {
                <div
                  [pTooltip]="resolveTooltip(icon)"
                  tooltipPosition="left"
                  appendTo="body"
                >
                  <svg
                    class="custom-icon-svg w-[1.25rem] h-[1.25rem] cursor-pointer action hover"
                    [class.opacity-50]="icon.disabled"
                    [class.pointer-events-none]="icon.disabled"
                    [attr.aria-label]="icon.tooltip"
                    (click)="
                      !icon.disabled &&
                        executeAction(icon, $event, icon.id === 'changeState')
                    "
                    (keydown.enter)="
                      !icon.disabled &&
                        executeAction(icon, $event, icon.id === 'changeState')
                    "
                  >
                    <use [attr.href]="icon.icon"></use>
                  </svg>
                </div>
              }
              <!-- PrimeNG icon -->
              @else {
                <span
                  class="{{ icon.icon }} action hover !text-xl cursor-pointer"
                  [class.opacity-50]="icon.disabled"
                  [attr.aria-label]="icon.tooltip"
                  [pTooltip]="resolveTooltip(icon)"
                  tooltipPosition="left"
                  appendTo="body"
                  (click)="
                    !icon.disabled &&
                      executeAction(icon, $event, icon.id === 'changeState')
                  "
                  (keydown.enter)="
                    !icon.disabled &&
                      executeAction(icon, $event, icon.id === 'changeState')
                  "
                ></span>
              }
            </div>
          }
        }
        <!-- Close button (always present) -->
        <button
          pButton
          icon="pi pi-times"
          class="p-button-rounded"
          (click)="onClosePanel()"
          [attr.aria-label]="'ADMIN.DETAILS_PANEL.ARIA.CLOSE' | translate"
        ></button>
      </div>
    </ng-template>
  </p-toolbar>
  <div class="flex-1 p-0 overflow-y-auto overflow-x-hidden min-w-0">
    @if (detailsService.isLoading() && !detailsService.isSoftRefreshing()) {
      <div class="flex flex-col justify-content-center items-center h-full">
        <p-progressSpinner class="w-8 h-8"></p-progressSpinner>
      </div>
    }
    @if (
      (!detailsService.isLoading() || detailsService.isSoftRefreshing()) &&
      detailsService.detailsComponent()
    ) {
      <ng-container
        [ngComponentOutlet]="detailsService.detailsComponent()!"
        [ngComponentOutletInputs]="detailsService.detailsInputs()"
      >
      </ng-container>
    }
  </div>
</div>

<!-- State popover (anchored to trigger) -->
<p-popover
  #statePopover
  appendTo="body"
  [dismissable]="true"
  styleClass="state-overlay-panel"
  (onShow)="setActivePopover('changeState')"
  (onHide)="setActivePopover(null)"
>
  <div class="px-3 py-2 border-b">
    <h3 class="text-sm font-medium">
      {{ 'ADMIN.DETAILS_PANEL.CHANGE_STATE_TITLE' | translate }}
    </h3>
  </div>
  <div class="max-h-72 overflow-auto py-1">
    @for (state of stateOptions; track state.value) {
      <button
        class="w-full text-left py-2 px-3 text-sm flex items-center hover:bg-emphasis cursor-pointer"
        (click)="updateState(state.value); statePopover.hide()"
      >
        <span class="flex-1">{{ state.label }}</span>
        @if (state.value === currentState) {
          <span class="pi pi-check text-primary ml-2"></span>
        }
      </button>
    }
  </div>
</p-popover>

<!-- Confirmation dialog -->
<p-confirmDialog>
  <ng-template #message let-message>
    <div class="py-2 px-1">
      <div [innerHTML]="message.message"></div>
    </div>
  </ng-template>
</p-confirmDialog>
`, styles: ['/* src/app/features/administration/components/details-panel/details-panel.component.scss */\n:host {\n  display: flex;\n  flex-direction: column;\n  height: 100%;\n  width: 100%;\n  overflow: hidden;\n  min-width: 0;\n}\n:host > div {\n  min-width: 0;\n  max-width: 100%;\n}\n.flex-1 {\n  flex: 1 1 auto;\n  min-height: 0;\n}\n::ng-deep .p-component-outlet {\n  height: 100%;\n}\n::ng-deep .p-component-outlet > * {\n  height: 100%;\n  display: block;\n}\n::ng-deep .state-popover {\n  background: var(--input-background-color);\n  animation: fadeIn 0.2s ease-in-out;\n}\n::ng-deep .state-overlay-panel {\n  border-radius: 0.5rem;\n  min-width: 220px;\n  max-width: min(320px, 90vw);\n  padding: 0;\n}\n::ng-deep .state-overlay-panel .p-popover-content {\n  padding: 0;\n}\n::ng-deep .p-toolbar {\n  min-width: 0;\n  flex-shrink: 1;\n}\n::ng-deep .p-toolbar .p-toolbar-group-start {\n  flex-shrink: 1;\n  min-width: 0;\n  overflow: hidden;\n}\n::ng-deep .p-toolbar .p-toolbar-group-start h2 {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n::ng-deep .p-toolbar .p-toolbar-group-end {\n  flex-shrink: 0;\n  min-width: 0;\n  display: flex !important;\n}\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container {\n  overflow-x: auto;\n  overflow-y: hidden;\n  display: flex;\n  gap: 0.5rem;\n  align-items: center;\n  max-width: 100%;\n  flex-wrap: nowrap;\n  position: relative;\n  scroll-behavior: smooth;\n  -webkit-overflow-scrolling: touch;\n  scrollbar-width: thin;\n  scrollbar-color: rgba(0, 0, 0, 0.2) transparent;\n}\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container::-webkit-scrollbar {\n  height: 4px;\n}\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container::-webkit-scrollbar-track {\n  background: transparent;\n}\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container::-webkit-scrollbar-thumb {\n  background-color: rgba(0, 0, 0, 0.2);\n  border-radius: 2px;\n}\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container::-webkit-scrollbar-thumb:hover {\n  background-color: rgba(0, 0, 0, 0.3);\n}\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container::after {\n  content: "";\n  position: absolute;\n  right: 0;\n  top: 0;\n  bottom: 4px;\n  width: 30px;\n  background:\n    linear-gradient(\n      to right,\n      transparent,\n      var(--surface-0, #ffffff) 70%);\n  pointer-events: none;\n  opacity: 0;\n  transition: opacity 0.2s ease;\n}\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container:not(:hover)::after {\n  opacity: 1;\n}\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container > * {\n  flex-shrink: 0;\n}\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container button,\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container span,\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container svg,\n::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container div {\n  flex-shrink: 0;\n}\n.toolbar-actions-container {\n  flex-wrap: nowrap !important;\n  -webkit-overflow-scrolling: touch;\n  max-width: 100%;\n  min-width: 0;\n}\n.toolbar-actions-container > * {\n  flex-shrink: 0 !important;\n}\n:host ::ng-deep app-details-panel {\n  min-width: 0;\n  max-width: 100%;\n}\n@media (max-width: 1024px) {\n  ::ng-deep .p-toolbar .p-toolbar-group-start h2 {\n    font-size: 0.95rem;\n  }\n  ::ng-deep .p-toolbar .p-toolbar-group-end .toolbar-actions-container {\n    gap: 0.375rem;\n  }\n}\n@keyframes fadeIn {\n  from {\n    opacity: 0;\n    transform: translateY(-10px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n.hover:hover {\n  opacity: 1 !important;\n  color: var(--color-primary-color);\n}\n/*# sourceMappingURL=details-panel.component.css.map */\n'] }]
  }], () => [], { closePanel: [{ type: Output, args: ["closePanel"] }], statePopover: [{
    type: ViewChild,
    args: ["statePopover"]
  }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(DetailsPanelComponent, { className: "DetailsPanelComponent", filePath: "src/app/features/administration/components/details-panel/details-panel.component.ts", lineNumber: 50 });
})();

// src/app/features/administration/components/admin-container/admin-main-content/admin-main-content.component.ts
var _c05 = (a0, a1, a2, a3) => ({ content: a0, scrollable: a1, minSize: a2, maxSize: a3 });
var _c13 = (a0, a1, a2) => ({ content: a0, scrollable: true, minSize: a1, maxSize: a2 });
var _c22 = (a0, a1) => [a0, a1];
var _c3 = () => [65, 35];
var _c4 = () => [100, 0];
var _c5 = () => [40, "320px"];
function AdminMainContentComponent_Conditional_0_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "app-details-panel", 7);
    \u0275\u0275listener("close", function AdminMainContentComponent_Conditional_0_ng_template_3_Template_app_details_panel_close_0_listener() {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.detailsService.closeDetailsPanel());
    });
    \u0275\u0275elementEnd();
  }
}
function AdminMainContentComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "router-outlet", 5);
    \u0275\u0275listener("activate", function AdminMainContentComponent_Conditional_0_Template_router_outlet_activate_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onRouterOutletActivate($event));
    });
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(2, "p-drawer", 6);
    \u0275\u0275twoWayListener("visibleChange", function AdminMainContentComponent_Conditional_0_Template_p_drawer_visibleChange_2_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      \u0275\u0275twoWayBindingSet(ctx_r1.drawerVisible, $event) || (ctx_r1.drawerVisible = $event);
      return \u0275\u0275resetView($event);
    });
    \u0275\u0275listener("onHide", function AdminMainContentComponent_Conditional_0_Template_p_drawer_onHide_2_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDrawerClose());
    });
    \u0275\u0275template(3, AdminMainContentComponent_Conditional_0_ng_template_3_Template, 1, 0, "ng-template", null, 0, \u0275\u0275templateRefExtractor);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275twoWayProperty("visible", ctx_r1.drawerVisible);
    \u0275\u0275property("modal", false);
  }
}
function AdminMainContentComponent_Conditional_1_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "router-outlet", 5);
    \u0275\u0275listener("activate", function AdminMainContentComponent_Conditional_1_ng_template_1_Template_router_outlet_activate_1_listener($event) {
      \u0275\u0275restoreView(_r4);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.onRouterOutletActivate($event));
    });
    \u0275\u0275elementEnd()();
  }
}
function AdminMainContentComponent_Conditional_1_ng_template_3_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "app-details-panel", 7);
    \u0275\u0275listener("close", function AdminMainContentComponent_Conditional_1_ng_template_3_Conditional_0_Template_app_details_panel_close_0_listener() {
      \u0275\u0275restoreView(_r5);
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.detailsService.closeDetailsPanel());
    });
    \u0275\u0275elementEnd();
  }
}
function AdminMainContentComponent_Conditional_1_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275conditionalCreate(0, AdminMainContentComponent_Conditional_1_ng_template_3_Conditional_0_Template, 1, 0, "app-details-panel", 8);
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275conditional(ctx_r1.detailsService.showPanel() ? 0 : -1);
  }
}
function AdminMainContentComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "app-adaptive-layout", 3);
    \u0275\u0275template(1, AdminMainContentComponent_Conditional_1_ng_template_1_Template, 2, 0, "ng-template", null, 1, \u0275\u0275templateRefExtractor)(3, AdminMainContentComponent_Conditional_1_ng_template_3_Template, 1, 1, "ng-template", null, 2, \u0275\u0275templateRefExtractor);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const nestedContent_r6 = \u0275\u0275reference(2);
    const detailsContent_r7 = \u0275\u0275reference(4);
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("ngClass", ctx_r1.detailsPanelClass()())("panels", \u0275\u0275pureFunction2(15, _c22, \u0275\u0275pureFunction4(6, _c05, nestedContent_r6, ctx_r1.nestedContentScrollable()(), ctx_r1.detailsService.showPanel() ? 40 : 100, ctx_r1.detailsService.showPanel() ? 80 : 100), \u0275\u0275pureFunction3(11, _c13, detailsContent_r7, ctx_r1.detailsService.showPanel() ? "320px" : 0, ctx_r1.detailsService.showPanel() ? 60 : 0)))("panelSizes", ctx_r1.detailsService.showPanel() ? \u0275\u0275pureFunction0(18, _c3) : \u0275\u0275pureFunction0(19, _c4))("minSizes", ctx_r1.detailsService.showPanel() ? \u0275\u0275pureFunction0(20, _c5) : \u0275\u0275pureFunction0(21, _c4))("defaultLayout", "horizontal")("responsive", true);
  }
}
var AdminMainContentComponent = class _AdminMainContentComponent {
  detailsService = inject(EntityDetailsService);
  // Input properties
  nestedContentScrollable = input.required(...ngDevMode ? [{ debugName: "nestedContentScrollable" }] : (
    /* istanbul ignore next */
    []
  ));
  detailsPanelClass = input.required(...ngDevMode ? [{ debugName: "detailsPanelClass" }] : (
    /* istanbul ignore next */
    []
  ));
  shouldUseDrawer = input(() => false, ...ngDevMode ? [{ debugName: "shouldUseDrawer" }] : (
    /* istanbul ignore next */
    []
  ));
  // Local writable signal for drawer visibility
  drawerVisible = signal(false, ...ngDevMode ? [{ debugName: "drawerVisible" }] : (
    /* istanbul ignore next */
    []
  ));
  // Output events
  routerOutletActivate = output();
  constructor() {
    effect(() => {
      const shouldUseDrawer = this.shouldUseDrawer()();
      const showPanel = this.detailsService.showPanel();
      this.drawerVisible.set(shouldUseDrawer && showPanel);
    });
  }
  /**
   * Handles router outlet activation
   */
  onRouterOutletActivate(component) {
    this.routerOutletActivate.emit(component);
  }
  /**
   * Handles drawer close
   */
  onDrawerClose() {
    this.drawerVisible.set(false);
    this.detailsService.closeDetailsPanel();
  }
  static \u0275fac = function AdminMainContentComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminMainContentComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _AdminMainContentComponent, selectors: [["app-admin-main-content"]], inputs: { nestedContentScrollable: [1, "nestedContentScrollable"], detailsPanelClass: [1, "detailsPanelClass"], shouldUseDrawer: [1, "shouldUseDrawer"] }, outputs: { routerOutletActivate: "routerOutletActivate" }, decls: 2, vars: 1, consts: [["headless", ""], ["nestedContent", ""], ["detailsContent", ""], ["stateKey", "details-layout", 1, "h-full", "w-full", "overflow-hidden", 3, "ngClass", "panels", "panelSizes", "minSizes", "defaultLayout", "responsive"], [1, "h-full", "w-full", "flex", "flex-col"], [3, "activate"], ["position", "right", "styleClass", "sm:!w-full md:!w-80 lg:!w-[30rem]", 3, "visibleChange", "onHide", "visible", "modal"], [1, "h-full", "flex", "flex-col", 3, "close"], [1, "h-full", "flex", "flex-col"]], template: function AdminMainContentComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275conditionalCreate(0, AdminMainContentComponent_Conditional_0_Template, 5, 2)(1, AdminMainContentComponent_Conditional_1_Template, 5, 22, "app-adaptive-layout", 3);
    }
    if (rf & 2) {
      \u0275\u0275conditional(ctx.shouldUseDrawer()() ? 0 : 1);
    }
  }, dependencies: [
    CommonModule,
    NgClass,
    RouterModule,
    RouterOutlet,
    AdaptiveLayoutComponent,
    DetailsPanelComponent,
    DrawerModule,
    Drawer
  ], styles: ["\n[_nghost-%COMP%] {\n  display: block;\n  height: 100%;\n  --p-drawer-background: var(--main-background-color) !important;\n}\n/*# sourceMappingURL=admin-main-content.component.css.map */"], changeDetection: 0 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminMainContentComponent, [{
    type: Component,
    args: [{ selector: "app-admin-main-content", imports: [
      CommonModule,
      RouterModule,
      AdaptiveLayoutComponent,
      DetailsPanelComponent,
      DrawerModule
    ], changeDetection: ChangeDetectionStrategy.OnPush, template: `<!-- Use drawer for tablet & mobile devices -->
@if (shouldUseDrawer()()) {
  <!-- Full width content on mobile & tablet -->
  <div class="h-full w-full flex flex-col">
    <router-outlet (activate)="onRouterOutletActivate($event)"></router-outlet>
  </div>

  <!-- Details as drawer -->
  <p-drawer
    [(visible)]="drawerVisible"
    position="right"
    [modal]="false"
    styleClass="sm:!w-full md:!w-80 lg:!w-[30rem]"
    (onHide)="onDrawerClose()"
  >
    <ng-template #headless>
      <app-details-panel
        (close)="detailsService.closeDetailsPanel()"
        class="h-full flex flex-col"
      ></app-details-panel>
    </ng-template>
  </p-drawer>
} @else {
  <!-- Use adaptive layout for desktop -->
  <app-adaptive-layout
    [ngClass]="detailsPanelClass()()"
    [panels]="[
      {
        content: nestedContent,
        scrollable: nestedContentScrollable()(),
        minSize: detailsService.showPanel() ? 40 : 100,
        maxSize: detailsService.showPanel() ? 80 : 100,
      },
      {
        content: detailsContent,
        scrollable: true,
        minSize: detailsService.showPanel() ? '320px' : 0,
        maxSize: detailsService.showPanel() ? 60 : 0,
      },
    ]"
    [panelSizes]="detailsService.showPanel() ? [65, 35] : [100, 0]"
    [minSizes]="detailsService.showPanel() ? [40, '320px'] : [100, 0]"
    [defaultLayout]="'horizontal'"
    [responsive]="true"
    stateKey="details-layout"
    class="h-full w-full overflow-hidden"
  >
    <!-- Nested router outlet template - always present -->
    <ng-template #nestedContent>
      <div class="h-full w-full flex flex-col">
        <router-outlet
          (activate)="onRouterOutletActivate($event)"
        ></router-outlet>
      </div>
    </ng-template>

    <!-- Details panel template - conditionally rendered -->
    <ng-template #detailsContent>
      @if (detailsService.showPanel()) {
        <app-details-panel
          (close)="detailsService.closeDetailsPanel()"
          class="h-full flex flex-col"
        ></app-details-panel>
      }
    </ng-template>
  </app-adaptive-layout>
}
`, styles: ["/* src/app/features/administration/components/admin-container/admin-main-content/admin-main-content.component.scss */\n:host {\n  display: block;\n  height: 100%;\n  --p-drawer-background: var(--main-background-color) !important;\n}\n/*# sourceMappingURL=admin-main-content.component.css.map */\n"] }]
  }], () => [], { nestedContentScrollable: [{ type: Input, args: [{ isSignal: true, alias: "nestedContentScrollable", required: true }] }], detailsPanelClass: [{ type: Input, args: [{ isSignal: true, alias: "detailsPanelClass", required: true }] }], shouldUseDrawer: [{ type: Input, args: [{ isSignal: true, alias: "shouldUseDrawer", required: false }] }], routerOutletActivate: [{ type: Output, args: ["routerOutletActivate"] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(AdminMainContentComponent, { className: "AdminMainContentComponent", filePath: "src/app/features/administration/components/admin-container/admin-main-content/admin-main-content.component.ts", lineNumber: 31 });
})();

// src/app/features/administration/services/navigation/admin-route-handler.service.ts
var AdminRouteHandlerService = class _AdminRouteHandlerService {
  router = inject(Router);
  activatedRoute = inject(ActivatedRoute);
  adminDataService = inject(AdminDataService);
  containerState = inject(AdminContainerStateService);
  detailsService = inject(EntityDetailsService);
  clientDataService = inject(ClientDataService);
  /**
   * Initializes route handling and sets up navigation event subscriptions.
   * @param callbacks Component callbacks and the component's DestroyRef so that
   *   subscriptions are cleaned up when the component is destroyed, preventing
   *   subscription accumulation across multiple admin visits.
   */
  initializeRouteHandling(callbacks) {
    this.handleInitialClientNavigation();
    this.loadInitialDataAndSetup(callbacks);
    this.setupNavigationEventHandling(callbacks);
    this.setupScrollableStateHandling(callbacks.destroyRef);
  }
  /**
   * Handles initial client navigation if no route path is present
   */
  handleInitialClientNavigation() {
    const selectedClient = this.clientDataService.getSelectedClient();
    const currentUrl = this.router.url;
    const isAtBaseAdminRoute = currentUrl === "/admin" || currentUrl.startsWith("/admin?");
    if (isDefined(selectedClient) && hasProperty(selectedClient, "id") && isString(selectedClient.id) && isAtBaseAdminRoute) {
      this.router.navigate(["/admin", "client", selectedClient.id]).catch((err) => console.error("Failed to navigate to default client:", err));
    }
  }
  /**
   * Loads initial data and performs setup
   */
  loadInitialDataAndSetup(callbacks) {
    const { keyToSelect, typeToSelect } = this.getEntityInfoFromRoute();
    const routeClientId = typeToSelect === ADMIN_TREE.NODE_TYPES.CLIENT && isDefined(keyToSelect) ? keyToSelect : void 0;
    this.adminDataService.loadInitialData(routeClientId).then(() => {
      callbacks.ensureSuperAdminNodeExpansion();
      callbacks.selectNodeFromRouteOrState();
      callbacks.onChangeDetection();
    }).catch(() => {
      callbacks.selectNodeFromRouteOrState();
      callbacks.onChangeDetection();
    });
  }
  /**
   * Sets up navigation event handling for details panel query params and route synchronization
   */
  setupNavigationEventHandling(callbacks) {
    this.router.events.pipe(filter((event) => event instanceof NavigationEnd), takeUntilDestroyed(callbacks.destroyRef)).subscribe(() => {
      this.handleDetailsQueryParams();
      callbacks.selectNodeFromRouteOrState();
    });
  }
  /**
   * Handles details panel query parameters
   */
  handleDetailsQueryParams() {
    const currentQueryParams = this.activatedRoute.snapshot.queryParams;
    const hasDetailsParams = isDefined(currentQueryParams["details"]) && isDefined(currentQueryParams["type"]);
    if (this.detailsService.showPanel() && !hasDetailsParams) {
      const item = this.detailsService.selectedItem();
      if (isDefined(item) && hasProperty(item, "id") && hasProperty(item, "type")) {
        const baseUrl = this.router.url.split("?")[0];
        this.router.navigate([baseUrl], {
          queryParams: { details: item["id"], type: item["type"] ?? "" },
          queryParamsHandling: "merge",
          replaceUrl: true
        }).catch(() => {
        });
      }
    }
  }
  /**
   * Sets up handling for nested content scrollable state based on route data
   */
  setupScrollableStateHandling(destroyRef) {
    const getScrollableState = (route) => {
      let currentRoute = route;
      while (currentRoute.firstChild) {
        currentRoute = currentRoute.firstChild;
      }
      const routeData = currentRoute.snapshot.data;
      if (isDefined(routeData) && typeof routeData["nestedContentScrollable"] === "boolean") {
        return routeData["nestedContentScrollable"];
      }
      return false;
    };
    this.containerState.setNestedContentScrollable(getScrollableState(this.activatedRoute));
    this.router.events.pipe(
      filter((event) => event instanceof NavigationEnd),
      map(() => getScrollableState(this.activatedRoute)),
      // Reuse the logic
      takeUntilDestroyed(destroyRef)
    ).subscribe((scrollableValue) => {
      this.containerState.setNestedContentScrollable(scrollableValue);
    });
  }
  /**
   * Gets entity information from route parameters
   */
  getEntityInfoFromRoute() {
    const routeResult = this.extractFromRouteTree();
    if (isDefined(routeResult.keyToSelect) && isDefined(routeResult.typeToSelect)) {
      return routeResult;
    }
    return this.extractFromUrl();
  }
  /**
   * Extracts entity info by traversing the route tree
   */
  extractFromRouteTree() {
    let currentRoute = this.activatedRoute;
    let keyToSelect = null;
    let typeToSelect = null;
    while (currentRoute !== null) {
      const snapshot = currentRoute.snapshot;
      if (snapshot.url.length > 0) {
        const entityTypePath = snapshot.url[0]?.path;
        if (entityTypePath) {
          typeToSelect = this.getEntityTypeFromPath(entityTypePath);
          const entityId = snapshot.params[`${entityTypePath}Id`] ?? snapshot.params["superAdminId"] ?? snapshot.params["id"] ?? (snapshot.url.length > 1 ? snapshot.url[1]?.path : null);
          if (isDefined(entityId) && isDefined(typeToSelect)) {
            keyToSelect = typeToSelect === ADMIN_TREE.NODE_TYPES.SUPER_ADMIN ? "super-admin-root" : entityId;
            break;
          }
        }
      }
      currentRoute = currentRoute.firstChild;
    }
    return { keyToSelect, typeToSelect };
  }
  /**
   * Extracts entity info by parsing the URL directly
   */
  extractFromUrl() {
    let keyToSelect = null;
    let typeToSelect = null;
    const adminRoutePattern = /\/admin\/(client|vendor|distributor|group|super-admin)\/([^/?]+)/;
    const urlMatch = adminRoutePattern.exec(this.router.url);
    if (urlMatch) {
      const [, pathType, entityId] = urlMatch;
      typeToSelect = this.getEntityTypeFromPath(pathType);
      keyToSelect = typeToSelect === ADMIN_TREE.NODE_TYPES.SUPER_ADMIN ? "super-admin-root" : entityId;
    }
    return { keyToSelect, typeToSelect };
  }
  /**
   * Maps URL path to entity type
   */
  getEntityTypeFromPath(path) {
    switch (path) {
      case ADMIN_TREE.NODE_TYPES.CLIENT:
      case "client":
        return ADMIN_TREE.NODE_TYPES.CLIENT;
      case ADMIN_TREE.NODE_TYPES.VENDOR:
      case "vendor":
        return ADMIN_TREE.NODE_TYPES.VENDOR;
      case ADMIN_TREE.NODE_TYPES.DISTRIBUTOR:
      case "distributor":
        return ADMIN_TREE.NODE_TYPES.DISTRIBUTOR;
      case ADMIN_TREE.NODE_TYPES.GROUP:
      case "group":
        return ADMIN_TREE.NODE_TYPES.GROUP;
      case ADMIN_TREE.NODE_TYPES.SUPER_ADMIN:
      case "super-admin":
        return ADMIN_TREE.NODE_TYPES.SUPER_ADMIN;
      default:
        return null;
    }
  }
  static \u0275fac = function AdminRouteHandlerService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminRouteHandlerService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminRouteHandlerService, factory: _AdminRouteHandlerService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminRouteHandlerService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/services/tree/admin-tree-refresh.service.ts
var AdminTreeRefreshService = class _AdminTreeRefreshService {
  adminState = inject(AdminStateService);
  adminTreeService = inject(AdminTreeService);
  broadcastService = inject(BroadcastEventMessageService);
  apiSchemaService = inject(ApiSchemaService);
  /**
   * Initializes the service by setting up event listeners for tree refresh operations
   */
  initialize() {
    this.broadcastService.on("tree-refresh-required").subscribe((message) => {
      this.handleTreeRefreshRequired(message);
    });
    this.broadcastService.on("vendor-filter-changed").subscribe((message) => {
      this.handleVendorFilterChange(message);
    });
  }
  /**
   * Handles refreshing vendor data in the tree based on state changes
   * @param distributorId - The ID of the parent distributor
   * @param includeDeleted - Whether to include deleted vendors (for filtering)
   */
  async refreshVendorData(distributorId, includeDeleted = false) {
    try {
      const filter2 = includeDeleted ? void 0 : "state!=deleted";
      const vendors = await this.adminTreeService.listDistributorVendors(distributorId, filter2);
      this.adminState.setDistributorVendors(distributorId, vendors);
    } catch (error) {
      console.error("Error refreshing vendor data:", error);
      throw error;
    }
  }
  /**
   * Handles refreshing distributor data under super-admin
   */
  refreshDistributorData(_message) {
    const usesModernSchema = this.apiSchemaService.usesModernSchema();
    const superAdminExpandedNodes = this.adminState.expandedNodes()["super-admin"] ?? /* @__PURE__ */ new Set();
    if (usesModernSchema || superAdminExpandedNodes.has("super-admin-root")) {
      this.adminState.setInitialLoading(true);
      this.adminTreeService.listDistributors(DEFAULT_SYSTEM_ID).then((distributors) => {
        this.adminState.setDistributors(distributors);
      }).catch((error) => {
        console.error("Error refreshing distributor list:", error);
      }).finally(() => {
        this.adminState.setInitialLoading(false);
      });
    }
  }
  /**
   * Handles refreshing client data for a specific vendor
   * @param vendorId - The ID of the vendor whose clients should be refreshed
   * @param distributorId - The ID of the parent distributor
   * @param operation - The operation that triggered the refresh (affects strategy)
   */
  async refreshClientData(vendorId, distributorId, operation = "update") {
    try {
      const isVendorExpanded = this.adminState.isDistributorNodeExpanded(distributorId, vendorId);
      if (!isVendorExpanded) {
        return;
      }
      const currentClients = this.adminState.getDistributorVendorClients?.(distributorId, vendorId);
      const strategy = this.determineClientRefreshStrategy(operation, currentClients, null);
      switch (strategy) {
        case "full-replace":
          await this.fullClientReplace(vendorId, distributorId);
          break;
        case "smart-update":
          await this.smartClientUpdate(vendorId, distributorId, operation);
          break;
        case "paginated-refresh":
          await this.paginatedClientRefresh(vendorId, distributorId, null);
          break;
        default:
          console.warn("Unknown refresh strategy:", strategy);
          await this.fullClientReplace(vendorId, distributorId);
      }
    } catch (error) {
      console.error("Error refreshing client data:", error);
      throw error;
    }
  }
  /**
   * Determines the best refresh strategy based on current state and operation
   */
  determineClientRefreshStrategy(operation, currentClients, _paginationInfo) {
    if (currentClients === void 0 || currentClients === null || Array.isArray(currentClients) && currentClients.length === 0) {
      return "full-replace";
    }
    switch (operation) {
      case "delete":
        return "full-replace";
      case "create":
        return "full-replace";
      case "update":
        return "smart-update";
      default:
        return "full-replace";
    }
  }
  /**
   * Full replace strategy - replaces all client data with fresh first page
   */
  async fullClientReplace(vendorId, distributorId) {
    const result = await this.adminTreeService.listVendorClients(vendorId);
    this.adminState.addDistributorVendorClients(distributorId, vendorId, result.clients, {
      offset: 0,
      limit: DEFAULT_LIST_SIZE,
      total: result.total,
      hasMore: result.hasMore
    }, false);
  }
  /**
   * Smart update strategy - handles specific operations without losing pagination
   */
  async smartClientUpdate(vendorId, distributorId, _operation) {
    await this.fullClientReplace(vendorId, distributorId);
  }
  /**
   * Paginated refresh strategy - refreshes all currently loaded pages
   */
  async paginatedClientRefresh(vendorId, distributorId, _currentPaginationInfo) {
    await this.fullClientReplace(vendorId, distributorId);
  }
  /**
   * Handles tree refresh requirements after entity operations
   */
  handleTreeRefreshRequired(message) {
    if (!isDefined(message) || message === null || typeof message !== "object" || !("entityType" in message) || !("operation" in message)) {
      return;
    }
    const m = message;
    switch (m.entityType) {
      case "vendor":
        this.handleVendorRefresh(m);
        break;
      case "distributor":
        this.handleDistributorRefresh(m);
        break;
      case "client":
        this.handleClientRefresh(m);
        break;
      default:
        console.warn("Unhandled entity type for tree refresh:", m.entityType);
    }
  }
  /**
   * Handles vendor-specific refresh logic
   */
  handleVendorRefresh(message) {
    const distributorId = message.distributorId ?? this.adminState.vendorDistributor()?.id;
    if (isNullOrUndefined(distributorId) || distributorId === "") {
      console.warn("Vendor refresh requested but no distributorId available.");
      return;
    }
    if (distributorId) {
      this.adminTreeService.listDistributorVendors(distributorId).then((vendors) => {
        this.adminState.setDistributorVendors(distributorId, vendors);
      }).catch((error) => {
        console.error("Error handling vendor refresh:", error);
      });
    }
    if (!this.adminState.isDistributorExpanded(distributorId)) {
      return;
    }
    const includeDeleted = message.includeDeleted ?? false;
    this.refreshVendorData(distributorId, includeDeleted).catch((error) => {
      console.error("Error handling vendor refresh:", error);
    });
  }
  /**
   * Handles distributor-specific refresh logic
   */
  handleDistributorRefresh(_message) {
    this.refreshDistributorData(_message);
  }
  /**
   * Handles client-specific refresh logic
   */
  handleClientRefresh(message) {
    const context = this.extractClientContext(message);
    const { vendorId, distributorId } = context;
    if (isNullOrUndefined(vendorId) || vendorId === "" || isNullOrUndefined(distributorId) || distributorId === "") {
      console.warn("Client refresh requested but no vendorId or distributorId available.", context);
      return;
    }
    if (isDefined(vendorId) && isDefined(distributorId)) {
      const isExpanded = this.adminState.isDistributorNodeExpanded(distributorId, vendorId);
      if (isExpanded) {
        this.refreshClientData(vendorId, distributorId).catch((error) => {
          console.error("Error refreshing client data:", error);
        });
      } else {
        this.adminState.removeDistributorLoadedVendor(distributorId, vendorId);
      }
    }
  }
  /**
   * Extracts vendor and distributor context for client operations
   */
  extractClientContext(message) {
    let vendorId = message.vendorId;
    let distributorId = message.distributorId;
    if (isNullOrUndefined(vendorId) || vendorId === "") {
      vendorId = this.adminState.clientVendor()?.id;
    }
    if (isNullOrUndefined(distributorId) || distributorId === "") {
      distributorId = this.adminState.vendorDistributor()?.id;
    }
    if (isDefined(vendorId) && vendorId !== "" && (isNullOrUndefined(distributorId) || distributorId === "")) {
      const vendor = this.adminState.clientVendor();
      if (vendor?.id === vendorId) {
        distributorId = vendor.owner?.id;
      }
    }
    return { vendorId, distributorId };
  }
  /**
   * Handles vendor filter changes from the grid
   */
  handleVendorFilterChange(message) {
    if (!isDefined(message) || message === null || typeof message !== "object" || !("distributorId" in message) || !("includeDeleted" in message)) {
      return;
    }
    const m = message;
    this.refreshVendorData(m.distributorId, m.includeDeleted).catch((error) => {
      console.error("Error handling vendor filter change:", error);
    });
  }
  /**
   * Manually triggers a vendor refresh for a specific distributor
   * @param distributorId - The distributor ID
   * @param includeDeleted - Whether to include deleted vendors
   */
  async triggerVendorRefresh(distributorId, includeDeleted = false) {
    return this.refreshVendorData(distributorId, includeDeleted);
  }
  /**
   * Manually triggers a client refresh for a specific vendor
   * @param vendorId - The vendor ID
   * @param distributorId - The distributor ID
   * @param operation - The type of operation that triggered this refresh
   */
  async triggerClientRefresh(vendorId, distributorId, operation = "update") {
    return this.refreshClientData(vendorId, distributorId, operation);
  }
  static \u0275fac = function AdminTreeRefreshService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminTreeRefreshService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AdminTreeRefreshService, factory: _AdminTreeRefreshService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminTreeRefreshService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/shared/services/grid-base.service.ts
var GridBaseService = class _GridBaseService {
  handleFilters(currentFilters, field, value, transformations, filterMatchMode) {
    if (field === "*" && value === null) {
      return {};
    }
    const newFilters = __spreadValues({}, currentFilters);
    if (isDefined(value) && value.toString().length > 0) {
      const transformedField = transformations?.[field] ? transformations[field](field) : field;
      let processedValue = value;
      let operator = "=";
      if (filterMatchMode === "contains" && typeof value === "string") {
        processedValue = `*${value}*`;
      } else if (filterMatchMode === "startsWith" && typeof value === "string") {
        processedValue = `${value}*`;
      } else if (filterMatchMode === "endsWith" && typeof value === "string") {
        processedValue = `*${value}`;
      } else if (filterMatchMode === "in") {
        operator = "in";
      }
      newFilters[transformedField] = { value: processedValue, operator };
    } else {
      delete newFilters[field];
    }
    return newFilters;
  }
  constructGridParams(baseParams, event, defaultPageSize = DEFAULT_PAGE_SIZE) {
    return __spreadProps(__spreadValues({}, baseParams), {
      owner: baseParams["owner"] || DEFAULT_SYSTEM_ID,
      offset: isDefined(event?.first) ? event.first : 0,
      limit: isDefined(event?.rows) ? event.rows : defaultPageSize,
      sortField: isDefined(event?.sortField) ? event.sortField : "name",
      sortOrder: isDefined(event?.sortOrder) ? event.sortOrder : "asc"
    });
  }
  static \u0275fac = function GridBaseService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _GridBaseService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _GridBaseService, factory: _GridBaseService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GridBaseService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/device-providers/device-provider-create-wizard/device-provider-create-wizard.component.ts
var _c06 = ["deviceProviderDetailsStep"];
var _c14 = ["deviceProviderParametersStep"];
var _c23 = ["confirmationTemplate"];
var _c32 = ["deviceProviderDetailsForm"];
var _c42 = ["deviceProviderParametersForm"];
var _c52 = (a0) => ({ entity: a0 });
function DeviceProviderCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-device-provider-details-section", 7, 3);
    \u0275\u0275listener("formValidityChange", function DeviceProviderCreateWizardComponent_ng_template_1_Template_app_device_provider_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function DeviceProviderCreateWizardComponent_ng_template_1_Template_app_device_provider_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateDeviceProviderDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newDeviceProvider())("ownerId", ctx_r1.ownerId);
  }
}
function DeviceProviderCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-device-provider-parameters-section", 8, 4);
    \u0275\u0275listener("formValidityChange", function DeviceProviderCreateWizardComponent_ng_template_3_Template_app_device_provider_parameters_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onParametersFormValidityChange($event));
    })("formValueChange", function DeviceProviderCreateWizardComponent_ng_template_3_Template_app_device_provider_parameters_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateDeviceProviderParameters($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newDeviceProvider())("deviceProviderType", ctx_r1.deviceProviderType());
  }
}
function DeviceProviderCreateWizardComponent_ng_template_5_For_24_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 16)(1, "div", 17);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 18);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_16_0;
    const item_r4 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1("", item_r4.title, ":");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", (tmp_16_0 = ctx_r1.newDeviceProvider().parameters) == null ? null : tmp_16_0[item_r4.key], " ");
  }
}
function DeviceProviderCreateWizardComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 9);
    \u0275\u0275element(1, "span", 10);
    \u0275\u0275elementStart(2, "div", 11)(3, "div", 12)(4, "div", 13)(5, "p", 14);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 15)(10, "div", 16)(11, "div", 17);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 18);
    \u0275\u0275text(15);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(16, "div", 16)(17, "div", 17);
    \u0275\u0275text(18);
    \u0275\u0275pipe(19, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(20, "div", 18);
    \u0275\u0275text(21);
    \u0275\u0275pipe(22, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275repeaterCreate(23, DeviceProviderCreateWizardComponent_ng_template_5_For_24_Template, 5, 2, "div", 16, \u0275\u0275repeaterTrackByIndex);
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(25, "div", 19)(26, "p", 20);
    \u0275\u0275text(27);
    \u0275\u0275pipe(28, "translate");
    \u0275\u0275pipe(29, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_7_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 8, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(22, _c52, \u0275\u0275pipeBind1(7, 6, "ADMIN.ENTITIES.DEVICE_PROVIDER"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 11, "ADMIN.DEVICES.PROVIDERS.DETAILS.FIELDS.TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", (tmp_7_0 = ctx_r1.newDeviceProvider().deviceProviderType) == null ? null : tmp_7_0.name, " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(19, 13, "ADMIN.DEVICES.PROVIDERS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newDeviceProvider().name || \u0275\u0275pipeBind1(22, 15, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.deviceProviderParametersFields());
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(29, 19, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(24, _c52, \u0275\u0275pipeBind1(28, 17, "ADMIN.ENTITIES.DEVICE_PROVIDER"))), " ");
  }
}
var DeviceProviderCreateWizardComponent = class _DeviceProviderCreateWizardComponent {
  deviceProviderDetailsStep = viewChild("deviceProviderDetailsStep", ...ngDevMode ? [{ debugName: "deviceProviderDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  deviceProviderParametersStep = viewChild("deviceProviderParametersStep", ...ngDevMode ? [{ debugName: "deviceProviderParametersStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  deviceProviderDetailsForm = viewChild("deviceProviderDetailsForm", ...ngDevMode ? [{ debugName: "deviceProviderDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  deviceProviderParametersForm = viewChild("deviceProviderParametersForm", ...ngDevMode ? [{ debugName: "deviceProviderParametersForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  cacheService = inject(RequestCacheService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // DeviceProvider model to collect data across steps
  newDeviceProvider = signal({}, ...ngDevMode ? [{ debugName: "newDeviceProvider" }] : (
    /* istanbul ignore next */
    []
  ));
  deviceProviderParametersFields = signal(null, ...ngDevMode ? [{ debugName: "deviceProviderParametersFields" }] : (
    /* istanbul ignore next */
    []
  ));
  deviceProviderType = signal(null, ...ngDevMode ? [{ debugName: "deviceProviderType" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const deviceProviderDetailsStep = this.deviceProviderDetailsStep();
      const deviceProviderParametersStep = this.deviceProviderParametersStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(deviceProviderDetailsStep) || isNullOrUndefined(deviceProviderParametersStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.DEVICES.PROVIDERS.DETAILS.TITLE"),
          content: deviceProviderDetailsStep,
          complete: false,
          validate: () => this.deviceProviderDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("ADMIN.DEVICES.PROVIDERS.PARAMETERS.TITLE"),
          content: deviceProviderParametersStep,
          complete: false,
          validate: () => this.deviceProviderParametersForm()?.validate() ?? false
        },
        {
          value: 3,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newDeviceProvider.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newDeviceProvider.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  onParametersFormValidityChange(isValid) {
    this.steps[1].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  updateDeviceProviderDetails(formData) {
    this.newDeviceProvider.update((current) => {
      const _a = formData, { ownerId: _ownerId } = _a, rest = __objRest(_a, ["ownerId"]);
      const updated = __spreadValues(__spreadValues({}, current), rest);
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
    this.loadDeviceProviderType(this.newDeviceProvider().deviceProviderType?.id).catch(() => {
    });
  }
  updateDeviceProviderParameters(formData) {
    this.newDeviceProvider.update((current) => {
      const updated = __spreadValues(__spreadValues({}, current), formData);
      return updated;
    });
  }
  onWizardComplete() {
    const deviceProviderData = this.newDeviceProvider();
    if (isNullOrUndefined(deviceProviderData) || Object.keys(deviceProviderData).length === 0) {
      return;
    }
    this.isSubmitting.set(true);
    this.entitiesService.createDeviceProvider(deviceProviderData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "device-provider",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newDeviceProvider.set({});
    this.dialogRef.close();
  }
  async loadDeviceProviderType(deviceProviderTypeId) {
    if (isNullOrUndefined(deviceProviderTypeId))
      return;
    await this.cacheService.getOrFetchAsync(`deviceProviderType-${deviceProviderTypeId}`, () => firstValueFrom(this.entitiesService.getDeviceProviderType(deviceProviderTypeId)), 3e5).then((response) => {
      this.deviceProviderType.set(response);
      const params = JSON.parse(response.parameters);
      const values = [];
      params.groups.forEach((group) => {
        group.fields.forEach((field) => {
          values.push({
            key: field.id,
            title: field.title
          });
        });
      });
      this.deviceProviderParametersFields.set(values);
    });
  }
  static \u0275fac = function DeviceProviderCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DeviceProviderCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _DeviceProviderCreateWizardComponent, selectors: [["app-device-provider-create-wizard"]], viewQuery: function DeviceProviderCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.deviceProviderDetailsStep, _c06, 5)(ctx.deviceProviderParametersStep, _c14, 5)(ctx.confirmationTemplate, _c23, 5)(ctx.deviceProviderDetailsForm, _c32, 5)(ctx.deviceProviderParametersForm, _c42, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(5);
    }
  }, decls: 7, vars: 5, consts: [["deviceProviderDetailsStep", ""], ["deviceProviderParametersStep", ""], ["confirmationTemplate", ""], ["deviceProviderDetailsForm", ""], ["deviceProviderParametersForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "deviceProviderType"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function DeviceProviderCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 5);
      \u0275\u0275listener("complete", function DeviceProviderCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function DeviceProviderCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, DeviceProviderCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, DeviceProviderCreateWizardComponent_ng_template_3_Template, 3, 3, "ng-template", null, 1, \u0275\u0275templateRefExtractor)(5, DeviceProviderCreateWizardComponent_ng_template_5_Template, 30, 26, "ng-template", null, 2, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    DeviceProviderDetailsSectionComponent,
    DeviceProviderParametersSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DeviceProviderCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-device-provider-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      DeviceProviderDetailsSectionComponent,
      DeviceProviderParametersSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Device Provider Details -->
<ng-template #deviceProviderDetailsStep let-step>
  <div class="p-fluid">
    <app-device-provider-details-section
      #deviceProviderDetailsForm
      [createMode]="true"
      [entity]="newDeviceProvider()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateDeviceProviderDetails($event)"
    ></app-device-provider-details-section>
  </div>
</ng-template>

<ng-template #deviceProviderParametersStep let-step>
  <div class="p-fluid">
    <app-device-provider-parameters-section
      #deviceProviderParametersForm
      [createMode]="true"
      [entity]="newDeviceProvider()"
      [deviceProviderType]="deviceProviderType()"
      (formValidityChange)="onParametersFormValidityChange($event)"
      (formValueChange)="updateDeviceProviderParameters($event)"
    ></app-device-provider-parameters-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Device Provider Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.DEVICE_PROVIDER' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DEVICES.PROVIDERS.DETAILS.FIELDS.TYPE' | translate }}:
              </div>
              <div class="w-2/3">
                {{ newDeviceProvider().deviceProviderType?.name }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DEVICES.PROVIDERS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newDeviceProvider().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            @for (item of deviceProviderParametersFields(); track $index) {
              <div class="flex">
                <div class="w-1/3 font-medium">{{ item.title }}:</div>
                <div class="w-2/3">
                  {{ newDeviceProvider().parameters?.[item.key] }}
                </div>
              </div>
            }
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.DEVICE_PROVIDER' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { deviceProviderDetailsStep: [{ type: ViewChild, args: ["deviceProviderDetailsStep", { isSignal: true }] }], deviceProviderParametersStep: [{ type: ViewChild, args: ["deviceProviderParametersStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], deviceProviderDetailsForm: [{ type: ViewChild, args: ["deviceProviderDetailsForm", { isSignal: true }] }], deviceProviderParametersForm: [{ type: ViewChild, args: ["deviceProviderParametersForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(DeviceProviderCreateWizardComponent, { className: "DeviceProviderCreateWizardComponent", filePath: "src/app/features/administration/components/device-providers/device-provider-create-wizard/device-provider-create-wizard.component.ts", lineNumber: 50 });
})();

// src/app/features/administration/configs/device-providers.grid.config.ts
var DeviceProvidersGridConfig = class _DeviceProvidersGridConfig {
  deviceProvidersService = inject(DeviceProvidersDataAdapter);
  entitiesService = inject(EntitiesService);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  destroyRef = inject(DestroyRef);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [
    DeviceProviderListItem.StateEnum.Active
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = {};
  fieldTransformations = {
    type: () => "deviceProviderType.id"
  };
  dialogRef = null;
  dialogService = inject(DialogService);
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "device-providers";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.deviceProvidersService;
  ownerId = "";
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.DEVICES.PROVIDERS.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "type",
        header: this.translate.instant("ADMIN.DEVICES.PROVIDERS.COLUMNS.TYPE"),
        filter: true,
        filterType: "toggle",
        filterMatchMode: "in",
        filterConfig: {
          type: "toggle",
          options: []
        },
        valueGetter: (rowData) => {
          return rowData.deviceProviderType?.name ?? "";
        }
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.DEVICES.PROVIDERS.COLUMNS.STATE"),
        filter: true,
        filterType: "toggle",
        filterMatchMode: "in",
        filterConfig: {
          type: "toggle",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: DeviceProviderListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: DeviceProviderListItem.StateEnum.Deleted
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.SUSPENDED"),
              value: DeviceProviderListItem.StateEnum.Suspended
            }
          ],
          defaultSelected: this.defaultStateValues
        },
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : ""
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.DEVICES.PROVIDERS.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  loadFilterOptions() {
    this.entitiesService.listDeviceProviderTypes(this.ownerId).pipe(takeUntilDestroyed(this.destroyRef), tap((types) => {
      const typeColumn = this.columns.find((col) => col.field === "type");
      if (typeColumn?.filterConfig) {
        typeColumn.filterConfig.options = types.items.map((type) => ({
          label: type.name,
          value: type.id
        })).sort((a, b) => a.label.localeCompare(b.label));
      }
    })).subscribe();
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({});
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    } else if (isDefined(this.params["distributorId"])) {
      this.ownerId = String(this.params["distributorId"]);
    }
    this.columns = this.buildColumns();
    this.loadFilterOptions();
  }
  getTitle() {
    return this.translate.instant("ADMIN.DEVICES.PROVIDERS.TITLE");
  }
  getRequestParams(event) {
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField: "name",
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.openDeviceProviderCreationDialog(this.ownerId).pipe(takeUntilDestroyed(this.destroyRef)).subscribe((createdDeviceProvider) => {
      if (isDefined(createdDeviceProvider)) {
      }
    });
  }
  openDeviceProviderCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(DeviceProviderCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.DEVICE_PROVIDER")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function DeviceProvidersGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DeviceProvidersGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _DeviceProvidersGridConfig, factory: _DeviceProvidersGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DeviceProvidersGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/email-providers/email-provider-create-wizard/email-provider-create-wizard.component.ts
var _c07 = ["emailProviderDetailsStep"];
var _c15 = ["emailProviderParametersStep"];
var _c24 = ["confirmationTemplate"];
var _c33 = ["emailProviderDetailsForm"];
var _c43 = ["emailProviderParametersForm"];
var _c53 = (a0) => ({ entity: a0 });
function EmailProviderCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-email-provider-details-section", 7, 3);
    \u0275\u0275listener("formValidityChange", function EmailProviderCreateWizardComponent_ng_template_1_Template_app_email_provider_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function EmailProviderCreateWizardComponent_ng_template_1_Template_app_email_provider_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateEmailProviderDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newEmailProvider())("ownerId", ctx_r1.ownerId);
  }
}
function EmailProviderCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-email-provider-parameters-section", 8, 4);
    \u0275\u0275listener("formValidityChange", function EmailProviderCreateWizardComponent_ng_template_3_Template_app_email_provider_parameters_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onParametersFormValidityChange($event));
    })("formValueChange", function EmailProviderCreateWizardComponent_ng_template_3_Template_app_email_provider_parameters_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateEmailProviderParameters($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newEmailProvider())("emailProviderType", ctx_r1.emailProviderType());
  }
}
function EmailProviderCreateWizardComponent_ng_template_5_For_24_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 16)(1, "div", 17);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 18);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_16_0;
    const item_r4 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1("", item_r4.title, ":");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", (tmp_16_0 = ctx_r1.newEmailProvider().parameters) == null ? null : tmp_16_0[item_r4.key], " ");
  }
}
function EmailProviderCreateWizardComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 9);
    \u0275\u0275element(1, "span", 10);
    \u0275\u0275elementStart(2, "div", 11)(3, "div", 12)(4, "div", 13)(5, "p", 14);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 15)(10, "div", 16)(11, "div", 17);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 18);
    \u0275\u0275text(15);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(16, "div", 16)(17, "div", 17);
    \u0275\u0275text(18);
    \u0275\u0275pipe(19, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(20, "div", 18);
    \u0275\u0275text(21);
    \u0275\u0275pipe(22, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275repeaterCreate(23, EmailProviderCreateWizardComponent_ng_template_5_For_24_Template, 5, 2, "div", 16, \u0275\u0275repeaterTrackByIndex);
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(25, "div", 19)(26, "p", 20);
    \u0275\u0275text(27);
    \u0275\u0275pipe(28, "translate");
    \u0275\u0275pipe(29, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_7_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 8, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(22, _c53, \u0275\u0275pipeBind1(7, 6, "ADMIN.ENTITIES.EMAIL_PROVIDER"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 11, "ADMIN.EMAIL_PROVIDERS.DETAILS.FIELDS.TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", (tmp_7_0 = ctx_r1.newEmailProvider().type) == null ? null : tmp_7_0.name, " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(19, 13, "ADMIN.EMAIL_PROVIDERS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newEmailProvider().name || \u0275\u0275pipeBind1(22, 15, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.emailProviderParametersFields());
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(29, 19, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(24, _c53, \u0275\u0275pipeBind1(28, 17, "ADMIN.ENTITIES.EMAIL_PROVIDER"))), " ");
  }
}
var EmailProviderCreateWizardComponent = class _EmailProviderCreateWizardComponent {
  emailProviderDetailsStep = viewChild("emailProviderDetailsStep", ...ngDevMode ? [{ debugName: "emailProviderDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  emailProviderParametersStep = viewChild("emailProviderParametersStep", ...ngDevMode ? [{ debugName: "emailProviderParametersStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  emailProviderDetailsForm = viewChild("emailProviderDetailsForm", ...ngDevMode ? [{ debugName: "emailProviderDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  emailProviderParametersForm = viewChild("emailProviderParametersForm", ...ngDevMode ? [{ debugName: "emailProviderParametersForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  cacheService = inject(RequestCacheService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // EmailProvider model to collect data across steps
  newEmailProvider = signal({}, ...ngDevMode ? [{ debugName: "newEmailProvider" }] : (
    /* istanbul ignore next */
    []
  ));
  emailProviderParametersFields = signal(null, ...ngDevMode ? [{ debugName: "emailProviderParametersFields" }] : (
    /* istanbul ignore next */
    []
  ));
  emailProviderType = signal(null, ...ngDevMode ? [{ debugName: "emailProviderType" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const emailProviderDetailsStep = this.emailProviderDetailsStep();
      const emailProviderParametersStep = this.emailProviderParametersStep();
      const confirmationTemplate = this.confirmationTemplate();
      if (isNullOrUndefined(emailProviderDetailsStep) || isNullOrUndefined(emailProviderParametersStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.EMAIL_PROVIDERS.DETAILS.TITLE"),
          content: emailProviderDetailsStep,
          complete: false,
          validate: () => this.emailProviderDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("ADMIN.EMAIL_PROVIDERS.PARAMETERS.TITLE"),
          content: emailProviderParametersStep,
          complete: false,
          validate: () => this.emailProviderParametersForm()?.validate() ?? false
        },
        {
          value: 3,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newEmailProvider.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
    this.steps = [
      {
        value: 1,
        header: this.translate.instant("ADMIN.EMAIL_PROVIDERS.DETAILS.TITLE"),
        complete: false,
        content: null
      },
      {
        value: 2,
        header: this.translate.instant("ADMIN.EMAIL_PROVIDERS.PARAMETERS.TITLE"),
        content: null,
        complete: false
      },
      {
        value: 3,
        header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
        complete: true,
        content: null
      }
    ];
  }
  openWizard() {
    this.displayWizard = true;
    this.newEmailProvider.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  onParametersFormValidityChange(isValid) {
    this.steps[1].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  updateEmailProviderDetails(formData) {
    this.newEmailProvider.update((current) => {
      const _a = formData, { ownerId: _ownerId } = _a, rest = __objRest(_a, ["ownerId"]);
      const updated = __spreadValues(__spreadValues({}, current), rest);
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
    this.loadEmailProviderType(this.newEmailProvider()["type"]?.id).catch(() => {
    });
  }
  updateEmailProviderParameters(formData) {
    this.newEmailProvider.update((current) => {
      const updated = __spreadValues(__spreadValues({}, current), formData);
      return updated;
    });
  }
  onWizardComplete() {
    const emailProviderData = this.newEmailProvider();
    if (isNullOrUndefined(emailProviderData) || Object.keys(emailProviderData).length === 0) {
      return;
    }
    this.isSubmitting.set(true);
    this.entitiesService.createEmailProvider(emailProviderData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "email-provider",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newEmailProvider.set({});
    this.dialogRef.close();
  }
  async loadEmailProviderType(emailProviderTypeId) {
    if (isNullOrUndefined(emailProviderTypeId))
      return;
    try {
      const response = await this.cacheService.getOrFetchAsync(`emailProviderType-${emailProviderTypeId}`, () => firstValueFrom(this.entitiesService.getEmailProviderType(emailProviderTypeId)), 3e5);
      this.emailProviderType.set(response);
      this.processEmailProviderParameters(response);
    } catch (error) {
      console.error("Failed to load email provider type:", error);
    }
  }
  processEmailProviderParameters(emailProviderType) {
    const parameterDefinition = emailProviderType?.parameterDefinition ?? "{}";
    try {
      const params = JSON.parse(parameterDefinition);
      const fields = this.extractFieldsFromGroups(params.groups);
      this.emailProviderParametersFields.set(fields);
    } catch (error) {
      console.error("Failed to parse parameter definition:", error);
      this.emailProviderParametersFields.set([]);
    }
  }
  extractFieldsFromGroups(groups) {
    const fields = [];
    groups.forEach((group) => {
      group.fields.forEach((field) => {
        this.addFieldToCollection(field, fields);
        this.addConditionalFields(field, fields);
      });
    });
    return fields;
  }
  addFieldToCollection(field, fields) {
    fields.push({
      key: field.id,
      title: field.title
    });
  }
  addConditionalFields(field, fields) {
    if (!field.values)
      return;
    field.values.forEach((value) => {
      if (isDefined(value.fields)) {
        value.fields.forEach((subField) => {
          this.addFieldToCollection(subField, fields);
          this.addConditionalFields(subField, fields);
        });
      }
    });
  }
  static \u0275fac = function EmailProviderCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _EmailProviderCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _EmailProviderCreateWizardComponent, selectors: [["app-email-provider-create-wizard"]], viewQuery: function EmailProviderCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.emailProviderDetailsStep, _c07, 5)(ctx.emailProviderParametersStep, _c15, 5)(ctx.confirmationTemplate, _c24, 5)(ctx.emailProviderDetailsForm, _c33, 5)(ctx.emailProviderParametersForm, _c43, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(5);
    }
  }, decls: 7, vars: 5, consts: [["emailProviderDetailsStep", ""], ["emailProviderParametersStep", ""], ["confirmationTemplate", ""], ["emailProviderDetailsForm", ""], ["emailProviderParametersForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "emailProviderType"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function EmailProviderCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 5);
      \u0275\u0275listener("complete", function EmailProviderCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function EmailProviderCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, EmailProviderCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, EmailProviderCreateWizardComponent_ng_template_3_Template, 3, 3, "ng-template", null, 1, \u0275\u0275templateRefExtractor)(5, EmailProviderCreateWizardComponent_ng_template_5_Template, 30, 26, "ng-template", null, 2, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    EmailProviderDetailsSectionComponent,
    EmailProviderParametersSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(EmailProviderCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-email-provider-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      EmailProviderDetailsSectionComponent,
      EmailProviderParametersSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Email Provider Details -->
<ng-template #emailProviderDetailsStep let-step>
  <div class="p-fluid">
    <app-email-provider-details-section
      #emailProviderDetailsForm
      [createMode]="true"
      [entity]="newEmailProvider()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateEmailProviderDetails($event)"
    ></app-email-provider-details-section>
  </div>
</ng-template>

<ng-template #emailProviderParametersStep let-step>
  <div class="p-fluid">
    <app-email-provider-parameters-section
      #emailProviderParametersForm
      [createMode]="true"
      [entity]="newEmailProvider()"
      [emailProviderType]="emailProviderType()"
      (formValidityChange)="onParametersFormValidityChange($event)"
      (formValueChange)="updateEmailProviderParameters($event)"
    ></app-email-provider-parameters-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Email Provider Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.EMAIL_PROVIDER' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.EMAIL_PROVIDERS.DETAILS.FIELDS.TYPE' | translate }}:
              </div>
              <div class="w-2/3">
                {{ newEmailProvider().type?.name }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.EMAIL_PROVIDERS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newEmailProvider().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            @for (item of emailProviderParametersFields(); track $index) {
              <div class="flex">
                <div class="w-1/3 font-medium">{{ item.title }}:</div>
                <div class="w-2/3">
                  {{ newEmailProvider().parameters?.[item.key] }}
                </div>
              </div>
            }
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.EMAIL_PROVIDER' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { emailProviderDetailsStep: [{ type: ViewChild, args: ["emailProviderDetailsStep", { isSignal: true }] }], emailProviderParametersStep: [{ type: ViewChild, args: ["emailProviderParametersStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], emailProviderDetailsForm: [{ type: ViewChild, args: ["emailProviderDetailsForm", { isSignal: true }] }], emailProviderParametersForm: [{ type: ViewChild, args: ["emailProviderParametersForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(EmailProviderCreateWizardComponent, { className: "EmailProviderCreateWizardComponent", filePath: "src/app/features/administration/components/email-providers/email-provider-create-wizard/email-provider-create-wizard.component.ts", lineNumber: 61 });
})();

// src/app/features/administration/configs/email-providers.grid.config.ts
var EmailProvidersGridConfig = class _EmailProvidersGridConfig {
  emailProvidersService = inject(EmailProvidersDataAdapter);
  entitiesService = inject(EntitiesService);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  apiSchemaService = inject(ApiSchemaService);
  destroyRef = inject(DestroyRef);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [
    EmailProviderListItem.StateEnum.Active
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = {};
  fieldTransformations = {
    type: "type.id"
  };
  dialogRef = null;
  dialogService = inject(DialogService);
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "email-providers";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.emailProvidersService;
  ownerId = "";
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.EMAIL_PROVIDERS.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "type",
        header: this.translate.instant("ADMIN.EMAIL_PROVIDERS.COLUMNS.TYPE"),
        filter: true,
        filterType: "toggle",
        filterMatchMode: "in",
        filterConfig: {
          type: "toggle",
          options: []
        },
        valueGetter: (rowData) => {
          const typeValue = this.apiSchemaService.getEntityType(rowData, "emailprovider");
          return typeValue?.name ?? "";
        }
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.EMAIL_PROVIDERS.COLUMNS.STATE"),
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: EmailProviderListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: EmailProviderListItem.StateEnum.Deleted
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.SUSPENDED"),
              value: EmailProviderListItem.StateEnum.Suspended
            }
          ],
          defaultSelected: this.defaultStateValues
        },
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : ""
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.EMAIL_PROVIDERS.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  loadFilterOptions() {
    this.entitiesService.listEmailProviderTypes(this.ownerId).pipe(takeUntilDestroyed(this.destroyRef), tap((types) => {
      const typeColumn = this.columns.find((col) => col.field === "type");
      if (typeColumn?.filterConfig) {
        typeColumn.filterConfig.options = types.items.map((type) => ({
          label: type.name ?? "",
          value: type.id
        })).sort((a, b) => a.label.localeCompare(b.label));
      }
    })).subscribe();
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({});
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    } else if (isDefined(this.params["distributorId"])) {
      this.ownerId = String(this.params["distributorId"]);
    }
    this.columns = this.buildColumns();
    this.loadFilterOptions();
  }
  getTitle() {
    return this.translate.instant("ADMIN.EMAIL_PROVIDERS.TITLE");
  }
  getRequestParams(event) {
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField: "name",
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const actualField = this.fieldTransformations[field] || field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.openEmailProviderCreationDialog(this.ownerId).pipe(takeUntilDestroyed(this.destroyRef)).subscribe((createdEmailProvider) => {
      if (isDefined(createdEmailProvider)) {
      }
    });
  }
  openEmailProviderCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(EmailProviderCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.EMAIL_PROVIDER")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function EmailProvidersGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _EmailProvidersGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _EmailProvidersGridConfig, factory: _EmailProvidersGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(EmailProvidersGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/alerts/alert-create-wizard/alert-create-wizard.component.ts
var _c08 = ["alertDetailsStep"];
var _c16 = ["confirmationTemplate"];
var _c25 = ["alertDetailsForm"];
var _c34 = (a0) => ({ entity: a0 });
function AlertCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-alert-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function AlertCreateWizardComponent_ng_template_1_Template_app_alert_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function AlertCreateWizardComponent_ng_template_1_Template_app_alert_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateAlertDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newAlert())("ownerId", ctx_r1.ownerId);
  }
}
function AlertCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(24, "div", 13)(25, "div", 14);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "div", 15);
    \u0275\u0275text(29);
    \u0275\u0275pipe(30, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(31, "div", 13)(32, "div", 14);
    \u0275\u0275text(33);
    \u0275\u0275pipe(34, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(35, "div", 15);
    \u0275\u0275text(36);
    \u0275\u0275pipe(37, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(38, "div", 16)(39, "p", 17);
    \u0275\u0275text(40);
    \u0275\u0275pipe(41, "translate");
    \u0275\u0275pipe(42, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_8_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 12, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(36, _c34, \u0275\u0275pipeBind1(7, 10, "ADMIN.ENTITIES.ALERT"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 15, "ADMIN.ALERTS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAlert().name || \u0275\u0275pipeBind1(16, 17, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 19, "ADMIN.ALERTS.DETAILS.FIELDS.COST_CENTRE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_8_0 = ctx_r1.newAlert().costCentre) == null ? null : tmp_8_0.name) || \u0275\u0275pipeBind1(23, 21, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(27, 23, "ADMIN.ALERTS.DETAILS.FIELDS.PRIORITY"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAlert().priority || \u0275\u0275pipeBind1(30, 25, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(34, 27, "ADMIN.ALERTS.DETAILS.FIELDS.NOTIFY"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.getNotificationIndividualNames(ctx_r1.newAlert().notify) || \u0275\u0275pipeBind1(37, 29, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(42, 33, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(38, _c34, \u0275\u0275pipeBind1(41, 31, "ADMIN.ENTITIES.ALERT"))), " ");
  }
}
var AlertCreateWizardComponent = class _AlertCreateWizardComponent {
  alertDetailsStep = viewChild("alertDetailsStep", ...ngDevMode ? [{ debugName: "alertDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  alertDetailsForm = viewChild("alertDetailsForm", ...ngDevMode ? [{ debugName: "alertDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cdr = inject(ChangeDetectorRef);
  alertIconStorageService = inject(AlertIconStorageService);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // Alert model to collect data across steps
  newAlert = signal({}, ...ngDevMode ? [{ debugName: "newAlert" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  steps = [];
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const detailsStep = this.alertDetailsStep();
      const confirmStep = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (detailsStep && confirmStep) {
        this.steps = [
          {
            value: 1,
            header: this.translate.instant("ADMIN.ALERTS.WIZARD.STEPS.DETAILS"),
            content: detailsStep,
            complete: false,
            validate: () => this.alertDetailsForm()?.validate() ?? false
          },
          {
            value: 2,
            header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
            content: confirmStep,
            complete: true
          }
        ];
        this.cdr.detectChanges();
      }
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newAlert.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.newAlert.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the alert state with form data
  updateAlertDetails(formData) {
    this.newAlert.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const alertData = this.newAlert();
    const iconName = alertData?.iconName;
    const _a = alertData, { iconName: _ } = _a, apiAlertData = __objRest(_a, ["iconName"]);
    this.isSubmitting.set(true);
    this.entitiesService.createAlert(apiAlertData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        if (isDefined(iconName) && iconName !== "") {
          this.alertIconStorageService.setAlertIcon(response.id, iconName);
        }
        this.broadcastService.broadcast("entity-updated", {
          entityType: "alert",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating alert:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newAlert.set({});
    this.dialogRef.close();
  }
  getNotificationIndividualNames(notify) {
    if (!notify)
      return "";
    return notify.map((individual) => individual.name).join(", ");
  }
  static \u0275fac = function AlertCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AlertCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _AlertCreateWizardComponent, selectors: [["app-alert-create-wizard"]], viewQuery: function AlertCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.alertDetailsStep, _c08, 5)(ctx.confirmationTemplate, _c16, 5)(ctx.alertDetailsForm, _c25, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["alertDetailsStep", ""], ["confirmationTemplate", ""], ["alertDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function AlertCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function AlertCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function AlertCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, AlertCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, AlertCreateWizardComponent_ng_template_3_Template, 43, 40, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    AlertDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AlertCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-alert-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      AlertDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Alert Details -->
<ng-template #alertDetailsStep let-step>
  <div class="p-fluid">
    <app-alert-details-section
      #alertDetailsForm
      [createMode]="true"
      [entity]="newAlert()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateAlertDetails($event)"
    ></app-alert-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Alert Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate: { entity: ('ADMIN.ENTITIES.ALERT' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ALERTS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newAlert().name || ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ALERTS.DETAILS.FIELDS.COST_CENTRE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newAlert().costCentre?.name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ALERTS.DETAILS.FIELDS.PRIORITY' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newAlert().priority ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ALERTS.DETAILS.FIELDS.NOTIFY' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  getNotificationIndividualNames(newAlert().notify) ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate: { entity: ('ADMIN.ENTITIES.ALERT' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { alertDetailsStep: [{ type: ViewChild, args: ["alertDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], alertDetailsForm: [{ type: ViewChild, args: ["alertDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(AlertCreateWizardComponent, { className: "AlertCreateWizardComponent", filePath: "src/app/features/administration/components/alerts/alert-create-wizard/alert-create-wizard.component.ts", lineNumber: 48 });
})();

// src/app/features/administration/components/alerts/vendor-alert-picker/vendor-alert-picker.component.ts
var _c09 = (a0) => ({ type: a0 });
var _forTrack03 = ($index, $item) => $item.id;
function VendorAlertPickerComponent_Conditional_12_For_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "p-chip", 16);
    \u0275\u0275listener("onRemove", function VendorAlertPickerComponent_Conditional_12_For_6_Template_p_chip_onRemove_0_listener() {
      const alert_r4 = \u0275\u0275restoreView(_r3).$implicit;
      const ctx_r4 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r4.removeSelected(alert_r4));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const alert_r4 = ctx.$implicit;
    \u0275\u0275property("label", alert_r4.name)("removable", true);
  }
}
function VendorAlertPickerComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 7)(1, "span", 13);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 14);
    \u0275\u0275repeaterCreate(5, VendorAlertPickerComponent_Conditional_12_For_6_Template, 1, 2, "p-chip", 15, _forTrack03);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r4 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate2(" ", \u0275\u0275pipeBind1(3, 2, "SHARED.COMMON.SELECTION.SELECTED_ITEMS"), " (", ctx_r4.selectedAlerts.length, ") ");
    \u0275\u0275advance(3);
    \u0275\u0275repeater(ctx_r4.selectedAlerts);
  }
}
var VendorAlertPickerComponent = class _VendorAlertPickerComponent {
  config = inject(DynamicDialogConfig);
  ref = inject(DynamicDialogRef);
  alertsDataAdapter = inject(AlertsDataAdapter);
  alertsGridConfig = inject(AlertsGridConfig);
  vendorId = this.config.data.vendorId;
  selectedAlerts = [];
  vendorAlerts = signal([], ...ngDevMode ? [{ debugName: "vendorAlerts" }] : (
    /* istanbul ignore next */
    []
  ));
  isLoading = signal(false, ...ngDevMode ? [{ debugName: "isLoading" }] : (
    /* istanbul ignore next */
    []
  ));
  totalRecords = signal(0, ...ngDevMode ? [{ debugName: "totalRecords" }] : (
    /* istanbul ignore next */
    []
  ));
  searchInput = signal("", ...ngDevMode ? [{ debugName: "searchInput" }] : (
    /* istanbul ignore next */
    []
  ));
  searchSubject = new Subject();
  lastLazyLoadEvent;
  constructor() {
    this.searchSubject.pipe(debounceTime(300), distinctUntilChanged(), takeUntilDestroyed()).subscribe((query) => {
      this.searchInput.set(query);
      if (this.lastLazyLoadEvent) {
        this.lastLazyLoadEvent = __spreadProps(__spreadValues({}, this.lastLazyLoadEvent), {
          first: 0
        });
        this.loadVendorAlerts(this.lastLazyLoadEvent);
      }
    });
  }
  onSearch(query) {
    this.searchSubject.next(query);
  }
  onLazyLoad(event) {
    this.lastLazyLoadEvent = event;
    this.loadVendorAlerts(event);
  }
  loadVendorAlerts(event) {
    this.isLoading.set(true);
    let filters = "";
    const query = this.searchInput().trim();
    if (query !== "") {
      filters = `name=like=*${query}*`;
    }
    const params = {
      owner: this.vendorId,
      offset: isDefined(event.first) ? event.first : 0,
      limit: isDefined(event.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      filters
    };
    this.alertsDataAdapter.getList(params).subscribe({
      next: (response) => {
        this.vendorAlerts.set(response.items);
        this.totalRecords.set(isDefined(response.count) ? response.count : response.items.length);
        this.isLoading.set(false);
      },
      error: () => {
        this.isLoading.set(false);
      }
    });
  }
  onRowSelect(event) {
    const alert = event.data;
    if (isDefined(alert)) {
      this.selectedAlerts = [...this.selectedAlerts, alert];
    }
  }
  onRowUnselect(event) {
    const alert = event.data;
    if (isDefined(alert)) {
      this.removeSelected(alert);
    }
  }
  removeSelected(alert) {
    this.selectedAlerts = this.selectedAlerts.filter((a) => a.id !== alert.id);
  }
  onImport() {
    this.ref.close(this.selectedAlerts);
  }
  onCancel() {
    this.ref.close();
  }
  static \u0275fac = function VendorAlertPickerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _VendorAlertPickerComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _VendorAlertPickerComponent, selectors: [["app-vendor-alert-picker"]], decls: 22, vars: 26, consts: [["searchBox", ""], [1, "flex", "flex-col", "h-full", "justify-between"], [1, "flex-grow-1", "overflow-auto", "p-4", "flex", "flex-col", "gap-4"], [1, "flex", "flex-col", "gap-2"], ["for", "vendor-alert-search", 1, "font-bold", "text-lg"], ["id", "vendor-alert-search", "pInputText", "", "type", "text", 1, "w-full", 3, "input", "placeholder"], [1, "pi", "pi-search"], [1, "flex", "flex-col", "gap-3"], [1, "flex-grow-1", "min-h-0"], [1, "h-full", "block", 3, "onLazyLoad", "rowSelect", "rowUnselect", "columns", "data", "loading", "selectionMode", "lazy", "totalRecords", "pagination"], [1, "flex", "justify-content-end", "gap-3", "p-4", "border-top-1"], ["type", "button", "severity", "secondary", 1, "p-button-outlined", 3, "onClick"], ["type", "button", "severity", "primary", 3, "onClick", "disabled"], [1, "font-bold", "text-lg"], [1, "flex", "flex-wrap", "gap-2", "surface-50", "p-3", "border-round", "border-1", "surface-border"], [1, "custom-chip", 3, "label", "removable"], [1, "custom-chip", 3, "onRemove", "label", "removable"]], template: function VendorAlertPickerComponent_Template(rf, ctx) {
    if (rf & 1) {
      const _r1 = \u0275\u0275getCurrentView();
      \u0275\u0275elementStart(0, "div", 1)(1, "div", 2)(2, "div", 3)(3, "label", 4);
      \u0275\u0275text(4);
      \u0275\u0275pipe(5, "translate");
      \u0275\u0275pipe(6, "translate");
      \u0275\u0275elementEnd();
      \u0275\u0275elementStart(7, "p-iconfield")(8, "input", 5, 0);
      \u0275\u0275pipe(10, "translate");
      \u0275\u0275listener("input", function VendorAlertPickerComponent_Template_input_input_8_listener() {
        \u0275\u0275restoreView(_r1);
        const searchBox_r2 = \u0275\u0275reference(9);
        return \u0275\u0275resetView(ctx.onSearch(searchBox_r2.value));
      });
      \u0275\u0275elementEnd();
      \u0275\u0275element(11, "p-inputicon", 6);
      \u0275\u0275elementEnd()();
      \u0275\u0275conditionalCreate(12, VendorAlertPickerComponent_Conditional_12_Template, 7, 4, "div", 7);
      \u0275\u0275elementStart(13, "div", 8)(14, "app-grid", 9);
      \u0275\u0275listener("onLazyLoad", function VendorAlertPickerComponent_Template_app_grid_onLazyLoad_14_listener($event) {
        return ctx.onLazyLoad($event);
      })("rowSelect", function VendorAlertPickerComponent_Template_app_grid_rowSelect_14_listener($event) {
        return ctx.onRowSelect($event);
      })("rowUnselect", function VendorAlertPickerComponent_Template_app_grid_rowUnselect_14_listener($event) {
        return ctx.onRowUnselect($event);
      });
      \u0275\u0275elementEnd()()();
      \u0275\u0275elementStart(15, "div", 10)(16, "p-button", 11);
      \u0275\u0275listener("onClick", function VendorAlertPickerComponent_Template_p_button_onClick_16_listener() {
        return ctx.onCancel();
      });
      \u0275\u0275text(17);
      \u0275\u0275pipe(18, "translate");
      \u0275\u0275elementEnd();
      \u0275\u0275elementStart(19, "p-button", 12);
      \u0275\u0275listener("onClick", function VendorAlertPickerComponent_Template_p_button_onClick_19_listener() {
        return ctx.onImport();
      });
      \u0275\u0275text(20);
      \u0275\u0275pipe(21, "translate");
      \u0275\u0275elementEnd()()();
    }
    if (rf & 2) {
      \u0275\u0275advance(4);
      \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(6, 15, "SHARED.COMMON.SELECTION.SEARCH", \u0275\u0275pureFunction1(24, _c09, \u0275\u0275pipeBind1(5, 13, "ADMIN.ENTITIES.ALERTS"))), " ");
      \u0275\u0275advance(4);
      \u0275\u0275property("placeholder", \u0275\u0275pipeBind1(10, 18, "SHARED.COMMON.SEARCH"));
      \u0275\u0275advance(4);
      \u0275\u0275conditional(ctx.selectedAlerts.length > 0 ? 12 : -1);
      \u0275\u0275advance(2);
      \u0275\u0275property("columns", ctx.alertsGridConfig.columns)("data", ctx.vendorAlerts())("loading", ctx.isLoading())("selectionMode", "multiple")("lazy", true)("totalRecords", ctx.totalRecords())("pagination", true);
      \u0275\u0275advance(3);
      \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(18, 20, "SHARED.COMMON.BUTTONS.CANCEL"), " ");
      \u0275\u0275advance(2);
      \u0275\u0275property("disabled", ctx.selectedAlerts.length === 0);
      \u0275\u0275advance();
      \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(21, 22, "SHARED.COMMON.BUTTONS.IMPORT"), " ");
    }
  }, dependencies: [
    GridComponent,
    CommonModule,
    ButtonModule,
    Button,
    FormsModule,
    ChipModule,
    Chip,
    InputTextModule,
    InputText,
    InputIcon,
    IconField,
    TranslatePipe
  ], styles: ["\n[_nghost-%COMP%] {\n  height: 100%;\n}\n/*# sourceMappingURL=vendor-alert-picker.component.css.map */"] });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(VendorAlertPickerComponent, [{
    type: Component,
    args: [{ selector: "app-vendor-alert-picker", imports: [
      GridComponent,
      CommonModule,
      ButtonModule,
      FormsModule,
      ChipModule,
      InputTextModule,
      InputIcon,
      IconField,
      TranslatePipe
    ], template: `<div class="flex flex-col h-full justify-between">
  <!-- Content Area -->
  <div class="flex-grow-1 overflow-auto p-4 flex flex-col gap-4">
    <div class="flex flex-col gap-2">
      <label for="vendor-alert-search" class="font-bold text-lg">
        {{
          'SHARED.COMMON.SELECTION.SEARCH'
            | translate: { type: ('ADMIN.ENTITIES.ALERTS' | translate) }
        }}
      </label>
      <p-iconfield>
        <input
          id="vendor-alert-search"
          pInputText
          type="text"
          [placeholder]="'SHARED.COMMON.SEARCH' | translate"
          class="w-full"
          (input)="onSearch(searchBox.value)"
          #searchBox
        />
        <p-inputicon class="pi pi-search" />
      </p-iconfield>
    </div>

    @if (selectedAlerts.length > 0) {
      <div class="flex flex-col gap-3">
        <span class="font-bold text-lg">
          {{ 'SHARED.COMMON.SELECTION.SELECTED_ITEMS' | translate }}
          ({{ selectedAlerts.length }})
        </span>
        <div
          class="flex flex-wrap gap-2 surface-50 p-3 border-round border-1 surface-border"
        >
          @for (alert of selectedAlerts; track alert.id) {
            <p-chip
              [label]="alert.name"
              [removable]="true"
              (onRemove)="removeSelected(alert)"
              class="custom-chip"
            />
          }
        </div>
      </div>
    }

    <div class="flex-grow-1 min-h-0">
      <app-grid
        [columns]="alertsGridConfig.columns"
        [data]="vendorAlerts()"
        [loading]="isLoading()"
        [selectionMode]="'multiple'"
        [lazy]="true"
        [totalRecords]="totalRecords()"
        [pagination]="true"
        (onLazyLoad)="onLazyLoad($event)"
        (rowSelect)="onRowSelect($event)"
        (rowUnselect)="onRowUnselect($event)"
        class="h-full block"
      ></app-grid>
    </div>
  </div>

  <!-- Footer Actions -->
  <div class="flex justify-content-end gap-3 p-4 border-top-1">
    <p-button
      type="button"
      severity="secondary"
      class="p-button-outlined"
      (onClick)="onCancel()"
    >
      {{ 'SHARED.COMMON.BUTTONS.CANCEL' | translate }}
    </p-button>
    <p-button
      type="button"
      severity="primary"
      [disabled]="selectedAlerts.length === 0"
      (onClick)="onImport()"
    >
      {{ 'SHARED.COMMON.BUTTONS.IMPORT' | translate }}
    </p-button>
  </div>
</div>
`, styles: ["/* src/app/features/administration/components/alerts/vendor-alert-picker/vendor-alert-picker.component.scss */\n:host {\n  height: 100%;\n}\n/*# sourceMappingURL=vendor-alert-picker.component.css.map */\n"] }]
  }], () => [], null);
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(VendorAlertPickerComponent, { className: "VendorAlertPickerComponent", filePath: "src/app/features/administration/components/alerts/vendor-alert-picker/vendor-alert-picker.component.ts", lineNumber: 42 });
})();

// src/app/features/administration/configs/alerts.grid.config.ts
var AlertsGridConfig = class _AlertsGridConfig {
  alertsService = inject(AlertsDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  adminDataService = inject(AdminDataService);
  entitiesService = inject(AlertsService);
  dialogRef = null;
  dialogService = inject(DialogService);
  messageService = inject(MessageService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  broadcastService = inject(BroadcastEventMessageService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  refreshTrigger = signal(0, ...ngDevMode ? [{ debugName: "refreshTrigger" }] : (
    /* istanbul ignore next */
    []
  ));
  defaultStateValues = [AlertListItem.StateEnum.Active];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { clientId: "" };
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  fieldTransformations = {
    costCentre: "costCentre.id"
  };
  priorityLabels = {
    high: this.translate.instant("ADMIN.ALERTS.PRIORITIES.HIGH"),
    medium: this.translate.instant("ADMIN.ALERTS.PRIORITIES.MEDIUM"),
    low: this.translate.instant("ADMIN.ALERTS.PRIORITIES.LOW")
  };
  entityType = "alerts";
  bulkImportIcon;
  bulkImportLabel;
  onBulkImport;
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.alertsService;
  executeBulkImport() {
    const clientId = this.params["clientId"];
    if (!isDefined(clientId) || clientId === "") {
      return;
    }
    const client = this.adminDataService["adminState"].selectedClient();
    const vendorId = client?.owner?.id;
    if (!isDefined(vendorId)) {
      return;
    }
    this.dialogRef = this.dialogService.open(VendorAlertPickerComponent, {
      header: this.translate.instant("ADMIN.ALERTS.IMPORT_VENDOR_ALERTS"),
      width: "75%",
      height: "80vh",
      contentStyle: {
        display: "flex",
        flexDirection: "column",
        padding: "0",
        height: "100%"
      },
      baseZIndex: 1e4,
      data: { vendorId }
    });
    if (this.dialogRef) {
      this.dialogRef.onClose.subscribe((selectedAlerts) => {
        if (isDefined(selectedAlerts) && selectedAlerts.length > 0) {
          this.importAlerts(selectedAlerts, clientId);
        }
      });
    }
  }
  importAlerts(alerts, clientId) {
    const importRequests = alerts.map((alert) => {
      const request = {
        name: alert.name,
        priority: alert.priority,
        state: alert.state,
        filter: alert.filter,
        costCentre: alert.costCentre,
        ownerId: clientId,
        parentId: alert.id
      };
      return this.entitiesService.createAlert(request).pipe(tap((createdAlert) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "alert",
          operation: "create",
          entityId: createdAlert.id
        });
      }), catchError((error) => {
        console.error(`Failed to import alert "${alert.name}":`, error);
        return of({ success: false, alertName: alert.name, error });
      }));
    });
    forkJoin(importRequests).pipe(finalize(() => {
      this.refreshTrigger.update((v) => v + 1);
    })).subscribe((results) => {
      const successCount = results.filter((r) => !("success" in r) || r.success !== false).length;
      const failureCount = results.length - successCount;
      if (failureCount === 0) {
        this.messageService.add({
          severity: "success",
          summary: "Import Successful",
          detail: `Successfully imported ${successCount} alert${successCount === 1 ? "" : "s"}`
        });
      } else if (successCount === 0) {
        this.messageService.add({
          severity: "error",
          summary: "Import Failed",
          detail: `Failed to import ${failureCount} alert${failureCount === 1 ? "" : "s"}`
        });
      } else {
        this.messageService.add({
          severity: "warn",
          summary: "Partial Import",
          detail: `Imported ${successCount} alert${successCount === 1 ? "" : "s"}, ${failureCount} failed`
        });
      }
    });
  }
  ownerId = "";
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.ALERTS.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "costCentre",
        header: this.translate.instant("ADMIN.ALERTS.COLUMNS.COST_CENTRE"),
        valueGetter: (rowData) => isDefined(rowData.costCentre?.name) ? rowData.costCentre?.name : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: []
        }
      },
      {
        field: "priority",
        header: this.translate.instant("ADMIN.ALERTS.COLUMNS.PRIORITY"),
        valueGetter: (rowData) => isDefined(rowData.priority) ? this.priorityLabels[rowData.priority] : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("ADMIN.ALERTS.PRIORITIES.HIGH"),
              value: AlertListItem.PriorityEnum.High
            },
            {
              label: this.translate.instant("ADMIN.ALERTS.PRIORITIES.MEDIUM"),
              value: AlertListItem.PriorityEnum.Medium
            },
            {
              label: this.translate.instant("ADMIN.ALERTS.PRIORITIES.LOW"),
              value: AlertListItem.PriorityEnum.Low
            }
          ]
        }
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.ALERTS.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: AlertListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: AlertListItem.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        }
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.ALERTS.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
      this.bulkImportIcon = "pi pi-copy";
      this.bulkImportLabel = this.translate.instant("ADMIN.ALERTS.IMPORT_VENDOR_ALERTS");
      this.onBulkImport = () => this.executeBulkImport();
    } else {
      this.ownerId = isDefined(this.params["vendorId"]) ? String(this.params["vendorId"]) : "";
      this.bulkImportIcon = void 0;
      this.bulkImportLabel = void 0;
      this.onBulkImport = void 0;
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.ALERTS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const actualField = this.fieldTransformations[field] || field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    const ownerId = this.ownerId;
    this.openAlertCreationDialog(ownerId).subscribe((createdAlert) => {
      if (isDefined(createdAlert)) {
      }
    });
  }
  openAlertCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(AlertCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.ALERT")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function AlertsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AlertsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AlertsGridConfig, factory: _AlertsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AlertsGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/asset-tags/asset-tag-create-wizard/asset-tag-create-wizard.component.ts
var _c010 = ["assetTagDetailsStep"];
var _c17 = ["confirmationTemplate"];
var _c26 = ["assetTagDetailsForm"];
var _c35 = (a0) => ({ entity: a0 });
function AssetTagCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-asset-tag-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function AssetTagCreateWizardComponent_ng_template_1_Template_app_asset_tag_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function AssetTagCreateWizardComponent_ng_template_1_Template_app_asset_tag_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateAssetTagDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newAssetTag())("ownerId", ctx_r1.ownerId);
  }
}
function AssetTagCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(24, "div", 13)(25, "div", 14);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "div", 15);
    \u0275\u0275text(29);
    \u0275\u0275pipe(30, "translate");
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(31, "div", 13)(32, "div", 14);
    \u0275\u0275text(33);
    \u0275\u0275pipe(34, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(35, "div", 15);
    \u0275\u0275text(36);
    \u0275\u0275elementEnd()()()();
    \u0275\u0275elementStart(37, "div", 16)(38, "p", 17);
    \u0275\u0275text(39);
    \u0275\u0275pipe(40, "translate");
    \u0275\u0275pipe(41, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_10_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 12, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(34, _c35, \u0275\u0275pipeBind1(7, 10, "ADMIN.ENTITIES.ASSET_TAG"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 15, "ADMIN.ASSET_TAGS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAssetTag().name || \u0275\u0275pipeBind1(16, 17, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 19, "ADMIN.ASSET_TAGS.DETAILS.FIELDS.SERIAL_NUMBER"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAssetTag().serialNumber || \u0275\u0275pipeBind1(23, 21, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(27, 23, "ADMIN.ASSET_TAGS.DETAILS.FIELDS.ASSET_TAG_TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_10_0 = ctx_r1.newAssetTag().assetTagType) == null ? null : tmp_10_0.name) || \u0275\u0275pipeBind1(30, 25, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(34, 27, "ADMIN.ASSET_TAGS.DETAILS.FIELDS.MODE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAssetTag().mode, " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(41, 31, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(36, _c35, \u0275\u0275pipeBind1(40, 29, "ADMIN.ENTITIES.ASSET_TAG"))), " ");
  }
}
var AssetTagCreateWizardComponent = class _AssetTagCreateWizardComponent {
  assetTagDetailsStep = viewChild("assetTagDetailsStep", ...ngDevMode ? [{ debugName: "assetTagDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  assetTagDetailsForm = viewChild("assetTagDetailsForm", ...ngDevMode ? [{ debugName: "assetTagDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cdr = inject(ChangeDetectorRef);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // AssetTag model to collect data across steps
  newAssetTag = signal({}, ...ngDevMode ? [{ debugName: "newAssetTag" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const detailsStep = this.assetTagDetailsStep();
      const confirmStep = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (detailsStep && confirmStep) {
        this.steps = [
          {
            value: 1,
            header: this.translate.instant("ADMIN.ASSET_TAGS.WIZARD.STEPS.DETAILS"),
            content: detailsStep,
            complete: false,
            validate: () => this.assetTagDetailsForm()?.validate() ?? false
          },
          {
            value: 2,
            header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
            content: confirmStep,
            complete: true
          }
        ];
        this.cdr.detectChanges();
      }
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newAssetTag.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newAssetTag.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the assetTag state with form data
  updateAssetTagDetails(formData) {
    this.newAssetTag.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const assetTagData = this.newAssetTag();
    this.isSubmitting.set(true);
    this.entitiesService.createAssetTag(assetTagData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "asset-tag",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating asset tag:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newAssetTag.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function AssetTagCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetTagCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _AssetTagCreateWizardComponent, selectors: [["app-asset-tag-create-wizard"]], viewQuery: function AssetTagCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.assetTagDetailsStep, _c010, 5)(ctx.confirmationTemplate, _c17, 5)(ctx.assetTagDetailsForm, _c26, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["assetTagDetailsStep", ""], ["confirmationTemplate", ""], ["assetTagDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function AssetTagCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function AssetTagCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function AssetTagCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, AssetTagCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, AssetTagCreateWizardComponent_ng_template_3_Template, 42, 38, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    AssetTagDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetTagCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-asset-tag-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      AssetTagDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: AssetTag Details -->
<ng-template #assetTagDetailsStep let-step>
  <div class="p-fluid">
    <app-asset-tag-details-section
      #assetTagDetailsForm
      [createMode]="true"
      [entity]="newAssetTag()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateAssetTagDetails($event)"
    ></app-asset-tag-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- AssetTag Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.ASSET_TAG' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ASSET_TAGS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newAssetTag().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.ASSET_TAGS.DETAILS.FIELDS.SERIAL_NUMBER' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newAssetTag().serialNumber ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.ASSET_TAGS.DETAILS.FIELDS.ASSET_TAG_TYPE' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newAssetTag().assetTagType?.name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>

          <div class="flex">
            <div class="w-1/3 font-medium">
              {{ 'ADMIN.ASSET_TAGS.DETAILS.FIELDS.MODE' | translate }}:
            </div>
            <div class="w-2/3">
              {{ newAssetTag().mode }}
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate: { entity: ('ADMIN.ENTITIES.ASSET_TAG' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { assetTagDetailsStep: [{ type: ViewChild, args: ["assetTagDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], assetTagDetailsForm: [{ type: ViewChild, args: ["assetTagDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(AssetTagCreateWizardComponent, { className: "AssetTagCreateWizardComponent", filePath: "src/app/features/administration/components/asset-tags/asset-tag-create-wizard/asset-tag-create-wizard.component.ts", lineNumber: 46 });
})();

// src/app/features/administration/configs/asset-tags.grid.config.ts
var AssetTagsGridConfig = class _AssetTagsGridConfig {
  assetTagsService = inject(AssetTagsDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  dialogRef = null;
  dialogService = inject(DialogService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [AssetTagListItem.StateEnum.Active];
  filterState = signal({
    state: { value: this.defaultStateValues, operator: "in" }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { clientId: "" };
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  fieldTransformations = {
    tagType: "assetTagType.name",
    asset: "asset.name"
  };
  entityType = "asset-tags";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.assetTagsService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.ASSET_TAGS.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "serialNumber",
        header: this.translate.instant("ADMIN.ASSET_TAGS.COLUMNS.SERIAL_NUMBER"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "tagType",
        header: this.translate.instant("ADMIN.ASSET_TAGS.COLUMNS.ASSET_TAG_TYPE"),
        valueGetter: (rowData) => rowData.assetTagType?.name ?? "",
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "mode",
        header: this.translate.instant("ADMIN.ASSET_TAGS.COLUMNS.MODE"),
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("ADMIN.ASSET_TAGS.MODES.MOVABLE"),
              value: AssetTagListItem.ModeEnum.Movable
            },
            {
              label: this.translate.instant("ADMIN.ASSET_TAGS.MODES.FIXED"),
              value: AssetTagListItem.ModeEnum.Fixed
            },
            {
              label: this.translate.instant("ADMIN.ASSET_TAGS.MODES.STOCK"),
              value: AssetTagListItem.ModeEnum.Stock
            }
          ]
        },
        transform: "capitalize"
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.ASSET_TAGS.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: AssetTagListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: AssetTagListItem.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        }
      },
      {
        field: "asset",
        header: this.translate.instant("ADMIN.ASSET_TAGS.COLUMNS.ASSET"),
        valueGetter: (rowData) => rowData.asset?.name ?? "",
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.ASSET_TAGS.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData["modifiedDate"]) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData["modifiedDate"]), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: { value: this.defaultStateValues, operator: "in" }
    });
  }
  initialize(params) {
    this.params = params;
  }
  getTitle() {
    return this.translate.instant("ADMIN.ASSET_TAGS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.params["clientId"],
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const actualField = this.fieldTransformations[field] || field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    const clientId = this.params["clientId"];
    this.openAssetTagCreationDialog(clientId).subscribe();
  }
  openAssetTagCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(AssetTagCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.ASSET_TAG")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function AssetTagsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetTagsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AssetTagsGridConfig, factory: _AssetTagsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetTagsGridConfig, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/shared/services/xlsx-export.service.ts
var XLSX_MIME_TYPE = "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet";
var XlsxExportService = class _XlsxExportService {
  /**
   * Build and download `filename` containing one worksheet with a header row
   * followed by one row per item. Returns an `Observable<void>` that completes
   * once the download has been triggered, so callers can drive a loading
   * indicator. ExcelJS is imported lazily to keep it out of the main bundle.
   */
  export(columns, rows, filename, sheetName = "Export") {
    const exportCols = columns.filter((col) => col.exportable !== false);
    return from(import("./chunk-5MOIHJGX.js")).pipe(switchMap((excelModule) => {
      const ExcelJS = excelModule.default ?? excelModule;
      const workbook = new ExcelJS.Workbook();
      const worksheet = workbook.addWorksheet(sheetName);
      worksheet.columns = exportCols.map((col) => ({
        header: col.header,
        // Reasonable default width; long identifiers stay readable.
        width: Math.max(col.header.length + 2, 15)
      }));
      worksheet.getRow(1).font = { bold: true };
      for (const row of rows) {
        worksheet.addRow(exportCols.map((col) => String(this.resolveValue(col, row) ?? "")));
      }
      return from(workbook.xlsx.writeBuffer());
    }), map((buffer) => {
      this.triggerDownload(buffer, filename);
    }), catchError((error) => {
      console.error(`XlsxExportService: failed to export "${filename}"`, error);
      return throwError(() => error);
    }));
  }
  triggerDownload(buffer, filename) {
    const blob = new Blob([buffer], { type: XLSX_MIME_TYPE });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = filename;
    a.click();
    URL.revokeObjectURL(url);
  }
  resolveValue(col, row) {
    if (col.valueGetter)
      return col.valueGetter(row);
    const field = col.field;
    if (field.includes(".")) {
      return this.resolvePath(row, field);
    }
    return String(row[field] ?? "");
  }
  resolvePath(obj, path) {
    const value = path.split(".").reduce((acc, key) => {
      return acc != null && typeof acc === "object" ? acc[key] : void 0;
    }, obj);
    return String(value ?? "");
  }
  static \u0275fac = function XlsxExportService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _XlsxExportService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _XlsxExportService, factory: _XlsxExportService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(XlsxExportService, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/features/administration/components/device/device-create-wizard/device-create-wizard.component.ts
var _c011 = ["deviceDetailsStep"];
var _c18 = ["deviceInfoStep"];
var _c27 = ["confirmationTemplate"];
var _c36 = ["deviceDetailsForm"];
var _c44 = ["deviceInfoForm"];
var _c54 = (a0) => ({ entity: a0 });
function DeviceCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-device-details-section", 7, 3);
    \u0275\u0275listener("formValidityChange", function DeviceCreateWizardComponent_ng_template_1_Template_app_device_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function DeviceCreateWizardComponent_ng_template_1_Template_app_device_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateDeviceDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newDevice())("ownerId", ctx_r1.ownerId);
  }
}
function DeviceCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-info-section", 8, 4);
    \u0275\u0275listener("formValidityChange", function DeviceCreateWizardComponent_ng_template_3_Template_app_info_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onInfoFormValidityChange($event));
    })("formValueChange", function DeviceCreateWizardComponent_ng_template_3_Template_app_info_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateDeviceInfo($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newDevice());
  }
}
function DeviceCreateWizardComponent_ng_template_5_For_32_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 16)(1, "div", 17);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 18);
    \u0275\u0275text(4);
    \u0275\u0275pipe(5, "translate");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_16_0;
    const item_r4 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(item_r4 == null ? null : item_r4["title"]);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", ((tmp_16_0 = ctx_r1.newDevice()["fields"]) == null ? null : tmp_16_0[item_r4["id"]]) || \u0275\u0275pipeBind1(5, 2, "SHARED.COMMON.NOT_PROVIDED"), " ");
  }
}
function DeviceCreateWizardComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 9);
    \u0275\u0275element(1, "span", 10);
    \u0275\u0275elementStart(2, "div", 11)(3, "div", 12)(4, "div", 13)(5, "p", 14);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 15)(10, "div", 16)(11, "div", 17);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 18);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 16)(18, "div", 17);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 18);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(24, "div", 16)(25, "div", 17);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "div", 18);
    \u0275\u0275text(29);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(30, "div", 15);
    \u0275\u0275repeaterCreate(31, DeviceCreateWizardComponent_ng_template_5_For_32_Template, 6, 4, "div", 16, \u0275\u0275repeaterTrackByIndex);
    \u0275\u0275elementEnd()()()();
    \u0275\u0275elementStart(33, "div", 19)(34, "p", 20);
    \u0275\u0275text(35);
    \u0275\u0275pipe(36, "translate");
    \u0275\u0275pipe(37, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_9_0;
    let tmp_11_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 10, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(28, _c54, \u0275\u0275pipeBind1(7, 8, "ADMIN.ENTITIES.DEVICE"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 13, "ADMIN.DEVICES.DETAILS.FIELDS.SERIAL_NUMBER"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newDevice().name || \u0275\u0275pipeBind1(16, 15, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 17, "ADMIN.DEVICES.DETAILS.FIELDS.DEVICE_TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_9_0 = ctx_r1.newDevice().deviceType) == null ? null : tmp_9_0.name) || \u0275\u0275pipeBind1(23, 19, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(27, 21, "ADMIN.DEVICES.DETAILS.FIELDS.CONFIG_PROFILE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate((tmp_11_0 = ctx_r1.newDevice().configProfile) == null ? null : tmp_11_0.name);
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.entityCustomFields());
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(37, 25, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(30, _c54, \u0275\u0275pipeBind1(36, 23, "ADMIN.ENTITIES.DEVICE"))), " ");
  }
}
var DeviceCreateWizardComponent = class _DeviceCreateWizardComponent {
  deviceDetailsStep = viewChild("deviceDetailsStep", ...ngDevMode ? [{ debugName: "deviceDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  deviceInfoStep = viewChild("deviceInfoStep", ...ngDevMode ? [{ debugName: "deviceInfoStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  deviceDetailsForm = viewChild("deviceDetailsForm", ...ngDevMode ? [{ debugName: "deviceDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  deviceInfoForm = viewChild("deviceInfoForm", ...ngDevMode ? [{ debugName: "deviceInfoForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  adminStateService = inject(AdminStateService);
  entityCustomFields = signal([], ...ngDevMode ? [{ debugName: "entityCustomFields" }] : (
    /* istanbul ignore next */
    []
  ));
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // Device model to collect data across steps
  newDevice = signal({}, ...ngDevMode ? [{ debugName: "newDevice" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const deviceDetailsStep = this.deviceDetailsStep();
      const deviceInfoStep = this.deviceInfoStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(deviceDetailsStep) || isNullOrUndefined(deviceInfoStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.DEVICES.DETAILS.TITLE"),
          content: deviceDetailsStep,
          complete: false,
          validate: () => this.deviceDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("ADMIN.SHARED.INFORMATION.TITLE"),
          content: deviceInfoStep,
          complete: false,
          validate: () => this.deviceInfoForm()?.validate() ?? false
        },
        {
          value: 3,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    this.newDevice.update((current) => __spreadProps(__spreadValues({}, current), {
      parameters: {
        active_input: "",
        hours_input: "default",
        idling_input: "speed",
        idling_input_invert: false,
        io: {}
      }
    }));
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newDevice.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
    const selectedClient = this.adminStateService.selectedClient();
    if (selectedClient) {
      this.entityCustomFields.set([
        ...selectedClient.customFields?.["device"] ?? []
      ]);
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newDevice.set({
      parameters: {
        active_input: "",
        hours_input: "default",
        idling_input: "speed",
        idling_input_invert: false,
        io: {}
      }
    });
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  onInfoFormValidityChange(isValid) {
    this.steps[1].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the device state with form data
  updateDeviceDetails(formData) {
    this.newDevice.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  updateDeviceInfo(formData) {
    this.newDevice.update((current) => {
      const updated = __spreadValues({}, current);
      updated["fields"] = isEmpty(formData["fields"]) ? void 0 : formData["fields"];
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const deviceData = this.newDevice();
    this.isSubmitting.set(true);
    this.entitiesService.createDevice(deviceData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "device",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating device:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newDevice.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function DeviceCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DeviceCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _DeviceCreateWizardComponent, selectors: [["app-device-create-wizard"]], viewQuery: function DeviceCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.deviceDetailsStep, _c011, 5)(ctx.deviceInfoStep, _c18, 5)(ctx.confirmationTemplate, _c27, 5)(ctx.deviceDetailsForm, _c36, 5)(ctx.deviceInfoForm, _c44, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(5);
    }
  }, decls: 7, vars: 5, consts: [["deviceDetailsStep", ""], ["deviceInfoStep", ""], ["confirmationTemplate", ""], ["deviceDetailsForm", ""], ["deviceInfoForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], ["entityType", "device", 3, "formValidityChange", "formValueChange", "createMode", "entity"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function DeviceCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 5);
      \u0275\u0275listener("complete", function DeviceCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function DeviceCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, DeviceCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, DeviceCreateWizardComponent_ng_template_3_Template, 3, 2, "ng-template", null, 1, \u0275\u0275templateRefExtractor)(5, DeviceCreateWizardComponent_ng_template_5_Template, 38, 32, "ng-template", null, 2, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    DeviceDetailsSectionComponent,
    InfoSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DeviceCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-device-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      DeviceDetailsSectionComponent,
      InfoSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Device Details -->
<ng-template #deviceDetailsStep let-step>
  <div class="p-fluid">
    <app-device-details-section
      #deviceDetailsForm
      [createMode]="true"
      [entity]="newDevice()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateDeviceDetails($event)"
    ></app-device-details-section>
  </div>
</ng-template>

<ng-template #deviceInfoStep let-step>
  <div class="p-fluid">
    <app-info-section
      #deviceInfoForm
      [createMode]="true"
      [entity]="newDevice()"
      entityType="device"
      (formValidityChange)="onInfoFormValidityChange($event)"
      (formValueChange)="updateDeviceInfo($event)"
    ></app-info-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Device Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : {
                      entity: ('ADMIN.ENTITIES.DEVICE' | translate),
                    }
            }}
          </p>

          <div class="space-y-2">
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DEVICES.DETAILS.FIELDS.SERIAL_NUMBER' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newDevice().name || ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DEVICES.DETAILS.FIELDS.DEVICE_TYPE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newDevice().deviceType?.name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DEVICES.DETAILS.FIELDS.CONFIG_PROFILE' | translate }}:
              </div>
              <div class="w-2/3">{{ newDevice().configProfile?.name }}</div>
            </div>

            <!-- Device Info Section -->
            <div class="space-y-2">
              @for (item of entityCustomFields(); track $index) {
                <div class="flex">
                  <div class="w-1/3 font-medium">{{ item?.['title'] }}</div>
                  <div class="w-2/3">
                    {{
                      newDevice()['fields']?.[item['id']] ||
                        ('SHARED.COMMON.NOT_PROVIDED' | translate)
                    }}
                  </div>
                </div>
              }
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate: { entity: ('ADMIN.ENTITIES.DEVICE' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { deviceDetailsStep: [{ type: ViewChild, args: ["deviceDetailsStep", { isSignal: true }] }], deviceInfoStep: [{ type: ViewChild, args: ["deviceInfoStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], deviceDetailsForm: [{ type: ViewChild, args: ["deviceDetailsForm", { isSignal: true }] }], deviceInfoForm: [{ type: ViewChild, args: ["deviceInfoForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(DeviceCreateWizardComponent, { className: "DeviceCreateWizardComponent", filePath: "src/app/features/administration/components/device/device-create-wizard/device-create-wizard.component.ts", lineNumber: 50 });
})();

// src/app/features/administration/configs/device.grid.config.ts
var EXPORT_PAGE_SIZE = 200;
var DevicesGridConfig = class _DevicesGridConfig {
  devicesService = inject(DevicesDataAdapter);
  xlsxExportService = inject(XlsxExportService);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  dialogRef = null;
  dialogService = inject(DialogService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [
    DeviceListItem.StateEnum.Active,
    DeviceListItem.StateEnum.Inactive,
    DeviceListItem.StateEnum.Suspended
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { clientId: "" };
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  fieldTransformations = {
    ["01usv"]: "fields.01usv",
    ["02udr"]: "fields.02udr",
    simCard: "simcard.name",
    asset: "asset.name",
    configProfile: "configProfile.name",
    deviceType: "deviceType.name"
  };
  entityType = "devices";
  selectionMode = "multiple";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.devicesService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.DEVICES.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "deviceType",
        header: this.translate.instant("ADMIN.DEVICES.COLUMNS.DEVICE_TYPE"),
        valueGetter: (rowData) => isDefined(rowData.deviceType?.name) ? rowData.deviceType?.name : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: []
        }
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.DEVICES.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: DeviceListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: DeviceListItem.StateEnum.Deleted
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.INACTIVE"),
              value: DeviceListItem.StateEnum.Inactive
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.SUSPENDED"),
              value: DeviceListItem.StateEnum.Suspended
            }
          ],
          defaultSelected: this.defaultStateValues
        },
        transform: "capitalize"
      },
      {
        field: "asset",
        header: this.translate.instant("ADMIN.DEVICES.COLUMNS.ASSET"),
        valueGetter: (rowData) => isDefined(rowData.asset?.name) ? rowData.asset?.name : "",
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "simCard",
        header: this.translate.instant("ADMIN.DEVICES.COLUMNS.SIM_CARD"),
        valueGetter: (rowData) => isDefined(rowData.simcard?.name) ? rowData.simcard?.name : "",
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "configProfile",
        header: this.translate.instant("ADMIN.DEVICES.COLUMNS.CONFIG_PROFILE"),
        valueGetter: (rowData) => isDefined(rowData.configProfile?.name) ? rowData.configProfile?.name : "",
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "01usv",
        header: this.translate.instant("ADMIN.DEVICES.COLUMNS.DEVICE_SERIAL"),
        valueGetter: (rowData) => isDefined(rowData.fields?.["01usv"]) && isString(rowData.fields?.["01usv"]) ? rowData.fields?.["01usv"] : "",
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "02udr",
        header: this.translate.instant("ADMIN.DEVICES.COLUMNS.DEVICE_REMARKS"),
        valueGetter: (rowData) => isDefined(rowData.fields?.["02udr"]) && isString(rowData.fields?.["02udr"]) ? rowData.fields?.["02udr"] : "",
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.DEVICES.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData["modifiedDate"]) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData["modifiedDate"]), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
  }
  getTitle() {
    return this.translate.instant("ADMIN.DEVICES.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.params["clientId"],
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  /**
   * Export every device matching the current filters/sort — not just the page
   * rendered in the grid. The grid holds a single server-fetched page in memory,
   * so PrimeNG's native export would truncate the file to that page and, because
   * it ignores column `valueGetter`s, emit `[object Object]` for object-valued
   * columns (Type, Asset, SIM Card, Config Profile). Instead we sweep all pages
   * and hand the rows to {@link XlsxExportService}, which resolves each cell
   * through its `valueGetter`.
   *
   * Exports as `.xlsx` rather than CSV so that long numeric identifiers (IMEIs,
   * SIM serials) open correctly in Excel: CSV values get converted to scientific
   * notation on open, and modern Excel ignores the in-band text hints that used
   * to prevent that.
   *
   * Returns an `Observable<void>` that completes once the file is generated (or
   * errors if a fetch fails) so the container can drive its loading indicator.
   */
  onExport() {
    const columns = this.columnsSignal();
    const date = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
    return this.fetchAllDevicesForExport().pipe(switchMap((devices) => this.xlsxExportService.export(columns, devices, `devices-${date}.xlsx`, "Devices")));
  }
  /**
   * Fetch every device for the current client/filters by draining all pages,
   * reusing {@link getRequestParams} so the export honours the same
   * owner/sort/filters as the on-screen grid.
   */
  fetchAllDevicesForExport() {
    const baseParams = this.getRequestParams();
    return this.fetchAllPages((offset, limit) => this.devicesService.getList(__spreadProps(__spreadValues({}, baseParams), { offset, limit })));
  }
  /**
   * Drain every page of a paginated list query, accumulating all items in order.
   * Stops when a page comes back empty or the reported total `count` has been
   * reached, so it is resilient to a backend that caps the page size below the
   * requested limit.
   */
  fetchAllPages(query) {
    const fetchFrom = (offset) => query(offset, EXPORT_PAGE_SIZE).pipe(map((response) => {
      const items = response.items ?? [];
      return {
        items,
        nextOffset: offset + items.length,
        count: response.count ?? 0
      };
    }));
    return fetchFrom(0).pipe(expand((page) => page.items.length > 0 && page.nextOffset < page.count ? fetchFrom(page.nextOffset) : EMPTY), reduce((acc, page) => acc.concat(page.items), []));
  }
  onFilterChange(field, value) {
    const actualField = this.fieldTransformations[field] || field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    const clientId = this.params["clientId"];
    this.openDeviceCreationDialog(clientId).subscribe();
  }
  openDeviceCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(DeviceCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.DEVICE")
      }),
      baseZIndex: 1e4,
      data: {
        ownerId
      },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  onBulkImport() {
    const clientId = this.params["clientId"];
    this.openBulkImportDialog(clientId).then((obs) => obs.subscribe()).catch(console.error);
  }
  async openBulkImportDialog(ownerId) {
    const { BulkImportDialogComponent } = await import("./chunk-NCXIB3D3.js");
    this.dialogRef = this.dialogService.open(BulkImportDialogComponent, {
      header: this.translate.instant("ADMIN.DEVICES.BULK_IMPORT_TITLE"),
      baseZIndex: 1e4,
      data: {
        ownerId
      },
      position: "top",
      modal: true,
      width: "800px"
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function DevicesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DevicesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _DevicesGridConfig, factory: _DevicesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DevicesGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/geofences/geofence-coordinates-section/geofence-coordinates-section.component.ts
var _c012 = (a0) => ({ "icon-primary": a0 });
function GeofenceCoordinatesSectionComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 5)(1, "span", 7);
    \u0275\u0275pipe(2, "translate");
    \u0275\u0275listener("click", function GeofenceCoordinatesSectionComponent_Conditional_6_Template_span_click_1_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onEdit());
    })("keydown.enter", function GeofenceCoordinatesSectionComponent_Conditional_6_Template_span_keydown_enter_1_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onEdit());
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    \u0275\u0275advance();
    \u0275\u0275attribute("aria-label", \u0275\u0275pipeBind1(2, 1, "ADMIN.ZONES.COORDS.ARIA.EDIT"));
  }
}
function GeofenceCoordinatesSectionComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "app-dynamic-form", 8);
    \u0275\u0275listener("formSubmit", function GeofenceCoordinatesSectionComponent_Conditional_10_Template_app_dynamic_form_formSubmit_0_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onSubmit($event));
    })("formValidityChange", function GeofenceCoordinatesSectionComponent_Conditional_10_Template_app_dynamic_form_formValidityChange_0_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onFormValidityChange($event));
    })("formValueChange", function GeofenceCoordinatesSectionComponent_Conditional_10_Template_app_dynamic_form_formValueChange_0_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onFormValueChange($event));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("fields", ctx_r1.formFields)("actions", ctx_r1.actions)("hideActions", ctx_r1.createMode());
  }
}
var GeofenceCoordinatesSectionComponent = class _GeofenceCoordinatesSectionComponent extends TypedEntitySectionBaseComponent {
  ownerId = input(null, ...ngDevMode ? [{ debugName: "ownerId" }] : (
    /* istanbul ignore next */
    []
  ));
  measurementUnitsService = inject(MeasurementUnitsService);
  entitiesService = inject(EntitiesService);
  zoneGroups = [];
  costCenters = [];
  constructor() {
    super();
  }
  updateFormFields() {
    const geofence = this.entity();
    this.formFields = [
      createNumberField("lat", "Latitude", geofence?.["lat"] ?? void 0, {
        name: "lat",
        validations: [
          {
            name: "required",
            validator: Validators.required,
            message: "Latitude is required"
          },
          {
            name: "min",
            validator: Validators.min(-90),
            message: "Latitude must be greater or equal to -90"
          },
          {
            name: "max",
            validator: Validators.max(90),
            message: "Latitude must be less or equal to 90"
          }
        ],
        props: {
          appendTo: "body",
          showClear: true,
          min: -90,
          max: 90,
          step: 1e-5,
          maxFractionDigits: 5
        }
      }),
      createNumberField("lon", "Longitude", geofence?.["lon"], {
        name: "lon",
        validations: [
          {
            name: "required",
            validator: Validators.required,
            message: "Longitude is required"
          },
          {
            name: "min",
            validator: Validators.min(-180),
            message: "Longitude must be greater or equal to -180"
          },
          {
            name: "max",
            validator: Validators.max(180),
            message: "Longitude must be less or equal to 180"
          }
        ],
        props: {
          appendTo: "body",
          showClear: true,
          min: -180,
          max: 180,
          step: 1e-5,
          maxFractionDigits: 5
        }
      }),
      createDropdownField("shape", "Shape", [
        { label: "Hexagon", value: "hexagon" },
        { label: "Octagon", value: "octagon" },
        { label: "Square", value: "square" },
        { label: "Triangle", value: "triangle" }
      ], geofence?.["shape"] ?? "", {
        name: "shape",
        validations: [
          {
            name: "required",
            validator: Validators.required,
            message: "Shape is required"
          }
        ],
        props: {
          appendTo: "body",
          showClear: true
        }
      }),
      createNumberField("shapeRadius", "Radius", geofence?.["shapeRadius"], {
        name: "shapeRadius",
        validations: [
          {
            name: "required",
            validator: Validators.required,
            message: "Radius is required"
          },
          {
            name: "min",
            validator: Validators.min(0),
            message: "Radius must be greater or equal to 0"
          }
        ],
        props: {
          appendTo: "body",
          showClear: true,
          suffix: this.measurementUnitsService.unitSymbol(MeasurementUnitType.DISTANCE)
        }
      })
    ];
  }
  getUpdateMethod() {
    return (id, data) => this.entitiesService.updateZone(id, data);
  }
  transformFormData(formData) {
    const normalizedFormData = Array.isArray(formData) ? formData.reduce((acc, item) => __spreadValues(__spreadValues({}, acc), item), {}) : formData;
    const transformedData = {};
    if (normalizedFormData["lat"] !== void 0) {
      transformedData["lat"] = normalizedFormData["lat"];
    }
    if (normalizedFormData["lon"] !== void 0) {
      transformedData["lon"] = normalizedFormData["lon"];
    }
    if (normalizedFormData["shape"] !== void 0) {
      transformedData["shape"] = normalizedFormData["shape"];
    }
    if (normalizedFormData["shapeRadius"] !== void 0) {
      const radiusValue = normalizedFormData["shapeRadius"];
      transformedData["shapeRadius"] = this.measurementUnitsService.toBackend(MeasurementUnitType.DISTANCE, radiusValue);
    }
    const entity = this.entity();
    if (entity && "id" in entity) {
      transformedData["id"] = entity["id"];
    }
    const ownerId = this.ownerId();
    if (ownerId !== null && ownerId !== void 0) {
      transformedData["ownerId"] = ownerId;
    }
    return transformedData;
  }
  static \u0275fac = function GeofenceCoordinatesSectionComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _GeofenceCoordinatesSectionComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _GeofenceCoordinatesSectionComponent, selectors: [["app-geofence-coordinates-section"]], inputs: { ownerId: [1, "ownerId"] }, features: [\u0275\u0275InheritDefinitionFeature], decls: 11, vars: 11, consts: [[1, "card", "pt-2"], [1, "mb-4", "p-2", "flex", "justify-start", "items-center"], [1, "pi", "pi-map-marker", "!text-[2.5rem]", 3, "ngClass"], [1, "pl-6", "flex-1"], [1, "flex", "justify-between", "items-center"], [1, "icon-light"], [3, "fields", "actions", "hideActions"], [1, "pi", "pi-pen-to-square", "action", "hover", "!text-2xl", 3, "click", "keydown.enter"], [3, "formSubmit", "formValidityChange", "formValueChange", "fields", "actions", "hideActions"]], template: function GeofenceCoordinatesSectionComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "div", 0)(1, "div", 1);
      \u0275\u0275element(2, "span", 2);
      \u0275\u0275pipe(3, "translate");
      \u0275\u0275elementStart(4, "div", 3)(5, "div", 4);
      \u0275\u0275conditionalCreate(6, GeofenceCoordinatesSectionComponent_Conditional_6_Template, 3, 3, "div", 5);
      \u0275\u0275elementEnd();
      \u0275\u0275elementStart(7, "span");
      \u0275\u0275text(8);
      \u0275\u0275pipe(9, "translate");
      \u0275\u0275elementEnd()()();
      \u0275\u0275conditionalCreate(10, GeofenceCoordinatesSectionComponent_Conditional_10_Template, 1, 3, "app-dynamic-form", 6);
      \u0275\u0275elementEnd();
    }
    if (rf & 2) {
      \u0275\u0275advance(2);
      \u0275\u0275property("ngClass", \u0275\u0275pureFunction1(9, _c012, ctx.isFormEditable()));
      \u0275\u0275attribute("aria-label", \u0275\u0275pipeBind1(3, 5, "ADMIN.ZONES.COORDS.ARIA.MAP_MARKER_ICON"));
      \u0275\u0275advance(4);
      \u0275\u0275conditional(!ctx.isFormEditable() && !ctx.createMode() ? 6 : -1);
      \u0275\u0275advance(2);
      \u0275\u0275textInterpolate(\u0275\u0275pipeBind1(9, 7, "ADMIN.ZONES.COORDS.DESCRIPTION"));
      \u0275\u0275advance(2);
      \u0275\u0275conditional(ctx.isFormEditable() || ctx.createMode() && ctx.formFields.length ? 10 : -1);
    }
  }, dependencies: [CommonModule, NgClass, DynamicFormComponent, TranslatePipe], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GeofenceCoordinatesSectionComponent, [{
    type: Component,
    args: [{ selector: "app-geofence-coordinates-section", imports: [CommonModule, DynamicFormComponent, TranslatePipe], template: `<div class="card pt-2">
  <div class="mb-4 p-2 flex justify-start items-center">
    <span
      class="pi pi-map-marker !text-[2.5rem]"
      [ngClass]="{ 'icon-primary': isFormEditable() }"
      [attr.aria-label]="'ADMIN.ZONES.COORDS.ARIA.MAP_MARKER_ICON' | translate"
    ></span>

    <div class="pl-6 flex-1">
      <div class="flex justify-between items-center">
        @if (!isFormEditable() && !createMode()) {
          <div class="icon-light">
            <span
              class="pi pi-pen-to-square action hover !text-2xl"
              [attr.aria-label]="'ADMIN.ZONES.COORDS.ARIA.EDIT' | translate"
              (click)="onEdit()"
              (keydown.enter)="onEdit()"
            ></span>
          </div>
        }
      </div>
      <span>{{ 'ADMIN.ZONES.COORDS.DESCRIPTION' | translate }}</span>
    </div>
  </div>

  @if (isFormEditable() || (createMode() && formFields.length)) {
    <app-dynamic-form
      [fields]="formFields"
      [actions]="actions"
      [hideActions]="createMode()"
      (formSubmit)="onSubmit($event)"
      (formValidityChange)="onFormValidityChange($event)"
      (formValueChange)="onFormValueChange($event)"
    >
    </app-dynamic-form>
  }
</div>
` }]
  }], () => [], { ownerId: [{ type: Input, args: [{ isSignal: true, alias: "ownerId", required: false }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(GeofenceCoordinatesSectionComponent, { className: "GeofenceCoordinatesSectionComponent", filePath: "src/app/features/administration/components/geofences/geofence-coordinates-section/geofence-coordinates-section.component.ts", lineNumber: 30 });
})();

// src/app/features/administration/components/geofences/geofence-create-wizard/geofence-create-wizard.component.ts
var _c013 = ["geofenceDetailsStep"];
var _c19 = ["geofenceCoordinatesStep"];
var _c28 = ["confirmationTemplate"];
var _c37 = ["geofenceDetailsForm"];
var _c45 = ["geofenceCoordinatesForm"];
var _c55 = (a0) => ({ entity: a0 });
function GeofenceCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-geofence-details-section", 7, 3);
    \u0275\u0275listener("formValidityChange", function GeofenceCreateWizardComponent_ng_template_1_Template_app_geofence_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function GeofenceCreateWizardComponent_ng_template_1_Template_app_geofence_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateGeofenceDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newGeofence())("ownerId", ctx_r1.ownerId);
  }
}
function GeofenceCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-geofence-coordinates-section", 8, 4);
    \u0275\u0275listener("formValidityChange", function GeofenceCreateWizardComponent_ng_template_3_Template_app_geofence_coordinates_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onCoordinatesFormValidityChange($event));
    })("formValueChange", function GeofenceCreateWizardComponent_ng_template_3_Template_app_geofence_coordinates_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateGeofenceCoordinates($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newGeofence());
  }
}
function GeofenceCreateWizardComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 9);
    \u0275\u0275element(1, "span", 10);
    \u0275\u0275elementStart(2, "div", 11)(3, "div", 12)(4, "div", 13)(5, "p", 14);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 15)(10, "div", 16)(11, "div", 17);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 18);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 16)(18, "div", 17);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 18);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(24, "div", 16)(25, "div", 17);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "div", 18);
    \u0275\u0275text(29);
    \u0275\u0275pipe(30, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(31, "div", 16)(32, "div", 17);
    \u0275\u0275text(33);
    \u0275\u0275pipe(34, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(35, "div", 18);
    \u0275\u0275text(36);
    \u0275\u0275pipe(37, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(38, "div", 16)(39, "div", 17);
    \u0275\u0275text(40);
    \u0275\u0275pipe(41, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(42, "div", 18);
    \u0275\u0275text(43);
    \u0275\u0275pipe(44, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(45, "div", 16)(46, "div", 17);
    \u0275\u0275text(47);
    \u0275\u0275pipe(48, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(49, "div", 18);
    \u0275\u0275text(50);
    \u0275\u0275pipe(51, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(52, "div", 16)(53, "div", 17);
    \u0275\u0275text(54);
    \u0275\u0275pipe(55, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(56, "div", 18);
    \u0275\u0275text(57);
    \u0275\u0275pipe(58, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(59, "div", 16)(60, "div", 17);
    \u0275\u0275text(61);
    \u0275\u0275pipe(62, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(63, "div", 18);
    \u0275\u0275text(64);
    \u0275\u0275pipe(65, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(66, "div", 16)(67, "div", 17);
    \u0275\u0275text(68);
    \u0275\u0275pipe(69, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(70, "div", 18);
    \u0275\u0275text(71);
    \u0275\u0275pipe(72, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(73, "div", 19)(74, "p", 20);
    \u0275\u0275text(75);
    \u0275\u0275pipe(76, "translate");
    \u0275\u0275pipe(77, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_11_0;
    let tmp_13_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 22, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(66, _c55, \u0275\u0275pipeBind1(7, 20, "ADMIN.ENTITIES.ZONE"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 25, "ADMIN.ZONES.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newGeofence().name || \u0275\u0275pipeBind1(16, 27, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 29, "ADMIN.ZONES.DETAILS.FIELDS.TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newGeofence().zoneType || \u0275\u0275pipeBind1(23, 31, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(27, 33, "ADMIN.ZONES.DETAILS.FIELDS.COST_CENTRE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_11_0 = ctx_r1.newGeofence().costCentre) == null ? null : tmp_11_0.name) || \u0275\u0275pipeBind1(30, 35, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(34, 37, "ADMIN.ZONES.DETAILS.FIELDS.GROUP"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_13_0 = ctx_r1.newGeofence().group) == null ? null : tmp_13_0.name) || \u0275\u0275pipeBind1(37, 39, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(41, 41, "ADMIN.ZONES.DETAILS.FIELDS.SPEED"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newGeofence().speed || \u0275\u0275pipeBind1(44, 43, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(48, 45, "ADMIN.ZONES.DETAILS.FIELDS.RADIUS"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newGeofence().radius || \u0275\u0275pipeBind1(51, 47, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(55, 49, "ADMIN.ZONES.COORDS.FIELDS.LATITUDE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", (ctx_r1.geofenceCoordinates == null ? null : ctx_r1.geofenceCoordinates["lat"]) || \u0275\u0275pipeBind1(58, 51, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(62, 53, "ADMIN.ZONES.COORDS.FIELDS.LONGITUDE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", (ctx_r1.geofenceCoordinates == null ? null : ctx_r1.geofenceCoordinates["lon"]) || \u0275\u0275pipeBind1(65, 55, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(69, 57, "ADMIN.ZONES.COORDS.FIELDS.SHAPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", (ctx_r1.geofenceCoordinates == null ? null : ctx_r1.geofenceCoordinates["shape"]) || \u0275\u0275pipeBind1(72, 59, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(77, 63, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(68, _c55, \u0275\u0275pipeBind1(76, 61, "ADMIN.ENTITIES.ZONE"))), " ");
  }
}
var GeofenceCreateWizardComponent = class _GeofenceCreateWizardComponent {
  geofenceDetailsStep = viewChild("geofenceDetailsStep", ...ngDevMode ? [{ debugName: "geofenceDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  geofenceCoordinatesStep = viewChild("geofenceCoordinatesStep", ...ngDevMode ? [{ debugName: "geofenceCoordinatesStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  geofenceDetailsForm = viewChild("geofenceDetailsForm", ...ngDevMode ? [{ debugName: "geofenceDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  geofenceCoordinatesForm = viewChild("geofenceCoordinatesForm", ...ngDevMode ? [{ debugName: "geofenceCoordinatesForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  measurementUnitsService = inject(MeasurementUnitsService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  apiSchemaService = inject(ApiSchemaService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  shapePoints = [3, 4, 6, 8];
  shapes = ["triangle", "square", "hexagon", "octagon"];
  geofenceCoordinates;
  // Geofence model to collect data across steps
  newGeofence = signal({}, ...ngDevMode ? [{ debugName: "newGeofence" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const geofenceDetailsStep = this.geofenceDetailsStep();
      const geofenceCoordinatesStep = this.geofenceCoordinatesStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(geofenceDetailsStep) || isNullOrUndefined(geofenceCoordinatesStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.ZONES.WIZARD.STEPS.DETAILS"),
          content: geofenceDetailsStep,
          complete: false,
          validate: () => this.geofenceDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("ADMIN.ZONES.WIZARD.STEPS.COORDINATES"),
          content: geofenceCoordinatesStep,
          complete: false,
          validate: () => this.geofenceCoordinatesForm()?.validate() ?? false
        },
        {
          value: 3,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newGeofence.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newGeofence.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  onCoordinatesFormValidityChange(isValid) {
    this.steps[1].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the geofence state with form data
  updateGeofenceDetails(formData) {
    this.newGeofence.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  updateGeofenceCoordinates(formData) {
    this.geofenceCoordinates = formData;
    const radius = this.measurementUnitsService.toBackend(MeasurementUnitType.DISTANCE, formData["shapeRadius"]);
    const points = SpatialUtils.generateRegularPolygon({ x: formData["lon"], y: formData["lat"] }, radius, this.shapePoints[this.shapes.indexOf(formData["shape"])], this.apiSchemaService.usesModernSchema());
    this.newGeofence.update((current) => __spreadProps(__spreadValues({}, current), {
      points,
      center: {
        x: formData["lon"],
        y: formData["lat"]
      }
    }));
  }
  onWizardComplete() {
    const geofenceData = this.newGeofence();
    this.isSubmitting.set(true);
    this.entitiesService.createZone(geofenceData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "geofence",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating geofence:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newGeofence.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function GeofenceCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _GeofenceCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _GeofenceCreateWizardComponent, selectors: [["app-geofence-create-wizard"]], viewQuery: function GeofenceCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.geofenceDetailsStep, _c013, 5)(ctx.geofenceCoordinatesStep, _c19, 5)(ctx.confirmationTemplate, _c28, 5)(ctx.geofenceDetailsForm, _c37, 5)(ctx.geofenceCoordinatesForm, _c45, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(5);
    }
  }, decls: 7, vars: 5, consts: [["geofenceDetailsStep", ""], ["geofenceCoordinatesStep", ""], ["confirmationTemplate", ""], ["geofenceDetailsForm", ""], ["geofenceCoordinatesForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [3, "formValidityChange", "formValueChange", "createMode", "entity"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function GeofenceCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 5);
      \u0275\u0275listener("complete", function GeofenceCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function GeofenceCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, GeofenceCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, GeofenceCreateWizardComponent_ng_template_3_Template, 3, 2, "ng-template", null, 1, \u0275\u0275templateRefExtractor)(5, GeofenceCreateWizardComponent_ng_template_5_Template, 78, 70, "ng-template", null, 2, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    GeofenceDetailsSectionComponent,
    GeofenceCoordinatesSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GeofenceCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-geofence-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      GeofenceDetailsSectionComponent,
      GeofenceCoordinatesSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Geofence Details -->
<ng-template #geofenceDetailsStep let-step>
  <div class="p-fluid">
    <app-geofence-details-section
      #geofenceDetailsForm
      [createMode]="true"
      [entity]="newGeofence()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateGeofenceDetails($event)"
    ></app-geofence-details-section>
  </div>
</ng-template>

<!-- Step 2: Geofence Coordinates -->
<ng-template #geofenceCoordinatesStep let-step>
  <div class="p-fluid">
    <app-geofence-coordinates-section
      #geofenceCoordinatesForm
      [createMode]="true"
      [entity]="newGeofence()"
      (formValidityChange)="onCoordinatesFormValidityChange($event)"
      (formValueChange)="updateGeofenceCoordinates($event)"
    ></app-geofence-coordinates-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Geofence Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate: { entity: ('ADMIN.ENTITIES.ZONE' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ZONES.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{ newGeofence().name || ('SHARED.COMMON.NONE' | translate) }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ZONES.DETAILS.FIELDS.TYPE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newGeofence().zoneType || ('SHARED.COMMON.NONE' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ZONES.DETAILS.FIELDS.COST_CENTRE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newGeofence().costCentre?.name ||
                    ('SHARED.COMMON.NONE' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ZONES.DETAILS.FIELDS.GROUP' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newGeofence().group?.name ||
                    ('SHARED.COMMON.NONE' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ZONES.DETAILS.FIELDS.SPEED' | translate }}:
              </div>
              <div class="w-2/3">
                {{ newGeofence().speed || ('SHARED.COMMON.NONE' | translate) }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ZONES.DETAILS.FIELDS.RADIUS' | translate }}:
              </div>
              <div class="w-2/3">
                {{ newGeofence().radius || ('SHARED.COMMON.NONE' | translate) }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ZONES.COORDS.FIELDS.LATITUDE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  geofenceCoordinates?.['lat'] ||
                    ('SHARED.COMMON.NONE' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ZONES.COORDS.FIELDS.LONGITUDE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  geofenceCoordinates?.['lon'] ||
                    ('SHARED.COMMON.NONE' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ZONES.COORDS.FIELDS.SHAPE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  geofenceCoordinates?.['shape'] ||
                    ('SHARED.COMMON.NONE' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate: { entity: ('ADMIN.ENTITIES.ZONE' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { geofenceDetailsStep: [{ type: ViewChild, args: ["geofenceDetailsStep", { isSignal: true }] }], geofenceCoordinatesStep: [{ type: ViewChild, args: ["geofenceCoordinatesStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], geofenceDetailsForm: [{ type: ViewChild, args: ["geofenceDetailsForm", { isSignal: true }] }], geofenceCoordinatesForm: [{ type: ViewChild, args: ["geofenceCoordinatesForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(GeofenceCreateWizardComponent, { className: "GeofenceCreateWizardComponent", filePath: "src/app/features/administration/components/geofences/geofence-create-wizard/geofence-create-wizard.component.ts", lineNumber: 53 });
})();

// src/app/features/administration/configs/geofences.grid.config.ts
var GeofencesGridConfig = class _GeofencesGridConfig {
  entityType = "geofences";
  geofencesService = inject(GeofencesDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  entitiesService = inject(EntitiesService);
  dialogRef = null;
  dialogService = inject(DialogService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = ["active", "inactive"];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { clientId: "" };
  destroyRef = inject(DestroyRef);
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  fieldTransformations = {
    costCentre: "costCentre.id",
    groups: "groups.id"
  };
  zoneTypeTranslationKeys = {
    location: "ADMIN.ZONES.DETAILS.TYPES.LOCATION",
    keepin: "ADMIN.ZONES.DETAILS.TYPES.KEEP_IN",
    nogo: "ADMIN.ZONES.DETAILS.TYPES.NO_GO",
    route: "ADMIN.ZONES.DETAILS.TYPES.ROUTE"
  };
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  defaultVisibleColumns = ["name", "zoneType", "costCentre", "group", "state"];
  dataService = this.geofencesService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.ZONES.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "zoneType",
        header: this.translate.instant("ADMIN.ZONES.COLUMNS.TYPE"),
        sortable: true,
        filterType: "multiselect",
        filter: true,
        filterMatchMode: "in",
        valueGetter: (rowData) => {
          if (!isDefined(rowData.zoneType)) {
            return "";
          }
          const key = this.zoneTypeTranslationKeys[rowData.zoneType];
          return isDefined(key) ? this.translate.instant(key) : rowData.zoneType;
        },
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("ADMIN.ZONES.DETAILS.TYPES.LOCATION"),
              value: "location"
            },
            {
              label: this.translate.instant("ADMIN.ZONES.DETAILS.TYPES.KEEP_IN"),
              value: "keepin"
            },
            {
              label: this.translate.instant("ADMIN.ZONES.DETAILS.TYPES.NO_GO"),
              value: "nogo"
            },
            {
              label: this.translate.instant("ADMIN.ZONES.DETAILS.TYPES.ROUTE"),
              value: "route"
            }
          ]
        }
      },
      {
        field: "costCentre",
        header: this.translate.instant("ADMIN.ZONES.COLUMNS.COST_CENTRE"),
        valueGetter: (rowData) => isDefined(rowData.costCentre?.name) ? rowData.costCentre?.name : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: []
        }
      },
      {
        field: "group",
        header: this.translate.instant("ADMIN.ZONES.COLUMNS.GROUP"),
        valueGetter: (rowData) => isDefined(rowData.group?.name) ? rowData.group?.name : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: []
        }
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.ZONES.COLUMNS.STATE"),
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: "active"
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.INACTIVE"),
              value: "inactive"
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: "deleted"
            }
          ],
          defaultSelected: this.defaultStateValues
        },
        transform: "capitalize"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.ZONES.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    this.columns = this.buildColumns();
    this.loadFilterOptions();
  }
  getTitle() {
    return this.translate.instant("ADMIN.ZONES.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.params["clientId"],
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const actualField = this.fieldTransformations[field] || field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  loadFilterOptions() {
    combineLatest([
      this.entitiesService.listCostCentres(this.params["clientId"]),
      this.entitiesService.listZoneGroups(this.params["clientId"])
    ]).pipe(takeUntilDestroyed(this.destroyRef), catchError(() => {
      return EMPTY;
    })).subscribe(([costCentres, zoneGroups]) => {
      const costCentreColumn = this.columns.find((col) => col.field === "costCentre");
      if (costCentreColumn?.filterConfig) {
        costCentreColumn.filterConfig.options = costCentres.items.map((costCentre) => ({
          label: costCentre.name ?? "",
          value: costCentre.id
        })).sort((a, b) => a.label.localeCompare(b.label));
      }
      const groupColumn = this.columns.find((col) => col.field === "group");
      if (groupColumn?.filterConfig) {
        groupColumn.filterConfig.options = zoneGroups.items.map((group) => ({
          label: group.name ?? "",
          value: group.id
        })).sort((a, b) => a.label.localeCompare(b.label));
      }
    });
  }
  onAdd() {
    const clientId = this.params["clientId"];
    this.openGeofenceCreationDialog(clientId).subscribe();
  }
  openGeofenceCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(GeofenceCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.ZONE")
      }),
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function GeofencesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _GeofencesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _GeofencesGridConfig, factory: _GeofencesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GeofencesGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/sim-cards/sim-card-create-wizard/sim-card-create-wizard.component.ts
var _c014 = ["simCardDetailsStep"];
var _c110 = ["confirmationTemplate"];
var _c29 = ["simCardDetailsForm"];
var _c38 = (a0) => ({ entity: a0 });
function SimCardCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-sim-card-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function SimCardCreateWizardComponent_ng_template_1_Template_app_sim_card_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function SimCardCreateWizardComponent_ng_template_1_Template_app_sim_card_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateSimCardDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newSimCard())("ownerId", ctx_r1.ownerId);
  }
}
function SimCardCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(24, "div", 13)(25, "div", 14);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "div", 15);
    \u0275\u0275text(29);
    \u0275\u0275pipe(30, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(31, "div", 13)(32, "div", 14);
    \u0275\u0275text(33);
    \u0275\u0275pipe(34, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(35, "div", 15);
    \u0275\u0275text(36);
    \u0275\u0275pipe(37, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(38, "div", 16)(39, "p", 17);
    \u0275\u0275text(40);
    \u0275\u0275pipe(41, "translate");
    \u0275\u0275pipe(42, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 12, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(36, _c38, \u0275\u0275pipeBind1(7, 10, "ADMIN.ENTITIES.SIM_CARD"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 15, "ADMIN.SIM_CARDS.DETAILS.FIELDS.SERIAL_NUMBER"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newSimCard().name || \u0275\u0275pipeBind1(16, 17, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 19, "ADMIN.SIM_CARDS.DETAILS.FIELDS.NETWORK"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newSimCard().networkName || \u0275\u0275pipeBind1(23, 21, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(27, 23, "ADMIN.SIM_CARDS.DETAILS.FIELDS.NUMBER"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newSimCard().number || \u0275\u0275pipeBind1(30, 25, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(34, 27, "ADMIN.SIM_CARDS.DETAILS.FIELDS.DESCRIPTION"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newSimCard().description || \u0275\u0275pipeBind1(37, 29, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(42, 33, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(38, _c38, \u0275\u0275pipeBind1(41, 31, "ADMIN.ENTITIES.SIM_CARD"))), " ");
  }
}
var SimCardCreateWizardComponent = class _SimCardCreateWizardComponent {
  simCardDetailsStep = viewChild("simCardDetailsStep", ...ngDevMode ? [{ debugName: "simCardDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  simCardDetailsForm = viewChild("simCardDetailsForm", ...ngDevMode ? [{ debugName: "simCardDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cdr = inject(ChangeDetectorRef);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // SimCard model to collect data across steps
  newSimCard = signal({}, ...ngDevMode ? [{ debugName: "newSimCard" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const simCardDetailsStep = this.simCardDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(simCardDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.SIM_CARDS.DETAILS.TITLE"),
          content: simCardDetailsStep,
          complete: false,
          validate: () => this.simCardDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
      this.cdr.detectChanges();
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newSimCard.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newSimCard.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the simCard state with form data
  updateSimCardDetails(formData) {
    this.newSimCard.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const simCardData = this.newSimCard();
    this.isSubmitting.set(true);
    this.entitiesService.createSimCard(simCardData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "simcard",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating sim card:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newSimCard.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function SimCardCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _SimCardCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _SimCardCreateWizardComponent, selectors: [["app-sim-card-create-wizard"]], viewQuery: function SimCardCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.simCardDetailsStep, _c014, 5)(ctx.confirmationTemplate, _c110, 5)(ctx.simCardDetailsForm, _c29, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["simCardDetailsStep", ""], ["confirmationTemplate", ""], ["simCardDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function SimCardCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function SimCardCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function SimCardCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, SimCardCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, SimCardCreateWizardComponent_ng_template_3_Template, 43, 40, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    SimCardDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SimCardCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-sim-card-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      SimCardDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: SimCard Details -->
<ng-template #simCardDetailsStep let-step>
  <div class="p-fluid">
    <app-sim-card-details-section
      #simCardDetailsForm
      [createMode]="true"
      [entity]="newSimCard()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateSimCardDetails($event)"
    ></app-sim-card-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- SimCard Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate: { entity: ('ADMIN.ENTITIES.SIM_CARD' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.SIM_CARDS.DETAILS.FIELDS.SERIAL_NUMBER' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newSimCard().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.SIM_CARDS.DETAILS.FIELDS.NETWORK' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newSimCard().networkName ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.SIM_CARDS.DETAILS.FIELDS.NUMBER' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newSimCard().number ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.SIM_CARDS.DETAILS.FIELDS.DESCRIPTION' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newSimCard().description ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate: { entity: ('ADMIN.ENTITIES.SIM_CARD' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { simCardDetailsStep: [{ type: ViewChild, args: ["simCardDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], simCardDetailsForm: [{ type: ViewChild, args: ["simCardDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(SimCardCreateWizardComponent, { className: "SimCardCreateWizardComponent", filePath: "src/app/features/administration/components/sim-cards/sim-card-create-wizard/sim-card-create-wizard.component.ts", lineNumber: 46 });
})();

// src/app/features/administration/configs/sim-cards.grid.config.ts
var SimCardsGridConfig = class _SimCardsGridConfig {
  simCardsService = inject(SimCardsDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  dialogRef = null;
  dialogService = inject(DialogService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [
    SimCardListItem.StateEnum.Active,
    SimCardListItem.StateEnum.Inactive,
    SimCardListItem.StateEnum.Suspended
  ];
  filterState = signal({
    state: { value: this.defaultStateValues, operator: "in" }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { clientId: "" };
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  fieldTransformations = {
    device: "device.name"
  };
  entityType = "sim-cards";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.simCardsService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.SIM_CARDS.COLUMNS.SERIAL_NUMBER"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "networkName",
        header: this.translate.instant("ADMIN.SIM_CARDS.COLUMNS.NETWORK"),
        valueGetter: (rowData) => rowData.networkName ?? "",
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "number",
        header: this.translate.instant("ADMIN.SIM_CARDS.COLUMNS.NUMBER"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.SIM_CARDS.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: SimCardListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.INACTIVE"),
              value: SimCardListItem.StateEnum.Inactive
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.SUSPENDED"),
              value: SimCardListItem.StateEnum.Suspended
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: SimCardListItem.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        }
      },
      {
        field: "description",
        header: this.translate.instant("ADMIN.SIM_CARDS.COLUMNS.DESCRIPTION"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "device",
        header: this.translate.instant("ADMIN.SIM_CARDS.COLUMNS.DEVICE"),
        valueGetter: (rowData) => rowData.device?.name ?? "",
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.SIM_CARDS.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData["modifiedDate"]) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData["modifiedDate"]), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: { value: this.defaultStateValues, operator: "in" }
    });
  }
  initialize(params) {
    this.params = params;
  }
  getTitle() {
    return this.translate.instant("ADMIN.SIM_CARDS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.params["clientId"],
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const actualField = this.fieldTransformations[field] || field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    const clientId = this.params["clientId"];
    this.openSimCardCreationDialog(clientId).subscribe((createdSimCard) => {
      if (isDefined(createdSimCard)) {
      }
    });
  }
  openSimCardCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(SimCardCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.SIM_CARD")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function SimCardsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _SimCardsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _SimCardsGridConfig, factory: _SimCardsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SimCardsGridConfig, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/features/administration/components/asset-rating-profiles/asset-rating-profile-create-wizard/asset-rating-profile-create-wizard.component.ts
var _c015 = ["assetRatingProfileDetailsStep"];
var _c111 = ["confirmationTemplate"];
var _c210 = ["assetRatingProfileDetailsForm"];
var _c39 = (a0) => ({ entity: a0 });
function AssetRatingProfileCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-asset-rating-profile-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function AssetRatingProfileCreateWizardComponent_ng_template_1_Template_app_asset_rating_profile_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function AssetRatingProfileCreateWizardComponent_ng_template_1_Template_app_asset_rating_profile_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateAssetRatingProfileDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newAssetRatingProfile())("ownerId", ctx_r1.ownerId);
  }
}
function AssetRatingProfileCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(17, "div", 16)(18, "p", 17);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275pipe(21, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 6, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(18, _c39, \u0275\u0275pipeBind1(7, 4, "ADMIN.ENTITIES.ASSET_RATING_PROFILE"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 9, "ADMIN.ASSET_RATING_PROFILES.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAssetRatingProfile().name || \u0275\u0275pipeBind1(16, 11, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(21, 15, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(20, _c39, \u0275\u0275pipeBind1(20, 13, "ADMIN.ENTITIES.ASSET_RATING_PROFILE"))), " ");
  }
}
var AssetRatingProfileCreateWizardComponent = class _AssetRatingProfileCreateWizardComponent {
  assetRatingProfileDetailsStep = viewChild("assetRatingProfileDetailsStep", ...ngDevMode ? [{ debugName: "assetRatingProfileDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  cdr = inject(ChangeDetectorRef);
  // References to form components
  assetRatingProfileDetailsForm = viewChild("assetRatingProfileDetailsForm", ...ngDevMode ? [{ debugName: "assetRatingProfileDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // AssetRatingProfile model to collect data across steps
  newAssetRatingProfile = signal({}, ...ngDevMode ? [{ debugName: "newAssetRatingProfile" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const detailsStep = this.assetRatingProfileDetailsStep();
      const confirmStep = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (detailsStep && confirmStep) {
        this.steps = [
          {
            value: 1,
            header: this.translate.instant("ADMIN.ASSET_RATING_PROFILES.WIZARD.STEPS.DETAILS"),
            content: detailsStep,
            complete: false,
            validate: () => this.assetRatingProfileDetailsForm()?.validate() ?? false
          },
          {
            value: 2,
            header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
            content: confirmStep,
            complete: true
          }
        ];
        this.cdr.detectChanges();
      }
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newAssetRatingProfile.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newAssetRatingProfile.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the assetRatingProfile state with form data
  updateAssetRatingProfileDetails(formData) {
    this.newAssetRatingProfile.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const assetRatingProfileData = this.newAssetRatingProfile();
    this.isSubmitting.set(true);
    this.entitiesService.createAssetRatingProfile(assetRatingProfileData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "asset-rating-profile",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating asset rating profile:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newAssetRatingProfile.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function AssetRatingProfileCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetRatingProfileCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _AssetRatingProfileCreateWizardComponent, selectors: [["app-asset-rating-profile-create-wizard"]], viewQuery: function AssetRatingProfileCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.assetRatingProfileDetailsStep, _c015, 5)(ctx.confirmationTemplate, _c111, 5)(ctx.assetRatingProfileDetailsForm, _c210, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["assetRatingProfileDetailsStep", ""], ["confirmationTemplate", ""], ["assetRatingProfileDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function AssetRatingProfileCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function AssetRatingProfileCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function AssetRatingProfileCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, AssetRatingProfileCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, AssetRatingProfileCreateWizardComponent_ng_template_3_Template, 22, 22, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    AssetRatingProfileDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetRatingProfileCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-asset-rating-profile-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      AssetRatingProfileDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: AssetRatingProfile Details -->
<ng-template #assetRatingProfileDetailsStep let-step>
  <div class="p-fluid">
    <app-asset-rating-profile-details-section
      #assetRatingProfileDetailsForm
      [createMode]="true"
      [entity]="newAssetRatingProfile()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateAssetRatingProfileDetails($event)"
    ></app-asset-rating-profile-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- AssetRatingProfile Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : {
                      entity:
                        ('ADMIN.ENTITIES.ASSET_RATING_PROFILE' | translate),
                    }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.ASSET_RATING_PROFILES.DETAILS.FIELDS.NAME' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newAssetRatingProfile().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : {
                    entity: ('ADMIN.ENTITIES.ASSET_RATING_PROFILE' | translate),
                  }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { assetRatingProfileDetailsStep: [{ type: ViewChild, args: ["assetRatingProfileDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], assetRatingProfileDetailsForm: [{ type: ViewChild, args: ["assetRatingProfileDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(AssetRatingProfileCreateWizardComponent, { className: "AssetRatingProfileCreateWizardComponent", filePath: "src/app/features/administration/components/asset-rating-profiles/asset-rating-profile-create-wizard/asset-rating-profile-create-wizard.component.ts", lineNumber: 46 });
})();

// src/app/features/administration/configs/asset-rating-profiles.grid.config.ts
var AssetRatingProfilesGridConfig = class _AssetRatingProfilesGridConfig {
  assetRatingProfilesService = inject(AssetRatingProfilesDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [
    AssetRatingProfileListItem.StateEnum.Active
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { clientId: "" };
  ownerId = "";
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "asset-rating-profiles";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.assetRatingProfilesService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.ASSET_RATING_PROFILES.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.ASSET_RATING_PROFILES.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: AssetRatingProfileListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: AssetRatingProfileListItem.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        }
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.ASSET_RATING_PROFILES.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.ASSET_RATING_PROFILES.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.openAssetRatingProfileCreationDialog(this.ownerId).subscribe();
  }
  openAssetRatingProfileCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(AssetRatingProfileCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.ASSET_RATING_PROFILE")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function AssetRatingProfilesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetRatingProfilesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AssetRatingProfilesGridConfig, factory: _AssetRatingProfilesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetRatingProfilesGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/asset-state-profiles/asset-state-profile-create-wizard/asset-state-profile-create-wizard.component.ts
var _c016 = ["assetStateProfileDetailsStep"];
var _c112 = ["confirmationTemplate"];
var _c211 = ["assetStateProfileDetailsForm"];
var _c310 = (a0) => ({ entity: a0 });
function AssetStateProfileCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-asset-state-profile-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function AssetStateProfileCreateWizardComponent_ng_template_1_Template_app_asset_state_profile_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function AssetStateProfileCreateWizardComponent_ng_template_1_Template_app_asset_state_profile_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateAssetStateProfileDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newAssetStateProfile())("ownerId", ctx_r1.ownerId);
  }
}
function AssetStateProfileCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(17, "div", 16)(18, "p", 17);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275pipe(21, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 6, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(18, _c310, \u0275\u0275pipeBind1(7, 4, "ADMIN.ENTITIES.ASSET_STATE_PROFILE"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 9, "ADMIN.ASSET_STATE_PROFILES.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAssetStateProfile().name || \u0275\u0275pipeBind1(16, 11, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(21, 15, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(20, _c310, \u0275\u0275pipeBind1(20, 13, "ADMIN.ENTITIES.ASSET_STATE_PROFILE"))), " ");
  }
}
var AssetStateProfileCreateWizardComponent = class _AssetStateProfileCreateWizardComponent {
  assetStateProfileDetailsStep = viewChild("assetStateProfileDetailsStep", ...ngDevMode ? [{ debugName: "assetStateProfileDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  cdr = inject(ChangeDetectorRef);
  // References to form components
  assetStateProfileDetailsForm = viewChild("assetStateProfileDetailsForm", ...ngDevMode ? [{ debugName: "assetStateProfileDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  apiSchema = inject(ApiSchemaService);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // AssetStateProfile model to collect data across steps
  newAssetStateProfile = signal({}, ...ngDevMode ? [{ debugName: "newAssetStateProfile" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const detailsStep = this.assetStateProfileDetailsStep();
      const confirmStep = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (detailsStep && confirmStep) {
        this.steps = [
          {
            value: 1,
            header: this.translate.instant("ADMIN.ASSET_STATE_PROFILES.WIZARD.STEPS.DETAILS"),
            content: detailsStep,
            complete: false,
            validate: () => this.assetStateProfileDetailsForm()?.validate() ?? false
          },
          {
            value: 2,
            header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
            content: confirmStep,
            complete: true
          }
        ];
        this.cdr.detectChanges();
      }
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newAssetStateProfile.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newAssetStateProfile.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the assetStateProfile state with form data
  updateAssetStateProfileDetails(formData) {
    this.newAssetStateProfile.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const assetStateProfileData = this.newAssetStateProfile();
    if (this.apiSchema.usesModernSchema()) {
      assetStateProfileData.state = "active";
    }
    this.isSubmitting.set(true);
    this.entitiesService.createAssetStateProfile(assetStateProfileData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "asset-state-profile",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating asset state profile:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newAssetStateProfile.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function AssetStateProfileCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetStateProfileCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _AssetStateProfileCreateWizardComponent, selectors: [["app-asset-state-profile-create-wizard"]], viewQuery: function AssetStateProfileCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.assetStateProfileDetailsStep, _c016, 5)(ctx.confirmationTemplate, _c112, 5)(ctx.assetStateProfileDetailsForm, _c211, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["assetStateProfileDetailsStep", ""], ["confirmationTemplate", ""], ["assetStateProfileDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function AssetStateProfileCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function AssetStateProfileCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function AssetStateProfileCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, AssetStateProfileCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, AssetStateProfileCreateWizardComponent_ng_template_3_Template, 22, 22, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    AssetStateProfileDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetStateProfileCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-asset-state-profile-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      AssetStateProfileDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: AssetStateProfile Details -->
<ng-template #assetStateProfileDetailsStep let-step>
  <div class="p-fluid">
    <app-asset-state-profile-details-section
      #assetStateProfileDetailsForm
      [createMode]="true"
      [entity]="newAssetStateProfile()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateAssetStateProfileDetails($event)"
    ></app-asset-state-profile-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- AssetStateProfile Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : {
                      entity:
                        ('ADMIN.ENTITIES.ASSET_STATE_PROFILE' | translate),
                    }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.ASSET_STATE_PROFILES.DETAILS.FIELDS.NAME' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newAssetStateProfile().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.ASSET_STATE_PROFILE' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { assetStateProfileDetailsStep: [{ type: ViewChild, args: ["assetStateProfileDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], assetStateProfileDetailsForm: [{ type: ViewChild, args: ["assetStateProfileDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(AssetStateProfileCreateWizardComponent, { className: "AssetStateProfileCreateWizardComponent", filePath: "src/app/features/administration/components/asset-state-profiles/asset-state-profile-create-wizard/asset-state-profile-create-wizard.component.ts", lineNumber: 47 });
})();

// src/app/features/administration/configs/asset-state-profiles.grid.config.ts
var AssetStateProfilesGridConfig = class _AssetStateProfilesGridConfig {
  assetStateProfilesService = inject(AssetStateProfilesDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [
    AssetStateProfileListItem.StateEnum.Active
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { clientId: "" };
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "asset-state-profiles";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  ownerId = "";
  dataService = this.assetStateProfilesService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.ASSET_STATE_PROFILES.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.ASSET_STATE_PROFILES.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: AssetStateProfileListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: AssetStateProfileListItem.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        }
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.ASSET_STATE_PROFILES.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.ASSET_STATE_PROFILES.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.openAssetStateProfileCreationDialog(this.ownerId).subscribe();
  }
  openAssetStateProfileCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(AssetStateProfileCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.ASSET_STATE_PROFILE")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function AssetStateProfilesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetStateProfilesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AssetStateProfilesGridConfig, factory: _AssetStateProfilesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetStateProfilesGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/io-types/io-type-create-wizard/io-type-create-wizard.component.ts
var _c017 = ["ioTypeDetailsStep"];
var _c113 = ["confirmationTemplate"];
var _c212 = ["ioTypeDetailsForm"];
var _c311 = (a0) => ({ entity: a0 });
function IoTypeCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-io-type-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function IoTypeCreateWizardComponent_ng_template_1_Template_app_io_type_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function IoTypeCreateWizardComponent_ng_template_1_Template_app_io_type_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateIoTypeDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newIoType())("ownerId", ctx_r1.ownerId);
  }
}
function IoTypeCreateWizardComponent_ng_template_3_Conditional_17_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 13)(1, "div", 14);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 18);
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(3, 2, "ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.decamelize(ctx_r1.newIoType().type), " ");
  }
}
function IoTypeCreateWizardComponent_ng_template_3_Conditional_18_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 13)(1, "div", 14);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 15);
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(3, 2, "ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.UNIT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(ctx_r1.newIoType().unit);
  }
}
function IoTypeCreateWizardComponent_ng_template_3_Conditional_19_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 13)(1, "div", 14);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 15);
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(3, 2, "ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.SMOOTHING_TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.decamelize(ctx_r1.newIoType().smoothingType), " ");
  }
}
function IoTypeCreateWizardComponent_ng_template_3_Conditional_20_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 13)(1, "div", 14);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 15);
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_6_0;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(3, 2, "ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.ACTIVE_TEXT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate((tmp_6_0 = ctx_r1.newIoType().text) == null ? null : tmp_6_0.active);
  }
}
function IoTypeCreateWizardComponent_ng_template_3_Conditional_21_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 13)(1, "div", 14);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 15);
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_6_0;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(3, 2, "ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.INACTIVE_TEXT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate((tmp_6_0 = ctx_r1.newIoType().text) == null ? null : tmp_6_0.inactive);
  }
}
function IoTypeCreateWizardComponent_ng_template_3_Conditional_22_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 13)(1, "div", 14);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 15);
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_6_0;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(3, 2, "ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.RATE_TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate((tmp_6_0 = ctx_r1.newIoType().rate) == null ? null : tmp_6_0.type);
  }
}
function IoTypeCreateWizardComponent_ng_template_3_Conditional_23_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 13)(1, "div", 14);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 15);
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_6_0;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(3, 2, "ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.RATE_MULTIPLIER"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate((tmp_6_0 = ctx_r1.newIoType().rate) == null ? null : tmp_6_0.multiplier);
  }
}
function IoTypeCreateWizardComponent_ng_template_3_Conditional_24_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 13)(1, "div", 14);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 15);
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_6_0;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(3, 2, "ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.RATE_UNIT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate((tmp_6_0 = ctx_r1.newIoType().rate) == null ? null : tmp_6_0.unit);
  }
}
function IoTypeCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275conditionalCreate(17, IoTypeCreateWizardComponent_ng_template_3_Conditional_17_Template, 6, 4, "div", 13);
    \u0275\u0275conditionalCreate(18, IoTypeCreateWizardComponent_ng_template_3_Conditional_18_Template, 6, 4, "div", 13);
    \u0275\u0275conditionalCreate(19, IoTypeCreateWizardComponent_ng_template_3_Conditional_19_Template, 6, 4, "div", 13);
    \u0275\u0275conditionalCreate(20, IoTypeCreateWizardComponent_ng_template_3_Conditional_20_Template, 6, 4, "div", 13);
    \u0275\u0275conditionalCreate(21, IoTypeCreateWizardComponent_ng_template_3_Conditional_21_Template, 6, 4, "div", 13);
    \u0275\u0275conditionalCreate(22, IoTypeCreateWizardComponent_ng_template_3_Conditional_22_Template, 6, 4, "div", 13);
    \u0275\u0275conditionalCreate(23, IoTypeCreateWizardComponent_ng_template_3_Conditional_23_Template, 6, 4, "div", 13);
    \u0275\u0275conditionalCreate(24, IoTypeCreateWizardComponent_ng_template_3_Conditional_24_Template, 6, 4, "div", 13);
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(25, "div", 16)(26, "p", 17);
    \u0275\u0275text(27);
    \u0275\u0275pipe(28, "translate");
    \u0275\u0275pipe(29, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_10_0;
    let tmp_11_0;
    let tmp_12_0;
    let tmp_13_0;
    let tmp_14_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 14, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(26, _c311, \u0275\u0275pipeBind1(7, 12, "ADMIN.ENTITIES.IO_TYPE"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 17, "ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newIoType().name || \u0275\u0275pipeBind1(16, 19, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.newIoType().type ? 17 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.newIoType().unit ? 18 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.newIoType().smoothingType ? 19 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(((tmp_10_0 = ctx_r1.newIoType().text) == null ? null : tmp_10_0.active) ? 20 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(((tmp_11_0 = ctx_r1.newIoType().text) == null ? null : tmp_11_0.inactive) ? 21 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(((tmp_12_0 = ctx_r1.newIoType().rate) == null ? null : tmp_12_0.type) ? 22 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(((tmp_13_0 = ctx_r1.newIoType().rate) == null ? null : tmp_13_0.multiplier) ? 23 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(((tmp_14_0 = ctx_r1.newIoType().rate) == null ? null : tmp_14_0.unit) ? 24 : -1);
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(29, 23, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(28, _c311, \u0275\u0275pipeBind1(28, 21, "ADMIN.ENTITIES.IO_TYPE"))), " ");
  }
}
var IoTypeCreateWizardComponent = class _IoTypeCreateWizardComponent {
  ioTypeDetailsStep = viewChild("ioTypeDetailsStep", ...ngDevMode ? [{ debugName: "ioTypeDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  ioTypeDetailsForm = viewChild("ioTypeDetailsForm", ...ngDevMode ? [{ debugName: "ioTypeDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  utilityService = inject(UtilityService);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // IoType model to collect data across steps
  newIoType = signal({}, ...ngDevMode ? [{ debugName: "newIoType" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const ioTypeDetailsStep = this.ioTypeDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(ioTypeDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.DEVICES.IO_TYPES.DETAILS.TITLE"),
          content: ioTypeDetailsStep,
          complete: false,
          validate: () => this.ioTypeDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newIoType.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newIoType.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the ioType state with form data
  updateIoTypeDetails(formData) {
    this.newIoType.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const ioTypeData = this.newIoType();
    this.isSubmitting.set(true);
    this.entitiesService.createIoType(ioTypeData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "iotype",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating IO type:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newIoType.set({});
    this.dialogRef.close();
  }
  decamelize(str) {
    return this.utilityService.decamelize(str);
  }
  static \u0275fac = function IoTypeCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _IoTypeCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _IoTypeCreateWizardComponent, selectors: [["app-io-type-create-wizard"]], viewQuery: function IoTypeCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.ioTypeDetailsStep, _c017, 5)(ctx.confirmationTemplate, _c113, 5)(ctx.ioTypeDetailsForm, _c212, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["ioTypeDetailsStep", ""], ["confirmationTemplate", ""], ["ioTypeDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"], [1, "w-2/3", "capitalize"]], template: function IoTypeCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function IoTypeCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function IoTypeCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, IoTypeCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, IoTypeCreateWizardComponent_ng_template_3_Template, 30, 30, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    IoTypeDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(IoTypeCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-io-type-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      IoTypeDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: IoType Details -->
<ng-template #ioTypeDetailsStep let-step>
  <div class="p-fluid">
    <app-io-type-details-section
      #ioTypeDetailsForm
      [createMode]="true"
      [entity]="newIoType()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateIoTypeDetails($event)"
    ></app-io-type-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- IoType Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate: { entity: ('ADMIN.ENTITIES.IO_TYPE' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newIoType().name || ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            @if (newIoType().type) {
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{
                    'ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.TYPE' | translate
                  }}:
                </div>
                <div class="w-2/3 capitalize">
                  {{ decamelize(newIoType().type) }}
                </div>
              </div>
            }

            @if (newIoType().unit) {
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{
                    'ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.UNIT' | translate
                  }}:
                </div>
                <div class="w-2/3">{{ newIoType().unit }}</div>
              </div>
            }
            @if (newIoType().smoothingType) {
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{
                    'ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.SMOOTHING_TYPE'
                      | translate
                  }}:
                </div>
                <div class="w-2/3">
                  {{ decamelize(newIoType().smoothingType) }}
                </div>
              </div>
            }

            @if (newIoType().text?.active) {
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{
                    'ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.ACTIVE_TEXT'
                      | translate
                  }}:
                </div>
                <div class="w-2/3">{{ newIoType().text?.active }}</div>
              </div>
            }

            @if (newIoType().text?.inactive) {
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{
                    'ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.INACTIVE_TEXT'
                      | translate
                  }}:
                </div>
                <div class="w-2/3">{{ newIoType().text?.inactive }}</div>
              </div>
            }

            @if (newIoType().rate?.type) {
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{
                    'ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.RATE_TYPE'
                      | translate
                  }}:
                </div>
                <div class="w-2/3">{{ newIoType().rate?.type }}</div>
              </div>
            }
            @if (newIoType().rate?.multiplier) {
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{
                    'ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.RATE_MULTIPLIER'
                      | translate
                  }}:
                </div>
                <div class="w-2/3">{{ newIoType().rate?.multiplier }}</div>
              </div>
            }

            @if (newIoType().rate?.unit) {
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{
                    'ADMIN.DEVICES.IO_TYPES.DETAILS.FIELDS.RATE_UNIT'
                      | translate
                  }}:
                </div>
                <div class="w-2/3">{{ newIoType().rate?.unit }}</div>
              </div>
            }
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate: { entity: ('ADMIN.ENTITIES.IO_TYPE' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { ioTypeDetailsStep: [{ type: ViewChild, args: ["ioTypeDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], ioTypeDetailsForm: [{ type: ViewChild, args: ["ioTypeDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(IoTypeCreateWizardComponent, { className: "IoTypeCreateWizardComponent", filePath: "src/app/features/administration/components/io-types/io-type-create-wizard/io-type-create-wizard.component.ts", lineNumber: 46 });
})();

// src/app/features/administration/configs/io-types.grid.config.ts
var IoTypesGridConfig = class _IoTypesGridConfig {
  ioTypesService = inject(IoTypesDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  apiSchemaService = inject(ApiSchemaService);
  dialogRef = null;
  dialogService = inject(DialogService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [IoTypeListItem.StateEnum.Active];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { ownerId: "" };
  ownerId = "";
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  typeTranslationKeys = {
    analog_input: "ADMIN.DEVICES.IO_TYPES.TYPES.ANALOG_INPUT",
    camera_input: "ADMIN.DEVICES.IO_TYPES.TYPES.CAMERA_INPUT",
    digital_input: "ADMIN.DEVICES.IO_TYPES.TYPES.DIGITAL_INPUT",
    digital_output: "ADMIN.DEVICES.IO_TYPES.TYPES.DIGITAL_OUTPUT",
    can_input: "ADMIN.DEVICES.IO_TYPES.TYPES.CAN_INPUT",
    counter_input: "ADMIN.DEVICES.IO_TYPES.TYPES.COUNTER_INPUT",
    temperature_input: "ADMIN.DEVICES.IO_TYPES.TYPES.TEMPERATURE_INPUT",
    value_input: "ADMIN.DEVICES.IO_TYPES.TYPES.VALUE_INPUT"
  };
  entityType = "io-types";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.ioTypesService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.DEVICES.IO_TYPES.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "type",
        header: this.translate.instant("ADMIN.DEVICES.IO_TYPES.COLUMNS.TYPE"),
        valueGetter: (rowData) => {
          const typeValue = this.apiSchemaService.getEntityType(rowData, "iotype");
          return isDefined(typeValue) && isString(typeValue) ? this.translate.instant(this.typeTranslationKeys[typeValue]) : "";
        },
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("ADMIN.DEVICES.IO_TYPES.TYPES.DIGITAL_INPUT"),
              value: IoTypeListItem.TypeEnum.DigitalInput
            },
            {
              label: this.translate.instant("ADMIN.DEVICES.IO_TYPES.TYPES.DIGITAL_OUTPUT"),
              value: IoTypeListItem.TypeEnum.DigitalOutput
            },
            {
              label: this.translate.instant("ADMIN.DEVICES.IO_TYPES.TYPES.ANALOG_INPUT"),
              value: IoTypeListItem.TypeEnum.AnalogInput
            },
            {
              label: this.translate.instant("ADMIN.DEVICES.IO_TYPES.TYPES.TEMPERATURE_INPUT"),
              value: IoTypeListItem.TypeEnum.TemperatureInput
            },
            {
              label: this.translate.instant("ADMIN.DEVICES.IO_TYPES.TYPES.CAN_INPUT"),
              value: IoTypeListItem.TypeEnum.CanInput
            },
            {
              label: this.translate.instant("ADMIN.DEVICES.IO_TYPES.TYPES.COUNTER_INPUT"),
              value: IoTypeListItem.TypeEnum.CounterInput
            },
            {
              label: this.translate.instant("ADMIN.DEVICES.IO_TYPES.TYPES.CAMERA_INPUT"),
              value: IoTypeListItem.TypeEnum.CameraInput
            },
            {
              label: this.translate.instant("ADMIN.DEVICES.IO_TYPES.TYPES.VALUE_INPUT"),
              value: IoTypeListItem.TypeEnum.ValueInput
            }
          ]
        }
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.DEVICES.IO_TYPES.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: IoTypeListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: IoTypeListItem.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        },
        transform: "capitalize"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.DEVICES.IO_TYPES.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.DEVICES.IO_TYPES.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      const rawField = String(event.sortField);
      const apiField = rawField === "type" ? this.apiSchemaService.getTypePropertyKey("iotype") : rawField;
      sortField = `${apiField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.openIoTypeCreationDialog(this.ownerId).subscribe();
  }
  openIoTypeCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(IoTypeCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.IO_TYPE")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function IoTypesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _IoTypesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _IoTypesGridConfig, factory: _IoTypesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(IoTypesGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/overspeed-profiles/overspeed-profile-create-wizard/overspeed-profile-create-wizard.component.ts
var _c018 = ["overspeedProfileDetailsStep"];
var _c114 = ["confirmationTemplate"];
var _c213 = ["overspeedProfileDetailsForm"];
var _c312 = (a0) => ({ entity: a0 });
function OverspeedProfileCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-overspeed-profile-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function OverspeedProfileCreateWizardComponent_ng_template_1_Template_app_overspeed_profile_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function OverspeedProfileCreateWizardComponent_ng_template_1_Template_app_overspeed_profile_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateOverspeedProfileDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newOverspeedProfile())("ownerId", ctx_r1.ownerId);
  }
}
function OverspeedProfileCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(17, "div", 16)(18, "p", 17);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275pipe(21, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 6, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(18, _c312, \u0275\u0275pipeBind1(7, 4, "ADMIN.ENTITIES.OVERSPEED_PROFILE"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 9, "ADMIN.PROFILES.OVERSPEED.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newOverspeedProfile().name || \u0275\u0275pipeBind1(16, 11, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(21, 15, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(20, _c312, \u0275\u0275pipeBind1(20, 13, "ADMIN.ENTITIES.OVERSPEED_PROFILE"))), " ");
  }
}
var OverspeedProfileCreateWizardComponent = class _OverspeedProfileCreateWizardComponent {
  overspeedProfileDetailsStep = viewChild("overspeedProfileDetailsStep", ...ngDevMode ? [{ debugName: "overspeedProfileDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  overspeedProfileDetailsForm = viewChild("overspeedProfileDetailsForm", ...ngDevMode ? [{ debugName: "overspeedProfileDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // OverspeedProfile model to collect data across steps
  newOverspeedProfile = signal({}, ...ngDevMode ? [{ debugName: "newOverspeedProfile" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const overspeedProfileDetailsStep = this.overspeedProfileDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(overspeedProfileDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.PROFILES.OVERSPEED.WIZARD.STEPS.DETAILS"),
          content: overspeedProfileDetailsStep,
          complete: false,
          validate: () => this.overspeedProfileDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newOverspeedProfile.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newOverspeedProfile.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the overspeedProfile state with form data
  updateOverspeedProfileDetails(formData) {
    this.newOverspeedProfile.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const overspeedProfileData = this.newOverspeedProfile();
    this.isSubmitting.set(true);
    this.entitiesService.createOverspeedProfile(overspeedProfileData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "overspeed-profile",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating overspeed profile:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newOverspeedProfile.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function OverspeedProfileCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _OverspeedProfileCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _OverspeedProfileCreateWizardComponent, selectors: [["app-overspeed-profile-create-wizard"]], viewQuery: function OverspeedProfileCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.overspeedProfileDetailsStep, _c018, 5)(ctx.confirmationTemplate, _c114, 5)(ctx.overspeedProfileDetailsForm, _c213, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["overspeedProfileDetailsStep", ""], ["confirmationTemplate", ""], ["overspeedProfileDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function OverspeedProfileCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function OverspeedProfileCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function OverspeedProfileCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, OverspeedProfileCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, OverspeedProfileCreateWizardComponent_ng_template_3_Template, 22, 22, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    OverspeedProfileDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OverspeedProfileCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-overspeed-profile-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      OverspeedProfileDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: OverspeedProfile Details -->
<ng-template #overspeedProfileDetailsStep let-step>
  <div class="p-fluid">
    <app-overspeed-profile-details-section
      #overspeedProfileDetailsForm
      [createMode]="true"
      [entity]="newOverspeedProfile()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateOverspeedProfileDetails($event)"
    ></app-overspeed-profile-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- OverspeedProfile Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.OVERSPEED_PROFILE' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.PROFILES.OVERSPEED.DETAILS.FIELDS.NAME' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newOverspeedProfile().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.OVERSPEED_PROFILE' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { overspeedProfileDetailsStep: [{ type: ViewChild, args: ["overspeedProfileDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], overspeedProfileDetailsForm: [{ type: ViewChild, args: ["overspeedProfileDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(OverspeedProfileCreateWizardComponent, { className: "OverspeedProfileCreateWizardComponent", filePath: "src/app/features/administration/components/overspeed-profiles/overspeed-profile-create-wizard/overspeed-profile-create-wizard.component.ts", lineNumber: 45 });
})();

// src/app/features/administration/configs/overspeed-profiles.grid.config.ts
var OverspeedProfilesGridConfig = class _OverspeedProfilesGridConfig {
  overspeedProfilesService = inject(OverspeedProfilesDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [
    OverspeedProfileListItem.StateEnum.Active
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { ownerId: "" };
  ownerId = "";
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "overspeed-profiles";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.overspeedProfilesService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.PROFILES.OVERSPEED.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.PROFILES.OVERSPEED.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: OverspeedProfileListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: OverspeedProfileListItem.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        }
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.PROFILES.OVERSPEED.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.PROFILES.OVERSPEED.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.openOverspeedProfileCreationDialog(this.ownerId).subscribe();
  }
  openOverspeedProfileCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(OverspeedProfileCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.OVERSPEED_PROFILE")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function OverspeedProfilesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _OverspeedProfilesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _OverspeedProfilesGridConfig, factory: _OverspeedProfilesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OverspeedProfilesGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/road-profiles/road-profile-create-wizard/road-profile-create-wizard.component.ts
var _c019 = ["roadProfileDetailsStep"];
var _c115 = ["confirmationTemplate"];
var _c214 = ["roadProfileDetailsForm"];
var _c313 = (a0) => ({ entity: a0 });
function RoadProfileCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-road-profile-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function RoadProfileCreateWizardComponent_ng_template_1_Template_app_road_profile_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function RoadProfileCreateWizardComponent_ng_template_1_Template_app_road_profile_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateRoadProfileDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newRoadProfile())("ownerId", ctx_r1.ownerId);
  }
}
function RoadProfileCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(17, "div", 16)(18, "p", 17);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275pipe(21, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 6, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(18, _c313, \u0275\u0275pipeBind1(7, 4, "ADMIN.ENTITIES.ROAD_PROFILE"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 9, "ADMIN.PROFILES.ROAD.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newRoadProfile().name || \u0275\u0275pipeBind1(16, 11, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(21, 15, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(20, _c313, \u0275\u0275pipeBind1(20, 13, "ADMIN.ENTITIES.ROAD_PROFILE"))), " ");
  }
}
var RoadProfileCreateWizardComponent = class _RoadProfileCreateWizardComponent {
  roadProfileDetailsStep = viewChild("roadProfileDetailsStep", ...ngDevMode ? [{ debugName: "roadProfileDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  roadProfileDetailsForm = viewChild("roadProfileDetailsForm", ...ngDevMode ? [{ debugName: "roadProfileDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // RoadProfile model to collect data across steps
  newRoadProfile = signal({}, ...ngDevMode ? [{ debugName: "newRoadProfile" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const roadProfileDetailsStep = this.roadProfileDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(roadProfileDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.PROFILES.ROAD.WIZARD.STEPS.DETAILS"),
          content: roadProfileDetailsStep,
          complete: false,
          validate: () => this.roadProfileDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newRoadProfile.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newRoadProfile.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the roadProfile state with form data
  updateRoadProfileDetails(formData) {
    this.newRoadProfile.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const roadProfileData = this.newRoadProfile();
    this.isSubmitting.set(true);
    this.entitiesService.createRoadProfile(roadProfileData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "road-profile",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating road profile:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newRoadProfile.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function RoadProfileCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _RoadProfileCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _RoadProfileCreateWizardComponent, selectors: [["app-road-profile-create-wizard"]], viewQuery: function RoadProfileCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.roadProfileDetailsStep, _c019, 5)(ctx.confirmationTemplate, _c115, 5)(ctx.roadProfileDetailsForm, _c214, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["roadProfileDetailsStep", ""], ["confirmationTemplate", ""], ["roadProfileDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function RoadProfileCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function RoadProfileCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function RoadProfileCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, RoadProfileCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, RoadProfileCreateWizardComponent_ng_template_3_Template, 22, 22, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    RoadProfileDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RoadProfileCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-road-profile-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      RoadProfileDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: RoadProfile Details -->
<ng-template #roadProfileDetailsStep let-step>
  <div class="p-fluid">
    <app-road-profile-details-section
      #roadProfileDetailsForm
      [createMode]="true"
      [entity]="newRoadProfile()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateRoadProfileDetails($event)"
    ></app-road-profile-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- RoadProfile Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.ROAD_PROFILE' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.PROFILES.ROAD.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newRoadProfile().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.ROAD_PROFILE' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { roadProfileDetailsStep: [{ type: ViewChild, args: ["roadProfileDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], roadProfileDetailsForm: [{ type: ViewChild, args: ["roadProfileDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(RoadProfileCreateWizardComponent, { className: "RoadProfileCreateWizardComponent", filePath: "src/app/features/administration/components/road-profiles/road-profile-create-wizard/road-profile-create-wizard.component.ts", lineNumber: 45 });
})();

// src/app/features/administration/configs/road-profiles.grid.config.ts
var RoadProfilesGridConfig = class _RoadProfilesGridConfig {
  roadProfilesService = inject(RoadProfilesDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [RoadProfileListItem.StateEnum.Active];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { ownerId: "" };
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "road-profiles";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.roadProfilesService;
  ownerId = "";
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.PROFILES.ROAD.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.PROFILES.ROAD.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: RoadProfileListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: RoadProfileListItem.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        }
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.PROFILES.ROAD.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    } else if (isDefined(this.params["distributorId"])) {
      this.ownerId = String(this.params["distributorId"]);
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.PROFILES.ROAD.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.openRoadProfileCreationDialog(this.ownerId).subscribe();
  }
  openRoadProfileCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(RoadProfileCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.ROAD_PROFILE")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function RoadProfilesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _RoadProfilesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _RoadProfilesGridConfig, factory: _RoadProfilesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RoadProfilesGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/sms-gateway-providers/sms-gateway-provider-create-wizard/sms-gateway-provider-create-wizard.component.ts
var _c020 = ["smsGatewayProviderDetailsStep"];
var _c116 = ["smsGatewayProviderParametersStep"];
var _c215 = ["confirmationTemplate"];
var _c314 = ["smsGatewayProviderDetailsForm"];
var _c46 = ["smsGatewayProviderParametersForm"];
var _c56 = (a0) => ({ entity: a0 });
function SmsGatewayProviderCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-sms-gateway-provider-details-section", 7, 3);
    \u0275\u0275listener("formValidityChange", function SmsGatewayProviderCreateWizardComponent_ng_template_1_Template_app_sms_gateway_provider_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function SmsGatewayProviderCreateWizardComponent_ng_template_1_Template_app_sms_gateway_provider_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateSmsGatewayProviderDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newSmsGatewayProvider())("ownerId", ctx_r1.ownerId);
  }
}
function SmsGatewayProviderCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-sms-gateway-provider-parameters-section", 8, 4);
    \u0275\u0275listener("formValidityChange", function SmsGatewayProviderCreateWizardComponent_ng_template_3_Template_app_sms_gateway_provider_parameters_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onParametersFormValidityChange($event));
    })("formValueChange", function SmsGatewayProviderCreateWizardComponent_ng_template_3_Template_app_sms_gateway_provider_parameters_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateSmsGatewayProviderParameters($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newSmsGatewayProvider())("smsGatewayProviderType", ctx_r1.smsGatewayProviderType());
  }
}
function SmsGatewayProviderCreateWizardComponent_ng_template_5_For_24_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 16)(1, "div", 17);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 18);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const item_r4 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", ctx_r1.getParametersTitle(item_r4.key), ": ");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", item_r4.value, " ");
  }
}
function SmsGatewayProviderCreateWizardComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 9);
    \u0275\u0275element(1, "span", 10);
    \u0275\u0275elementStart(2, "div", 11)(3, "div", 12)(4, "div", 13)(5, "p", 14);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 15)(10, "div", 16)(11, "div", 17);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 18);
    \u0275\u0275text(15);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(16, "div", 16)(17, "div", 17);
    \u0275\u0275text(18);
    \u0275\u0275pipe(19, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(20, "div", 18);
    \u0275\u0275text(21);
    \u0275\u0275pipe(22, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275repeaterCreate(23, SmsGatewayProviderCreateWizardComponent_ng_template_5_For_24_Template, 5, 2, "div", 16, \u0275\u0275repeaterTrackByIndex);
    \u0275\u0275pipe(25, "keyvalue");
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(26, "div", 19)(27, "p", 20);
    \u0275\u0275text(28);
    \u0275\u0275pipe(29, "translate");
    \u0275\u0275pipe(30, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_7_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 8, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(24, _c56, \u0275\u0275pipeBind1(7, 6, "ADMIN.ENTITIES.SMS_GATEWAY_PROVIDER"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 11, "ADMIN.SMS_GATEWAY_PROVIDERS.DETAILS.FIELDS.TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", (tmp_7_0 = ctx_r1.newSmsGatewayProvider()[ctx_r1.typePropertyKey]) == null ? null : tmp_7_0.name, " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(19, 13, "ADMIN.SMS_GATEWAY_PROVIDERS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newSmsGatewayProvider().name || \u0275\u0275pipeBind1(22, 15, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(2);
    \u0275\u0275repeater(\u0275\u0275pipeBind1(25, 17, ctx_r1.newSmsGatewayProvider().parameters));
    \u0275\u0275advance(5);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(30, 21, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(26, _c56, \u0275\u0275pipeBind1(29, 19, "ADMIN.ENTITIES.SMS_GATEWAY_PROVIDER"))), " ");
  }
}
var SmsGatewayProviderCreateWizardComponent = class _SmsGatewayProviderCreateWizardComponent {
  smsGatewayProviderDetailsStep = viewChild("smsGatewayProviderDetailsStep", ...ngDevMode ? [{ debugName: "smsGatewayProviderDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  smsGatewayProviderParametersStep = viewChild("smsGatewayProviderParametersStep", ...ngDevMode ? [{ debugName: "smsGatewayProviderParametersStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  smsGatewayProviderDetailsForm = viewChild("smsGatewayProviderDetailsForm", ...ngDevMode ? [{ debugName: "smsGatewayProviderDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  smsGatewayProviderParametersForm = viewChild("smsGatewayProviderParametersForm", ...ngDevMode ? [{ debugName: "smsGatewayProviderParametersForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  cacheService = inject(RequestCacheService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  apiSchemaService = inject(ApiSchemaService);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  typePropertyKey = this.apiSchemaService.getTypePropertyKey("smsgatewayprovider");
  // SmsGatewayProvider model to collect data across steps
  newSmsGatewayProvider = signal({}, ...ngDevMode ? [{ debugName: "newSmsGatewayProvider" }] : (
    /* istanbul ignore next */
    []
  ));
  smsGatewayProviderParametersFields = signal(null, ...ngDevMode ? [{ debugName: "smsGatewayProviderParametersFields" }] : (
    /* istanbul ignore next */
    []
  ));
  smsGatewayProviderType = signal(null, ...ngDevMode ? [{ debugName: "smsGatewayProviderType" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const smsGatewayProviderDetailsStep = this.smsGatewayProviderDetailsStep();
      const smsGatewayProviderParametersStep = this.smsGatewayProviderParametersStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(smsGatewayProviderDetailsStep) || isNullOrUndefined(smsGatewayProviderParametersStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.SMS_GATEWAY_PROVIDERS.DETAILS.TITLE"),
          content: smsGatewayProviderDetailsStep,
          complete: false,
          validate: () => this.smsGatewayProviderDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("ADMIN.SMS_GATEWAY_PROVIDERS.PARAMETERS.TITLE"),
          content: smsGatewayProviderParametersStep,
          complete: false,
          validate: () => this.smsGatewayProviderParametersForm()?.validate() ?? false
        },
        {
          value: 3,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newSmsGatewayProvider.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
    this.steps = [
      {
        value: 1,
        header: this.translate.instant("ADMIN.SMS_GATEWAY_PROVIDERS.DETAILS.TITLE"),
        complete: false,
        content: null
      },
      {
        value: 2,
        header: this.translate.instant("ADMIN.SMS_GATEWAY_PROVIDERS.PARAMETERS.TITLE"),
        content: null,
        complete: false
      },
      {
        value: 3,
        header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
        complete: true,
        content: null
      }
    ];
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  onParametersFormValidityChange(isValid) {
    this.steps[1].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  updateSmsGatewayProviderDetails(formData) {
    this.newSmsGatewayProvider.update((current) => {
      const _a = formData, { ownerId: _ownerId } = _a, rest = __objRest(_a, ["ownerId"]);
      const updated = __spreadValues(__spreadValues({}, current), rest);
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
    this.loadSmsGatewayProviderType(this.newSmsGatewayProvider()[this.typePropertyKey]?.id).catch(() => {
    });
  }
  updateSmsGatewayProviderParameters(formData) {
    this.newSmsGatewayProvider.update((current) => {
      const updated = __spreadValues(__spreadValues({}, current), { parameters: formData });
      return updated;
    });
  }
  onWizardComplete() {
    const smsGatewayProviderData = this.newSmsGatewayProvider();
    if (isNullOrUndefined(smsGatewayProviderData) || Object.keys(smsGatewayProviderData).length === 0) {
      return;
    }
    this.isSubmitting.set(true);
    this.entitiesService.createSmsGatewayProvider(smsGatewayProviderData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "sms-gateway-provider",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newSmsGatewayProvider.set({});
    this.dialogRef.close();
  }
  async loadSmsGatewayProviderType(smsGatewayProviderTypeId) {
    if (isNullOrUndefined(smsGatewayProviderTypeId))
      return;
    const response = await this.cacheService.getOrFetchAsync(`smsGatewayProviderType-${smsGatewayProviderTypeId}`, () => firstValueFrom(this.entitiesService.getSmsGatewayProviderType(smsGatewayProviderTypeId)), 3e5);
    this.smsGatewayProviderType.set(response);
    const params = JSON.parse(response.parameterDefinition ?? '{"groups":[]}');
    const values = this.extractParameterFields(params);
    this.smsGatewayProviderParametersFields.set(values);
  }
  extractParameterFields(params) {
    const values = [];
    for (const group of params.groups) {
      for (const field of group.fields) {
        values.push({
          key: field.id,
          title: field.title
        });
        if (field.type === "combo" && field.values) {
          this.processComboFieldValues(field.values, values);
        }
      }
    }
    return values;
  }
  processComboFieldValues(options, values) {
    for (const option of options) {
      if (isDefined(option.fields)) {
        this.processOptionFields(option.fields, values);
      }
    }
  }
  processOptionFields(fields, values) {
    for (const subField of fields) {
      values.push({
        key: subField.id,
        title: subField.title
      });
    }
  }
  getParametersTitle(key) {
    return this.smsGatewayProviderParametersFields()?.find((field) => field.key === key)?.title ?? "";
  }
  static \u0275fac = function SmsGatewayProviderCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _SmsGatewayProviderCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _SmsGatewayProviderCreateWizardComponent, selectors: [["app-sms-gateway-provider-create-wizard"]], viewQuery: function SmsGatewayProviderCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.smsGatewayProviderDetailsStep, _c020, 5)(ctx.smsGatewayProviderParametersStep, _c116, 5)(ctx.confirmationTemplate, _c215, 5)(ctx.smsGatewayProviderDetailsForm, _c314, 5)(ctx.smsGatewayProviderParametersForm, _c46, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(5);
    }
  }, decls: 7, vars: 5, consts: [["smsGatewayProviderDetailsStep", ""], ["smsGatewayProviderParametersStep", ""], ["confirmationTemplate", ""], ["smsGatewayProviderDetailsForm", ""], ["smsGatewayProviderParametersForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "loading", "useWizardButtonsOnly"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "smsGatewayProviderType"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function SmsGatewayProviderCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 5);
      \u0275\u0275listener("complete", function SmsGatewayProviderCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function SmsGatewayProviderCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, SmsGatewayProviderCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, SmsGatewayProviderCreateWizardComponent_ng_template_3_Template, 3, 3, "ng-template", null, 1, \u0275\u0275templateRefExtractor)(5, SmsGatewayProviderCreateWizardComponent_ng_template_5_Template, 31, 28, "ng-template", null, 2, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("loading", ctx.isSubmitting())("useWizardButtonsOnly", true);
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    SmsGatewayProviderDetailsSectionComponent,
    SmsGatewayProviderParametersSectionComponent,
    KeyValuePipe,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SmsGatewayProviderCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-sms-gateway-provider-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      SmsGatewayProviderDetailsSectionComponent,
      SmsGatewayProviderParametersSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [loading]="isSubmitting()"
  [useWizardButtonsOnly]="true"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Sms Gateway Provider Details -->
<ng-template #smsGatewayProviderDetailsStep let-step>
  <div class="p-fluid">
    <app-sms-gateway-provider-details-section
      #smsGatewayProviderDetailsForm
      [createMode]="true"
      [entity]="newSmsGatewayProvider()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateSmsGatewayProviderDetails($event)"
    ></app-sms-gateway-provider-details-section>
  </div>
</ng-template>

<ng-template #smsGatewayProviderParametersStep let-step>
  <div class="p-fluid">
    <app-sms-gateway-provider-parameters-section
      #smsGatewayProviderParametersForm
      [createMode]="true"
      [entity]="newSmsGatewayProvider()"
      [smsGatewayProviderType]="smsGatewayProviderType()"
      (formValidityChange)="onParametersFormValidityChange($event)"
      (formValueChange)="updateSmsGatewayProviderParameters($event)"
    ></app-sms-gateway-provider-parameters-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Sms Gateway Provider Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : {
                      entity:
                        ('ADMIN.ENTITIES.SMS_GATEWAY_PROVIDER' | translate),
                    }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.SMS_GATEWAY_PROVIDERS.DETAILS.FIELDS.TYPE' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{ newSmsGatewayProvider()[typePropertyKey]?.name }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.SMS_GATEWAY_PROVIDERS.DETAILS.FIELDS.NAME' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newSmsGatewayProvider().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            @for (
              item of newSmsGatewayProvider().parameters | keyvalue;
              track $index
            ) {
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{ getParametersTitle(item.key) }}:
                </div>
                <div class="w-2/3">
                  {{ item.value }}
                </div>
              </div>
            }
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : {
                    entity: ('ADMIN.ENTITIES.SMS_GATEWAY_PROVIDER' | translate),
                  }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { smsGatewayProviderDetailsStep: [{ type: ViewChild, args: ["smsGatewayProviderDetailsStep", { isSignal: true }] }], smsGatewayProviderParametersStep: [{ type: ViewChild, args: ["smsGatewayProviderParametersStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], smsGatewayProviderDetailsForm: [{ type: ViewChild, args: ["smsGatewayProviderDetailsForm", { isSignal: true }] }], smsGatewayProviderParametersForm: [{ type: ViewChild, args: ["smsGatewayProviderParametersForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(SmsGatewayProviderCreateWizardComponent, { className: "SmsGatewayProviderCreateWizardComponent", filePath: "src/app/features/administration/components/sms-gateway-providers/sms-gateway-provider-create-wizard/sms-gateway-provider-create-wizard.component.ts", lineNumber: 51 });
})();

// src/app/features/administration/configs/sms-gateway-provider.grid.config.ts
var SmsGatewayProvidersGridConfig = class _SmsGatewayProvidersGridConfig {
  smsGatewayProvidersService = inject(SmsGatewayProvidersDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  entitiesService = inject(EntitiesService);
  dialogRef = null;
  dialogService = inject(DialogService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  apiSchemaService = inject(ApiSchemaService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [
    SmsGatewayProviderListItem.StateEnum.Active,
    SmsGatewayProviderListItem.StateEnum.Suspended,
    SmsGatewayProviderListItem.StateEnum.Deleted
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { ownerId: "" };
  destroyRef = inject(DestroyRef);
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  fieldTransformations = {
    type: "type.id"
  };
  entityType = "sms-gateway-providers";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  ownerId = "";
  dataService = this.smsGatewayProvidersService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.SMS_GATEWAY_PROVIDERS.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "type",
        header: this.translate.instant("ADMIN.SMS_GATEWAY_PROVIDERS.COLUMNS.TYPE"),
        sortable: true,
        filterType: "multiselect",
        filter: true,
        filterMatchMode: "in",
        valueGetter: (rowData) => {
          const typeValue = this.apiSchemaService.getEntityType(rowData, "smsgatewayprovider");
          return isDefined(typeValue?.name) ? typeValue?.name : "";
        },
        filterConfig: {
          type: "multiselect",
          options: []
        }
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.SMS_GATEWAY_PROVIDERS.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: SmsGatewayProviderListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.SUSPENDED"),
              value: SmsGatewayProviderListItem.StateEnum.Suspended
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: SmsGatewayProviderListItem.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        }
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.SMS_GATEWAY_PROVIDERS.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    } else if (isDefined(this.params["distributorId"])) {
      this.ownerId = String(this.params["distributorId"]);
    }
    this.columns = this.buildColumns();
    this.loadFilterOptions();
  }
  getTitle() {
    return this.translate.instant("ADMIN.SMS_GATEWAY_PROVIDERS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      const rawField = String(event.sortField);
      const apiField = rawField === "type" ? this.apiSchemaService.getTypePropertyKey("smsgatewayprovider") : rawField;
      sortField = `${apiField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const actualField = this.fieldTransformations[field] || field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  loadFilterOptions() {
    this.entitiesService.listSmsGatewayProviderTypes(this.ownerId).pipe(takeUntilDestroyed(this.destroyRef), catchError(() => {
      return EMPTY;
    })).subscribe((smsGatewayProviderTypes) => {
      const typeColumn = this.columns.find((col) => col.field === "type");
      if (typeColumn?.filterConfig) {
        typeColumn.filterConfig.options = smsGatewayProviderTypes.items.map((type) => ({
          label: type.name ?? "",
          value: type.id
        })).sort((a, b) => a.label.localeCompare(b.label));
      }
    });
  }
  onAdd() {
    this.openSmsGatewayProviderCreationDialog(this.ownerId).pipe(takeUntilDestroyed(this.destroyRef)).subscribe();
  }
  openSmsGatewayProviderCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(SmsGatewayProviderCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.SMS_GATEWAY_PROVIDER")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function SmsGatewayProvidersGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _SmsGatewayProvidersGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _SmsGatewayProvidersGridConfig, factory: _SmsGatewayProvidersGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SmsGatewayProvidersGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/themes/theme-create-wizard/theme-create-wizard.component.ts
var _c021 = ["themeDetailsStep"];
var _c117 = ["confirmationTemplate"];
var _c216 = ["themeDetailsForm"];
var _c315 = (a0) => ({ entity: a0 });
function ThemeCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-theme-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function ThemeCreateWizardComponent_ng_template_1_Template_app_theme_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function ThemeCreateWizardComponent_ng_template_1_Template_app_theme_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateThemeDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newTheme())("ownerId", ctx_r1.ownerId);
  }
}
function ThemeCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(24, "div", 13)(25, "div", 14);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "div", 15);
    \u0275\u0275text(29);
    \u0275\u0275pipe(30, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(31, "div", 16)(32, "p", 17);
    \u0275\u0275text(33);
    \u0275\u0275pipe(34, "translate");
    \u0275\u0275pipe(35, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_10_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 10, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(30, _c315, \u0275\u0275pipeBind1(7, 8, "ADMIN.ENTITIES.THEME"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 13, "ADMIN.THEMES.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newTheme().name || \u0275\u0275pipeBind1(16, 15, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 17, "ADMIN.THEMES.DETAILS.FIELDS.DESCRIPTION"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newTheme().description || \u0275\u0275pipeBind1(23, 19, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(27, 21, "ADMIN.THEMES.DETAILS.FIELDS.CSS_SRC"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_10_0 = ctx_r1.newTheme().settings) == null ? null : tmp_10_0["cssSrc"]) || \u0275\u0275pipeBind1(30, 23, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(35, 27, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(32, _c315, \u0275\u0275pipeBind1(34, 25, "ADMIN.ENTITIES.THEME"))), " ");
  }
}
var ThemeCreateWizardComponent = class _ThemeCreateWizardComponent {
  themeDetailsStep = viewChild("themeDetailsStep", ...ngDevMode ? [{ debugName: "themeDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  themeDetailsForm = viewChild("themeDetailsForm", ...ngDevMode ? [{ debugName: "themeDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cdr = inject(ChangeDetectorRef);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // Theme model to collect data across steps
  newTheme = signal({}, ...ngDevMode ? [{ debugName: "newTheme" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  steps = [];
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const themeDetailsStep = this.themeDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(themeDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.THEMES.DETAILS.TITLE"),
          content: themeDetailsStep,
          complete: false,
          validate: () => this.themeDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newTheme.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.newTheme.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the theme state with form data
  updateThemeDetails(formData) {
    this.newTheme.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const themeData = this.newTheme();
    this.isSubmitting.set(true);
    this.entitiesService.createTheme(__spreadProps(__spreadValues({}, themeData), { theme: "default" })).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "theme",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating theme:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newTheme.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function ThemeCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ThemeCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _ThemeCreateWizardComponent, selectors: [["app-theme-create-wizard"]], viewQuery: function ThemeCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.themeDetailsStep, _c021, 5)(ctx.confirmationTemplate, _c117, 5)(ctx.themeDetailsForm, _c216, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["themeDetailsStep", ""], ["confirmationTemplate", ""], ["themeDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function ThemeCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function ThemeCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function ThemeCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, ThemeCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, ThemeCreateWizardComponent_ng_template_3_Template, 36, 34, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    ThemeDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ThemeCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-theme-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      ThemeDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Theme Details -->
<ng-template #themeDetailsStep let-step>
  <div class="p-fluid">
    <app-theme-details-section
      #themeDetailsForm
      [createMode]="true"
      [entity]="newTheme()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateThemeDetails($event)"
    ></app-theme-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Theme Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate: { entity: ('ADMIN.ENTITIES.THEME' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.THEMES.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newTheme().name || ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.THEMES.DETAILS.FIELDS.DESCRIPTION' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newTheme().description ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.THEMES.DETAILS.FIELDS.CSS_SRC' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newTheme().settings?.['cssSrc'] ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate: { entity: ('ADMIN.ENTITIES.THEME' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { themeDetailsStep: [{ type: ViewChild, args: ["themeDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], themeDetailsForm: [{ type: ViewChild, args: ["themeDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(ThemeCreateWizardComponent, { className: "ThemeCreateWizardComponent", filePath: "src/app/features/administration/components/themes/theme-create-wizard/theme-create-wizard.component.ts", lineNumber: 46 });
})();

// src/app/features/administration/configs/themes.grid.config.ts
var ThemesGridConfig = class _ThemesGridConfig {
  themesService = inject(ThemesDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [ThemeResponse.StateEnum.Active];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { clientId: "" };
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  fieldTransformations = {};
  baseThemes = Object.entries(getThemeDictionary()).map((key) => ({
    value: key[0],
    label: key[1].name ?? ""
  }));
  entityType = "themes";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.themesService;
  ownerId = "";
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.THEMES.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "description",
        header: this.translate.instant("ADMIN.THEMES.COLUMNS.DESCRIPTION"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "theme",
        header: this.translate.instant("ADMIN.THEMES.COLUMNS.BASE_THEME"),
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: this.baseThemes
        },
        valueGetter: (rowData) => isDefined(rowData["theme"]) ? this.baseThemes.find((theme) => theme.value === rowData["theme"])?.label ?? "" : ""
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.THEMES.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData["state"]) ? this.translate.instant(`SHARED.COMMON.STATUS.${String(rowData["state"]).toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: ThemeResponse.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: ThemeResponse.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        },
        transform: "capitalize"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.THEMES.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData["modifiedDate"]) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData["modifiedDate"]), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    } else if (isDefined(this.params["distributorId"])) {
      this.ownerId = String(this.params["distributorId"]);
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.THEMES.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const actualField = this.fieldTransformations[field] || field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    const clientId = this.ownerId;
    this.openThemeCreationDialog(clientId).subscribe();
  }
  openThemeCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(ThemeCreateWizardComponent, {
      header: "Theme Wizard",
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function ThemesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ThemesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _ThemesGridConfig, factory: _ThemesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ThemesGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/device-config-profiles/device-config-profile-create-wizard/device-config-profile-create-wizard.component.ts
var _c022 = ["deviceConfigProfileDetailsStep"];
var _c118 = ["confirmationTemplate"];
var _c217 = ["deviceConfigProfileDetailsForm"];
var _c316 = (a0) => ({ entity: a0 });
function DeviceConfigProfileCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-device-config-profile-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function DeviceConfigProfileCreateWizardComponent_ng_template_1_Template_app_device_config_profile_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function DeviceConfigProfileCreateWizardComponent_ng_template_1_Template_app_device_config_profile_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateDeviceConfigProfileDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newDeviceConfigProfile())("ownerId", ctx_r1.ownerId);
  }
}
function DeviceConfigProfileCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(23, "div", 16)(24, "p", 17);
    \u0275\u0275text(25);
    \u0275\u0275pipe(26, "translate");
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_8_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 8, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(22, _c316, \u0275\u0275pipeBind1(7, 6, "ADMIN.ENTITIES.DEVICE_CONFIG_PROFILE"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 11, "ADMIN.DEVICES.CONFIG_PROFILES.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newDeviceConfigProfile().name || \u0275\u0275pipeBind1(16, 13, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 15, "ADMIN.DEVICES.CONFIG_PROFILES.DETAILS.FIELDS.DEVICE_TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", (tmp_8_0 = ctx_r1.newDeviceConfigProfile().deviceType) == null ? null : tmp_8_0.name, " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(27, 19, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(24, _c316, \u0275\u0275pipeBind1(26, 17, "ADMIN.ENTITIES.DEVICE_CONFIG_PROFILE"))), " ");
  }
}
var DeviceConfigProfileCreateWizardComponent = class _DeviceConfigProfileCreateWizardComponent {
  deviceConfigProfileDetailsStep = viewChild("deviceConfigProfileDetailsStep", ...ngDevMode ? [{ debugName: "deviceConfigProfileDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  deviceConfigProfileDetailsForm = viewChild("deviceConfigProfileDetailsForm", ...ngDevMode ? [{ debugName: "deviceConfigProfileDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // DeviceConfigProfile model to collect data across steps
  newDeviceConfigProfile = signal({}, ...ngDevMode ? [{ debugName: "newDeviceConfigProfile" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const deviceConfigProfileDetailsStep = this.deviceConfigProfileDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(deviceConfigProfileDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.DEVICES.CONFIG_PROFILES.DETAILS.TITLE"),
          content: deviceConfigProfileDetailsStep,
          complete: false,
          validate: () => this.deviceConfigProfileDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newDeviceConfigProfile.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newDeviceConfigProfile.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  updateDeviceConfigProfileDetails(formData) {
    this.newDeviceConfigProfile.update((current) => {
      const _a = formData, { ownerId: _ownerId } = _a, rest = __objRest(_a, ["ownerId"]);
      const updated = __spreadValues(__spreadValues({}, current), rest);
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const deviceConfigProfileData = this.newDeviceConfigProfile();
    if (isNullOrUndefined(deviceConfigProfileData) || Object.keys(deviceConfigProfileData).length === 0) {
      return;
    }
    this.isSubmitting.set(true);
    this.entitiesService.createDeviceConfigProfile(deviceConfigProfileData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "device-config-profile",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newDeviceConfigProfile.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function DeviceConfigProfileCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DeviceConfigProfileCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _DeviceConfigProfileCreateWizardComponent, selectors: [["app-device-config-profile-create-wizard"]], viewQuery: function DeviceConfigProfileCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.deviceConfigProfileDetailsStep, _c022, 5)(ctx.confirmationTemplate, _c118, 5)(ctx.deviceConfigProfileDetailsForm, _c217, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["deviceConfigProfileDetailsStep", ""], ["confirmationTemplate", ""], ["deviceConfigProfileDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function DeviceConfigProfileCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function DeviceConfigProfileCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function DeviceConfigProfileCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, DeviceConfigProfileCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, DeviceConfigProfileCreateWizardComponent_ng_template_3_Template, 28, 26, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    DeviceConfigProfileDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DeviceConfigProfileCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-device-config-profile-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      DeviceConfigProfileDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Device Config Profile Details -->
<ng-template #deviceConfigProfileDetailsStep let-step>
  <div class="p-fluid">
    <app-device-config-profile-details-section
      #deviceConfigProfileDetailsForm
      [createMode]="true"
      [entity]="newDeviceConfigProfile()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateDeviceConfigProfileDetails($event)"
    ></app-device-config-profile-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- DeviceConfigProfile Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : {
                      entity:
                        ('ADMIN.ENTITIES.DEVICE_CONFIG_PROFILE' | translate),
                    }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.DEVICES.CONFIG_PROFILES.DETAILS.FIELDS.NAME'
                    | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newDeviceConfigProfile().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.DEVICES.CONFIG_PROFILES.DETAILS.FIELDS.DEVICE_TYPE'
                    | translate
                }}:
              </div>
              <div class="w-2/3">
                {{ newDeviceConfigProfile().deviceType?.name }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : {
                    entity:
                      ('ADMIN.ENTITIES.DEVICE_CONFIG_PROFILE' | translate),
                  }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { deviceConfigProfileDetailsStep: [{ type: ViewChild, args: ["deviceConfigProfileDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], deviceConfigProfileDetailsForm: [{ type: ViewChild, args: ["deviceConfigProfileDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(DeviceConfigProfileCreateWizardComponent, { className: "DeviceConfigProfileCreateWizardComponent", filePath: "src/app/features/administration/components/device-config-profiles/device-config-profile-create-wizard/device-config-profile-create-wizard.component.ts", lineNumber: 45 });
})();

// src/app/features/administration/configs/device-config-profiles.config.ts
var DeviceConfigProfilesGridConfig = class _DeviceConfigProfilesGridConfig {
  deviceConfigProfilesService = inject(DeviceConfigProfilesDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  entitiesService = inject(EntitiesService);
  adminStateService = inject(AdminStateService);
  adminTreeService = inject(AdminTreeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  defaultStateValues = [
    DeviceConfigProfileListItem.StateEnum.Active
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { ownerId: "" };
  ownerId = "";
  destroyRef = inject(DestroyRef);
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  fieldTransformations = {
    type: "deviceType.id"
  };
  stateLabels = {
    active: "Active",
    deleted: "Deleted"
  };
  entityType = "device-config-profiles";
  columns = [
    {
      field: "name",
      header: "Name",
      filter: true,
      filterType: "text",
      filterMatchMode: "contains"
    },
    {
      field: "type",
      header: "Type",
      sortable: true,
      filterType: "multiselect",
      filter: true,
      filterMatchMode: "in",
      valueGetter: (rowData) => {
        return rowData.deviceType?.name ?? "";
      },
      filterConfig: {
        type: "multiselect",
        options: []
      }
    },
    {
      field: "state",
      header: "State",
      valueGetter: (rowData) => isDefined(rowData.state) ? this.stateLabels[rowData.state] : "",
      filter: true,
      filterType: "multiselect",
      filterMatchMode: "in",
      filterConfig: {
        type: "multiselect",
        options: [
          {
            label: "Active",
            value: DeviceConfigProfileListItem.StateEnum.Active
          },
          {
            label: "Deleted",
            value: DeviceConfigProfileListItem.StateEnum.Deleted
          }
        ],
        defaultSelected: this.defaultStateValues
      }
    },
    {
      field: "modifiedDate",
      header: "Modified",
      filter: false,
      valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData["modifiedDate"]), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
    }
  ];
  dataService = this.deviceConfigProfilesService;
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    }
    this.loadFilterOptions().catch(() => {
    });
  }
  getTitle() {
    return "Device Config Profiles";
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      const rawField = String(event.sortField);
      const apiField = rawField === "type" ? "deviceType" : rawField;
      sortField = `${apiField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const actualField = this.fieldTransformations[field] || field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.openDeviceConfigProfileCreationDialog(this.ownerId).pipe(takeUntilDestroyed(this.destroyRef)).subscribe((createdDeviceConfigProfile) => {
      if (isDefined(createdDeviceConfigProfile)) {
      }
    });
  }
  openDeviceConfigProfileCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(DeviceConfigProfileCreateWizardComponent, {
      header: "Device Config Profile Wizard",
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  async loadFilterOptions() {
    try {
      const deviceTypesResponse = await firstValueFrom(this.entitiesService.listDeviceTypes(this.ownerId).pipe(catchError(() => {
        console.error("Failed to load device types");
        return EMPTY;
      })));
      if (isNullOrUndefined(deviceTypesResponse) || isEmpty(deviceTypesResponse.items)) {
        console.warn("No device types found for client");
        return;
      }
      const deviceTypes = deviceTypesResponse.items;
      let allowedRootDeviceTypeIds = await this.getAvailableRootDeviceTypeIdsForClient();
      if (isEmpty(allowedRootDeviceTypeIds)) {
        allowedRootDeviceTypeIds = await this.getAvailableRootDeviceTypeIdsForVendor();
      }
      const typeColumn = this.columns.find((col) => col.field === "type");
      if (typeColumn?.filterConfig) {
        const exceptionDeviceTypeIds = [];
        const filteredOptions = this.filterAllowedDeviceTypes(this.ownerId, deviceTypes, allowedRootDeviceTypeIds, exceptionDeviceTypeIds);
        typeColumn.filterConfig.options = filteredOptions.map((option) => ({
          label: option.value,
          value: option.key
        }));
      }
    } catch (error) {
      console.error("Error loading device type filter options:", error);
    }
  }
  /**
   * Gets the available root device type IDs for the current client
   * @returns Promise resolving to an array of device type IDs
   */
  async getAvailableRootDeviceTypeIdsForClient() {
    if (isNullOrUndefined(this.params["clientId"])) {
      return [];
    }
    const client = this.adminStateService.getClientById(this.params["clientId"]);
    if (client && isDefined(client.availableDeviceTypes)) {
      return client.availableDeviceTypes.map((deviceType) => deviceType.id);
    }
    try {
      const fetchedClient = await this.adminTreeService.tryLoadClient(this.params["clientId"]);
      if (fetchedClient?.availableDeviceTypes) {
        return fetchedClient.availableDeviceTypes.map((deviceType) => deviceType.id);
      }
    } catch (error) {
      console.error("Error loading client device types:", error);
    }
    return [];
  }
  /**
   * Gets the available root device type IDs for a selected vendor
   * @returns Promise resolving to an array of device type IDs
   */
  async getAvailableRootDeviceTypeIdsForVendor() {
    const clientId = this.params["clientId"];
    let client = null;
    let vendorId = null;
    if (isDefined(clientId)) {
      client = this.adminStateService.getClientById(clientId) ?? await this.adminTreeService.tryLoadClient(clientId);
      vendorId = client ? client.owner.id : null;
    } else {
      vendorId = this.params["vendorId"];
    }
    if (isNullOrUndefined(vendorId)) {
      return [];
    }
    const vendor = this.adminStateService.getVendorById(vendorId);
    if (vendor && isDefined(vendor.availableDeviceTypes)) {
      return vendor.availableDeviceTypes.map((deviceType) => deviceType.id);
    }
    try {
      const fetchedVendor = await this.adminTreeService.fetchClientVendor(vendorId);
      if (fetchedVendor && isDefined(fetchedVendor.availableDeviceTypes)) {
        return fetchedVendor.availableDeviceTypes.map((deviceType) => deviceType.id);
      }
    } catch (error) {
      console.error("Error loading vendor device types:", error);
    }
    return [];
  }
  /**
   * Filters device types based on allowed types and exceptions,
   * and formats them for dropdown selection
   *
   * @param ownerId - ID of the owner (client/vendor)
   * @param deviceTypes - List of device types to filter
   * @param allowedTypes - List of allowed root device type IDs
   * @param exceptions - Device type IDs that are always allowed
   * @returns Array of formatted key-value pairs for dropdown options
   */
  filterAllowedDeviceTypes(ownerId, deviceTypes, allowedTypes, exceptions) {
    return deviceTypes.filter((item) => {
      if (exceptions.includes(item.id)) {
        return true;
      }
      return item.state === "active" && isDefined(item.parent) && (item.owner.id === ownerId || allowedTypes.length === 0 || allowedTypes.includes(item.parent.id));
    }).map((item) => {
      const parentName = deviceTypes.find((x) => x.id === item?.parent?.id)?.name ?? "Unknown";
      return {
        key: item.id,
        value: `${parentName} - ${item.name}`
      };
    }).sort((a, b) => a.value.localeCompare(b.value));
  }
  static \u0275fac = function DeviceConfigProfilesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DeviceConfigProfilesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _DeviceConfigProfilesGridConfig, factory: _DeviceConfigProfilesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DeviceConfigProfilesGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/custom-fields/custom-field-create-wizard/custom-field-create-wizard.component.ts
var _c023 = ["customFieldDetailsStep"];
var _c119 = ["confirmationTemplate"];
var _c218 = ["customFieldDetailsForm"];
var _c317 = (a0) => ({ entity: a0 });
function CustomFieldCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-custom-field-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function CustomFieldCreateWizardComponent_ng_template_1_Template_app_custom_field_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function CustomFieldCreateWizardComponent_ng_template_1_Template_app_custom_field_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateCustomFieldDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newCustomField());
  }
}
function CustomFieldCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(16, "div", 13)(17, "div", 14);
    \u0275\u0275text(18);
    \u0275\u0275pipe(19, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(20, "div", 15);
    \u0275\u0275text(21);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(22, "div", 13)(23, "div", 14);
    \u0275\u0275text(24);
    \u0275\u0275pipe(25, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(26, "div", 15);
    \u0275\u0275text(27);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(28, "div", 13)(29, "div", 14);
    \u0275\u0275text(30);
    \u0275\u0275pipe(31, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(32, "div", 15);
    \u0275\u0275text(33);
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(34, "div", 16)(35, "p", 17);
    \u0275\u0275text(36);
    \u0275\u0275pipe(37, "translate");
    \u0275\u0275pipe(38, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_10_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 12, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(28, _c317, \u0275\u0275pipeBind1(7, 10, "ADMIN.ENTITIES.CUSTOM_FIELD"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 15, "ADMIN.PROFILES.CUSTOM_FIELDS.DETAILS.FIELDS.ID"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newCustomField()["tag"], " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(19, 17, "ADMIN.PROFILES.CUSTOM_FIELDS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newCustomField().title, " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(25, 19, "ADMIN.PROFILES.CUSTOM_FIELDS.DETAILS.FIELDS.ENTITY"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", (tmp_10_0 = ctx_r1.newCustomField()["entityType"]) == null ? null : tmp_10_0["name"], " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(31, 21, "ADMIN.PROFILES.CUSTOM_FIELDS.DETAILS.FIELDS.TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newCustomField().type, " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(38, 25, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(30, _c317, \u0275\u0275pipeBind1(37, 23, "ADMIN.ENTITIES.CUSTOM_FIELD"))), " ");
  }
}
var CustomFieldCreateWizardComponent = class _CustomFieldCreateWizardComponent {
  customFieldDetailsStep = viewChild("customFieldDetailsStep", ...ngDevMode ? [{ debugName: "customFieldDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  customFieldDetailsForm = viewChild("customFieldDetailsForm", ...ngDevMode ? [{ debugName: "customFieldDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  accountsService = inject(AccountsService);
  broadcastService = inject(BroadcastEventMessageService);
  customFieldsAdapter = inject(CustomFieldsDataAdapter);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cdr = inject(ChangeDetectorRef);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // CustomField model to collect data across steps
  newCustomField = signal({}, ...ngDevMode ? [{ debugName: "newCustomField" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  loading = signal(false, ...ngDevMode ? [{ debugName: "loading" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const detailsStep = this.customFieldDetailsStep();
      const confirmStep = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (detailsStep && confirmStep) {
        this.steps = [
          {
            value: 1,
            header: this.translate.instant("ADMIN.PROFILES.CUSTOM_FIELDS.DETAILS.TITLE"),
            content: detailsStep,
            complete: false,
            validate: () => this.customFieldDetailsForm()?.validate() ?? false
          },
          {
            value: 2,
            header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
            content: confirmStep,
            complete: true
          }
        ];
        this.cdr.detectChanges();
      }
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newCustomField.update((current) => __spreadValues({}, current));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newCustomField.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the customField state with form data
  updateCustomFieldDetails(formData) {
    this.newCustomField.update((current) => {
      const updated = __spreadValues(__spreadValues({}, current), formData);
      return updated;
    });
  }
  onWizardComplete() {
    this.loading.set(true);
    firstValueFrom(this.accountsService.getCompany(this.ownerId ?? "")).then((company) => {
      const customFieldData = {
        id: this.newCustomField()["tag"] ?? "",
        title: this.newCustomField()["title"] ?? "",
        type: this.newCustomField()["type"] ?? "",
        required: this.newCustomField()["required"] ?? false,
        values: (this.newCustomField()["values"] ?? [])?.map((value) => ({
          key: value.key ?? "",
          value: value.value ?? ""
        })),
        owner: company.type
      };
      customFieldData.required = !!(customFieldData.required ?? false);
      customFieldData.values = customFieldData.values ?? [];
      const customFields = JSON.parse(JSON.stringify(company.customFields));
      const entityTypeId = this.newCustomField()["entityType"]?.["id"] ?? "";
      const bucket = customFields[entityTypeId] ?? [];
      bucket.push(customFieldData);
      customFields[entityTypeId] = bucket;
      function getDuplicateFieldCount(fields, id) {
        let count = 0;
        for (const arr of Object.values(fields)) {
          if (Array.isArray(arr)) {
            for (const x of arr) {
              if (x.id === id)
                count++;
            }
          }
        }
        return count;
      }
      const duplicateCount = getDuplicateFieldCount(customFields, customFieldData.id);
      if (duplicateCount > 1) {
        throw new Error("A custom field with the same ID already exists");
      }
      const updateDetails$ = company.type === "client" ? this.accountsService.updateClientDetails(this.ownerId ?? "", { customFields }, "body") : this.accountsService.updateVendorDetails(this.ownerId ?? "", { customFields }, "body");
      return updateDetails$.pipe(takeUntilDestroyed(this.destroyRef)).subscribe({
        next: (response) => {
          this.customFieldsAdapter.invalidateCache(this.ownerId ?? "");
          this.broadcastService.broadcast("entity-updated", {
            entityType: "custom-field",
            operation: "create",
            entityId: response.id
          });
          this.loading.set(false);
          this.dialogRef.close(response);
        },
        error: (error) => {
          this.loading.set(false);
          console.error("Error creating custom field:", error);
        }
      });
    }).catch(() => {
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newCustomField.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function CustomFieldCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CustomFieldCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _CustomFieldCreateWizardComponent, selectors: [["app-custom-field-create-wizard"]], viewQuery: function CustomFieldCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.customFieldDetailsStep, _c023, 5)(ctx.confirmationTemplate, _c119, 5)(ctx.customFieldDetailsForm, _c218, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["customFieldDetailsStep", ""], ["confirmationTemplate", ""], ["customFieldDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function CustomFieldCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function CustomFieldCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function CustomFieldCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, CustomFieldCreateWizardComponent_ng_template_1_Template, 3, 2, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, CustomFieldCreateWizardComponent_ng_template_3_Template, 39, 32, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.loading());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    CustomFieldDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CustomFieldCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-custom-field-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      CustomFieldDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="loading()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: CustomField Details -->
<ng-template #customFieldDetailsStep let-step>
  <div class="p-fluid">
    <app-custom-field-details-section
      #customFieldDetailsForm
      [createMode]="true"
      [entity]="newCustomField()"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateCustomFieldDetails($event)"
    ></app-custom-field-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- CustomField Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.CUSTOM_FIELD' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.PROFILES.CUSTOM_FIELDS.DETAILS.FIELDS.ID' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{ newCustomField()['tag'] }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.PROFILES.CUSTOM_FIELDS.DETAILS.FIELDS.NAME'
                    | translate
                }}:
              </div>
              <div class="w-2/3">
                {{ newCustomField().title }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.PROFILES.CUSTOM_FIELDS.DETAILS.FIELDS.ENTITY'
                    | translate
                }}:
              </div>
              <div class="w-2/3">
                {{ newCustomField()['entityType']?.['name'] }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.PROFILES.CUSTOM_FIELDS.DETAILS.FIELDS.TYPE'
                    | translate
                }}:
              </div>
              <div class="w-2/3">
                {{ newCustomField().type }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.CUSTOM_FIELD' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { customFieldDetailsStep: [{ type: ViewChild, args: ["customFieldDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], customFieldDetailsForm: [{ type: ViewChild, args: ["customFieldDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(CustomFieldCreateWizardComponent, { className: "CustomFieldCreateWizardComponent", filePath: "src/app/features/administration/components/custom-fields/custom-field-create-wizard/custom-field-create-wizard.component.ts", lineNumber: 47 });
})();

// src/app/features/administration/configs/custom-fields.grid.config.ts
var CustomFieldsGridConfig = class _CustomFieldsGridConfig {
  customFieldsService = inject(CustomFieldsDataAdapter);
  assetTypes = this.customFieldsService.assetTypes;
  types = this.customFieldsService.types;
  dialogRef = null;
  dialogService = inject(DialogService);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  params = {};
  filterState = signal({}, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "custom-fields";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.customFieldsService;
  ownerId = "";
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.PROFILES.CUSTOM_FIELDS.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "entityType",
        header: this.translate.instant("ADMIN.PROFILES.CUSTOM_FIELDS.COLUMNS.ENTITY"),
        valueGetter: (rowData) => isDefined(rowData?._entityType?.name) ? rowData._entityType.name : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: this.assetTypes().map((type) => ({
            label: type.name ?? "",
            value: type.id ?? ""
          }))
        }
      },
      {
        field: "type",
        header: this.translate.instant("ADMIN.PROFILES.CUSTOM_FIELDS.COLUMNS.TYPE"),
        valueGetter: (rowData) => isDefined(rowData?.type?.name) ? rowData.type.name : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: this.types().map((type) => ({
            label: type.name ?? "",
            value: type.id ?? ""
          }))
        }
      }
    ];
  }
  clearFilters() {
    this.filterState.set({});
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.PROFILES.CUSTOM_FIELDS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const column = this.columns.find((col) => col.field === field);
    const filterFieldMap = {
      entityType: "_entityType.id",
      type: "type.id"
    };
    const filterField = filterFieldMap[field] ?? field;
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), filterField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    const clientId = this.ownerId;
    this.openCustomFieldCreationDialog(clientId).subscribe((createdCustomField) => {
      if (isDefined(createdCustomField)) {
      }
    });
  }
  openCustomFieldCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(CustomFieldCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.CUSTOM_FIELD")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function CustomFieldsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CustomFieldsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _CustomFieldsGridConfig, factory: _CustomFieldsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CustomFieldsGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/user-roles/user-role-create-wizard/user-role-create-wizard.component.ts
var _c024 = ["userRoleDetailsStep"];
var _c120 = ["confirmationTemplate"];
var _c219 = ["userRoleDetailsForm"];
var _c318 = (a0) => ({ entity: a0 });
function UserRoleCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-user-role-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function UserRoleCreateWizardComponent_ng_template_1_Template_app_user_role_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function UserRoleCreateWizardComponent_ng_template_1_Template_app_user_role_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateUserRoleDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newUserRole());
  }
}
function UserRoleCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(17, "div", 16)(18, "p", 17);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275pipe(21, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 6, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(18, _c318, \u0275\u0275pipeBind1(7, 4, "ADMIN.ENTITIES.USER_ROLE"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 9, "ADMIN.USER_ROLES.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newUserRole().name || \u0275\u0275pipeBind1(16, 11, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(21, 15, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(20, _c318, \u0275\u0275pipeBind1(20, 13, "ADMIN.ENTITIES.USER_ROLE"))), " ");
  }
}
var UserRoleCreateWizardComponent = class _UserRoleCreateWizardComponent {
  userRoleDetailsStep = viewChild("userRoleDetailsStep", ...ngDevMode ? [{ debugName: "userRoleDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  cdr = inject(ChangeDetectorRef);
  // References to form components
  userRoleDetailsForm = viewChild("userRoleDetailsForm", ...ngDevMode ? [{ debugName: "userRoleDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  accountsService = inject(AccountsService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // UserRole model to collect data across steps
  newUserRole = signal({}, ...ngDevMode ? [{ debugName: "newUserRole" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const userRoleDetailsStep = this.userRoleDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(userRoleDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.USER_ROLES.DETAILS.TITLE"),
          content: userRoleDetailsStep,
          complete: false,
          validate: () => this.userRoleDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newUserRole.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newUserRole.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the userRole state with form data
  updateUserRoleDetails(formData) {
    this.newUserRole.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const userRoleData = this.newUserRole();
    this.isSubmitting.set(true);
    this.accountsService.createUserRole(userRoleData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "userrole",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating user role:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newUserRole.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function UserRoleCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _UserRoleCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _UserRoleCreateWizardComponent, selectors: [["app-user-role-create-wizard"]], viewQuery: function UserRoleCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.userRoleDetailsStep, _c024, 5)(ctx.confirmationTemplate, _c120, 5)(ctx.userRoleDetailsForm, _c219, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["userRoleDetailsStep", ""], ["confirmationTemplate", ""], ["userRoleDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function UserRoleCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function UserRoleCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function UserRoleCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, UserRoleCreateWizardComponent_ng_template_1_Template, 3, 2, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, UserRoleCreateWizardComponent_ng_template_3_Template, 22, 22, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    UserRoleDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(UserRoleCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-user-role-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      UserRoleDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: UserRole Details -->
<ng-template #userRoleDetailsStep let-step>
  <div class="p-fluid">
    <app-user-role-details-section
      #userRoleDetailsForm
      [createMode]="true"
      [entity]="newUserRole()"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateUserRoleDetails($event)"
    ></app-user-role-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- UserRole Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.USER_ROLE' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.USER_ROLES.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newUserRole().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
              | translate: { entity: ('ADMIN.ENTITIES.USER_ROLE' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { userRoleDetailsStep: [{ type: ViewChild, args: ["userRoleDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], userRoleDetailsForm: [{ type: ViewChild, args: ["userRoleDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(UserRoleCreateWizardComponent, { className: "UserRoleCreateWizardComponent", filePath: "src/app/features/administration/components/user-roles/user-role-create-wizard/user-role-create-wizard.component.ts", lineNumber: 46 });
})();

// src/app/features/administration/configs/user-roles.grid.config.ts
var UserRolesGridConfig = class _UserRolesGridConfig {
  userRolesService = inject(UserRolesDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  filterState = signal({}, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = {};
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "user-roles";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.USER_ROLES.GRID.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "description",
        header: this.translate.instant("ADMIN.USER_ROLES.GRID.DESCRIPTION"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.USER_ROLES.GRID.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  dataService = this.userRolesService;
  ownerId = "";
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({});
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    } else if (isDefined(this.params["distributorId"])) {
      this.ownerId = String(this.params["distributorId"]);
    } else if (isDefined(this.params["superAdminId"])) {
      this.ownerId = String(this.params["superAdminId"]);
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.USER_ROLES.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    const clientId = this.ownerId;
    this.openUserRoleCreationDialog(clientId).subscribe();
  }
  openUserRoleCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(UserRoleCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.USER_ROLE")
      }),
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function UserRolesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _UserRolesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _UserRolesGridConfig, factory: _UserRolesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(UserRolesGridConfig, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/features/administration/components/users/user-create-wizard/user-create-wizard.component.ts
var _c025 = ["userDetailsStep"];
var _c121 = ["userRolesStep"];
var _c220 = ["confirmationTemplate"];
var _c319 = ["userDetailsForm"];
var _c47 = ["userRolesForm"];
var _c57 = () => ({ entity: "User" });
function UserCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-user-details-section", 7, 3);
    \u0275\u0275listener("formValidityChange", function UserCreateWizardComponent_ng_template_1_Template_app_user_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function UserCreateWizardComponent_ng_template_1_Template_app_user_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateUserDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newUser())("ownerId", ctx_r1.ownerId);
  }
}
function UserCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-user-roles-section", 7, 4);
    \u0275\u0275listener("formValidityChange", function UserCreateWizardComponent_ng_template_3_Template_app_user_roles_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onRolesFormValidityChange($event));
    })("formValueChange", function UserCreateWizardComponent_ng_template_3_Template_app_user_roles_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateUserRoles($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newUser())("ownerId", ctx_r1.ownerId);
  }
}
function UserCreateWizardComponent_ng_template_5_Conditional_30_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 15)(1, "div", 16);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 17);
    \u0275\u0275text(5);
    \u0275\u0275pipe(6, "translate");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_7_0;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(3, 2, "ADMIN.USERS.DETAILS.FIELDS.COST_CENTRE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_7_0 = ctx_r1.newUser().costCentre) == null ? null : tmp_7_0.name) || \u0275\u0275pipeBind1(6, 4, "SHARED.COMMON.NOT_PROVIDED"), " ");
  }
}
function UserCreateWizardComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 8);
    \u0275\u0275element(1, "span", 9);
    \u0275\u0275elementStart(2, "div", 10)(3, "div", 11)(4, "div", 12)(5, "p", 13);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(8, "div", 14)(9, "div", 15)(10, "div", 16);
    \u0275\u0275text(11);
    \u0275\u0275pipe(12, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(13, "div", 17);
    \u0275\u0275text(14);
    \u0275\u0275pipe(15, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(16, "div", 15)(17, "div", 16);
    \u0275\u0275text(18);
    \u0275\u0275pipe(19, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(20, "div", 17);
    \u0275\u0275text(21);
    \u0275\u0275pipe(22, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(23, "div", 15)(24, "div", 16);
    \u0275\u0275text(25);
    \u0275\u0275pipe(26, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(27, "div", 17);
    \u0275\u0275text(28);
    \u0275\u0275pipe(29, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275conditionalCreate(30, UserCreateWizardComponent_ng_template_5_Conditional_30_Template, 7, 6, "div", 15);
    \u0275\u0275elementStart(31, "div", 15)(32, "div", 16);
    \u0275\u0275text(33);
    \u0275\u0275pipe(34, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(35, "div", 17);
    \u0275\u0275text(36);
    \u0275\u0275pipe(37, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(38, "div", 15)(39, "div", 16);
    \u0275\u0275text(40);
    \u0275\u0275pipe(41, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(42, "div", 17);
    \u0275\u0275text(43);
    \u0275\u0275pipe(44, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(45, "div", 15)(46, "div", 16);
    \u0275\u0275text(47);
    \u0275\u0275pipe(48, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(49, "div", 17);
    \u0275\u0275text(50);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(51, "div", 15)(52, "div", 16);
    \u0275\u0275text(53);
    \u0275\u0275pipe(54, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(55, "div", 17);
    \u0275\u0275text(56);
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(57, "div", 18)(58, "p", 19);
    \u0275\u0275text(59);
    \u0275\u0275pipe(60, "translate");
    \u0275\u0275elementEnd()()()()()();
  }
  if (rf & 2) {
    let tmp_16_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(7, 17, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction0(47, _c57)), " ");
    \u0275\u0275advance(5);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(12, 20, "ADMIN.USERS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newUser().name || \u0275\u0275pipeBind1(15, 22, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(19, 24, "ADMIN.USERS.DETAILS.FIELDS.EMAIL_ADDRESS"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newUser().emailAddress || \u0275\u0275pipeBind1(22, 26, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(26, 28, "ADMIN.USERS.DETAILS.FIELDS.MOBILE_NUMBER"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newUser().mobile || \u0275\u0275pipeBind1(29, 30, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.newUser().costCentre ? 30 : -1);
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(34, 32, "ADMIN.USERS.DETAILS.FIELDS.TIMEZONE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newUser().timeZoneId || \u0275\u0275pipeBind1(37, 34, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(41, 36, "ADMIN.USERS.DETAILS.FIELDS.DEFAULT_CLIENT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_16_0 = ctx_r1.newUser().defaultClient) == null ? null : tmp_16_0.name) || \u0275\u0275pipeBind1(44, 38, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(48, 40, "ADMIN.USERS.DETAILS.FIELDS.SEND_INVITE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newUser()["invite"] ? "Yes" : "No", " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(54, 42, "ADMIN.USERS.ROLES.FIELDS.ROLES"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.getRoleNames(ctx_r1.newUser().roles), " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(60, 44, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction0(48, _c57)), " ");
  }
}
var UserCreateWizardComponent = class _UserCreateWizardComponent {
  userDetailsStep = viewChild("userDetailsStep", ...ngDevMode ? [{ debugName: "userDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  userRolesStep = viewChild("userRolesStep", ...ngDevMode ? [{ debugName: "userRolesStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  userDetailsForm = viewChild("userDetailsForm", ...ngDevMode ? [{ debugName: "userDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  userRolesForm = viewChild("userRolesForm", ...ngDevMode ? [{ debugName: "userRolesForm" }] : (
    /* istanbul ignore next */
    []
  ));
  accountsService = inject(AccountsService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  translate = inject(TranslateService);
  // User model to collect data across steps
  newUser = signal({}, ...ngDevMode ? [{ debugName: "newUser" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const userDetailsStep = this.userDetailsStep();
      const userRolesStep = this.userRolesStep();
      const confirmationTemplate = this.confirmationTemplate();
      if (isNullOrUndefined(userDetailsStep) || isNullOrUndefined(userRolesStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.USERS.DETAILS.TITLE"),
          content: userDetailsStep,
          complete: false,
          validate: () => this.userDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("ADMIN.USERS.ROLES.TITLE"),
          content: userRolesStep,
          complete: false,
          validate: () => this.userRolesForm()?.validate() ?? false
        },
        {
          value: 3,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newUser.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newUser.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
  }
  onRolesFormValidityChange(isValid) {
    this.steps[1].disableNext = !isValid;
  }
  // Update the user state with form data
  updateUserDetails(formData) {
    this.newUser.update((current) => {
      const updated = __spreadValues({}, current);
      for (const key of Object.keys(formData)) {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      }
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  updateUserRoles(formData) {
    this.newUser.update((current) => __spreadProps(__spreadValues({}, current), {
      roles: formData["roles"]
    }));
  }
  onWizardComplete() {
    const userData = this.newUser();
    this.isSubmitting.set(true);
    this.accountsService.createUser(userData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "user",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating user:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newUser.set({});
    this.dialogRef.close();
  }
  getRoleNames(roles) {
    if (isEmpty(roles)) {
      return "No roles assigned";
    }
    return roles?.map((role) => role.name).join(", ") ?? "No roles assigned";
  }
  static \u0275fac = function UserCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _UserCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _UserCreateWizardComponent, selectors: [["app-user-create-wizard"]], viewQuery: function UserCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.userDetailsStep, _c025, 5)(ctx.userRolesStep, _c121, 5)(ctx.confirmationTemplate, _c220, 5)(ctx.userDetailsForm, _c319, 5)(ctx.userRolesForm, _c47, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(5);
    }
  }, decls: 7, vars: 5, consts: [["userDetailsStep", ""], ["userRolesStep", ""], ["confirmationTemplate", ""], ["userDetailsForm", ""], ["userRolesForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function UserCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 5);
      \u0275\u0275listener("complete", function UserCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function UserCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275template(1, UserCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, UserCreateWizardComponent_ng_template_3_Template, 3, 3, "ng-template", null, 1, \u0275\u0275templateRefExtractor)(5, UserCreateWizardComponent_ng_template_5_Template, 61, 49, "ng-template", null, 2, \u0275\u0275templateRefExtractor);
      \u0275\u0275elementEnd();
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    UserDetailsSectionComponent,
    UserRolesSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(UserCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-user-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      UserDetailsSectionComponent,
      UserRolesSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
  <!-- Step 1: User Details -->
  <ng-template #userDetailsStep let-step>
    <div class="p-fluid">
      <app-user-details-section
        #userDetailsForm
        [createMode]="true"
        [entity]="newUser()"
        [ownerId]="ownerId"
        (formValidityChange)="onDetailsFormValidityChange($event)"
        (formValueChange)="updateUserDetails($event)"
      ></app-user-details-section>
    </div>
  </ng-template>

  <!-- Step 2: User Roles -->
  <ng-template #userRolesStep let-step>
    <div class="p-fluid">
      <app-user-roles-section
        #userRolesForm
        [createMode]="true"
        [entity]="newUser()"
        [ownerId]="ownerId"
        (formValidityChange)="onRolesFormValidityChange($event)"
        (formValueChange)="updateUserRoles($event)"
      ></app-user-roles-section>
    </div>
  </ng-template>

  <ng-template #confirmationTemplate let-step>
    <div class="flex gap-6">
      <span class="pi pi-check !text-5xl"></span>

      <div class="flex-1">
        <div class="grid">
          <!-- User Details Section -->
          <div class="col-12 md:col-6">
            <p class="mb-4">
              {{
                'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                  | translate: { entity: 'User' }
              }}
            </p>

            <div class="space-y-2">
              <!-- Each field in a side-by-side layout -->
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{ 'ADMIN.USERS.DETAILS.FIELDS.NAME' | translate }}:
                </div>
                <div class="w-2/3">
                  {{
                    newUser().name || ('SHARED.COMMON.NOT_PROVIDED' | translate)
                  }}
                </div>
              </div>

              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{ 'ADMIN.USERS.DETAILS.FIELDS.EMAIL_ADDRESS' | translate }}:
                </div>
                <div class="w-2/3">
                  {{
                    newUser().emailAddress ||
                      ('SHARED.COMMON.NOT_PROVIDED' | translate)
                  }}
                </div>
              </div>

              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{ 'ADMIN.USERS.DETAILS.FIELDS.MOBILE_NUMBER' | translate }}:
                </div>
                <div class="w-2/3">
                  {{
                    newUser().mobile ||
                      ('SHARED.COMMON.NOT_PROVIDED' | translate)
                  }}
                </div>
              </div>

              @if (newUser().costCentre) {
                <div class="flex">
                  <div class="w-1/3 font-medium">
                    {{ 'ADMIN.USERS.DETAILS.FIELDS.COST_CENTRE' | translate }}:
                  </div>
                  <div class="w-2/3">
                    {{
                      newUser().costCentre?.name ||
                        ('SHARED.COMMON.NOT_PROVIDED' | translate)
                    }}
                  </div>
                </div>
              }

              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{ 'ADMIN.USERS.DETAILS.FIELDS.TIMEZONE' | translate }}:
                </div>
                <div class="w-2/3">
                  {{
                    newUser().timeZoneId ||
                      ('SHARED.COMMON.NOT_PROVIDED' | translate)
                  }}
                </div>
              </div>

              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{ 'ADMIN.USERS.DETAILS.FIELDS.DEFAULT_CLIENT' | translate }}:
                </div>
                <div class="w-2/3">
                  {{
                    newUser().defaultClient?.name ||
                      ('SHARED.COMMON.NOT_PROVIDED' | translate)
                  }}
                </div>
              </div>
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{ 'ADMIN.USERS.DETAILS.FIELDS.SEND_INVITE' | translate }}:
                </div>
                <div class="w-2/3">
                  {{ newUser()['invite'] ? 'Yes' : 'No' }}
                </div>
              </div>
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{ 'ADMIN.USERS.ROLES.FIELDS.ROLES' | translate }}:
                </div>
                <div class="w-2/3">
                  {{ getRoleNames(newUser().roles) }}
                </div>
              </div>
            </div>

            <div
              class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
            >
              <p class="mb-0">
                {{
                  'SHARED.COMMON.WIZARD.FINISH_PROMPT'
                    | translate: { entity: 'User' }
                }}
              </p>
            </div>
          </div>
        </div>
      </div>
    </div></ng-template
  >
</app-wizard>
` }]
  }], () => [], { userDetailsStep: [{ type: ViewChild, args: ["userDetailsStep", { isSignal: true }] }], userRolesStep: [{ type: ViewChild, args: ["userRolesStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], userDetailsForm: [{ type: ViewChild, args: ["userDetailsForm", { isSignal: true }] }], userRolesForm: [{ type: ViewChild, args: ["userRolesForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(UserCreateWizardComponent, { className: "UserCreateWizardComponent", filePath: "src/app/features/administration/components/users/user-create-wizard/user-create-wizard.component.ts", lineNumber: 51 });
})();

// src/app/features/administration/configs/users.grid.config.ts
var UsersGridConfig = class _UsersGridConfig {
  usersService = inject(UsersDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  entitiesService = inject(EntitiesService);
  dialogRef = null;
  dialogService = inject(DialogService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = ["active", "suspended", "inactive"];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = {};
  destroyRef = inject(DestroyRef);
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  fieldTransformations = {
    costCentre: "costCentre.id"
  };
  baseColumns = this.buildColumns();
  columns = [];
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.usersService;
  ownerId = "";
  entityType = "users";
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.USERS.GRID.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains",
        transform: "text-ellipsis"
      },
      {
        field: "emailAddress",
        header: this.translate.instant("ADMIN.USERS.GRID.EMAIL_ADDRESS"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains",
        transform: "text-ellipsis"
      },
      {
        field: "mobile",
        header: this.translate.instant("ADMIN.USERS.GRID.MOBILE_NUMBER"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains",
        transform: "text-ellipsis"
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.USERS.GRID.STATE"),
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: "active"
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.SUSPENDED"),
              value: "suspended"
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: "deleted"
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.INACTIVE"),
              value: "inactive"
            }
          ],
          defaultSelected: this.defaultStateValues
        },
        transform: "capitalize"
      },
      {
        field: "costCentre",
        header: this.translate.instant("ADMIN.USERS.GRID.COST_CENTRE"),
        valueGetter: (rowData) => isDefined(rowData.costCentre?.name) ? rowData.costCentre?.name : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: []
        }
      },
      {
        field: "timeZoneId",
        header: this.translate.instant("ADMIN.USERS.GRID.TIMEZONE"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains",
        transform: "text-ellipsis"
      },
      {
        field: "language",
        header: this.translate.instant("ADMIN.USERS.GRID.LANGUAGE"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains",
        transform: "text-ellipsis"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.USERS.GRID.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    let isClientContext = false;
    this.columns = cloneDeepWith_default(this.baseColumns, (value) => {
      if (typeof value === "function") {
        return value;
      }
      return void 0;
    });
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
      isClientContext = true;
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    } else if (isDefined(this.params["distributorId"])) {
      this.ownerId = String(this.params["distributorId"]);
    } else if (isDefined(this.params["superAdminId"])) {
      this.ownerId = String(this.params["superAdminId"]);
    }
    if (isClientContext) {
      this.loadFilterOptions();
    } else {
      this.columns = this.columns.filter((col) => col.field !== "costCentre");
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.USERS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const actualField = this.fieldTransformations[field] || field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  loadFilterOptions() {
    this.entitiesService.listCostCentres(this.ownerId).pipe(takeUntilDestroyed(this.destroyRef), catchError(() => {
      return EMPTY;
    })).subscribe((costCentres) => {
      const costCentreColumn = this.columns.find((col) => col.field === "costCentre");
      if (costCentreColumn?.filterConfig) {
        costCentreColumn.filterConfig.options = costCentres.items.map((costCentre) => ({
          label: costCentre.name ?? "",
          value: costCentre.id
        })).sort((a, b) => a.label.localeCompare(b.label));
      }
    });
  }
  onAdd() {
    const clientId = this.ownerId;
    this.openUserCreationDialog(clientId).subscribe((createdUser) => {
      if (isDefined(createdUser)) {
      }
    });
  }
  openUserCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(UserCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.USER")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function UsersGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _UsersGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _UsersGridConfig, factory: _UsersGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(UsersGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/distributor-vendors/distributor-vendor-create-wizard/distributor-vendor-create-wizard.component.ts
var _c026 = ["vendorDetailsStep"];
var _c122 = ["confirmationTemplate"];
var _c221 = ["vendorDetailsForm"];
var _c320 = (a0) => ({ entity: a0 });
function DistributorVendorCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-vendor-details", 5, 2);
    \u0275\u0275listener("formValidityChange", function DistributorVendorCreateWizardComponent_ng_template_1_Template_app_vendor_details_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function DistributorVendorCreateWizardComponent_ng_template_1_Template_app_vendor_details_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateVendorDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newVendor())("ownerId", ctx_r1.ownerId);
  }
}
function DistributorVendorCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(24, "div", 13)(25, "div", 14);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "div", 15);
    \u0275\u0275text(29);
    \u0275\u0275pipe(30, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(31, "div", 16)(32, "p", 17);
    \u0275\u0275text(33);
    \u0275\u0275pipe(34, "translate");
    \u0275\u0275pipe(35, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 10, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(30, _c320, \u0275\u0275pipeBind1(7, 8, "ADMIN.ENTITIES.VENDOR"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 13, "ADMIN.VENDORS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newVendor().name || \u0275\u0275pipeBind1(16, 15, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 17, "ADMIN.VENDORS.DETAILS.FIELDS.WEBSITE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newVendor().website || \u0275\u0275pipeBind1(23, 19, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(27, 21, "ADMIN.VENDORS.DETAILS.FIELDS.GROUP"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.getGroupName(ctx_r1.newVendor().group) || \u0275\u0275pipeBind1(30, 23, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(35, 27, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(32, _c320, \u0275\u0275pipeBind1(34, 25, "ADMIN.ENTITIES.VENDOR"))), " ");
  }
}
var DistributorVendorCreateWizardComponent = class _DistributorVendorCreateWizardComponent {
  vendorDetailsStep = viewChild("vendorDetailsStep", ...ngDevMode ? [{ debugName: "vendorDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  vendorDetailsForm = viewChild("vendorDetailsForm", ...ngDevMode ? [{ debugName: "vendorDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  accountsService = inject(AccountsService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cacheService = inject(RequestCacheService);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // Vendor model to collect data across steps
  newVendor = signal({}, ...ngDevMode ? [{ debugName: "newVendor" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  companyGroups = [];
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const vendorDetailsStep = this.vendorDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(vendorDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.VENDORS.DETAILS.TITLE"),
          content: vendorDetailsStep,
          complete: false,
          validate: () => this.vendorDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newVendor.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
      this.cacheService.getOrFetch(`company-groups-${this.ownerId}`, () => this.accountsService.listCompanyGroups(this.ownerId), 3e5).pipe(takeUntilDestroyed(this.destroyRef), catchError(() => {
        return of({ items: [] });
      })).subscribe((data) => {
        this.companyGroups = [
          {
            value: DEFAULT_SYSTEM_ID,
            label: this.translate.instant("SHARED.COMMON.NONE")
          },
          ...data.items.map((item) => ({
            label: item.name ?? "",
            value: item.id ?? ""
          })).sort((a, b) => a.label.localeCompare(b.label))
        ];
      });
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newVendor.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the vendor state with form data
  updateVendorDetails(formData) {
    this.newVendor.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const vendorData = this.newVendor();
    this.isSubmitting.set(true);
    this.accountsService.createVendor(vendorData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "vendor",
          operation: "create",
          entityId: response.id
        });
        this.broadcastService.broadcast("tree-refresh-required", {
          entityType: "vendor",
          operation: "create",
          entityId: response.id,
          distributorId: this.ownerId
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating vendor:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newVendor.set({});
    this.dialogRef.close();
  }
  getGroupName(groupId) {
    if (isNullOrUndefined(groupId) || groupId === "") {
      return this.translate.instant("SHARED.COMMON.NOT_PROVIDED");
    }
    if (groupId === DEFAULT_SYSTEM_ID) {
      return this.translate.instant("SHARED.COMMON.NONE");
    }
    const group = this.companyGroups.find((g) => g.value === groupId);
    return isDefined(group) ? group.label : this.translate.instant("SHARED.COMMON.UNKNOWN");
  }
  static \u0275fac = function DistributorVendorCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DistributorVendorCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _DistributorVendorCreateWizardComponent, selectors: [["app-distributor-vendor-create-wizard"]], viewQuery: function DistributorVendorCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.vendorDetailsStep, _c026, 5)(ctx.confirmationTemplate, _c122, 5)(ctx.vendorDetailsForm, _c221, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["vendorDetailsStep", ""], ["confirmationTemplate", ""], ["vendorDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function DistributorVendorCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function DistributorVendorCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function DistributorVendorCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, DistributorVendorCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, DistributorVendorCreateWizardComponent_ng_template_3_Template, 36, 34, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    VendorDetailsComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DistributorVendorCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-distributor-vendor-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      VendorDetailsComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Vendor Details -->
<ng-template #vendorDetailsStep let-step>
  <div class="p-fluid">
    <app-vendor-details
      #vendorDetailsForm
      [createMode]="true"
      [entity]="newVendor()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateVendorDetails($event)"
    ></app-vendor-details>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Vendor Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : {
                      entity: ('ADMIN.ENTITIES.VENDOR' | translate),
                    }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.VENDORS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newVendor().name || ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.VENDORS.DETAILS.FIELDS.WEBSITE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newVendor().website ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.VENDORS.DETAILS.FIELDS.GROUP' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  getGroupName(newVendor().group) ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : {
                    entity: ('ADMIN.ENTITIES.VENDOR' | translate),
                  }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { vendorDetailsStep: [{ type: ViewChild, args: ["vendorDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], vendorDetailsForm: [{ type: ViewChild, args: ["vendorDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(DistributorVendorCreateWizardComponent, { className: "DistributorVendorCreateWizardComponent", filePath: "src/app/features/administration/components/distributor-vendors/distributor-vendor-create-wizard/distributor-vendor-create-wizard.component.ts", lineNumber: 47 });
})();

// src/app/features/administration/configs/vendors.grid.config.ts
var VendorsGridConfig = class _VendorsGridConfig {
  vendorsService = inject(VendorsDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  accountsService = inject(AccountsService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [
    VendorListItem.StateEnum.Active,
    VendorListItem.StateEnum.Inactive,
    VendorListItem.StateEnum.Suspended
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { distributorId: "" };
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  fieldTransformations = {
    type: "type.id"
  };
  entityType = "vendors";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.vendorsService;
  vendorGroupTypes = [];
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.VENDORS.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "website",
        header: this.translate.instant("ADMIN.VENDORS.COLUMNS.WEBSITE"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "group",
        header: this.translate.instant("ADMIN.VENDORS.COLUMNS.GROUP"),
        valueGetter: (rowData) => {
          if (isNullOrUndefined(rowData.group))
            return this.translate.instant("SHARED.COMMON.NONE");
          const value = this.vendorGroupTypes.find((group) => group.value === rowData.group)?.label;
          return value ?? this.translate.instant("SHARED.COMMON.NONE");
        },
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: []
        }
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.VENDORS.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: VendorListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: VendorListItem.StateEnum.Deleted
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.INACTIVE"),
              value: VendorListItem.StateEnum.Inactive
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.SUSPENDED"),
              value: VendorListItem.StateEnum.Suspended
            }
          ],
          defaultSelected: this.defaultStateValues
        },
        transform: "capitalize"
      },
      {
        field: "clients",
        header: this.translate.instant("ADMIN.VENDORS.COLUMNS.CLIENT"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["client"]) ? rowData.counts?.["client"] : 0
      },
      {
        field: "assets",
        header: this.translate.instant("ADMIN.VENDORS.COLUMNS.ASSET"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["asset"]) ? rowData.counts?.["asset"] : 0
      },
      {
        field: "devices",
        header: this.translate.instant("ADMIN.VENDORS.COLUMNS.DEVICE"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["device"]) ? rowData.counts?.["device"] : 0
      },
      {
        field: "companion-camera",
        header: this.translate.instant("ADMIN.VENDORS.COLUMNS.COMPANION_CAMERA"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["companionCameraDevice"]) ? rowData.counts?.["companionCameraDevice"] : 0
      },
      {
        field: "users",
        header: this.translate.instant("ADMIN.VENDORS.COLUMNS.USER"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["user"]) ? rowData.counts?.["user"] : 0
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.VENDORS.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    this.loadFilterOptions();
  }
  getTitle() {
    return this.translate.instant("ADMIN.VENDORS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.params["distributorId"],
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter(),
      counts: "client,asset,device,companion-camera,user"
    };
  }
  onFilterChange(field, value) {
    const actualField = this.fieldTransformations[field] || field;
    const column = this.columns.find((col) => col.field === field);
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
    if (field === "state") {
      const includeDeleted = Array.isArray(value) && value.includes(VendorListItem.StateEnum.Deleted);
      this.broadcastService.broadcast("vendor-filter-changed", {
        distributorId: this.params["distributorId"],
        includeDeleted
      });
    }
  }
  loadFilterOptions() {
    this.accountsService.listCompanyGroups(this.params["distributorId"]).pipe(takeUntilDestroyed(this.destroyRef), catchError(() => {
      return EMPTY;
    })).subscribe((groups) => {
      const typeColumn = this.columns.find((col) => col.field === "group");
      if (typeColumn?.filterConfig) {
        this.vendorGroupTypes = groups.items.map((type) => ({
          label: type.name ?? "",
          value: type.id
        })).sort((a, b) => a.label.localeCompare(b.label));
        typeColumn.filterConfig.options = this.vendorGroupTypes;
      }
    });
  }
  onAdd() {
    const ownerId = this.params["distributorId"];
    this.openVendorCreationDialog(ownerId).subscribe();
  }
  openVendorCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(DistributorVendorCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.VENDOR")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function VendorsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _VendorsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _VendorsGridConfig, factory: _VendorsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(VendorsGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/models/measurement-unit-defaults.constants.ts
var METRIC_MEASUREMENT_DEFAULTS = __spreadValues({}, DEFAULT_MEASUREMENT_UNITS);

// src/app/features/administration/components/vendor-clients/vendor-client-create-wizard/vendor-client-create-wizard.component.ts
var _c027 = ["clientDetailsStep"];
var _c123 = ["confirmationTemplate"];
var _c222 = ["clientDetailsForm"];
var _c321 = (a0) => ({ entity: a0 });
function VendorClientCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-client-details", 5, 2);
    \u0275\u0275listener("formValidityChange", function VendorClientCreateWizardComponent_ng_template_1_Template_app_client_details_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function VendorClientCreateWizardComponent_ng_template_1_Template_app_client_details_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateClientDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newClient())("ownerId", ctx_r1.ownerId);
  }
}
function VendorClientCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(24, "div", 13)(25, "div", 14);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "div", 15);
    \u0275\u0275text(29);
    \u0275\u0275pipe(30, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(31, "div", 16)(32, "p", 17);
    \u0275\u0275text(33);
    \u0275\u0275pipe(34, "translate");
    \u0275\u0275pipe(35, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 10, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(30, _c321, \u0275\u0275pipeBind1(7, 8, "ADMIN.ENTITIES.CLIENT"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 13, "ADMIN.CLIENTS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newClient().name || \u0275\u0275pipeBind1(16, 15, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 17, "ADMIN.CLIENTS.DETAILS.FIELDS.WEBSITE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newClient().website || \u0275\u0275pipeBind1(23, 19, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(27, 21, "ADMIN.CLIENTS.DETAILS.FIELDS.GROUP"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.getGroupName(ctx_r1.newClient().group) || \u0275\u0275pipeBind1(30, 23, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(35, 27, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(32, _c321, \u0275\u0275pipeBind1(34, 25, "ADMIN.ENTITIES.CLIENT"))), " ");
  }
}
var VendorClientCreateWizardComponent = class _VendorClientCreateWizardComponent {
  clientDetailsStep = viewChild("clientDetailsStep", ...ngDevMode ? [{ debugName: "clientDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  clientDetailsForm = viewChild("clientDetailsForm", ...ngDevMode ? [{ debugName: "clientDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  accountsService = inject(AccountsService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cacheService = inject(RequestCacheService);
  cdr = inject(ChangeDetectorRef);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // Client model to collect data across steps
  newClient = signal({}, ...ngDevMode ? [{ debugName: "newClient" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  companyGroups = [];
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const clientDetailsStep = this.clientDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(clientDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.CLIENTS.DETAILS.TITLE"),
          content: clientDetailsStep,
          complete: false,
          validate: () => this.clientDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newClient.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? "",
        measurementUnits: METRIC_MEASUREMENT_DEFAULTS
      }));
      this.cacheService.getOrFetch(`company-groups-${this.ownerId}`, () => this.accountsService.listCompanyGroups(this.ownerId), 3e5).pipe(takeUntilDestroyed(this.destroyRef), catchError(() => {
        return of({ items: [] });
      })).subscribe((data) => {
        this.companyGroups = [
          {
            value: DEFAULT_SYSTEM_ID,
            label: this.translate.instant("SHARED.COMMON.NONE")
          },
          ...data.items.map((item) => ({
            label: item.name ?? "",
            value: item.id ?? ""
          })).sort((a, b) => a.label.localeCompare(b.label))
        ];
      });
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newClient.set({
      ownerId: this.ownerId ?? "",
      measurementUnits: METRIC_MEASUREMENT_DEFAULTS
    });
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the client state with form data
  updateClientDetails(formData) {
    this.newClient.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const clientData = this.newClient();
    this.isSubmitting.set(true);
    this.accountsService.createClient(clientData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "client",
          operation: "create",
          entityId: response.id
        });
        this.broadcastService.broadcast("tree-refresh-required", {
          entityType: "client",
          operation: "create",
          entityId: response.id,
          vendorId: this.ownerId,
          distributorId: this.dialogConfig.data?.distributorId
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating client:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newClient.set({});
    this.dialogRef.close();
  }
  getGroupName(groupId) {
    if (isNullOrUndefined(groupId) || groupId === "") {
      return this.translate.instant("SHARED.COMMON.NOT_PROVIDED");
    }
    if (groupId === DEFAULT_SYSTEM_ID) {
      return this.translate.instant("SHARED.COMMON.NONE");
    }
    const group = this.companyGroups.find((g) => g.value === groupId);
    return isDefined(group) ? group.label : this.translate.instant("SHARED.COMMON.UNKNOWN");
  }
  static \u0275fac = function VendorClientCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _VendorClientCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _VendorClientCreateWizardComponent, selectors: [["app-vendor-client-create-wizard"]], viewQuery: function VendorClientCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.clientDetailsStep, _c027, 5)(ctx.confirmationTemplate, _c123, 5)(ctx.clientDetailsForm, _c222, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["clientDetailsStep", ""], ["confirmationTemplate", ""], ["clientDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function VendorClientCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function VendorClientCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function VendorClientCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, VendorClientCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, VendorClientCreateWizardComponent_ng_template_3_Template, 36, 34, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    ClientDetailsComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(VendorClientCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-vendor-client-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      ClientDetailsComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Client Details -->
<ng-template #clientDetailsStep let-step>
  <div class="p-fluid">
    <app-client-details
      #clientDetailsForm
      [createMode]="true"
      [entity]="newClient()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateClientDetails($event)"
    ></app-client-details>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Client Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate: { entity: ('ADMIN.ENTITIES.CLIENT' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.CLIENTS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newClient().name || ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.CLIENTS.DETAILS.FIELDS.WEBSITE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newClient().website ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.CLIENTS.DETAILS.FIELDS.GROUP' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  getGroupName(newClient().group) ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate: { entity: ('ADMIN.ENTITIES.CLIENT' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { clientDetailsStep: [{ type: ViewChild, args: ["clientDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], clientDetailsForm: [{ type: ViewChild, args: ["clientDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(VendorClientCreateWizardComponent, { className: "VendorClientCreateWizardComponent", filePath: "src/app/features/administration/components/vendor-clients/vendor-client-create-wizard/vendor-client-create-wizard.component.ts", lineNumber: 49 });
})();

// src/app/features/administration/configs/clients.grid.config.ts
var ClientsGridConfig = class _ClientsGridConfig {
  clientsService = inject(ClientsDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  accountsService = inject(AccountsService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  adminState = inject(AdminStateService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  destroyRef = inject(DestroyRef);
  defaultStateValues = [
    ClientListItem.StateEnum.Active,
    ClientListItem.StateEnum.Inactive,
    ClientListItem.StateEnum.Suspended
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { vendorId: "" };
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "clients";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.clientsService;
  clientGroupTypes = [];
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.CLIENTS.GRID.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "website",
        header: this.translate.instant("ADMIN.CLIENTS.GRID.WEBSITE"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "group",
        header: this.translate.instant("ADMIN.CLIENTS.GRID.GROUP"),
        valueGetter: (rowData) => {
          if (isNullOrUndefined(rowData.group))
            return this.translate.instant("SHARED.COMMON.NONE");
          const value = this.clientGroupTypes.find((group) => group.value === rowData.group)?.label;
          return value ?? this.translate.instant("SHARED.COMMON.NONE");
        },
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: []
        }
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.CLIENTS.GRID.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: ClientListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: ClientListItem.StateEnum.Deleted
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.INACTIVE"),
              value: ClientListItem.StateEnum.Inactive
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.SUSPENDED"),
              value: ClientListItem.StateEnum.Suspended
            }
          ],
          defaultSelected: this.defaultStateValues
        },
        transform: "capitalize"
      },
      {
        field: "assets",
        header: this.translate.instant("ADMIN.CLIENTS.GRID.ASSETS"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["asset"]) ? rowData.counts?.["asset"] : 0
      },
      {
        field: "devices",
        header: this.translate.instant("ADMIN.CLIENTS.GRID.DEVICES"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["device"]) ? rowData.counts?.["device"] : 0
      },
      {
        field: "companion-camera",
        header: this.translate.instant("ADMIN.CLIENTS.GRID.COMPANION_CAMERA_DEVICES"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["companion-camera"]) ? rowData.counts?.["companion-camera"] : 0
      },
      {
        field: "users",
        header: this.translate.instant("ADMIN.CLIENTS.GRID.USERS"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["user"]) ? rowData.counts?.["user"] : 0
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.CLIENTS.GRID.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    this.loadFilterOptions();
  }
  getTitle() {
    return this.translate.instant("ADMIN.CLIENTS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.params["vendorId"],
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter(),
      counts: "asset,device,companion-camera,user"
    };
  }
  onFilterChange(field, value) {
    const actualField = field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  loadFilterOptions() {
    this.accountsService.listCompanyGroups(this.params["vendorId"]).pipe(takeUntilDestroyed(this.destroyRef), catchError(() => {
      return EMPTY;
    })).subscribe((groups) => {
      const typeColumn = this.columns.find((col) => col.field === "type");
      if (typeColumn?.filterConfig) {
        this.clientGroupTypes = groups.items.map((type) => ({
          label: type.name ?? "",
          value: type.id
        })).sort((a, b) => a.label.localeCompare(b.label));
        typeColumn.filterConfig.options = this.clientGroupTypes;
      }
    });
  }
  onAdd() {
    const ownerId = this.params["vendorId"];
    const distributorId = this.params["distributorId"] ?? this.adminState.getActiveContextId();
    this.openClientCreationDialog(ownerId, distributorId).subscribe((createdClient) => {
      if (isDefined(createdClient)) {
      }
    });
  }
  openClientCreationDialog(ownerId, distributorId) {
    this.dialogRef = this.dialogService.open(VendorClientCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.CLIENT")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId, distributorId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function ClientsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ClientsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _ClientsGridConfig, factory: _ClientsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ClientsGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/configs/contacts.grid.config.ts
var ContactsGridConfig = class _ContactsGridConfig {
  contactsDataAdapter = inject(ContactsDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  dialogRef = null;
  filterState = signal({}, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { clientId: "" };
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "client-contacts";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.contactsDataAdapter;
  buildColumns() {
    const t = (key) => this.translate.instant(key);
    return [
      {
        field: "name",
        header: t("ADMIN.CONTACTS.GRID.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "type",
        header: t("ADMIN.CONTACTS.GRID.COLUMNS.TYPE"),
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: Object.keys(Contact.TypeEnum).map((key) => ({
            label: t(`SHARED.CONTACTS.TYPES.${key}`),
            value: Contact.TypeEnum[key]
          }))
        },
        transform: "capitalize"
      },
      {
        field: "emailAddress",
        header: t("ADMIN.CONTACTS.GRID.COLUMNS.EMAIL"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "phoneMobile",
        header: t("ADMIN.CONTACTS.GRID.COLUMNS.MOBILE"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "phoneOffice",
        header: t("ADMIN.CONTACTS.GRID.COLUMNS.OFFICE_PHONE"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "notes",
        header: t("ADMIN.CONTACTS.GRID.COLUMNS.NOTES"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({});
  }
  initialize(params) {
    this.params = params;
  }
  getTitle() {
    return this.translate.instant("ADMIN.ENTITIES.CONTACTS");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.params["clientId"],
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const column = this.columns.find((col) => col.field === field);
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.dialogRef = this.dialogService.open(ContactCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", { entity: this.translate.instant("ADMIN.ENTITIES.CONTACT") }),
      data: { ownerId: this.params["clientId"] },
      position: "top",
      modal: true,
      contentStyle: { overflow: "auto" }
    });
  }
  static \u0275fac = function ContactsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ContactsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _ContactsGridConfig, factory: _ContactsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ContactsGridConfig, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/features/administration/components/super-admin-distributors/super-admin-distributors-create-wizard/super-admin-distributors-create-wizard.component.ts
var _c028 = ["distributorDetailsStep"];
var _c124 = ["confirmationTemplate"];
var _c223 = ["distributorDetailsForm"];
var _c322 = (a0) => ({ entity: a0 });
function SuperAdminDistributorsCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-distributor-details", 5, 2);
    \u0275\u0275listener("formValidityChange", function SuperAdminDistributorsCreateWizardComponent_ng_template_1_Template_app_distributor_details_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function SuperAdminDistributorsCreateWizardComponent_ng_template_1_Template_app_distributor_details_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateDistributorDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newDistributor())("ownerId", ctx_r1.ownerId);
  }
}
function SuperAdminDistributorsCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(24, "div", 13)(25, "div", 14);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "div", 15);
    \u0275\u0275text(29);
    \u0275\u0275pipe(30, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(31, "div", 16)(32, "p", 17);
    \u0275\u0275text(33);
    \u0275\u0275pipe(34, "translate");
    \u0275\u0275pipe(35, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_10_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 10, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(30, _c322, \u0275\u0275pipeBind1(7, 8, "ADMIN.ENTITIES.DISTRIBUTOR"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 13, "ADMIN.DISTRIBUTORS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newDistributor().name || \u0275\u0275pipeBind1(16, 15, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 17, "ADMIN.DISTRIBUTORS.DETAILS.FIELDS.WEBSITE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newDistributor().website || \u0275\u0275pipeBind1(23, 19, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(27, 21, "ADMIN.DISTRIBUTORS.DETAILS.FIELDS.TAGS"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_10_0 = ctx_r1.newDistributor().tags) == null ? null : tmp_10_0.toString()) || \u0275\u0275pipeBind1(30, 23, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(35, 27, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(32, _c322, \u0275\u0275pipeBind1(34, 25, "ADMIN.ENTITIES.DISTRIBUTOR"))), " ");
  }
}
var SuperAdminDistributorsCreateWizardComponent = class _SuperAdminDistributorsCreateWizardComponent {
  distributorDetailsStep = viewChild("distributorDetailsStep", ...ngDevMode ? [{ debugName: "distributorDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  distributorDetailsForm = viewChild("distributorDetailsForm", ...ngDevMode ? [{ debugName: "distributorDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  accountsService = inject(AccountsService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // Distributor model to collect data across steps
  newDistributor = signal({}, ...ngDevMode ? [{ debugName: "newDistributor" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  steps = [];
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const distributorDetailsStep = this.distributorDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(distributorDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.DISTRIBUTORS.DETAILS.TITLE"),
          content: distributorDetailsStep,
          complete: false,
          validate: () => this.distributorDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newDistributor.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.newDistributor.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the distributor state with form data
  updateDistributorDetails(formData) {
    this.newDistributor.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const distributorData = this.newDistributor();
    this.isSubmitting.set(true);
    this.accountsService.createDistributor(distributorData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "distributor",
          operation: "create",
          entityId: response.id
        });
        this.broadcastService.broadcast("tree-refresh-required", {
          entityType: "distributor",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating distributor:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newDistributor.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function SuperAdminDistributorsCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _SuperAdminDistributorsCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _SuperAdminDistributorsCreateWizardComponent, selectors: [["app-super-admin-distributors-create-wizard"]], viewQuery: function SuperAdminDistributorsCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.distributorDetailsStep, _c028, 5)(ctx.confirmationTemplate, _c124, 5)(ctx.distributorDetailsForm, _c223, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["distributorDetailsStep", ""], ["confirmationTemplate", ""], ["distributorDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function SuperAdminDistributorsCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function SuperAdminDistributorsCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function SuperAdminDistributorsCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, SuperAdminDistributorsCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, SuperAdminDistributorsCreateWizardComponent_ng_template_3_Template, 36, 34, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    WizardComponent,
    ButtonModule,
    DistributorDetailsComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SuperAdminDistributorsCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-super-admin-distributors-create-wizard", imports: [
      CommonModule,
      WizardComponent,
      ButtonModule,
      DistributorDetailsComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Distributor Details -->
<ng-template #distributorDetailsStep let-step>
  <div class="p-fluid">
    <app-distributor-details
      #distributorDetailsForm
      [createMode]="true"
      [entity]="newDistributor()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateDistributorDetails($event)"
    ></app-distributor-details>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Distributor Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.DISTRIBUTOR' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DISTRIBUTORS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newDistributor().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DISTRIBUTORS.DETAILS.FIELDS.WEBSITE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newDistributor().website ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DISTRIBUTORS.DETAILS.FIELDS.TAGS' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newDistributor().tags?.toString() ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.DISTRIBUTOR' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { distributorDetailsStep: [{ type: ViewChild, args: ["distributorDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], distributorDetailsForm: [{ type: ViewChild, args: ["distributorDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(SuperAdminDistributorsCreateWizardComponent, { className: "SuperAdminDistributorsCreateWizardComponent", filePath: "src/app/features/administration/components/super-admin-distributors/super-admin-distributors-create-wizard/super-admin-distributors-create-wizard.component.ts", lineNumber: 43 });
})();

// src/app/features/administration/configs/distributors.grid.config.ts
var DistributorsGridConfig = class _DistributorsGridConfig {
  distributorsService = inject(DistributorsDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [
    DistributorListItem.StateEnum.Active,
    DistributorListItem.StateEnum.Inactive,
    DistributorListItem.StateEnum.Suspended
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = {};
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "distributors";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.distributorsService;
  distributorGroupTypes = [];
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.DISTRIBUTORS.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "website",
        header: this.translate.instant("ADMIN.DISTRIBUTORS.COLUMNS.WEBSITE"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.DISTRIBUTORS.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: DistributorListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: DistributorListItem.StateEnum.Deleted
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.INACTIVE"),
              value: DistributorListItem.StateEnum.Inactive
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.SUSPENDED"),
              value: DistributorListItem.StateEnum.Suspended
            }
          ],
          defaultSelected: this.defaultStateValues
        },
        transform: "capitalize"
      },
      {
        field: "vendor",
        header: this.translate.instant("ADMIN.DISTRIBUTORS.COLUMNS.VENDOR"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["vendor"]) ? rowData.counts?.["vendor"] : 0
      },
      {
        field: "clients",
        header: this.translate.instant("ADMIN.DISTRIBUTORS.COLUMNS.CLIENT"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["client"]) ? rowData.counts?.["client"] : 0
      },
      {
        field: "assets",
        header: this.translate.instant("ADMIN.DISTRIBUTORS.COLUMNS.ASSET"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["asset"]) ? rowData.counts?.["asset"] : 0
      },
      {
        field: "devices",
        header: this.translate.instant("ADMIN.DISTRIBUTORS.COLUMNS.DEVICE"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["device"]) ? rowData.counts?.["device"] : 0
      },
      {
        field: "companion-camera",
        header: this.translate.instant("ADMIN.DISTRIBUTORS.COLUMNS.COMPANION_CAMERA"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["companion-camera"]) ? rowData.counts?.["companion-camera"] : 0
      },
      {
        field: "users",
        header: this.translate.instant("ADMIN.DISTRIBUTORS.COLUMNS.USER"),
        valueGetter: (rowData) => isDefined(rowData.counts?.["user"]) ? rowData.counts?.["user"] : 0
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.DISTRIBUTORS.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
  }
  getTitle() {
    return this.translate.instant("ADMIN.DISTRIBUTORS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: DEFAULT_SYSTEM_ID,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter(),
      counts: "vendor,client,asset,device,companion-camera,user"
    };
  }
  onFilterChange(field, value) {
    const actualField = field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    const ownerId = DEFAULT_SYSTEM_ID;
    this.openDistributorCreationDialog(ownerId).subscribe((createdDistributor) => {
      if (isDefined(createdDistributor)) {
      }
    });
  }
  openDistributorCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(SuperAdminDistributorsCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.DISTRIBUTOR")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function DistributorsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DistributorsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _DistributorsGridConfig, factory: _DistributorsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DistributorsGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/geo-lock-profiles/geo-lock-profile-create-wizard/geo-lock-profile-create-wizard.component.ts
var _c029 = ["geoLockProfileDetailsStep"];
var _c125 = ["confirmationTemplate"];
var _c224 = ["geoLockProfileDetailsForm"];
var _c323 = (a0) => ({ entity: a0 });
function GeoLockProfileCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-geo-lock-profile-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function GeoLockProfileCreateWizardComponent_ng_template_1_Template_app_geo_lock_profile_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function GeoLockProfileCreateWizardComponent_ng_template_1_Template_app_geo_lock_profile_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateGeoLockProfileDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newGeoLockProfile())("ownerId", ctx_r1.ownerId);
  }
}
function GeoLockProfileCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(24, "div", 16)(25, "p", 17);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275pipe(28, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 8, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(24, _c323, \u0275\u0275pipeBind1(7, 6, "ADMIN.PROFILES.GEO_LOCK"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 11, "ADMIN.PROFILES.GEO_LOCK.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newGeoLockProfile().name || \u0275\u0275pipeBind1(16, 13, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 15, "ADMIN.PROFILES.GEO_LOCK.DETAILS.FIELDS.RADIUS"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.getRadius() || \u0275\u0275pipeBind1(23, 17, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(28, 21, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(26, _c323, \u0275\u0275pipeBind1(27, 19, "ADMIN.ENTITIES.GEO_LOCK_PROFILE"))), " ");
  }
}
var GeoLockProfileCreateWizardComponent = class _GeoLockProfileCreateWizardComponent {
  geoLockProfileDetailsStep = viewChild("geoLockProfileDetailsStep", ...ngDevMode ? [{ debugName: "geoLockProfileDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  cdr = inject(ChangeDetectorRef);
  // References to form components
  geoLockProfileDetailsForm = viewChild("geoLockProfileDetailsForm", ...ngDevMode ? [{ debugName: "geoLockProfileDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  measurementUnitsService = inject(MeasurementUnitsService);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // GeoLockProfile model to collect data across steps
  newGeoLockProfile = signal({}, ...ngDevMode ? [{ debugName: "newGeoLockProfile" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const geoLockProfileDetailsStep = this.geoLockProfileDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(geoLockProfileDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.PROFILES.GEO_LOCK.DETAILS.WIZARD.STEPS.DETAILS"),
          content: geoLockProfileDetailsStep,
          complete: false,
          validate: () => this.geoLockProfileDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newGeoLockProfile.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newGeoLockProfile.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the geoLockProfile state with form data
  updateGeoLockProfileDetails(formData) {
    this.newGeoLockProfile.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const geoLockProfileData = this.newGeoLockProfile();
    this.isSubmitting.set(true);
    this.entitiesService.createGeoLockProfile(geoLockProfileData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "geo-lock-profile",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating geo lock profile:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newGeoLockProfile.set({});
    this.dialogRef.close();
  }
  getRadius() {
    const radius = this.newGeoLockProfile().radiusKm;
    return isDefined(radius) ? this.measurementUnitsService.fromBackend(MeasurementUnitType.DISTANCE, radius, 2) : null;
  }
  static \u0275fac = function GeoLockProfileCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _GeoLockProfileCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _GeoLockProfileCreateWizardComponent, selectors: [["app-geo-lock-profile-create-wizard"]], viewQuery: function GeoLockProfileCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.geoLockProfileDetailsStep, _c029, 5)(ctx.confirmationTemplate, _c125, 5)(ctx.geoLockProfileDetailsForm, _c224, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["geoLockProfileDetailsStep", ""], ["confirmationTemplate", ""], ["geoLockProfileDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function GeoLockProfileCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function GeoLockProfileCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function GeoLockProfileCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, GeoLockProfileCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, GeoLockProfileCreateWizardComponent_ng_template_3_Template, 29, 28, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    GeoLockProfileDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GeoLockProfileCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-geo-lock-profile-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      GeoLockProfileDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: GeoLockProfile Details -->
<ng-template #geoLockProfileDetailsStep let-step>
  <div class="p-fluid">
    <app-geo-lock-profile-details-section
      #geoLockProfileDetailsForm
      [createMode]="true"
      [entity]="newGeoLockProfile()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateGeoLockProfileDetails($event)"
    ></app-geo-lock-profile-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- GeoLockProfile Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate: { entity: ('ADMIN.PROFILES.GEO_LOCK' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.PROFILES.GEO_LOCK.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newGeoLockProfile().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.PROFILES.GEO_LOCK.DETAILS.FIELDS.RADIUS' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{ getRadius() || ('SHARED.COMMON.NOT_PROVIDED' | translate) }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.GEO_LOCK_PROFILE' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { geoLockProfileDetailsStep: [{ type: ViewChild, args: ["geoLockProfileDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], geoLockProfileDetailsForm: [{ type: ViewChild, args: ["geoLockProfileDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(GeoLockProfileCreateWizardComponent, { className: "GeoLockProfileCreateWizardComponent", filePath: "src/app/features/administration/components/geo-lock-profiles/geo-lock-profile-create-wizard/geo-lock-profile-create-wizard.component.ts", lineNumber: 47 });
})();

// src/app/features/administration/configs/geo-lock-profiles.grid.config.ts
var GeoLockProfilesGridConfig = class _GeoLockProfilesGridConfig {
  geoLockProfilesService = inject(GeoLockProfilesDataAdapter);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [
    GeoLockProfileListItem.StateEnum.Active
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = {};
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "geo-lock-profiles";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.geoLockProfilesService;
  ownerId = "";
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.PROFILES.GEO_LOCK.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.PROFILES.GEO_LOCK.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: GeoLockProfileListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: GeoLockProfileListItem.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        }
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.PROFILES.GEO_LOCK.COLUMNS.MODIFIED"),
        filter: false,
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  getTitle() {
    return this.translate.instant("ADMIN.PROFILES.GEO_LOCK.TITLE");
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({});
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    } else if (isDefined(this.params["distributorId"])) {
      this.ownerId = String(this.params["distributorId"]);
    }
  }
  getRequestParams(event) {
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField: "name",
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const actualField = field;
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.openGeoLockProfileCreationDialog(this.ownerId).subscribe();
  }
  openGeoLockProfileCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(GeoLockProfileCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.GEO_LOCK_PROFILE")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function GeoLockProfilesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _GeoLockProfilesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _GeoLockProfilesGridConfig, factory: _GeoLockProfilesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GeoLockProfilesGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/export-tasks/export-task-create-wizard/export-task-create-wizard.component.ts
var _c030 = ["exportTaskDetailsStep"];
var _c126 = ["exportTaskSettingsStep"];
var _c225 = ["confirmationTemplate"];
var _c324 = ["exportTaskDetailsForm"];
var _c48 = ["exportTaskSettingsForm"];
var _c58 = (a0) => ({ entity: a0 });
function ExportTaskCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-export-task-details-section", 7, 3);
    \u0275\u0275listener("formValidityChange", function ExportTaskCreateWizardComponent_ng_template_1_Template_app_export_task_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function ExportTaskCreateWizardComponent_ng_template_1_Template_app_export_task_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onFormValueChange($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newExportTask())("ownerId", ctx_r1.ownerId);
  }
}
function ExportTaskCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 6)(1, "app-export-task-settings-section", 7, 4);
    \u0275\u0275listener("formValidityChange", function ExportTaskCreateWizardComponent_ng_template_3_Template_app_export_task_settings_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onSettingsFormValidityChange($event));
    })("formValueChange", function ExportTaskCreateWizardComponent_ng_template_3_Template_app_export_task_settings_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onFormValueChange($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newExportTask())("ownerId", ctx_r1.ownerId);
  }
}
function ExportTaskCreateWizardComponent_ng_template_5_Conditional_24_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 15)(1, "div", 16);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 17);
    \u0275\u0275text(5);
    \u0275\u0275pipe(6, "translate");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_7_0;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(3, 2, "ADMIN.EXPORT_TASKS.DETAILS.FIELDS.CLIENT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_7_0 = ctx_r1.newExportTask().client) == null ? null : tmp_7_0.name) || \u0275\u0275pipeBind1(6, 4, "SHARED.COMMON.NOT_PROVIDED"), " ");
  }
}
function ExportTaskCreateWizardComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 8);
    \u0275\u0275element(1, "span", 9);
    \u0275\u0275elementStart(2, "div", 10)(3, "div", 11)(4, "div", 12)(5, "p", 13);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 14)(10, "div", 15)(11, "div", 16);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 17);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 15)(18, "div", 16);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 17);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275conditionalCreate(24, ExportTaskCreateWizardComponent_ng_template_5_Conditional_24_Template, 7, 6, "div", 15);
    \u0275\u0275elementStart(25, "div", 15)(26, "div", 16);
    \u0275\u0275text(27);
    \u0275\u0275pipe(28, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(29, "div", 17);
    \u0275\u0275text(30);
    \u0275\u0275pipe(31, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(32, "div", 15)(33, "div", 16);
    \u0275\u0275text(34);
    \u0275\u0275pipe(35, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(36, "div", 17);
    \u0275\u0275text(37);
    \u0275\u0275pipe(38, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(39, "div", 18)(40, "p", 19);
    \u0275\u0275text(41);
    \u0275\u0275pipe(42, "translate");
    \u0275\u0275pipe(43, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_12_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 13, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(37, _c58, \u0275\u0275pipeBind1(7, 11, "ADMIN.ENTITIES.EXPORT_TASK"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 16, "ADMIN.EXPORT_TASKS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newExportTask().name || \u0275\u0275pipeBind1(16, 18, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 20, "ADMIN.EXPORT_TASKS.DETAILS.FIELDS.MODE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newExportTask().deliveryMethod || \u0275\u0275pipeBind1(23, 22, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.newExportTask().deliveryMethod !== "firehose" ? 24 : -1);
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(28, 24, "ADMIN.EXPORT_TASKS.SETTINGS.FIELDS.DOCUMENTS"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_12_0 = ctx_r1.newExportTask().documentTypes) == null ? null : tmp_12_0.join(", ")) || \u0275\u0275pipeBind1(31, 26, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(35, 28, "ADMIN.EXPORT_TASKS.SETTINGS.FIELDS.DOCUMENT_VERSION"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newExportTask().documentVersion || \u0275\u0275pipeBind1(38, 30, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(43, 34, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(39, _c58, \u0275\u0275pipeBind1(42, 32, "ADMIN.ENTITIES.EXPORT_TASK"))), " ");
  }
}
var ExportTaskCreateWizardComponent = class _ExportTaskCreateWizardComponent {
  exportTaskDetailsStep = viewChild("exportTaskDetailsStep", ...ngDevMode ? [{ debugName: "exportTaskDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  exportTaskSettingsStep = viewChild("exportTaskSettingsStep", ...ngDevMode ? [{ debugName: "exportTaskSettingsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  exportTaskDetailsForm = viewChild("exportTaskDetailsForm", ...ngDevMode ? [{ debugName: "exportTaskDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  exportTaskSettingsForm = viewChild("exportTaskSettingsForm", ...ngDevMode ? [{ debugName: "exportTaskSettingsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  newExportTask = signal({}, ...ngDevMode ? [{ debugName: "newExportTask" }] : (
    /* istanbul ignore next */
    []
  ));
  steps = [];
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const exportTaskDetailsStep = this.exportTaskDetailsStep();
      const exportTaskSettingsStep = this.exportTaskSettingsStep();
      const confirmationTemplate = this.confirmationTemplate();
      if (isNullOrUndefined(exportTaskDetailsStep) || isNullOrUndefined(exportTaskSettingsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: "Export Task Details",
          content: exportTaskDetailsStep,
          complete: false,
          validate: () => this.exportTaskDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: "Export Task Settings",
          content: exportTaskSettingsStep,
          complete: false,
          validate: () => this.exportTaskSettingsForm()?.validate() ?? false
        },
        {
          value: 3,
          header: "Confirmation",
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newExportTask.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
  }
  onSettingsFormValidityChange(isValid) {
    this.steps[1].disableNext = !isValid;
  }
  onFormValueChange(formData) {
    this.newExportTask.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated["ownerId"])) {
        updated["ownerId"] = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const formData = this.newExportTask();
    const isFirehose = formData["deliveryMethod"] === "firehose";
    const request = {
      ownerId: this.ownerId ?? "",
      name: formData["name"],
      deliveryMethod: formData["deliveryMethod"],
      documentTypes: formData["documentTypes"],
      documentVersion: formData["documentVersion"]
    };
    if (isFirehose) {
      request.client = { id: this.ownerId ?? "" };
    } else if (isDefined(formData["client"]) && typeof formData["client"] === "object") {
      const selected = formData["client"];
      request.client = { id: selected.id ?? "", name: selected.name };
    }
    if ("assets" in formData) {
      if (isFirehose) {
        const clients = formData["assets"] ?? [];
        request.assets = clients.map((c) => ({
          actorType: "client",
          actorTypeId: c.id,
          actorTypeName: c.name,
          actorSelectionType: "specific",
          actorId: c.id,
          actorName: c.name,
          text: c.name
        }));
      } else {
        request.assets = formData["assets"];
      }
    }
    const settingsFields = [
      "endpointType",
      "endpoint",
      "prefix",
      "encoding",
      "parameters",
      "accessKey"
    ];
    const settingsData = {};
    settingsFields.forEach((field) => {
      if (field in formData) {
        settingsData[field] = formData[field];
      }
    });
    if (Object.keys(settingsData).length > 0) {
      request.settings = settingsData;
    }
    this.isSubmitting.set(true);
    this.entitiesService.createExportTask(request).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "exporttask",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating export task:", error);
      }
    });
  }
  onWizardCancel() {
    this.newExportTask.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function ExportTaskCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ExportTaskCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _ExportTaskCreateWizardComponent, selectors: [["app-export-task-create-wizard"]], viewQuery: function ExportTaskCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.exportTaskDetailsStep, _c030, 5)(ctx.exportTaskSettingsStep, _c126, 5)(ctx.confirmationTemplate, _c225, 5)(ctx.exportTaskDetailsForm, _c324, 5)(ctx.exportTaskSettingsForm, _c48, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(5);
    }
  }, decls: 7, vars: 5, consts: [["exportTaskDetailsStep", ""], ["exportTaskSettingsStep", ""], ["confirmationTemplate", ""], ["exportTaskDetailsForm", ""], ["exportTaskSettingsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function ExportTaskCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 5);
      \u0275\u0275listener("complete", function ExportTaskCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function ExportTaskCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, ExportTaskCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, ExportTaskCreateWizardComponent_ng_template_3_Template, 3, 3, "ng-template", null, 1, \u0275\u0275templateRefExtractor)(5, ExportTaskCreateWizardComponent_ng_template_5_Template, 44, 41, "ng-template", null, 2, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    ExportTaskDetailsSectionComponent,
    ExportTaskSettingsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ExportTaskCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-export-task-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      ExportTaskDetailsSectionComponent,
      ExportTaskSettingsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Export Task Details -->
<ng-template #exportTaskDetailsStep let-step>
  <div class="p-fluid">
    <app-export-task-details-section
      #exportTaskDetailsForm
      [createMode]="true"
      [entity]="newExportTask()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="onFormValueChange($event)"
    ></app-export-task-details-section>
  </div>
</ng-template>

<!-- Step 2: Export Task Settings -->
<ng-template #exportTaskSettingsStep let-step>
  <div class="p-fluid">
    <app-export-task-settings-section
      #exportTaskSettingsForm
      [createMode]="true"
      [entity]="newExportTask()"
      [ownerId]="ownerId"
      (formValidityChange)="onSettingsFormValidityChange($event)"
      (formValueChange)="onFormValueChange($event)"
    ></app-export-task-settings-section>
  </div>
</ng-template>

<!-- Step 3: Confirmation -->
<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.EXPORT_TASK' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.EXPORT_TASKS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newExportTask().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.EXPORT_TASKS.DETAILS.FIELDS.MODE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newExportTask().deliveryMethod ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            @if (newExportTask().deliveryMethod !== 'firehose') {
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{ 'ADMIN.EXPORT_TASKS.DETAILS.FIELDS.CLIENT' | translate }}:
                </div>
                <div class="w-2/3">
                  {{
                    newExportTask().client?.name ||
                      ('SHARED.COMMON.NOT_PROVIDED' | translate)
                  }}
                </div>
              </div>
            }

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.EXPORT_TASKS.SETTINGS.FIELDS.DOCUMENTS' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newExportTask().documentTypes?.join(', ') ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.EXPORT_TASKS.SETTINGS.FIELDS.DOCUMENT_VERSION'
                    | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newExportTask().documentVersion ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.EXPORT_TASK' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { exportTaskDetailsStep: [{ type: ViewChild, args: ["exportTaskDetailsStep", { isSignal: true }] }], exportTaskSettingsStep: [{ type: ViewChild, args: ["exportTaskSettingsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], exportTaskDetailsForm: [{ type: ViewChild, args: ["exportTaskDetailsForm", { isSignal: true }] }], exportTaskSettingsForm: [{ type: ViewChild, args: ["exportTaskSettingsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(ExportTaskCreateWizardComponent, { className: "ExportTaskCreateWizardComponent", filePath: "src/app/features/administration/components/export-tasks/export-task-create-wizard/export-task-create-wizard.component.ts", lineNumber: 49 });
})();

// src/app/features/administration/configs/export-tasks.grid.configs.ts
var ExportTasksGridConfig = class _ExportTasksGridConfig {
  exportTasksService = inject(ExportTasksDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  dialogRef = null;
  dialogService = inject(DialogService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  defaultStateValues = [
    ExportTaskListItem.StateEnum.Active,
    ExportTaskListItem.StateEnum.Suspended
  ];
  defaultDeliveryMethodValues = [
    ExportTaskListItem.DeliveryMethodEnum.Http,
    ExportTaskListItem.DeliveryMethodEnum.Stream,
    ExportTaskListItem.DeliveryMethodEnum.Firehose
  ];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    },
    deliveryMethod: {
      value: this.defaultDeliveryMethodValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { ownerId: "" };
  ownerId = "";
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  stateLabels = {
    active: "Active",
    suspended: "Suspended",
    deleted: "Deleted"
  };
  entityType = "export-tasks";
  columns = [
    {
      field: "name",
      header: "Name",
      filter: true,
      filterType: "text",
      filterMatchMode: "contains"
    },
    {
      field: "deliveryMethod",
      header: "Mode",
      valueGetter: (rowData) => isDefined(rowData.deliveryMethod) ? this.utilityService.toTitleCase(rowData.deliveryMethod) : "",
      filter: true,
      filterType: "multiselect",
      filterMatchMode: "in",
      filterConfig: {
        type: "multiselect",
        options: enumToSelectOptionValues(ExportTaskListItem.DeliveryMethodEnum),
        defaultSelected: this.defaultDeliveryMethodValues
      },
      transform: "capitalize"
    },
    {
      field: "client",
      header: "Client",
      valueGetter: (rowData) => isDefined(rowData.client) && isString(rowData.client.name) ? rowData.client.name : "",
      filter: true,
      filterType: "text",
      filterMatchMode: "contains"
    },
    {
      field: "state",
      header: "State",
      valueGetter: (rowData) => isDefined(rowData.state) ? this.stateLabels[rowData.state] : "",
      filter: true,
      filterType: "multiselect",
      filterMatchMode: "in",
      filterConfig: {
        type: "multiselect",
        options: [
          { label: "Active", value: ExportTaskListItem.StateEnum.Active },
          { label: "Suspended", value: ExportTaskListItem.StateEnum.Suspended },
          { label: "Deleted", value: ExportTaskListItem.StateEnum.Deleted }
        ],
        defaultSelected: this.defaultStateValues
      },
      transform: "capitalize"
    },
    {
      field: "modifiedDate",
      header: "Modified",
      filter: false,
      valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
    }
  ];
  dataService = this.exportTasksService;
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      },
      deliveryMethod: {
        value: this.defaultDeliveryMethodValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    } else if (isDefined(this.params["distributorId"])) {
      this.ownerId = String(this.params["distributorId"]);
    }
  }
  getTitle() {
    return "Export Tasks";
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.openExportTaskCreationDialog(this.ownerId).subscribe();
  }
  openExportTaskCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(ExportTaskCreateWizardComponent, {
      header: "Export Task Wizard",
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function ExportTasksGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ExportTasksGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _ExportTasksGridConfig, factory: _ExportTasksGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ExportTasksGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/components/map-sets/map-set-create-wizard/map-set-create-wizard.component.ts
var _c031 = ["mapSetDetailsStep"];
var _c127 = ["confirmationTemplate"];
var _c226 = ["mapSetDetailsForm"];
var _c325 = (a0) => ({ entity: a0 });
var _c49 = (a0) => ({ count: a0 });
function MapSetCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-map-set-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function MapSetCreateWizardComponent_ng_template_1_Template_app_map_set_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function MapSetCreateWizardComponent_ng_template_1_Template_app_map_set_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateMapSetDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newMapSet())("ownerId", ctx_r1.ownerId);
  }
}
function MapSetCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 16);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(24, "div", 13)(25, "div", 14);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "div", 16);
    \u0275\u0275text(29);
    \u0275\u0275pipe(30, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(31, "div", 17)(32, "p", 18);
    \u0275\u0275text(33);
    \u0275\u0275pipe(34, "translate");
    \u0275\u0275pipe(35, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_8_0;
    let tmp_10_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 10, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(32, _c325, \u0275\u0275pipeBind1(7, 8, "ADMIN.ENTITIES.MAP_SET"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 13, "ADMIN.MAP_SETS.DETAILS.MODAL.FIELDS.NAME"), " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newMapSet().name || \u0275\u0275pipeBind1(16, 15, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 17, "ADMIN.MAP_SETS.DETAILS.FIELDS.LAYERS"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(23, 19, "ADMIN.MAP_SETS.WIZARD.CONFIRMATION.LAYER_COUNT", \u0275\u0275pureFunction1(34, _c49, ((tmp_8_0 = ctx_r1.newMapSet().layers) == null ? null : tmp_8_0.length) ?? 0)), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(27, 22, "ADMIN.MAP_SETS.DETAILS.FIELDS.OVERLAYS"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(30, 24, "ADMIN.MAP_SETS.WIZARD.CONFIRMATION.OVERLAY_COUNT", \u0275\u0275pureFunction1(36, _c49, ((tmp_10_0 = ctx_r1.newMapSet().overlays) == null ? null : tmp_10_0.length) ?? 0)), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(35, 29, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(38, _c325, \u0275\u0275pipeBind1(34, 27, "ADMIN.ENTITIES.MAP_SET"))), " ");
  }
}
var MapSetCreateWizardComponent = class _MapSetCreateWizardComponent {
  mapSetDetailsStep = viewChild("mapSetDetailsStep", ...ngDevMode ? [{ debugName: "mapSetDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  cdr = inject(ChangeDetectorRef);
  // References to form components
  mapSetDetailsForm = viewChild("mapSetDetailsForm", ...ngDevMode ? [{ debugName: "mapSetDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // MapSet model to collect data across steps
  newMapSet = signal({}, ...ngDevMode ? [{ debugName: "newMapSet" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const mapSetDetailsStep = this.mapSetDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(mapSetDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.MAP_SETS.DETAILS.TITLE"),
          content: mapSetDetailsStep,
          complete: false,
          validate: () => this.mapSetDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newMapSet.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newMapSet.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the MapSet state with form data
  updateMapSetDetails(formData) {
    this.newMapSet.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const mapSetData = this.newMapSet();
    this.isSubmitting.set(true);
    this.entitiesService.createMapSet(mapSetData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "map-set",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating map set:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newMapSet.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function MapSetCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _MapSetCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _MapSetCreateWizardComponent, selectors: [["app-map-set-create-wizard"]], viewQuery: function MapSetCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.mapSetDetailsStep, _c031, 5)(ctx.confirmationTemplate, _c127, 5)(ctx.mapSetDetailsForm, _c226, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["mapSetDetailsStep", ""], ["confirmationTemplate", ""], ["mapSetDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check-circle", "!text-5xl", "text-success"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-3"], [1, "flex", "border-b", "pb-2"], [1, "w-1/3", "font-medium", "text-secondary"], [1, "w-2/3", "font-semibold", "text-primary"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0", "text-muted"]], template: function MapSetCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function MapSetCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function MapSetCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, MapSetCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, MapSetCreateWizardComponent_ng_template_3_Template, 36, 40, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    MapSetDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MapSetCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-map-set-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      MapSetDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Map Set Details -->
<ng-template #mapSetDetailsStep let-step>
  <div class="p-fluid">
    <app-map-set-details-section
      #mapSetDetailsForm
      [createMode]="true"
      [entity]="newMapSet()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateMapSetDetails($event)"
    ></app-map-set-details-section>
  </div>
</ng-template>

<!-- Step 2: Confirmation -->
<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check-circle !text-5xl text-success"></span>

    <div class="flex-1">
      <div class="grid">
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate: { entity: ('ADMIN.ENTITIES.MAP_SET' | translate) }
            }}
          </p>

          <div class="space-y-3">
            <div class="flex border-b pb-2">
              <div class="w-1/3 font-medium text-secondary">
                {{ 'ADMIN.MAP_SETS.DETAILS.MODAL.FIELDS.NAME' | translate }}
              </div>
              <div class="w-2/3 font-semibold text-primary">
                {{
                  newMapSet().name || ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
            <div class="flex border-b pb-2">
              <div class="w-1/3 font-medium text-secondary">
                {{ 'ADMIN.MAP_SETS.DETAILS.FIELDS.LAYERS' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  'ADMIN.MAP_SETS.WIZARD.CONFIRMATION.LAYER_COUNT'
                    | translate: { count: newMapSet().layers?.length ?? 0 }
                }}
              </div>
            </div>
            <div class="flex border-b pb-2">
              <div class="w-1/3 font-medium text-secondary">
                {{ 'ADMIN.MAP_SETS.DETAILS.FIELDS.OVERLAYS' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  'ADMIN.MAP_SETS.WIZARD.CONFIRMATION.OVERLAY_COUNT'
                    | translate: { count: newMapSet().overlays?.length ?? 0 }
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0 text-muted">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate: { entity: ('ADMIN.ENTITIES.MAP_SET' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { mapSetDetailsStep: [{ type: ViewChild, args: ["mapSetDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], mapSetDetailsForm: [{ type: ViewChild, args: ["mapSetDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(MapSetCreateWizardComponent, { className: "MapSetCreateWizardComponent", filePath: "src/app/features/administration/components/map-sets/map-set-create-wizard/map-set-create-wizard.component.ts", lineNumber: 46 });
})();

// src/app/features/administration/configs/map-sets.grid.config.ts
var MapSetsGridConfig = class _MapSetsGridConfig {
  mapSetsService = inject(MapSetsDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  defaultStateValues = [MapSetListItem.StateEnum.Active];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  params = { ownerId: "" };
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(this.filterState()), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  entityType = "map-sets";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  dataService = this.mapSetsService;
  ownerId = "";
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.MAP_SETS.COLUMNS.NAME"),
        filter: true,
        filterType: "text",
        filterMatchMode: "contains"
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.MAP_SETS.COLUMNS.STATE"),
        valueGetter: (rowData) => isDefined(rowData.state) ? this.translate.instant(`SHARED.COMMON.STATUS.${rowData.state.toUpperCase()}`) : "",
        filter: true,
        filterType: "multiselect",
        filterMatchMode: "in",
        filterConfig: {
          type: "multiselect",
          options: [
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
              value: MapSetListItem.StateEnum.Active
            },
            {
              label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
              value: MapSetListItem.StateEnum.Deleted
            }
          ],
          defaultSelected: this.defaultStateValues
        }
      }
    ];
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    } else if (isDefined(this.params["distributorId"])) {
      this.ownerId = String(this.params["distributorId"]);
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.MAP_SETS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField)) {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  onFilterChange(field, value) {
    const column = this.columns.find((col) => col.field === field);
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), field, value, void 0, column?.filterMatchMode);
    this.filterState.set(newFilters);
  }
  onAdd() {
    this.openMapSetCreationDialog(this.ownerId).subscribe();
  }
  openMapSetCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(MapSetCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.MAP_SET")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef?.onClose;
  }
  static \u0275fac = function MapSetsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _MapSetsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _MapSetsGridConfig, factory: _MapSetsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MapSetsGridConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// src/app/features/administration/configs/admin-grid-configs-registration.ts
var GRID_ENTITY_TYPES = {
  // Client entity types
  CLIENT_DEVICE_PROVIDERS: "client-device-providers",
  CLIENT_GEOFENCES: "client-geofences",
  CLIENT_ALERTS: "client-alerts",
  CLIENT_HARDWARE_DEVICES: "client-hardware-devices",
  CLIENT_ASSET_TAGS: "client-asset-tags",
  CLIENT_SIM_CARDS: "client-sim-cards",
  CLIENT_SMS_GATEWAY_PROVIDERS: "client-sms-gateway-providers",
  CLIENT_IO_TYPES: "client-io-types",
  CLIENT_DEVICE_CONFIG_PROFILES: "client-device-config-profiles",
  CLIENT_ASSET_RATING_PROFILES: "client-asset-rating-profiles",
  CLIENT_ASSET_STATE_PROFILES: "client-asset-state-profiles",
  CLIENT_OVERSPEED_PROFILES: "client-overspeed-profiles",
  CLIENT_CUSTOM_FIELDS: "client-custom-fields",
  CLIENT_USERS: "client-users",
  CLIENT_USER_ROLES: "client-user-roles",
  CLIENT_ROAD_PROFILES: "client-road-profiles",
  CLIENT_GEO_LOCK_PROFILES: "client-geo-lock-profiles",
  // Vendor entity types
  VENDOR_ROAD_PROFILES: "vendor-road-profiles",
  VENDOR_OVERSPEED_PROFILES: "vendor-overspeed-profiles",
  VENDOR_IO_TYPES: "vendor-io-types",
  VENDOR_DEVICE_CONFIG_PROFILES: "vendor-device-config-profiles",
  VENDOR_THEMES: "vendor-themes",
  VENDOR_CUSTOM_FIELDS: "vendor-custom-fields",
  VENDOR_SMS_GATEWAY_PROVIDERS: "vendor-sms-gateway-providers",
  VENDOR_EMAIL_PROVIDERS: "vendor-email-providers",
  VENDOR_USERS: "vendor-users",
  VENDOR_USER_ROLES: "vendor-user-roles",
  VENDOR_CLIENTS: "vendor-clients",
  VENDOR_GEO_LOCK_PROFILES: "vendor-geo-lock-profiles",
  VENDOR_ALERTS: "vendor-alerts",
  VENDOR_EXPORT_TASKS: "vendor-export-tasks",
  VENDOR_MAP_SETS: "vendor-map-sets",
  // Distributor entity types
  DISTRIBUTOR_VENDORS: "distributor-vendors",
  DISTRIBUTOR_USERS: "distributor-users",
  DISTRIBUTOR_USER_ROLES: "distributor-user-roles",
  DISTRIBUTOR_ROAD_PROFILES: "distributor-road-profiles",
  DISTRIBUTOR_THEMES: "distributor-themes",
  DISTRIBUTOR_DEVICE_PROVIDERS: "distributor-device-providers",
  DISTRIBUTOR_EMAIL_PROVIDERS: "distributor-email-providers",
  DISTRIBUTOR_EXPORT_TASKS: "distributor-export-tasks",
  DISTRIBUTOR_MAP_SETS: "distributor-map-sets",
  // Super Admin entity types
  SUPER_ADMIN_DISTRIBUTORS: "super-admin-distributors",
  SUPER_ADMIN_USERS: "system-users",
  SUPER_ADMIN_USER_ROLES: "system-user-roles",
  CLIENT_CONTACTS: "client-contacts"
};
function createGridConfigEntry(entityType, config) {
  return { entityType, config };
}
var CLIENT_CONFIGS = [
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_DEVICE_PROVIDERS, DeviceProvidersGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_GEOFENCES, GeofencesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_ALERTS, AlertsGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_HARDWARE_DEVICES, DevicesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_ASSET_TAGS, AssetTagsGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_SIM_CARDS, SimCardsGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_SMS_GATEWAY_PROVIDERS, SmsGatewayProvidersGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_IO_TYPES, IoTypesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_DEVICE_CONFIG_PROFILES, DeviceConfigProfilesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_ASSET_RATING_PROFILES, AssetRatingProfilesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_ASSET_STATE_PROFILES, AssetStateProfilesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_OVERSPEED_PROFILES, OverspeedProfilesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_CUSTOM_FIELDS, CustomFieldsGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_USERS, UsersGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_USER_ROLES, UserRolesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_ROAD_PROFILES, RoadProfilesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_GEO_LOCK_PROFILES, GeoLockProfilesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.CLIENT_CONTACTS, ContactsGridConfig)
];
var VENDOR_CONFIGS = [
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_ROAD_PROFILES, RoadProfilesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_OVERSPEED_PROFILES, OverspeedProfilesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_IO_TYPES, IoTypesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_DEVICE_CONFIG_PROFILES, DeviceConfigProfilesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_THEMES, ThemesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_CUSTOM_FIELDS, CustomFieldsGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_SMS_GATEWAY_PROVIDERS, SmsGatewayProvidersGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_EMAIL_PROVIDERS, EmailProvidersGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_USERS, UsersGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_USER_ROLES, UserRolesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_CLIENTS, ClientsGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_GEO_LOCK_PROFILES, GeoLockProfilesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_ALERTS, AlertsGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_EXPORT_TASKS, ExportTasksGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.VENDOR_MAP_SETS, MapSetsGridConfig)
];
var DISTRIBUTOR_CONFIGS = [
  createGridConfigEntry(GRID_ENTITY_TYPES.DISTRIBUTOR_VENDORS, VendorsGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.DISTRIBUTOR_USERS, UsersGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.DISTRIBUTOR_USER_ROLES, UserRolesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.DISTRIBUTOR_ROAD_PROFILES, RoadProfilesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.DISTRIBUTOR_THEMES, ThemesGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.DISTRIBUTOR_DEVICE_PROVIDERS, DeviceProvidersGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.DISTRIBUTOR_EMAIL_PROVIDERS, EmailProvidersGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.DISTRIBUTOR_EXPORT_TASKS, ExportTasksGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.DISTRIBUTOR_MAP_SETS, MapSetsGridConfig)
];
var SUPER_ADMIN_CONFIGS = [
  createGridConfigEntry(GRID_ENTITY_TYPES.SUPER_ADMIN_DISTRIBUTORS, DistributorsGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.SUPER_ADMIN_USERS, UsersGridConfig),
  createGridConfigEntry(GRID_ENTITY_TYPES.SUPER_ADMIN_USER_ROLES, UserRolesGridConfig)
];
var ALL_GRID_CONFIGS = [
  ...CLIENT_CONFIGS,
  ...VENDOR_CONFIGS,
  ...DISTRIBUTOR_CONFIGS,
  ...SUPER_ADMIN_CONFIGS
];
var ADMIN_GRID_CONFIGS_PROVIDER = {
  provide: GRID_CONFIGS,
  useValue: ALL_GRID_CONFIGS,
  multi: true
};

// src/app/features/administration/components/asset-categories/asset-category-create-wizard/asset-category-create-wizard.component.ts
var _c032 = ["assetCategoryDetailsStep"];
var _c128 = ["confirmationTemplate"];
var _c227 = ["assetCategoryDetailsForm"];
var _c326 = (a0) => ({ entity: a0 });
function AssetCategoryCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-asset-category-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function AssetCategoryCreateWizardComponent_ng_template_1_Template_app_asset_category_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function AssetCategoryCreateWizardComponent_ng_template_1_Template_app_asset_category_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateAssetCategoryDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newAssetCategory())("ownerId", ctx_r1.ownerId);
  }
}
function AssetCategoryCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(24, "div", 16)(25, "p", 17);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275pipe(28, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_6_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 8, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(24, _c326, \u0275\u0275pipeBind1(7, 6, "ADMIN.ENTITIES.ASSET_CATEGORY"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 11, "ADMIN.ASSET_CATEGORIES.DETAILS.FIELDS.PARENT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_6_0 = ctx_r1.newAssetCategory().parent) == null ? null : tmp_6_0.name) || \u0275\u0275pipeBind1(16, 13, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 15, "ADMIN.ASSET_CATEGORIES.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAssetCategory().name || \u0275\u0275pipeBind1(23, 17, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(28, 21, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(26, _c326, \u0275\u0275pipeBind1(27, 19, "ADMIN.ENTITIES.ASSET_CATEGORY"))), " ");
  }
}
var AssetCategoryCreateWizardComponent = class _AssetCategoryCreateWizardComponent {
  assetCategoryDetailsStep = viewChild("assetCategoryDetailsStep", ...ngDevMode ? [{ debugName: "assetCategoryDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  assetCategoryDetailsForm = viewChild("assetCategoryDetailsForm", ...ngDevMode ? [{ debugName: "assetCategoryDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  cacheInvalidationService = inject(CacheInvalidationService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cdr = inject(ChangeDetectorRef);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // AssetCategory model to collect data across steps
  newAssetCategory = signal({}, ...ngDevMode ? [{ debugName: "newAssetCategory" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const detailsStep = this.assetCategoryDetailsStep();
      const confirmStep = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (detailsStep && confirmStep) {
        this.steps = [
          {
            value: 1,
            header: this.translate.instant("ADMIN.ASSET_CATEGORIES.WIZARD.STEPS.DETAILS"),
            content: detailsStep,
            complete: false,
            validate: () => this.assetCategoryDetailsForm()?.validate() ?? false
          },
          {
            value: 2,
            header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
            content: confirmStep,
            complete: true
          }
        ];
        this.cdr.detectChanges();
      }
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newAssetCategory.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.newAssetCategory.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the assetCategory state with form data
  updateAssetCategoryDetails(formData) {
    this.newAssetCategory.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const assetCategoryData = this.newAssetCategory();
    this.isSubmitting.set(true);
    this.entitiesService.createAssetCategory(assetCategoryData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.cacheInvalidationService.clearAssetCategoryCaches();
        this.broadcastService.broadcast("entity-updated", {
          entityType: "assetCategory",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating asset category:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newAssetCategory.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function AssetCategoryCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetCategoryCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _AssetCategoryCreateWizardComponent, selectors: [["app-asset-category-create-wizard"]], viewQuery: function AssetCategoryCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.assetCategoryDetailsStep, _c032, 5)(ctx.confirmationTemplate, _c128, 5)(ctx.assetCategoryDetailsForm, _c227, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["assetCategoryDetailsStep", ""], ["confirmationTemplate", ""], ["assetCategoryDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function AssetCategoryCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function AssetCategoryCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function AssetCategoryCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, AssetCategoryCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, AssetCategoryCreateWizardComponent_ng_template_3_Template, 29, 28, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    AssetCategoryDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetCategoryCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-asset-category-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      AssetCategoryDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Asset Category Details -->
<ng-template #assetCategoryDetailsStep let-step>
  <div class="p-fluid">
    <app-asset-category-details-section
      #assetCategoryDetailsForm
      [createMode]="true"
      [entity]="newAssetCategory()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateAssetCategoryDetails($event)"
    ></app-asset-category-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Asset Category Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.ASSET_CATEGORY' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.ASSET_CATEGORIES.DETAILS.FIELDS.PARENT' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newAssetCategory().parent?.name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ASSET_CATEGORIES.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newAssetCategory().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.ASSET_CATEGORY' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { assetCategoryDetailsStep: [{ type: ViewChild, args: ["assetCategoryDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], assetCategoryDetailsForm: [{ type: ViewChild, args: ["assetCategoryDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(AssetCategoryCreateWizardComponent, { className: "AssetCategoryCreateWizardComponent", filePath: "src/app/features/administration/components/asset-categories/asset-category-create-wizard/asset-category-create-wizard.component.ts", lineNumber: 47 });
})();

// src/app/features/administration/configs/asset-categories.grid.config.ts
var AssetCategoriesGridConfig = class _AssetCategoriesGridConfig {
  assetCategoriesService = inject(AssetCategoriesDataAdapter);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  params = { clientId: "" };
  entityType = "asset-categories";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  defaultVisibleColumns = ["name", "modifiedDate"];
  dataService = this.assetCategoriesService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.ASSET_CATEGORIES.COLUMNS.NAME"),
        filterType: "text"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.ASSET_CATEGORIES.COLUMNS.MODIFIED"),
        valueGetter: (rowData) => isDefined(rowData?.entity?.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData?.entity?.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  initialize(params) {
    this.params = params;
  }
  getTitle() {
    return this.translate.instant("ADMIN.ASSET_CATEGORIES.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField) && event.sortField !== "") {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.params["clientId"],
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: DEFAULT_LIST_SIZE,
      sortField
    };
  }
  onAdd() {
    const clientId = this.params["clientId"];
    this.openAssetCategoryCreationDialog(clientId).pipe(take(1)).subscribe();
  }
  openAssetCategoryCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(AssetCategoryCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.ASSET_CATEGORY")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function AssetCategoriesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetCategoriesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AssetCategoriesGridConfig, factory: _AssetCategoriesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetCategoriesGridConfig, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/features/administration/components/asset-groups/asset-group-create-wizard/asset-group-create-wizard.component.ts
var _c033 = ["assetGroupDetailsStep"];
var _c129 = ["confirmationTemplate"];
var _c228 = ["assetGroupDetailsForm"];
var _c327 = (a0) => ({ entity: a0 });
function AssetGroupCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-asset-group-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function AssetGroupCreateWizardComponent_ng_template_1_Template_app_asset_group_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function AssetGroupCreateWizardComponent_ng_template_1_Template_app_asset_group_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateAssetGroupDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newAssetGroup())("ownerId", ctx_r1.ownerId);
  }
}
function AssetGroupCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(24, "div", 16)(25, "p", 17);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275pipe(28, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_6_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 8, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(24, _c327, \u0275\u0275pipeBind1(7, 6, "ADMIN.ENTITIES.ASSET_GROUP"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 11, "ADMIN.ASSET_GROUPS.DETAILS.FIELDS.PARENT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_6_0 = ctx_r1.newAssetGroup().parent) == null ? null : tmp_6_0.name) || \u0275\u0275pipeBind1(16, 13, "ADMIN.ASSET_GROUPS.DETAILS.PARENT_OPTIONS.ROOT"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 15, "ADMIN.ASSET_GROUPS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAssetGroup().name || \u0275\u0275pipeBind1(23, 17, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(28, 21, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(26, _c327, \u0275\u0275pipeBind1(27, 19, "ADMIN.ENTITIES.ASSET_GROUP"))), " ");
  }
}
var AssetGroupCreateWizardComponent = class _AssetGroupCreateWizardComponent {
  assetGroupDetailsStep = viewChild("assetGroupDetailsStep", ...ngDevMode ? [{ debugName: "assetGroupDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  assetGroupDetailsForm = viewChild("assetGroupDetailsForm", ...ngDevMode ? [{ debugName: "assetGroupDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  cacheInvalidationService = inject(CacheInvalidationService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cdr = inject(ChangeDetectorRef);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // AssetGroup model to collect data across steps
  newAssetGroup = signal({}, ...ngDevMode ? [{ debugName: "newAssetGroup" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const detailsStep = this.assetGroupDetailsStep();
      const confirmStep = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (detailsStep && confirmStep) {
        this.steps = [
          {
            value: 1,
            header: this.translate.instant("ADMIN.ASSET_GROUPS.WIZARD.STEPS.DETAILS"),
            content: detailsStep,
            complete: false,
            validate: () => this.assetGroupDetailsForm()?.validate() ?? false
          },
          {
            value: 2,
            header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
            content: confirmStep,
            complete: true
          }
        ];
        this.cdr.detectChanges();
      }
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newAssetGroup.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.newAssetGroup.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the assetGroup state with form data
  updateAssetGroupDetails(formData) {
    this.newAssetGroup.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const assetGroupData = this.newAssetGroup();
    this.isSubmitting.set(true);
    this.entitiesService.createAssetGroup(assetGroupData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.cacheInvalidationService.clearAssetGroupCaches();
        this.broadcastService.broadcast("entity-updated", {
          entityType: "asset-group",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating asset group:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newAssetGroup.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function AssetGroupCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetGroupCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _AssetGroupCreateWizardComponent, selectors: [["app-asset-group-create-wizard"]], viewQuery: function AssetGroupCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.assetGroupDetailsStep, _c033, 5)(ctx.confirmationTemplate, _c129, 5)(ctx.assetGroupDetailsForm, _c228, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["assetGroupDetailsStep", ""], ["confirmationTemplate", ""], ["assetGroupDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function AssetGroupCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function AssetGroupCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function AssetGroupCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, AssetGroupCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, AssetGroupCreateWizardComponent_ng_template_3_Template, 29, 28, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    AssetGroupDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetGroupCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-asset-group-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      AssetGroupDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Asset Group Details -->
<ng-template #assetGroupDetailsStep let-step>
  <div class="p-fluid">
    <app-asset-group-details-section
      #assetGroupDetailsForm
      [createMode]="true"
      [entity]="newAssetGroup()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateAssetGroupDetails($event)"
    ></app-asset-group-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Asset Group Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.ASSET_GROUP' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ASSET_GROUPS.DETAILS.FIELDS.PARENT' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newAssetGroup().parent?.name ||
                    ('ADMIN.ASSET_GROUPS.DETAILS.PARENT_OPTIONS.ROOT'
                      | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ASSET_GROUPS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newAssetGroup().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.ASSET_GROUP' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { assetGroupDetailsStep: [{ type: ViewChild, args: ["assetGroupDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], assetGroupDetailsForm: [{ type: ViewChild, args: ["assetGroupDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(AssetGroupCreateWizardComponent, { className: "AssetGroupCreateWizardComponent", filePath: "src/app/features/administration/components/asset-groups/asset-group-create-wizard/asset-group-create-wizard.component.ts", lineNumber: 47 });
})();

// src/app/features/administration/configs/asset-groups.grid.config.ts
var AssetGroupsGridConfig = class _AssetGroupsGridConfig {
  assetGroupsService = inject(AssetGroupsDataAdapter);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  params = { clientId: "" };
  entityType = "asset-groups";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  defaultVisibleColumns = ["name", "modifiedDate"];
  dataService = this.assetGroupsService;
  ownerId = "";
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.ASSET_GROUPS.COLUMNS.NAME"),
        filterType: "text"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.ASSET_GROUPS.COLUMNS.MODIFIED"),
        valueGetter: (rowData) => isDefined(rowData?.entity?.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData?.entity?.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.ASSET_GROUPS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField) && event.sortField !== "") {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: DEFAULT_LIST_SIZE,
      sortField
    };
  }
  onAdd() {
    const clientId = this.params["clientId"];
    this.openAssetGroupCreationDialog(clientId).subscribe();
  }
  openAssetGroupCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(AssetGroupCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.ASSET_GROUP")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function AssetGroupsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetGroupsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AssetGroupsGridConfig, factory: _AssetGroupsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetGroupsGridConfig, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/features/administration/components/company-groups/company-group-create-wizard/company-group-create-wizard.component.ts
var _c034 = ["companyGroupDetailsStep"];
var _c130 = ["confirmationTemplate"];
var _c229 = ["companyGroupDetailsForm"];
var _c328 = (a0) => ({ entity: a0 });
function CompanyGroupCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-company-group-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function CompanyGroupCreateWizardComponent_ng_template_1_Template_app_company_group_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function CompanyGroupCreateWizardComponent_ng_template_1_Template_app_company_group_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateCompanyGroupDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newCompanyGroup())("ownerId", ctx_r1.ownerId);
  }
}
function CompanyGroupCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(24, "div", 16)(25, "p", 17);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275pipe(28, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_5_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 8, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(24, _c328, \u0275\u0275pipeBind1(7, 6, "ADMIN.ENTITIES.COMPANY_GROUP"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 11, "ADMIN.COMPANY_GROUPS.DETAILS.FIELDS.PARENT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_5_0 = ctx_r1.newCompanyGroup().parent) == null ? null : tmp_5_0.name) || \u0275\u0275pipeBind1(16, 13, "ADMIN.COMPANY_GROUPS.DETAILS.PARENT_OPTIONS.ROOT"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 15, "ADMIN.COMPANY_GROUPS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newCompanyGroup().name || \u0275\u0275pipeBind1(23, 17, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(28, 21, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(26, _c328, \u0275\u0275pipeBind1(27, 19, "ADMIN.ENTITIES.COMPANY_GROUP"))), " ");
  }
}
var CompanyGroupCreateWizardComponent = class _CompanyGroupCreateWizardComponent {
  companyGroupDetailsStep = viewChild("companyGroupDetailsStep", ...ngDevMode ? [{ debugName: "companyGroupDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  companyGroupDetailsForm = viewChild("companyGroupDetailsForm", ...ngDevMode ? [{ debugName: "companyGroupDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  accountsService = inject(AccountsService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cdr = inject(ChangeDetectorRef);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // CompanyGroup model to collect data across steps
  newCompanyGroup = signal({}, ...ngDevMode ? [{ debugName: "newCompanyGroup" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const detailsStep = this.companyGroupDetailsStep();
      const confirmStep = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (detailsStep && confirmStep) {
        this.steps = [
          {
            value: 1,
            header: this.translate.instant("ADMIN.COMPANY_GROUPS.DETAILS.TITLE"),
            content: detailsStep,
            complete: false,
            validate: () => this.companyGroupDetailsForm()?.validate() ?? false
          },
          {
            value: 2,
            header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
            content: confirmStep,
            complete: true
          }
        ];
        this.cdr.detectChanges();
      }
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newCompanyGroup.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.newCompanyGroup.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the companyGroup state with form data
  updateCompanyGroupDetails(formData) {
    this.newCompanyGroup.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const companyGroupData = this.newCompanyGroup();
    this.isSubmitting.set(true);
    this.accountsService.createCompanyGroup(companyGroupData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "company-group",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating company group:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newCompanyGroup.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function CompanyGroupCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CompanyGroupCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _CompanyGroupCreateWizardComponent, selectors: [["app-company-group-create-wizard"]], viewQuery: function CompanyGroupCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.companyGroupDetailsStep, _c034, 5)(ctx.confirmationTemplate, _c130, 5)(ctx.companyGroupDetailsForm, _c229, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["companyGroupDetailsStep", ""], ["confirmationTemplate", ""], ["companyGroupDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function CompanyGroupCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function CompanyGroupCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function CompanyGroupCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, CompanyGroupCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, CompanyGroupCreateWizardComponent_ng_template_3_Template, 29, 28, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    CompanyGroupDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CompanyGroupCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-company-group-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      CompanyGroupDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Company Group Details -->
<ng-template #companyGroupDetailsStep>
  <div class="p-fluid">
    <app-company-group-details-section
      #companyGroupDetailsForm
      [createMode]="true"
      [entity]="newCompanyGroup()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateCompanyGroupDetails($event)"
    ></app-company-group-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Company Group Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.COMPANY_GROUP' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.COMPANY_GROUPS.DETAILS.FIELDS.PARENT' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newCompanyGroup().parent?.name ||
                    ('ADMIN.COMPANY_GROUPS.DETAILS.PARENT_OPTIONS.ROOT'
                      | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.COMPANY_GROUPS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newCompanyGroup().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.COMPANY_GROUP' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { companyGroupDetailsStep: [{ type: ViewChild, args: ["companyGroupDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], companyGroupDetailsForm: [{ type: ViewChild, args: ["companyGroupDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(CompanyGroupCreateWizardComponent, { className: "CompanyGroupCreateWizardComponent", filePath: "src/app/features/administration/components/company-groups/company-group-create-wizard/company-group-create-wizard.component.ts", lineNumber: 46 });
})();

// src/app/features/administration/configs/company-groups.grid.config.ts
var CompanyGroupsGridConfig = class _CompanyGroupsGridConfig {
  companyGroupsService = inject(CompanyGroupsDataAdapter);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  params = {};
  entityType = "company-groups";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  defaultVisibleColumns = ["name", "modifiedDate"];
  dataService = this.companyGroupsService;
  ownerId = "";
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.COMPANY_GROUPS.COLUMNS.NAME"),
        filterType: "text"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.COMPANY_GROUPS.COLUMNS.MODIFIED"),
        valueGetter: (rowData) => isDefined(rowData.entity.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.entity.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  initialize(params) {
    this.params = params;
    if (isDefined(this.params["clientId"])) {
      this.ownerId = String(this.params["clientId"]);
    } else if (isDefined(this.params["vendorId"])) {
      this.ownerId = String(this.params["vendorId"]);
    } else if (isDefined(this.params["distributorId"])) {
      this.ownerId = String(this.params["distributorId"]);
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.COMPANY_GROUPS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField) && event.sortField !== "") {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.ownerId,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: DEFAULT_LIST_SIZE,
      sortField
    };
  }
  onAdd() {
    const clientId = this.ownerId;
    this.openCompanyGroupCreationDialog(clientId).pipe(take(1)).subscribe();
  }
  openCompanyGroupCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(CompanyGroupCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.COMPANY_GROUP")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function CompanyGroupsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CompanyGroupsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _CompanyGroupsGridConfig, factory: _CompanyGroupsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CompanyGroupsGridConfig, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/features/administration/components/cost-centres/cost-centre-create-wizard/cost-centre-create-wizard.component.ts
var _c035 = ["costCentreDetailsStep"];
var _c131 = ["confirmationTemplate"];
var _c230 = ["costCentreDetailsForm"];
var _c329 = (a0) => ({ entity: a0 });
function CostCentreCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-cost-centre-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function CostCentreCreateWizardComponent_ng_template_1_Template_app_cost_centre_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function CostCentreCreateWizardComponent_ng_template_1_Template_app_cost_centre_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateCostCentreDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newCostCentre())("ownerId", ctx_r1.ownerId);
  }
}
function CostCentreCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(24, "div", 16)(25, "p", 17);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275pipe(28, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_6_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 8, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(24, _c329, \u0275\u0275pipeBind1(7, 6, "ADMIN.ENTITIES.COST_CENTRE"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 11, "ADMIN.COST_CENTRES.DETAILS.FIELDS.PARENT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_6_0 = ctx_r1.newCostCentre().parent) == null ? null : tmp_6_0.name) || \u0275\u0275pipeBind1(16, 13, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 15, "ADMIN.COST_CENTRES.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newCostCentre().name || \u0275\u0275pipeBind1(23, 17, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(28, 21, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(26, _c329, \u0275\u0275pipeBind1(27, 19, "ADMIN.ENTITIES.COST_CENTRE"))), " ");
  }
}
var CostCentreCreateWizardComponent = class _CostCentreCreateWizardComponent {
  costCentreDetailsStep = viewChild("costCentreDetailsStep", ...ngDevMode ? [{ debugName: "costCentreDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  costCentreDetailsForm = viewChild("costCentreDetailsForm", ...ngDevMode ? [{ debugName: "costCentreDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  cacheInvalidationService = inject(CacheInvalidationService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cdr = inject(ChangeDetectorRef);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // CostCentre model to collect data across steps
  newCostCentre = signal({}, ...ngDevMode ? [{ debugName: "newCostCentre" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const detailsStep = this.costCentreDetailsStep();
      const confirmStep = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (detailsStep && confirmStep) {
        this.steps = [
          {
            value: 1,
            header: this.translate.instant("ADMIN.COST_CENTRES.DETAILS.TITLE"),
            content: detailsStep,
            complete: false,
            validate: () => this.costCentreDetailsForm()?.validate() ?? false
          },
          {
            value: 2,
            header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
            content: confirmStep,
            complete: true
          }
        ];
        this.cdr.detectChanges();
      }
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newCostCentre.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.newCostCentre.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the costCentre state with form data
  updateCostCentreDetails(formData) {
    this.newCostCentre.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const costCentreData = this.newCostCentre();
    this.isSubmitting.set(true);
    this.entitiesService.createCostCentre(costCentreData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.cacheInvalidationService.clearCostCentreCaches();
        this.broadcastService.broadcast("entity-updated", {
          entityType: "cost-centre",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating cost centre:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newCostCentre.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function CostCentreCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CostCentreCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _CostCentreCreateWizardComponent, selectors: [["app-cost-centre-create-wizard"]], viewQuery: function CostCentreCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.costCentreDetailsStep, _c035, 5)(ctx.confirmationTemplate, _c131, 5)(ctx.costCentreDetailsForm, _c230, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["costCentreDetailsStep", ""], ["confirmationTemplate", ""], ["costCentreDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function CostCentreCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function CostCentreCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function CostCentreCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, CostCentreCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, CostCentreCreateWizardComponent_ng_template_3_Template, 29, 28, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    CostCentreDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CostCentreCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-cost-centre-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      CostCentreDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Cost Centre Details -->
<ng-template #costCentreDetailsStep let-step>
  <div class="p-fluid">
    <app-cost-centre-details-section
      #costCentreDetailsForm
      [createMode]="true"
      [entity]="newCostCentre()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateCostCentreDetails($event)"
    ></app-cost-centre-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Cost Centre Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.COST_CENTRE' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.COST_CENTRES.DETAILS.FIELDS.PARENT' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newCostCentre().parent?.name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.COST_CENTRES.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newCostCentre().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.COST_CENTRE' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { costCentreDetailsStep: [{ type: ViewChild, args: ["costCentreDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], costCentreDetailsForm: [{ type: ViewChild, args: ["costCentreDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(CostCentreCreateWizardComponent, { className: "CostCentreCreateWizardComponent", filePath: "src/app/features/administration/components/cost-centres/cost-centre-create-wizard/cost-centre-create-wizard.component.ts", lineNumber: 47 });
})();

// src/app/features/administration/configs/cost-centres.grid.config.ts
var CostCentresGridConfig = class _CostCentresGridConfig {
  costCentresService = inject(CostCentresDataAdapter);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  params = { clientId: "" };
  entityType = "cost-centres";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  defaultVisibleColumns = ["name", "modifiedDate"];
  dataService = this.costCentresService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.COST_CENTRES.COLUMNS.NAME"),
        filterType: "text"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.COST_CENTRES.COLUMNS.MODIFIED"),
        valueGetter: (rowData) => isDefined(rowData?.entity?.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData?.entity?.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  initialize(params) {
    this.params = params;
  }
  getTitle() {
    return this.translate.instant("ADMIN.COST_CENTRES.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField) && event.sortField !== "") {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.params["clientId"],
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: DEFAULT_LIST_SIZE,
      sortField
    };
  }
  onAdd() {
    const clientId = this.params["clientId"];
    this.openCostCentreCreationDialog(clientId).pipe(take(1)).subscribe();
  }
  openCostCentreCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(CostCentreCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.COST_CENTRE")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function CostCentresGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CostCentresGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _CostCentresGridConfig, factory: _CostCentresGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CostCentresGridConfig, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/features/administration/components/geofence-groups/geofence-group-create-wizard/geofence-group-create-wizard.component.ts
var _c036 = ["geofenceGroupDetailsStep"];
var _c132 = ["confirmationTemplate"];
var _c231 = ["geofenceGroupDetailsForm"];
var _c330 = (a0) => ({ entity: a0 });
function GeofenceGroupCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-geofence-group-details-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function GeofenceGroupCreateWizardComponent_ng_template_1_Template_app_geofence_group_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function GeofenceGroupCreateWizardComponent_ng_template_1_Template_app_geofence_group_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateGeofenceGroupDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newGeofenceGroup())("ownerId", ctx_r1.ownerId);
  }
}
function GeofenceGroupCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "p", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 12)(10, "div", 13)(11, "div", 14);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 15);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 13)(18, "div", 14);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 15);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(24, "div", 16)(25, "p", 17);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275pipe(28, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_6_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 8, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(24, _c330, \u0275\u0275pipeBind1(7, 6, "ADMIN.ENTITIES.ZONE_GROUP"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 11, "ADMIN.ZONE_GROUPS.DETAILS.FIELDS.PARENT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_6_0 = ctx_r1.newGeofenceGroup().parent) == null ? null : tmp_6_0.name) || \u0275\u0275pipeBind1(16, 13, "ADMIN.ZONE_GROUPS.DETAILS.PARENT_OPTIONS.ROOT"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 15, "ADMIN.ZONE_GROUPS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newGeofenceGroup().name || \u0275\u0275pipeBind1(23, 17, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(28, 21, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(26, _c330, \u0275\u0275pipeBind1(27, 19, "ADMIN.ENTITIES.ZONE_GROUP"))), " ");
  }
}
var GeofenceGroupCreateWizardComponent = class _GeofenceGroupCreateWizardComponent {
  geofenceGroupDetailsStep = viewChild("geofenceGroupDetailsStep", ...ngDevMode ? [{ debugName: "geofenceGroupDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  geofenceGroupDetailsForm = viewChild("geofenceGroupDetailsForm", ...ngDevMode ? [{ debugName: "geofenceGroupDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cdr = inject(ChangeDetectorRef);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  // GeofenceGroup model to collect data across steps
  newGeofenceGroup = signal({}, ...ngDevMode ? [{ debugName: "newGeofenceGroup" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  steps = [];
  previousStepIndex = 0;
  currentStepIndex = 0;
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const geofenceGroupDetailsStep = this.geofenceGroupDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (isNullOrUndefined(geofenceGroupDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.ZONE_GROUPS.WIZARD.STEPS.DETAILS"),
          content: geofenceGroupDetailsStep,
          complete: false,
          validate: () => this.geofenceGroupDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newGeofenceGroup.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
  }
  openWizard() {
    this.newGeofenceGroup.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
  }
  // Update the geofenceGroup state with form data
  updateGeofenceGroupDetails(formData) {
    this.newGeofenceGroup.update((current) => {
      const updated = __spreadValues({}, current);
      Object.keys(formData).forEach((key) => {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      });
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const geofenceGroupData = this.newGeofenceGroup();
    this.isSubmitting.set(true);
    this.entitiesService.createZoneGroup(geofenceGroupData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "geofence-group",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating geofence group:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newGeofenceGroup.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function GeofenceGroupCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _GeofenceGroupCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _GeofenceGroupCreateWizardComponent, selectors: [["app-geofence-group-create-wizard"]], viewQuery: function GeofenceGroupCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.geofenceGroupDetailsStep, _c036, 5)(ctx.confirmationTemplate, _c132, 5)(ctx.geofenceGroupDetailsForm, _c231, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["geofenceGroupDetailsStep", ""], ["confirmationTemplate", ""], ["geofenceGroupDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "mt-4", "pt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function GeofenceGroupCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function GeofenceGroupCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function GeofenceGroupCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, GeofenceGroupCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, GeofenceGroupCreateWizardComponent_ng_template_3_Template, 29, 28, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    GeofenceGroupDetailsSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GeofenceGroupCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-geofence-group-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      GeofenceGroupDetailsSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: GeofenceGroup Details -->
<ng-template #geofenceGroupDetailsStep let-step>
  <div class="p-fluid">
    <app-geofence-group-details-section
      #geofenceGroupDetailsForm
      [createMode]="true"
      [entity]="newGeofenceGroup()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateGeofenceGroupDetails($event)"
    ></app-geofence-group-details-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- GeofenceGroup Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate
                  : { entity: ('ADMIN.ENTITIES.ZONE_GROUP' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ZONE_GROUPS.DETAILS.FIELDS.PARENT' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newGeofenceGroup().parent?.name ||
                    ('ADMIN.ZONE_GROUPS.DETAILS.PARENT_OPTIONS.ROOT'
                      | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ZONE_GROUPS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newGeofenceGroup().name || ('SHARED.COMMON.NONE' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="mt-4 pt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate: { entity: ('ADMIN.ENTITIES.ZONE_GROUP' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { geofenceGroupDetailsStep: [{ type: ViewChild, args: ["geofenceGroupDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], geofenceGroupDetailsForm: [{ type: ViewChild, args: ["geofenceGroupDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(GeofenceGroupCreateWizardComponent, { className: "GeofenceGroupCreateWizardComponent", filePath: "src/app/features/administration/components/geofence-groups/geofence-group-create-wizard/geofence-group-create-wizard.component.ts", lineNumber: 46 });
})();

// src/app/features/administration/configs/geofence-groups.grid.config.ts
var GeofenceGroupsGridConfig = class _GeofenceGroupsGridConfig {
  geofenceGroupsService = inject(GeofenceGroupsDataAdapter);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  params = { clientId: "" };
  entityType = "geofence-groups";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  defaultVisibleColumns = ["name", "modifiedDate"];
  dataService = this.geofenceGroupsService;
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.GEOFENCE_GROUPS.COLUMNS.NAME"),
        filterType: "text"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.GEOFENCE_GROUPS.COLUMNS.MODIFIED"),
        valueGetter: (rowData) => isDefined(rowData?.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  initialize(params) {
    this.params = params;
  }
  getTitle() {
    return this.translate.instant("ADMIN.GEOFENCE_GROUPS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField) && event.sortField !== "") {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.params["clientId"],
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: DEFAULT_LIST_SIZE,
      sortField
    };
  }
  onAdd() {
    const clientId = this.params["clientId"];
    this.openGeofenceGroupCreationDialog(clientId).subscribe();
  }
  openGeofenceGroupCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(GeofenceGroupCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.ZONE_GROUP")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function GeofenceGroupsGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _GeofenceGroupsGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _GeofenceGroupsGridConfig, factory: _GeofenceGroupsGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GeofenceGroupsGridConfig, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/features/administration/components/device-types/device-types-create-wizard/device-types-create-wizard.component.ts
var _c037 = ["deviceTypeDetailsStep"];
var _c133 = ["confirmationTemplate"];
var _c232 = ["deviceTypeDetailsForm"];
var _c331 = (a0) => ({ entity: a0 });
function DeviceTypesCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-device-types-information-section", 5, 2);
    \u0275\u0275listener("formValidityChange", function DeviceTypesCreateWizardComponent_ng_template_1_Template_app_device_types_information_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function DeviceTypesCreateWizardComponent_ng_template_1_Template_app_device_types_information_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateDeviceTypeDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newDeviceType())("ownerId", ctx_r1.ownerId);
  }
}
function DeviceTypesCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6);
    \u0275\u0275element(1, "span", 7);
    \u0275\u0275elementStart(2, "div", 8)(3, "div", 9)(4, "div", 10)(5, "h4", 11);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(8, "div", 12)(9, "div", 13)(10, "div", 14);
    \u0275\u0275text(11);
    \u0275\u0275pipe(12, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(13, "div", 15);
    \u0275\u0275text(14);
    \u0275\u0275pipe(15, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(16, "div", 13)(17, "div", 14);
    \u0275\u0275text(18);
    \u0275\u0275pipe(19, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(20, "div", 15);
    \u0275\u0275text(21);
    \u0275\u0275pipe(22, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(23, "div", 13)(24, "div", 14);
    \u0275\u0275text(25);
    \u0275\u0275pipe(26, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(27, "div", 15);
    \u0275\u0275text(28);
    \u0275\u0275pipe(29, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(30, "div", 13)(31, "div", 14);
    \u0275\u0275text(32);
    \u0275\u0275pipe(33, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(34, "div", 15);
    \u0275\u0275text(35);
    \u0275\u0275pipe(36, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(37, "div", 13)(38, "div", 14);
    \u0275\u0275text(39);
    \u0275\u0275pipe(40, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(41, "div", 15);
    \u0275\u0275text(42);
    \u0275\u0275pipe(43, "translate");
    \u0275\u0275elementEnd()()()()();
    \u0275\u0275elementStart(44, "div", 16)(45, "p", 17);
    \u0275\u0275text(46);
    \u0275\u0275pipe(47, "translate");
    \u0275\u0275pipe(48, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_12_0;
    let tmp_14_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(7, 12, "ADMIN.DEVICES.TYPES.DETAILS.TITLE"), " ");
    \u0275\u0275advance(5);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(12, 14, "ADMIN.DEVICES.TYPES.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newDeviceType().name || \u0275\u0275pipeBind1(15, 16, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(19, 18, "ADMIN.DEVICES.TYPES.DETAILS.FIELDS.SHORT_NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newDeviceType().shortName || \u0275\u0275pipeBind1(22, 20, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(26, 22, "ADMIN.DEVICES.TYPES.DETAILS.FIELDS.TAG"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newDeviceType().tag || \u0275\u0275pipeBind1(29, 24, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(33, 26, "ADMIN.DEVICES.TYPES.DETAILS.FIELDS.PARENT"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_12_0 = ctx_r1.newDeviceType().parent) == null ? null : tmp_12_0.name) || \u0275\u0275pipeBind1(36, 28, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(40, 30, "ADMIN.DEVICES.TYPES.DETAILS.FIELDS.DEVICE_PROVIDER"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_14_0 = ctx_r1.newDeviceType().deviceProviderType) == null ? null : tmp_14_0.name) || \u0275\u0275pipeBind1(43, 32, "SHARED.COMMON.NOT_PROVIDED"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(48, 36, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(39, _c331, \u0275\u0275pipeBind1(47, 34, "ADMIN.ENTITIES.DEVICE_TYPE"))), " ");
  }
}
var DeviceTypesCreateWizardComponent = class _DeviceTypesCreateWizardComponent {
  deviceTypeDetailsStep = viewChild("deviceTypeDetailsStep", ...ngDevMode ? [{ debugName: "deviceTypeDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  deviceTypeDetailsForm = viewChild("deviceTypeDetailsForm", ...ngDevMode ? [{ debugName: "deviceTypeDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  deviceTypesService = inject(DeviceTypesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cdr = inject(ChangeDetectorRef);
  languageService = inject(LanguageService);
  translate = inject(TranslateService);
  // Device type model to collect data across steps
  newDeviceType = signal({}, ...ngDevMode ? [{ debugName: "newDeviceType" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  steps = [];
  ownerId = null;
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  // Make Object available to template
  Object = Object;
  constructor() {
    effect(() => {
      const deviceTypeDetailsStep = this.deviceTypeDetailsStep();
      const confirmationTemplate = this.confirmationTemplate();
      if (isNullOrUndefined(deviceTypeDetailsStep) || isNullOrUndefined(confirmationTemplate)) {
        return;
      }
      this.steps = [
        {
          value: 1,
          header: this.translate.instant("ADMIN.DEVICES.TYPES.DETAILS.TITLE"),
          content: deviceTypeDetailsStep,
          complete: false,
          validate: () => this.deviceTypeDetailsForm()?.validate() ?? false
        },
        {
          value: 2,
          header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
          content: confirmationTemplate,
          complete: true
        }
      ];
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newDeviceType.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
    }
    this.formIsValid.set(false);
  }
  openWizard() {
    this.newDeviceType.set({});
    this.formIsValid.set(false);
    if (this.steps.length > 0) {
      this.steps[0].disableNext = true;
      this.steps[0].complete = false;
    }
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    this.steps[0].disableNext = !isValid;
    this.formIsValid.set(isValid);
    if (isDefined(this.steps[0])) {
      this.steps[0].complete = isValid;
    }
  }
  // Update the device type state with form data
  updateDeviceTypeDetails(formData) {
    this.newDeviceType.update((current) => {
      const updated = __spreadValues({}, current);
      for (const key of Object.keys(formData)) {
        if (key !== "ownerId") {
          updated[key] = formData[key];
        }
      }
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  onWizardComplete() {
    const deviceTypeData = this.newDeviceType();
    this.isSubmitting.set(true);
    this.deviceTypesService.createDeviceType(deviceTypeData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.broadcastService.broadcast("entity-updated", {
          entityType: "device-types",
          operation: "create",
          entityId: response.id
        });
        this.broadcastService.broadcast("tree-refresh-required", {
          entityType: "device-types",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating device type:", error);
      }
    });
  }
  // Update cancel to close dialog with no result
  onWizardCancel() {
    this.newDeviceType.set({});
    this.dialogRef.close();
  }
  static \u0275fac = function DeviceTypesCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DeviceTypesCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _DeviceTypesCreateWizardComponent, selectors: [["app-device-types-create-wizard"]], viewQuery: function DeviceTypesCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.deviceTypeDetailsStep, _c037, 5)(ctx.confirmationTemplate, _c133, 5)(ctx.deviceTypeDetailsForm, _c232, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(3);
    }
  }, decls: 5, vars: 5, consts: [["deviceTypeDetailsStep", ""], ["confirmationTemplate", ""], ["deviceTypeDetailsForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "lg:col-6"], [1, "mb-3", "text-900"], [1, "mb-4", "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "pt-4", "mt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function DeviceTypesCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 3);
      \u0275\u0275listener("complete", function DeviceTypesCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function DeviceTypesCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, DeviceTypesCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, DeviceTypesCreateWizardComponent_ng_template_3_Template, 49, 41, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    WizardComponent,
    ButtonModule,
    DeviceTypesInformationSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DeviceTypesCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-device-types-create-wizard", imports: [
      CommonModule,
      WizardComponent,
      ButtonModule,
      DeviceTypesInformationSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Device Type Details -->
<ng-template #deviceTypeDetailsStep let-step>
  <div class="p-fluid">
    <app-device-types-information-section
      #deviceTypeDetailsForm
      [createMode]="true"
      [entity]="newDeviceType()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateDeviceTypeDetails($event)"
    ></app-device-types-information-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Device Type Details Section -->
        <div class="col-12 lg:col-6">
          <h4 class="mb-3 text-900">
            {{ 'ADMIN.DEVICES.TYPES.DETAILS.TITLE' | translate }}
          </h4>
          <div class="mb-4 space-y-2">
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DEVICES.TYPES.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newDeviceType().name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.DEVICES.TYPES.DETAILS.FIELDS.SHORT_NAME' | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newDeviceType().shortName ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DEVICES.TYPES.DETAILS.FIELDS.TAG' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newDeviceType().tag ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.DEVICES.TYPES.DETAILS.FIELDS.PARENT' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newDeviceType().parent?.name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{
                  'ADMIN.DEVICES.TYPES.DETAILS.FIELDS.DEVICE_PROVIDER'
                    | translate
                }}:
              </div>
              <div class="w-2/3">
                {{
                  newDeviceType().deviceProviderType?.name ||
                    ('SHARED.COMMON.NOT_PROVIDED' | translate)
                }}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        class="pt-4 mt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate
                : { entity: ('ADMIN.ENTITIES.DEVICE_TYPE' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { deviceTypeDetailsStep: [{ type: ViewChild, args: ["deviceTypeDetailsStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], deviceTypeDetailsForm: [{ type: ViewChild, args: ["deviceTypeDetailsForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(DeviceTypesCreateWizardComponent, { className: "DeviceTypesCreateWizardComponent", filePath: "src/app/features/administration/components/device-types/device-types-create-wizard/device-types-create-wizard.component.ts", lineNumber: 44 });
})();

// src/app/features/administration/configs/device-types.grid.config.ts
var DeviceTypesGridConfig = class _DeviceTypesGridConfig {
  deviceTypesService = inject(DeviceTypesDataAdapter);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  dialogRef = null;
  dialogService = inject(DialogService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  params = { clientId: "" };
  dataService = this.deviceTypesService;
  entityType = "device-types";
  columns = this.buildColumns();
  columnsSignal = computed(() => {
    this.languageService.currentLanguage();
    return this.buildColumns();
  }, ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  buildColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.DEVICE_TYPES.COLUMNS.NAME"),
        filterType: "text"
      },
      {
        field: "shortName",
        header: this.translate.instant("ADMIN.DEVICE_TYPES.COLUMNS.SHORT_NAME"),
        filterType: "text"
      },
      {
        field: "tag",
        header: this.translate.instant("ADMIN.DEVICE_TYPES.COLUMNS.TAG"),
        filterType: "text"
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.DEVICE_TYPES.COLUMNS.STATUS"),
        filterType: "multiselect"
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.DEVICE_TYPES.COLUMNS.MODIFIED"),
        valueGetter: (rowData) => isDefined(rowData.modifiedDate) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData.modifiedDate), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : ""
      }
    ];
  }
  defaultVisibleColumns = ["name", "shortName", "state", "modifiedDate"];
  getTitle() {
    return this.translate.instant("ADMIN.DEVICE_TYPES.TITLE");
  }
  initialize(params) {
    this.params = params;
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField) && event.sortField !== "") {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: DEFAULT_SYSTEM_ID,
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: DEFAULT_LIST_SIZE,
      sortField,
      filters: ""
    };
  }
  onAdd() {
    const ownerId = DEFAULT_SYSTEM_ID;
    this.openDeviceTypeCreationDialog(ownerId).subscribe();
  }
  openDeviceTypeCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(DeviceTypesCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.DEVICE_TYPE")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: { ownerId },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function DeviceTypesGridConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DeviceTypesGridConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _DeviceTypesGridConfig, factory: _DeviceTypesGridConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DeviceTypesGridConfig, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/features/administration/components/assets/asset-create-wizard/asset-create-wizard.component.ts
var _c038 = ["assetDetailsStep"];
var _c134 = ["assetCategoriesStep"];
var _c233 = ["confirmationTemplate"];
var _c332 = ["infoSectionStep"];
var _c410 = ["assetDetailsForm"];
var _c59 = ["assetCategoriesForm"];
var _c6 = ["infoSectionForm"];
var _c7 = (a0) => ({ entity: a0 });
var _forTrack04 = ($index, $item) => $item.key;
function AssetCreateWizardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 8)(1, "app-asset-details-section", 9, 4);
    \u0275\u0275listener("formValidityChange", function AssetCreateWizardComponent_ng_template_1_Template_app_asset_details_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onDetailsFormValidityChange($event));
    })("formValueChange", function AssetCreateWizardComponent_ng_template_1_Template_app_asset_details_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateAssetDetails($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newAsset())("ownerId", ctx_r1.ownerId);
  }
}
function AssetCreateWizardComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 8)(1, "app-asset-categories-section", 9, 5);
    \u0275\u0275listener("formValidityChange", function AssetCreateWizardComponent_ng_template_3_Template_app_asset_categories_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onCategoriesFormValidityChange($event));
    })("formValueChange", function AssetCreateWizardComponent_ng_template_3_Template_app_asset_categories_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateAssetCategories($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newAsset())("ownerId", ctx_r1.ownerId);
  }
}
function AssetCreateWizardComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 8)(1, "app-info-section", 10, 6);
    \u0275\u0275listener("formValidityChange", function AssetCreateWizardComponent_ng_template_5_Template_app_info_section_formValidityChange_1_listener($event) {
      \u0275\u0275restoreView(_r4);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onInfoFormValidityChange($event));
    })("formValueChange", function AssetCreateWizardComponent_ng_template_5_Template_app_info_section_formValueChange_1_listener($event) {
      \u0275\u0275restoreView(_r4);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.updateInfoSection($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("createMode", true)("entity", ctx_r1.newAsset());
  }
}
function AssetCreateWizardComponent_ng_template_7_For_58_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 18)(1, "div", 19);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 20);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const category_r5 = ctx.$implicit;
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1("", category_r5.key, ":");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(category_r5.value);
  }
}
function AssetCreateWizardComponent_ng_template_7_Conditional_59_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 18)(1, "div", 19);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "div", 20);
    \u0275\u0275text(5);
    \u0275\u0275pipe(6, "translate");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(3, 2, "ADMIN.ASSETS.CATEGORIES.TITLE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(6, 4, "ADMIN.ASSETS.CATEGORIES.NO_CATEGORIES"), " ");
  }
}
function AssetCreateWizardComponent_ng_template_7_For_61_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 18)(1, "div", 19);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 20);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const field_r6 = ctx.$implicit;
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1("", field_r6.key, ":");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(field_r6.value);
  }
}
function AssetCreateWizardComponent_ng_template_7_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 11);
    \u0275\u0275element(1, "span", 12);
    \u0275\u0275elementStart(2, "div", 13)(3, "div", 14)(4, "div", 15)(5, "p", 16);
    \u0275\u0275text(6);
    \u0275\u0275pipe(7, "translate");
    \u0275\u0275pipe(8, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 17)(10, "div", 18)(11, "div", 19);
    \u0275\u0275text(12);
    \u0275\u0275pipe(13, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 20);
    \u0275\u0275text(15);
    \u0275\u0275pipe(16, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(17, "div", 18)(18, "div", 19);
    \u0275\u0275text(19);
    \u0275\u0275pipe(20, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "div", 20);
    \u0275\u0275text(22);
    \u0275\u0275pipe(23, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(24, "div", 18)(25, "div", 19);
    \u0275\u0275text(26);
    \u0275\u0275pipe(27, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "div", 20);
    \u0275\u0275text(29);
    \u0275\u0275pipe(30, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(31, "div", 18)(32, "div", 19);
    \u0275\u0275text(33);
    \u0275\u0275pipe(34, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(35, "div", 20);
    \u0275\u0275text(36);
    \u0275\u0275pipe(37, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(38, "div", 18)(39, "div", 19);
    \u0275\u0275text(40);
    \u0275\u0275pipe(41, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(42, "div", 20);
    \u0275\u0275text(43);
    \u0275\u0275pipe(44, "translate");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(45, "div", 18)(46, "div", 19);
    \u0275\u0275text(47);
    \u0275\u0275pipe(48, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(49, "div", 20);
    \u0275\u0275text(50);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(51, "div", 18)(52, "div", 19);
    \u0275\u0275text(53);
    \u0275\u0275pipe(54, "translate");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(55, "div", 20);
    \u0275\u0275text(56);
    \u0275\u0275elementEnd()();
    \u0275\u0275repeaterCreate(57, AssetCreateWizardComponent_ng_template_7_For_58_Template, 5, 2, "div", 18, _forTrack04);
    \u0275\u0275conditionalCreate(59, AssetCreateWizardComponent_ng_template_7_Conditional_59_Template, 7, 6, "div", 18);
    \u0275\u0275repeaterCreate(60, AssetCreateWizardComponent_ng_template_7_For_61_Template, 5, 2, "div", 18, _forTrack04);
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(62, "div", 21)(63, "p", 22);
    \u0275\u0275text(64);
    \u0275\u0275pipe(65, "translate");
    \u0275\u0275pipe(66, "translate");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    let tmp_10_0;
    let tmp_16_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(8, 19, "SHARED.COMMON.WIZARD.COMPLETION_TEXT", \u0275\u0275pureFunction1(51, _c7, \u0275\u0275pipeBind1(7, 17, "ADMIN.ENTITIES.ASSET"))), " ");
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(13, 22, "ADMIN.ASSETS.DETAILS.FIELDS.NAME"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAsset().name || \u0275\u0275pipeBind1(16, 24, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(20, 26, "ADMIN.ASSETS.DETAILS.FIELDS.TYPE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_10_0 = ctx_r1.newAsset().assetType) == null ? null : tmp_10_0.name) || \u0275\u0275pipeBind1(23, 28, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(27, 30, "ADMIN.ASSETS.DETAILS.FIELDS.COLOR"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAsset().color || \u0275\u0275pipeBind1(30, 32, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(34, 34, "ADMIN.ASSETS.DETAILS.FIELDS.ASSET_TAG"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.newAsset().assetTag || \u0275\u0275pipeBind1(37, 36, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(41, 38, "ADMIN.ASSETS.DETAILS.FIELDS.COST_CENTRE"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ((tmp_16_0 = ctx_r1.newAsset().costCentre) == null ? null : tmp_16_0.name) || \u0275\u0275pipeBind1(44, 40, "SHARED.COMMON.NONE"), " ");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(48, 42, "ADMIN.ASSETS.DETAILS.FIELDS.TAGS"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.convertStringCollectionToNameStrings(ctx_r1.newAsset().tags), " ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind1(54, 44, "ADMIN.ASSETS.DETAILS.FIELDS.GROUPS"), ": ");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.convertIdNameCollectionToNameStrings(ctx_r1.newAsset().groups), " ");
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.getCategoriesAsKeyValuePairs());
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.getCategoriesAsKeyValuePairs().length === 0 ? 59 : -1);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.infoSectionFields());
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate1(" ", \u0275\u0275pipeBind2(66, 48, "SHARED.COMMON.WIZARD.FINISH_PROMPT", \u0275\u0275pureFunction1(53, _c7, \u0275\u0275pipeBind1(65, 46, "ADMIN.ENTITIES.ASSET"))), " ");
  }
}
var AssetCreateWizardComponent = class _AssetCreateWizardComponent {
  // Signal-based ViewQueries
  // Using optional viewChild to allow safe checks in effect without throwing errors
  assetDetailsStep = viewChild("assetDetailsStep", ...ngDevMode ? [{ debugName: "assetDetailsStep" }] : (
    /* istanbul ignore next */
    []
  ));
  assetCategoriesStep = viewChild("assetCategoriesStep", ...ngDevMode ? [{ debugName: "assetCategoriesStep" }] : (
    /* istanbul ignore next */
    []
  ));
  confirmationTemplate = viewChild("confirmationTemplate", ...ngDevMode ? [{ debugName: "confirmationTemplate" }] : (
    /* istanbul ignore next */
    []
  ));
  infoSectionStep = viewChild("infoSectionStep", ...ngDevMode ? [{ debugName: "infoSectionStep" }] : (
    /* istanbul ignore next */
    []
  ));
  // References to form components
  assetDetailsForm = viewChild("assetDetailsForm", ...ngDevMode ? [{ debugName: "assetDetailsForm" }] : (
    /* istanbul ignore next */
    []
  ));
  assetCategoriesForm = viewChild("assetCategoriesForm", ...ngDevMode ? [{ debugName: "assetCategoriesForm" }] : (
    /* istanbul ignore next */
    []
  ));
  infoSectionForm = viewChild("infoSectionForm", ...ngDevMode ? [{ debugName: "infoSectionForm" }] : (
    /* istanbul ignore next */
    []
  ));
  entitiesService = inject(EntitiesService);
  broadcastService = inject(BroadcastEventMessageService);
  destroyRef = inject(DestroyRef);
  dialogRef = inject(DynamicDialogRef);
  dialogConfig = inject(DynamicDialogConfig);
  cacheInvalidationService = inject(CacheInvalidationService);
  assetGroupingService = inject(AssetGroupingService);
  cdr = inject(ChangeDetectorRef);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  // Asset model to collect data across steps
  newAsset = signal({}, ...ngDevMode ? [{ debugName: "newAsset" }] : (
    /* istanbul ignore next */
    []
  ));
  infoSectionFields = computed(() => {
    const asset = this.newAsset();
    if (!asset.fields) {
      return [];
    }
    const infoForm = this.infoSectionForm();
    const customFields = infoForm?.entityCustomFields?.();
    if (Array.isArray(customFields) && customFields.length > 0) {
      return customFields.map((field) => ({
        key: field["title"] ?? "Field",
        value: this.formatFieldValue(asset.fields?.[field["id"]], field["type"])
      }));
    }
    return Object.entries(asset.fields).map(([key, value]) => ({
      key: key.charAt(0).toUpperCase() + key.slice(1),
      value: this.formatFieldValue(value, "text")
    }));
  }, ...ngDevMode ? [{ debugName: "infoSectionFields" }] : (
    /* istanbul ignore next */
    []
  ));
  // Form validity state
  formIsValid = signal(false, ...ngDevMode ? [{ debugName: "formIsValid" }] : (
    /* istanbul ignore next */
    []
  ));
  displayWizard = false;
  steps = [];
  ownerId = null;
  assetCategories = [];
  isSubmitting = signal(false, ...ngDevMode ? [{ debugName: "isSubmitting" }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    effect(() => {
      const detailsStep = this.assetDetailsStep();
      const categoriesStep = this.assetCategoriesStep();
      const infoStep = this.infoSectionStep();
      const confirmStep = this.confirmationTemplate();
      this.languageService.currentLanguage();
      if (detailsStep && categoriesStep && infoStep && confirmStep) {
        this.steps = [
          {
            value: 1,
            header: this.translate.instant("ADMIN.ASSETS.WIZARD.STEPS.DETAILS"),
            content: detailsStep,
            complete: false,
            validate: () => this.assetDetailsForm()?.validate() ?? false
          },
          {
            value: 2,
            header: this.translate.instant("ADMIN.ASSETS.WIZARD.STEPS.CATEGORIES"),
            content: categoriesStep,
            complete: false,
            validate: () => this.assetCategoriesForm()?.validate() ?? false
          },
          {
            value: 3,
            header: this.translate.instant("ADMIN.ASSETS.WIZARD.STEPS.INFO"),
            content: infoStep,
            complete: false,
            validate: () => this.infoSectionForm()?.validate() ?? Promise.resolve(true)
          },
          {
            value: 4,
            header: this.translate.instant("SHARED.COMMON.DIALOG.CONFIRMATION"),
            content: confirmStep,
            complete: true
          }
        ];
        this.cdr.detectChanges();
      }
    });
  }
  ngOnInit() {
    if (isDefined(this.dialogConfig.data)) {
      this.ownerId = this.dialogConfig.data.ownerId ?? null;
      this.newAsset.update((current) => __spreadProps(__spreadValues({}, current), {
        ownerId: this.ownerId ?? ""
      }));
      this.assetGroupingService.getAssetCategories(this.ownerId ?? DEFAULT_SYSTEM_ID).then((assetCategories) => {
        this.assetCategories = assetCategories;
      }).catch(() => {
        this.assetCategories = [];
      });
    }
  }
  openWizard() {
    this.displayWizard = true;
    this.newAsset.set({});
  }
  // Form validity handlers
  onDetailsFormValidityChange(isValid) {
    if (isDefined(this.steps[0])) {
      this.steps[0].disableNext = !isValid;
    }
    this.formIsValid.set(isValid);
  }
  onCategoriesFormValidityChange(isValid) {
    if (isDefined(this.steps[1])) {
      this.steps[1].disableNext = !isValid;
    }
    this.formIsValid.set(isValid);
  }
  onInfoFormValidityChange(isValid) {
    if (isDefined(this.steps[2])) {
      this.steps[2].disableNext = !isValid;
    }
    this.formIsValid.set(isValid);
  }
  // Update the asset state with form data
  updateAssetDetails(formData) {
    this.newAsset.update((current) => {
      const _a = formData, { ownerId: _ownerId } = _a, fieldsToUpdate = __objRest(_a, ["ownerId"]);
      const updated = __spreadValues(__spreadValues({}, current), fieldsToUpdate);
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  updateAssetCategories(formData) {
    this.newAsset.update((current) => {
      const updated = __spreadValues({}, current);
      updated.categories = isEmpty(formData.categories) ? null : formData.categories ?? null;
      if (isNullOrUndefined(updated.ownerId)) {
        updated.ownerId = this.ownerId ?? "";
      }
      return updated;
    });
  }
  updateInfoSection(formData) {
    this.newAsset.update((current) => {
      const updated = __spreadValues({}, current);
      Object.assign(updated, formData);
      return updated;
    });
  }
  onWizardComplete() {
    const assetData = this.newAsset();
    this.isSubmitting.set(true);
    this.entitiesService.createAsset(assetData).pipe(takeUntilDestroyed(this.destroyRef), finalize(() => this.isSubmitting.set(false))).subscribe({
      next: (response) => {
        this.cacheInvalidationService.clearAssetCategoryCaches();
        this.broadcastService.broadcast("entity-updated", {
          entityType: "asset",
          operation: "create",
          entityId: response.id
        });
        this.dialogRef.close(response);
      },
      error: (error) => {
        console.error("Error creating asset:", error);
      }
    });
  }
  onWizardCancel() {
    this.newAsset.set({});
    this.dialogRef.close();
  }
  convertIdNameCollectionToNameStrings(collection) {
    if (!Array.isArray(collection))
      return "";
    return collection.map((item) => item.name).join(", ");
  }
  convertStringCollectionToNameStrings(collection) {
    if (!Array.isArray(collection))
      return "";
    return collection.join(", ");
  }
  // Get categories formatted as key-value pairs for display
  getCategoriesAsKeyValuePairs() {
    const asset = this.newAsset();
    if (!asset.categories || asset.categories.length === 0) {
      return [];
    }
    const parentCategories = this.assetCategories.filter((cat) => cat.parent === DEFAULT_SYSTEM_ID);
    const result = [];
    parentCategories.forEach((parentCat) => {
      const selectedCategory = (asset.categories ?? []).filter(isDefined).find((selectedCat) => {
        const fullCategory = this.assetCategories.find((cat) => cat.id === selectedCat.id);
        return fullCategory?.index === parentCat.index;
      });
      if (selectedCategory) {
        result.push({
          key: parentCat.name ?? "Category",
          value: selectedCategory.name ?? "Selected"
        });
      }
    });
    return result;
  }
  formatFieldValue(value, type) {
    if (value === null || value === void 0) {
      return "Not set";
    }
    switch (type) {
      case "boolean":
        return value === true || value === "true" ? "Yes" : "No";
      case "date":
        if (value instanceof Date) {
          return value.toLocaleDateString();
        }
        return typeof value === "string" ? value : JSON.stringify(value);
      case "number":
        return typeof value === "number" ? value.toString() : JSON.stringify(value);
      default:
        return typeof value === "string" ? value : JSON.stringify(value);
    }
  }
  static \u0275fac = function AssetCreateWizardComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetCreateWizardComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _AssetCreateWizardComponent, selectors: [["app-asset-create-wizard"]], viewQuery: function AssetCreateWizardComponent_Query(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275viewQuerySignal(ctx.assetDetailsStep, _c038, 5)(ctx.assetCategoriesStep, _c134, 5)(ctx.confirmationTemplate, _c233, 5)(ctx.infoSectionStep, _c332, 5)(ctx.assetDetailsForm, _c410, 5)(ctx.assetCategoriesForm, _c59, 5)(ctx.infoSectionForm, _c6, 5);
    }
    if (rf & 2) {
      \u0275\u0275queryAdvance(7);
    }
  }, decls: 9, vars: 5, consts: [["assetDetailsStep", ""], ["assetCategoriesStep", ""], ["infoSectionStep", ""], ["confirmationTemplate", ""], ["assetDetailsForm", ""], ["assetCategoriesForm", ""], ["infoSectionForm", ""], [3, "complete", "onCancel", "contentOnly", "steps", "showSteps", "useWizardButtonsOnly", "loading"], [1, "p-fluid"], [3, "formValidityChange", "formValueChange", "createMode", "entity", "ownerId"], ["entityType", "asset", 3, "formValidityChange", "formValueChange", "createMode", "entity"], [1, "flex", "gap-6"], [1, "pi", "pi-check", "!text-5xl"], [1, "flex-1"], [1, "grid"], [1, "col-12", "md:col-6"], [1, "mb-4"], [1, "space-y-2"], [1, "flex"], [1, "w-1/3", "font-medium"], [1, "w-2/3"], [1, "pt-4", "mt-4", "border-top-1", "border-surface-200", "dark:border-surface-700"], [1, "mb-0"]], template: function AssetCreateWizardComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "app-wizard", 7);
      \u0275\u0275listener("complete", function AssetCreateWizardComponent_Template_app_wizard_complete_0_listener() {
        return ctx.onWizardComplete();
      })("onCancel", function AssetCreateWizardComponent_Template_app_wizard_onCancel_0_listener() {
        return ctx.onWizardCancel();
      });
      \u0275\u0275elementEnd();
      \u0275\u0275template(1, AssetCreateWizardComponent_ng_template_1_Template, 3, 3, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, AssetCreateWizardComponent_ng_template_3_Template, 3, 3, "ng-template", null, 1, \u0275\u0275templateRefExtractor)(5, AssetCreateWizardComponent_ng_template_5_Template, 3, 2, "ng-template", null, 2, \u0275\u0275templateRefExtractor)(7, AssetCreateWizardComponent_ng_template_7_Template, 67, 55, "ng-template", null, 3, \u0275\u0275templateRefExtractor);
    }
    if (rf & 2) {
      \u0275\u0275property("contentOnly", true)("steps", ctx.steps)("showSteps", false)("useWizardButtonsOnly", true)("loading", ctx.isSubmitting());
    }
  }, dependencies: [
    CommonModule,
    ReactiveFormsModule,
    WizardComponent,
    ButtonModule,
    AssetDetailsSectionComponent,
    AssetCategoriesSectionComponent,
    InfoSectionComponent,
    TranslatePipe
  ], encapsulation: 2 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetCreateWizardComponent, [{
    type: Component,
    args: [{ selector: "app-asset-create-wizard", imports: [
      CommonModule,
      ReactiveFormsModule,
      WizardComponent,
      ButtonModule,
      AssetDetailsSectionComponent,
      AssetCategoriesSectionComponent,
      InfoSectionComponent,
      TranslatePipe
    ], template: `<app-wizard
  [contentOnly]="true"
  [steps]="steps"
  [showSteps]="false"
  [useWizardButtonsOnly]="true"
  [loading]="isSubmitting()"
  (complete)="onWizardComplete()"
  (onCancel)="onWizardCancel()"
>
</app-wizard>

<!-- Step 1: Asset Details -->
<ng-template #assetDetailsStep let-step>
  <div class="p-fluid">
    <app-asset-details-section
      #assetDetailsForm
      [createMode]="true"
      [entity]="newAsset()"
      [ownerId]="ownerId"
      (formValidityChange)="onDetailsFormValidityChange($event)"
      (formValueChange)="updateAssetDetails($event)"
    ></app-asset-details-section>
  </div>
</ng-template>

<ng-template #assetCategoriesStep let-step>
  <div class="p-fluid">
    <app-asset-categories-section
      #assetCategoriesForm
      [createMode]="true"
      [entity]="newAsset()"
      [ownerId]="ownerId"
      (formValidityChange)="onCategoriesFormValidityChange($event)"
      (formValueChange)="updateAssetCategories($event)"
    ></app-asset-categories-section>
  </div>
</ng-template>

<ng-template #infoSectionStep let-step>
  <div class="p-fluid">
    <app-info-section
      #infoSectionForm
      [createMode]="true"
      [entity]="newAsset()"
      entityType="asset"
      (formValidityChange)="onInfoFormValidityChange($event)"
      (formValueChange)="updateInfoSection($event)"
    ></app-info-section>
  </div>
</ng-template>

<ng-template #confirmationTemplate let-step>
  <div class="flex gap-6">
    <span class="pi pi-check !text-5xl"></span>

    <div class="flex-1">
      <div class="grid">
        <!-- Asset Details Section -->
        <div class="col-12 md:col-6">
          <p class="mb-4">
            {{
              'SHARED.COMMON.WIZARD.COMPLETION_TEXT'
                | translate: { entity: ('ADMIN.ENTITIES.ASSET' | translate) }
            }}
          </p>

          <div class="space-y-2">
            <!-- Each field in a side-by-side layout -->
            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ASSETS.DETAILS.FIELDS.NAME' | translate }}:
              </div>
              <div class="w-2/3">
                {{ newAsset().name || ('SHARED.COMMON.NONE' | translate) }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ASSETS.DETAILS.FIELDS.TYPE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newAsset().assetType?.name ||
                    ('SHARED.COMMON.NONE' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ASSETS.DETAILS.FIELDS.COLOR' | translate }}:
              </div>
              <div class="w-2/3">
                {{ newAsset().color || ('SHARED.COMMON.NONE' | translate) }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ASSETS.DETAILS.FIELDS.ASSET_TAG' | translate }}:
              </div>
              <div class="w-2/3">
                {{ newAsset().assetTag || ('SHARED.COMMON.NONE' | translate) }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ASSETS.DETAILS.FIELDS.COST_CENTRE' | translate }}:
              </div>
              <div class="w-2/3">
                {{
                  newAsset().costCentre?.name ||
                    ('SHARED.COMMON.NONE' | translate)
                }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ASSETS.DETAILS.FIELDS.TAGS' | translate }}:
              </div>
              <div class="w-2/3">
                {{ convertStringCollectionToNameStrings(newAsset().tags) }}
              </div>
            </div>

            <div class="flex">
              <div class="w-1/3 font-medium">
                {{ 'ADMIN.ASSETS.DETAILS.FIELDS.GROUPS' | translate }}:
              </div>
              <div class="w-2/3">
                {{ convertIdNameCollectionToNameStrings(newAsset().groups) }}
              </div>
            </div>

            <!-- Asset Categories Section -->
            @for (
              category of getCategoriesAsKeyValuePairs();
              track category.key
            ) {
              <div class="flex">
                <div class="w-1/3 font-medium">{{ category.key }}:</div>
                <div class="w-2/3">{{ category.value }}</div>
              </div>
            }

            @if (getCategoriesAsKeyValuePairs().length === 0) {
              <div class="flex">
                <div class="w-1/3 font-medium">
                  {{ 'ADMIN.ASSETS.CATEGORIES.TITLE' | translate }}:
                </div>
                <div class="w-2/3">
                  {{ 'ADMIN.ASSETS.CATEGORIES.NO_CATEGORIES' | translate }}
                </div>
              </div>
            }

            <!-- Info Section Custom Fields -->
            @for (field of infoSectionFields(); track field.key) {
              <div class="flex">
                <div class="w-1/3 font-medium">{{ field.key }}:</div>
                <div class="w-2/3">{{ field.value }}</div>
              </div>
            }
          </div>
        </div>
      </div>

      <div
        class="pt-4 mt-4 border-top-1 border-surface-200 dark:border-surface-700"
      >
        <p class="mb-0">
          {{
            'SHARED.COMMON.WIZARD.FINISH_PROMPT'
              | translate: { entity: ('ADMIN.ENTITIES.ASSET' | translate) }
          }}
        </p>
      </div>
    </div>
  </div>
</ng-template>
` }]
  }], () => [], { assetDetailsStep: [{ type: ViewChild, args: ["assetDetailsStep", { isSignal: true }] }], assetCategoriesStep: [{ type: ViewChild, args: ["assetCategoriesStep", { isSignal: true }] }], confirmationTemplate: [{ type: ViewChild, args: ["confirmationTemplate", { isSignal: true }] }], infoSectionStep: [{ type: ViewChild, args: ["infoSectionStep", { isSignal: true }] }], assetDetailsForm: [{ type: ViewChild, args: ["assetDetailsForm", { isSignal: true }] }], assetCategoriesForm: [{ type: ViewChild, args: ["assetCategoriesForm", { isSignal: true }] }], infoSectionForm: [{ type: ViewChild, args: ["infoSectionForm", { isSignal: true }] }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(AssetCreateWizardComponent, { className: "AssetCreateWizardComponent", filePath: "src/app/features/administration/components/assets/asset-create-wizard/asset-create-wizard.component.ts", lineNumber: 76 });
})();

// src/app/shared/services/csv-export.service.ts
var CsvExportService = class _CsvExportService {
  export(columns, rows, filename) {
    const exportCols = columns.filter((col) => col.exportable !== false);
    const header = exportCols.map((col) => this.escape(col.header)).join(",");
    const dataRows = rows.map((row) => exportCols.map((col) => this.escape(String(this.resolveValue(col, row) ?? ""))).join(","));
    const csv = [header, ...dataRows].join("\r\n");
    const blob = new Blob([csv], { type: "text/csv;charset=utf-8;" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = filename;
    a.click();
    URL.revokeObjectURL(url);
  }
  resolveValue(col, row) {
    if (col.valueGetter)
      return col.valueGetter(row);
    const field = col.field;
    if (field.includes(".")) {
      return this.resolvePath(row, field);
    }
    return String(row[field] ?? "");
  }
  resolvePath(obj, path) {
    const value = path.split(".").reduce((acc, key) => {
      return acc != null && typeof acc === "object" ? acc[key] : void 0;
    }, obj);
    return String(value ?? "");
  }
  escape(value) {
    if (/[,"\n\r]/.test(value)) {
      return `"${value.replace(/"/g, '""')}"`;
    }
    return value;
  }
  static \u0275fac = function CsvExportService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CsvExportService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _CsvExportService, factory: _CsvExportService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CsvExportService, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/features/administration/configs/assets-tree.grid.config.ts
var EXPORT_PAGE_SIZE2 = 200;
var AssetsTreeTableConfig = class _AssetsTreeTableConfig {
  entityType = "client-assets";
  selectionMode = "multiple";
  assetsService = inject(AssetsDataAdapter);
  utilityService = inject(UtilityService);
  gridBaseService = inject(GridBaseService);
  entitiesService = inject(EntitiesService);
  accountsService = inject(AccountsService);
  adminStateService = inject(AdminStateService);
  assetGroupingService = inject(AssetGroupingService);
  authDataService = inject(AuthDataService);
  csvExportService = inject(CsvExportService);
  apiSchemaService = inject(ApiSchemaService);
  defaultStateValues = ["active", "suspended", "inactive"];
  filterState = signal({
    state: {
      value: this.defaultStateValues,
      operator: "in"
    }
  }, ...ngDevMode ? [{ debugName: "filterState" }] : (
    /* istanbul ignore next */
    []
  ));
  paramsSignal = signal({
    clientId: ""
  }, ...ngDevMode ? [{ debugName: "paramsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  destroyRef = inject(DestroyRef);
  dialogRef = null;
  dialogService = inject(DialogService);
  measurementDateTimeService = inject(MeasurementDateTimeService);
  translate = inject(TranslateService);
  languageService = inject(LanguageService);
  rqlFilter = computed(() => this.utilityService.constructRqlFilter(__spreadValues({}, this.filterState())), ...ngDevMode ? [{ debugName: "rqlFilter" }] : (
    /* istanbul ignore next */
    []
  ));
  fieldTransformations = {
    costCentre: "costCentre.id",
    assetType: "assetType.id",
    groups: "groups.id",
    categories: "categories.id",
    sharedWith: "sharedWith.id",
    sharedFrom: "sharedFrom.id"
  };
  buildBaseColumns() {
    return [
      {
        field: "name",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.NAME"),
        widthWeight: 5,
        styleClass: "col-name",
        filterable: true,
        filterType: "text",
        sortable: true
      },
      {
        field: "assetType",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.TYPE"),
        widthWeight: 1,
        styleClass: "col-compact",
        valueGetter: (rowData) => isDefined(rowData.assetType?.name) ? rowData.assetType.name : "",
        filterable: true,
        filterType: "multiselect",
        sortable: true
      },
      {
        field: "state",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.STATE"),
        widthWeight: 1,
        filterable: true,
        filterType: "multiselect",
        filterOptions: [
          {
            label: this.translate.instant("SHARED.COMMON.STATUS.ACTIVE"),
            value: "active"
          },
          {
            label: this.translate.instant("SHARED.COMMON.STATUS.SUSPENDED"),
            value: "suspended"
          },
          {
            label: this.translate.instant("SHARED.COMMON.STATUS.DELETED"),
            value: "deleted"
          },
          {
            label: this.translate.instant("SHARED.COMMON.STATUS.INACTIVE"),
            value: "inactive"
          }
        ],
        valueGetter: (rowData) => isDefined(rowData.state) ? rowData.state : "",
        styleClass: "capitalize",
        sortable: true
      },
      {
        field: "costCentre",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.COST_CENTRE"),
        widthWeight: 1.5,
        valueGetter: (rowData) => isDefined(rowData.costCentre?.name) ? rowData.costCentre.name : "",
        filterable: true,
        filterType: "multiselect",
        sortable: true
      },
      {
        field: "groups",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.GROUPS"),
        widthWeight: 1.5,
        styleClass: "col-compact",
        valueGetter: (rowData) => isDefined(rowData.groups) ? rowData.groups.map((g) => g.name).join(", ") : "",
        filterable: true,
        filterType: "multiselect"
      },
      {
        field: "devices.name",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.DEVICES"),
        widthWeight: 2,
        styleClass: "col-compact",
        valueGetter: (rowData) => isDefined(rowData.devices) ? rowData.devices.map((d) => d.name).join(", ") : "",
        filterable: true,
        filterType: "text"
      },
      {
        field: "cameras",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.CAMERAS"),
        widthWeight: 0.75,
        filterable: false,
        exportable: false
      },
      {
        field: "linkedFrom",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.LINKED_FROM"),
        widthWeight: 2,
        styleClass: "col-compact",
        valueGetter: (rowData) => {
          return isDefined(rowData["linkedFrom"]) ? rowData["linkedFrom"].join(", ") : "";
        },
        filterable: true,
        filterType: "text"
      },
      {
        field: "linkedTo",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.LINKED_TO"),
        widthWeight: 2,
        styleClass: "col-compact",
        valueGetter: (rowData) => {
          return isDefined(rowData["linkedTo"]) ? rowData["linkedTo"].join(", ") : "";
        },
        filterable: true,
        filterType: "text"
      },
      {
        field: "sharedWith",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.SHARED_TO"),
        widthWeight: 1.5,
        styleClass: "col-compact",
        valueGetter: (rowData) => isDefined(rowData.sharedWith) ? rowData.sharedWith.map((s) => s.name).join(", ") : "",
        filterable: false
      },
      {
        field: "sharedFrom",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.SHARED_FROM"),
        widthWeight: 1.5,
        styleClass: "col-compact",
        valueGetter: (rowData) => isAssetSharedIn(rowData, this.adminStateService.selectedClient()?.id) ? rowData.sharedFrom?.name ?? "" : "",
        filterable: false
      },
      {
        field: "modifiedDate",
        header: this.translate.instant("ADMIN.ASSETS.COLUMNS.MODIFIED"),
        widthWeight: 1.75,
        styleClass: "col-compact",
        filterable: false,
        valueGetter: (rowData) => {
          return isDefined(rowData["modifiedDate"]) ? this.measurementDateTimeService.formatDateTime(this.measurementDateTimeService.createDateAssumingUtc(rowData["modifiedDate"]), DATE_FORMATS.DATE_TIME_SECONDS_YMD) : "";
        },
        sortable: true
      }
    ];
  }
  columnsSignal = signal((() => {
    const cols = [...this.buildBaseColumns()];
    if (this.apiSchemaService.usesModernSchema()) {
      this.addVoiceNumberColumn(cols);
    }
    return cols;
  })(), ...ngDevMode ? [{ debugName: "columnsSignal" }] : (
    /* istanbul ignore next */
    []
  ));
  get columns() {
    return this.columnsSignal();
  }
  defaultVisibleColumns = [
    "name",
    "assetType",
    "state",
    "costCentre",
    "groups",
    "devices.name",
    "cameras",
    "modifiedDate"
  ];
  dataService = {
    getList: (params) => {
      return this.getListAsTree(params);
    }
  };
  constructor() {
    effect(() => {
      const client = this.adminStateService.selectedClient();
      this.languageService.currentLanguage();
      if (isDefined(client?.id)) {
        this.buildColumns(client).catch(() => {
        });
      }
    });
  }
  async buildColumns(client) {
    const clientId = client.id;
    try {
      const assetCategories = await this.assetGroupingService.getAssetCategories(clientId);
      const newColumns = [
        ...this.buildBaseColumns()
      ];
      this.addCategoryColumns(newColumns, assetCategories);
      this.addCustomFieldColumns(newColumns, client);
      if (this.apiSchemaService.usesModernSchema() && !newColumns.some((col) => col.field === "voiceNumber")) {
        this.addVoiceNumberColumn(newColumns);
      }
      this.columnsSignal.set(newColumns);
      this.loadFilterOptions();
    } catch (err) {
      console.error("Error building columns:", err);
      const fallback = [...this.buildBaseColumns()];
      if (this.apiSchemaService.usesModernSchema()) {
        this.addVoiceNumberColumn(fallback);
      }
      this.columnsSignal.set(fallback);
    }
  }
  addCategoryColumns(columns, assetCategories) {
    const deviceIndex = columns.findIndex((col) => col.field === "devices");
    const insertIndex = deviceIndex >= 0 ? deviceIndex : columns.length - 2;
    const categoryColumns = assetCategories.filter((category) => category.parent === DEFAULT_SYSTEM_ID).map((category) => this.createCategoryColumn(category, assetCategories));
    columns.splice(insertIndex, 0, ...categoryColumns);
  }
  createCategoryColumn(category, _assetCategories) {
    return {
      field: `category.${category.index}`,
      header: isDefined(category.name) ? category.name : `Category ${category.index}`,
      widthWeight: 1.25,
      styleClass: "col-compact",
      valueGetter: (rowData) => {
        const cat = isDefined(category?.index) ? rowData.categories?.[category.index] : void 0;
        return isDefined(cat) ? cat.name : "";
      },
      filterable: true,
      filterType: "multiselect"
    };
  }
  addVoiceNumberColumn(columns) {
    const modifiedIndex = columns.findIndex((col) => col.field === "modifiedDate");
    const insertAt = modifiedIndex >= 0 ? modifiedIndex : columns.length;
    columns.splice(insertAt, 0, {
      field: "voiceNumber",
      header: this.translate.instant("ADMIN.ASSETS.COLUMNS.VOICE_NUMBER"),
      widthWeight: 1.25,
      styleClass: "col-compact",
      valueGetter: (rowData) => rowData["voiceNumber"] ?? "",
      filterable: true,
      filterType: "text"
    });
  }
  addCustomFieldColumns(columns, client) {
    const modifiedIndex = columns.findIndex((col) => col.field === "modifiedDate");
    const customFieldInsertIndex = modifiedIndex >= 0 ? modifiedIndex : columns.length;
    const customFieldColumns = (client?.customFields?.asset ?? []).map((customField) => ({
      field: `customFields.${customField.id}`,
      header: customField.title,
      widthWeight: 1.25,
      styleClass: "col-compact",
      filterable: true,
      filterType: "text"
    }));
    columns.splice(customFieldInsertIndex, 0, ...customFieldColumns);
  }
  getFilters() {
    return this.filterState();
  }
  clearFilters() {
    this.filterState.set({
      state: {
        value: this.defaultStateValues,
        operator: "in"
      }
    });
  }
  initialize(params) {
    this.paramsSignal.set(params);
    this.loadFilterOptions();
    const client = this.adminStateService.selectedClient();
    if (isDefined(client?.id)) {
      this.buildColumns(client).catch(() => {
      });
    }
  }
  getTitle() {
    return this.translate.instant("ADMIN.ASSETS.TITLE");
  }
  getRequestParams(event) {
    let sortField = "name:asc";
    if (isDefined(event?.sortField) && event.sortField !== "") {
      const sortOrder = event.sortOrder === 1 ? "asc" : "desc";
      sortField = `${event.sortField}:${sortOrder}`;
    }
    return {
      owner: this.paramsSignal()["clientId"],
      offset: isDefined(event?.first) ? event.first : 0,
      recurse: false,
      limit: isDefined(event?.rows) ? event.rows : DEFAULT_PAGE_SIZE,
      sortField,
      filters: this.rqlFilter()
    };
  }
  /**
   * Fetch assets as flat list and convert to tree structure
   * Strategy #2: Independent level filtering
   * - Root query: parentId=null + pagination + filters
   * - Children loaded on expand via lazy loading
   */
  getListAsTree(params) {
    return this.assetsService.getList(params).pipe(takeUntilDestroyed(this.destroyRef), map((response) => {
      const items = response?.items ?? [];
      const assetsWithParentId = items.map((item) => {
        const rawItem = item;
        return __spreadProps(__spreadValues({}, item), {
          parentId: rawItem["parentId"] ?? null,
          children: rawItem["children"] === true
        });
      });
      assetsWithParentId.forEach((a) => {
        if (!isDefined(a.parentId)) {
          a.parentId = null;
        }
      });
      const treeNodes = AssetTreeBuilder.buildTree(assetsWithParentId, this.authDataService.isSuperAdmin(), this.adminStateService.selectedClient()?.id);
      return {
        nodes: treeNodes,
        items: assetsWithParentId,
        count: response.count ?? 0,
        limit: params.limit
      };
    }));
  }
  onNodeExpand(node) {
    if (!isDefined(node.data?.id) || isDefined(node.children) && node.children.length > 0) {
      return;
    }
    const clientId = this.paramsSignal()["clientId"];
    node.loading = true;
    this.assetsService.getChildren(node.data.id, clientId).pipe(takeUntilDestroyed(this.destroyRef)).subscribe({
      next: (response) => {
        const children = response.items.map((item) => {
          const hasChildrenAttr = item.children;
          return __spreadProps(__spreadValues({}, item), {
            parentId: node.data.id,
            children: hasChildrenAttr
          });
        });
        const childNodes = AssetTreeBuilder.mapToNodes(children, this.authDataService.isSuperAdmin(), this.adminStateService.selectedClient()?.id);
        node.children = childNodes;
        node.loading = false;
      },
      error: () => {
        node.loading = false;
      }
    });
  }
  onFilterChange(field, value) {
    let actualField = this.fieldTransformations[field] || field;
    if (field.startsWith("category.")) {
      actualField = "categories.id";
    }
    if (Array.isArray(value)) {
      const newFilters2 = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, "in");
      this.filterState.set(newFilters2);
      return;
    }
    const newFilters = this.gridBaseService.handleFilters(this.filterState(), actualField, value, void 0, "contains");
    this.filterState.set(newFilters);
  }
  getVendorIdForClientFilters() {
    const vendor = this.adminStateService.clientVendor();
    if (isDefined(vendor?.id)) {
      return vendor.id;
    }
    return this.adminStateService.selectedClient()?.owner?.id ?? null;
  }
  loadFilterOptions() {
    const vendorId = this.getVendorIdForClientFilters();
    const clients$ = isDefined(vendorId) ? this.accountsService.listClients(vendorId, 0, 500, "name", this.utilityService.constructRqlFilter({
      state: { value: ADMIN_TREE.DELETED_STATE, operator: "!=" }
    })).pipe(catchError(() => {
      return of({ items: [], count: 0 });
    })) : of({ items: [], count: 0 });
    combineLatest([
      this.entitiesService.listAssetTypes(this.paramsSignal()["clientId"]),
      this.entitiesService.listCostCentres(this.paramsSignal()["clientId"]),
      this.entitiesService.listAssetGroups(this.paramsSignal()["clientId"]),
      clients$
    ]).pipe(takeUntilDestroyed(this.destroyRef), catchError(() => {
      return EMPTY;
    })).subscribe(([types, costCentres, listAssetGroups, clients]) => {
      const currentColumns = this.columnsSignal();
      const clientFilterOptions = clients.items.map((client) => ({
        label: client.name ?? "",
        value: client.id
      })).sort((a, b) => a.label.localeCompare(b.label));
      const typeColumn = currentColumns.find((col) => col.field === "assetType");
      if (typeColumn) {
        typeColumn.filterOptions = types.items.map((type) => ({
          label: type.name,
          value: type.id
        })).sort((a, b) => (a.label ?? "").localeCompare(b.label ?? ""));
      }
      const costCentreColumn = currentColumns.find((col) => col.field === "costCentre");
      if (costCentreColumn) {
        costCentreColumn.filterOptions = costCentres.items.map((costCentre) => ({
          label: costCentre.name ?? "",
          value: costCentre.id
        })).sort((a, b) => a.label.localeCompare(b.label));
      }
      const groupColumn = currentColumns.find((col) => col.field === "groups");
      if (groupColumn) {
        groupColumn.filterOptions = listAssetGroups.items.map((group) => ({
          label: group.name ?? "",
          value: group.id
        })).sort((a, b) => a.label.localeCompare(b.label));
      }
      const sharedWithColumn = currentColumns.find((col) => col.field === "sharedWith");
      if (sharedWithColumn) {
        sharedWithColumn.filterOptions = clientFilterOptions;
      }
      const sharedFromColumn = currentColumns.find((col) => col.field === "sharedFrom");
      if (sharedFromColumn) {
        sharedFromColumn.filterOptions = clientFilterOptions;
      }
      this.columnsSignal.set([...currentColumns]);
    });
  }
  /**
   * Export every asset for the selected client, not just the page currently
   * rendered in the grid. The grid only holds one page of root nodes in memory
   * (and lazily-loaded children), so exporting `this.nodes()` directly would
   * truncate the file to the visible page. Instead we sweep all pages of the
   * root list and recursively pull every descendant before building the CSV.
   *
   * The `nodes` argument from the tree table is intentionally ignored — it is
   * the in-memory (paginated) view, which is exactly what we must not export.
   *
   * Returns an `Observable<void>` that completes once the file has been
   * generated (or errors if a fetch fails) so the caller can show progress.
   */
  onExport(_nodes, visibleColumns) {
    const allColumns = this.columnsSignal();
    const exportColumns = allColumns.filter((col) => col.exportable !== false && (visibleColumns.length === 0 || visibleColumns.includes(col.field)));
    return this.fetchAllAssetsForExport().pipe(map((assets) => {
      const tree = AssetTreeBuilder.buildTree(assets, this.authDataService.isSuperAdmin(), this.adminStateService.selectedClient()?.id);
      this.triggerCsvDownload(tree, exportColumns);
    }));
  }
  /**
   * Fetch the complete asset hierarchy for the current client/filters: all
   * pages of root assets followed by every descendant, returned as a flat list
   * ready for {@link AssetTreeBuilder.buildTree}.
   */
  fetchAllAssetsForExport() {
    const owner = this.paramsSignal()["clientId"];
    const filters = this.rqlFilter();
    return this.fetchAllPages((offset, limit) => this.assetsService.getList({
      owner,
      offset,
      limit,
      recurse: false,
      sortField: "name:asc",
      filters
    })).pipe(map((items) => items.map((item) => this.toAssetWithParentId(item, null))), switchMap((roots) => this.loadDescendants(roots, owner)));
  }
  /**
   * Recursively load every descendant of the supplied parent assets, paginating
   * each level so child sets larger than a single page are not truncated. Only
   * parents flagged with `children === true` trigger a request, so a flat asset
   * list makes no extra calls at all. Returns the parents concatenated with all
   * of their descendants.
   */
  loadDescendants(parents, owner) {
    const parentsWithChildren = parents.filter((parent) => parent.children === true && isDefined(parent.id));
    if (parentsWithChildren.length === 0) {
      return of(parents);
    }
    return forkJoin(parentsWithChildren.map((parent) => this.fetchAllPages((offset, limit) => this.assetsService.getChildren(parent.id, owner, limit, offset)).pipe(map((items) => items.map((item) => this.toAssetWithParentId(item, parent.id))), switchMap((children) => this.loadDescendants(children, owner))))).pipe(map((groups) => parents.concat(...groups)));
  }
  /**
   * Drain every page of a paginated list query, accumulating all items in
   * order. Stops when a page comes back empty or the reported total `count` has
   * been reached, so it is resilient to a backend that caps the page size below
   * the requested limit.
   */
  fetchAllPages(query) {
    const fetchFrom = (offset) => query(offset, EXPORT_PAGE_SIZE2).pipe(map((response) => {
      const items = response.items ?? [];
      return {
        items,
        nextOffset: offset + items.length,
        count: response.count ?? 0
      };
    }));
    return fetchFrom(0).pipe(expand((page) => page.items.length > 0 && page.nextOffset < page.count ? fetchFrom(page.nextOffset) : EMPTY), reduce((acc, page) => acc.concat(page.items), []));
  }
  toAssetWithParentId(item, fallbackParentId) {
    const raw = item;
    return __spreadProps(__spreadValues({}, item), {
      parentId: raw["parentId"] ?? fallbackParentId,
      children: raw["children"] === true
    });
  }
  triggerCsvDownload(nodes, columns) {
    const flatItems = AssetTreeBuilder.flattenTree(nodes);
    const clientName = this.adminStateService.selectedClient()?.name ?? "assets";
    const date = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
    this.csvExportService.export(columns, flatItems, `${clientName}-assets-${date}.csv`);
  }
  onAdd() {
    const clientId = this.paramsSignal()["clientId"];
    this.openAssetCreationDialog(clientId).subscribe((createdAsset) => {
      if (isDefined(createdAsset)) {
      }
    });
  }
  openAssetCreationDialog(ownerId) {
    this.dialogRef = this.dialogService.open(AssetCreateWizardComponent, {
      header: this.translate.instant("SHARED.COMMON.WIZARD.TITLE", {
        entity: this.translate.instant("ADMIN.ENTITIES.ASSET")
      }),
      contentStyle: { overflow: "auto" },
      baseZIndex: 1e4,
      data: {
        ownerId
      },
      position: "top",
      modal: true
    });
    return this.dialogRef.onClose;
  }
  onBulkImport() {
    const clientId = this.paramsSignal()["clientId"];
    this.openBulkImportDialog(clientId).then((obs) => obs.subscribe()).catch(console.error);
  }
  async openBulkImportDialog(ownerId) {
    const { BulkImportDialogComponent } = await import("./chunk-NCXIB3D3.js");
    this.dialogRef = this.dialogService.open(BulkImportDialogComponent, {
      header: this.translate.instant("ADMIN.ASSETS.BULK_IMPORT_TITLE"),
      baseZIndex: 1e4,
      data: {
        ownerId
      },
      position: "top",
      modal: true,
      width: "800px"
    });
    return this.dialogRef.onClose;
  }
  static \u0275fac = function AssetsTreeTableConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AssetsTreeTableConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _AssetsTreeTableConfig, factory: _AssetsTreeTableConfig.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AssetsTreeTableConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();

// src/app/features/administration/configs/admin-tree-table-configs-registration.ts
function createTreeTableConfigEntry(entityType, config) {
  return {
    entityType,
    config
  };
}
var ADMIN_TREE_TABLE_CONFIGS = {
  provide: TREE_TABLE_CONFIGS,
  useValue: [
    createTreeTableConfigEntry("client-cost-centres", CostCentresGridConfig),
    createTreeTableConfigEntry("client-asset-groups", AssetGroupsGridConfig),
    createTreeTableConfigEntry("client-asset-categories", AssetCategoriesGridConfig),
    createTreeTableConfigEntry("client-assets", AssetsTreeTableConfig),
    createTreeTableConfigEntry("client-geofence-groups", GeofenceGroupsGridConfig),
    createTreeTableConfigEntry("vendor-company-groups", CompanyGroupsGridConfig),
    createTreeTableConfigEntry("distributor-company-groups", CompanyGroupsGridConfig),
    createTreeTableConfigEntry("super-admin-device-types", DeviceTypesGridConfig)
  ],
  multi: true
};

// src/app/features/administration/components/admin-container/admin-container.component.ts
var _c039 = (a0, a1) => ({ content: a0, scrollable: false, minSize: a1 });
var _c135 = (a0) => ({ content: a0, scrollable: true, minSize: 10 });
var _c234 = (a0, a1) => [a0, a1];
var _c333 = () => ["36px", 100];
var _c411 = () => [25, 75];
var _c510 = (a0) => [a0, 10];
function AdminContainerComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "div", 5)(1, "p-progressSpinner", 6);
  }
}
function AdminContainerComponent_Conditional_2_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "app-tree-navigation-panel", 7);
    \u0275\u0275listener("toggleTreePanel", function AdminContainerComponent_Conditional_2_ng_template_1_Template_app_tree_navigation_panel_toggleTreePanel_0_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.toggleTreePanel());
    })("navigateToSelectedClient", function AdminContainerComponent_Conditional_2_ng_template_1_Template_app_tree_navigation_panel_navigateToSelectedClient_0_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.navigateToSelectedClient());
    })("nodeExpand", function AdminContainerComponent_Conditional_2_ng_template_1_Template_app_tree_navigation_panel_nodeExpand_0_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.onNodeExpand($event));
    })("nodeCollapse", function AdminContainerComponent_Conditional_2_ng_template_1_Template_app_tree_navigation_panel_nodeCollapse_0_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.onNodeCollapse($event));
    })("nodeSelect", function AdminContainerComponent_Conditional_2_ng_template_1_Template_app_tree_navigation_panel_nodeSelect_0_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.onNodeSelect($event));
    })("nodeNotFoundInData", function AdminContainerComponent_Conditional_2_ng_template_1_Template_app_tree_navigation_panel_nodeNotFoundInData_0_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.onNodeNotFoundInData($event));
    })("searchQueryChange", function AdminContainerComponent_Conditional_2_ng_template_1_Template_app_tree_navigation_panel_searchQueryChange_0_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.onSearchQueryChange($event));
    })("searchResultSelect", function AdminContainerComponent_Conditional_2_ng_template_1_Template_app_tree_navigation_panel_searchResultSelect_0_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.onSearchResultSelect($event));
    })("searchClear", function AdminContainerComponent_Conditional_2_ng_template_1_Template_app_tree_navigation_panel_searchClear_0_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.onSearchClear());
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("treeCollapsed", ctx_r1.treeCollapsed)("hierarchyTree", ctx_r1.hierarchyTree)("isLoading", ctx_r1.isLoading)("searchingForNode", ctx_r1.searchingForNode)("searchingNodeKey", ctx_r1.searchingNodeKey)("selectedTreeNode", ctx_r1.selectedTreeNode)("scrollToNodeKey", ctx_r1.scrollToNodeKey)("scrollToCenterNodeKey", ctx_r1.scrollToCenterNodeKey)("searchResults", ctx_r1.searchResults)("isSearching", ctx_r1.isSearching);
  }
}
function AdminContainerComponent_Conditional_2_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "app-admin-main-content", 8);
    \u0275\u0275listener("routerOutletActivate", function AdminContainerComponent_Conditional_2_ng_template_3_Template_app_admin_main_content_routerOutletActivate_0_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.onRouterOutletActivate($event));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("nestedContentScrollable", ctx_r1.nestedContentScrollable)("detailsPanelClass", ctx_r1.detailsPanelClass)("shouldUseDrawer", ctx_r1.shouldUseDrawerForDetails);
  }
}
function AdminContainerComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "app-adaptive-layout", 3);
    \u0275\u0275template(1, AdminContainerComponent_Conditional_2_ng_template_1_Template, 1, 10, "ng-template", null, 0, \u0275\u0275templateRefExtractor)(3, AdminContainerComponent_Conditional_2_ng_template_3_Template, 1, 3, "ng-template", null, 1, \u0275\u0275templateRefExtractor);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const treeNavigationTemplate_r4 = \u0275\u0275reference(2);
    const mainContentTemplate_r5 = \u0275\u0275reference(4);
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("ngClass", ctx_r1.mainSplitterClass())("panels", \u0275\u0275pureFunction2(11, _c234, \u0275\u0275pureFunction2(6, _c039, treeNavigationTemplate_r4, ctx_r1.treeCollapsed() ? "36px" : 15), \u0275\u0275pureFunction1(9, _c135, mainContentTemplate_r5)))("panelSizes", ctx_r1.treeCollapsed() ? \u0275\u0275pureFunction0(14, _c333) : \u0275\u0275pureFunction0(15, _c411))("minSizes", \u0275\u0275pureFunction1(16, _c510, ctx_r1.treeCollapsed() ? "36px" : 15))("defaultLayout", "horizontal")("responsive", true);
  }
}
function AdminContainerComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 4)(1, "app-admin-main-content", 8);
    \u0275\u0275listener("routerOutletActivate", function AdminContainerComponent_Conditional_3_Template_app_admin_main_content_routerOutletActivate_1_listener($event) {
      \u0275\u0275restoreView(_r6);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onRouterOutletActivate($event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("nestedContentScrollable", ctx_r1.nestedContentScrollable)("detailsPanelClass", ctx_r1.detailsPanelClass)("shouldUseDrawer", ctx_r1.shouldUseDrawerForDetails);
  }
}
var AdminContainerComponent = class _AdminContainerComponent {
  adminState = inject(AdminStateService);
  messageService = inject(MessageService);
  clientDataService = inject(ClientDataService);
  cdr = inject(ChangeDetectorRef);
  detailsService = inject(EntityDetailsService);
  adminSearchService = inject(AdminSearchService);
  adminTreeService = inject(AdminTreeService);
  authDataService = inject(AuthDataService);
  apiSchemaService = inject(ApiSchemaService);
  contextExtractor = inject(AdminTreeContextExtractorService);
  treeManagementService = inject(AdminTreeManagementService);
  navigationService = inject(AdminNavigationService);
  treeEventHandler = inject(AdminTreeEventHandlerService);
  containerState = inject(AdminContainerStateService);
  routeHandler = inject(AdminRouteHandlerService);
  scrollManagement = inject(AdminScrollManagementService);
  treeRefreshService = inject(AdminTreeRefreshService);
  treeNodeService = inject(AdminTreeNodeService);
  router = inject(Router);
  searchNavigation = inject(AdminTreeSearchNavigationService);
  breakpointObserver = inject(BreakpointObserver);
  adminTreeErrorHandler = inject(AdminTreeErrorHandlerService);
  destroyRef = inject(DestroyRef);
  isLoading = this.adminState.isLoading;
  selectedItem = this.containerState.selectedItem;
  // Responsive breakpoint signals
  screenSize = signal("xl", ...ngDevMode ? [{ debugName: "screenSize" }] : (
    /* istanbul ignore next */
    []
  ));
  isMobile = computed(() => ["xs", "sm"].includes(this.screenSize()), ...ngDevMode ? [{ debugName: "isMobile" }] : (
    /* istanbul ignore next */
    []
  ));
  isTablet = computed(() => this.screenSize() === "md", ...ngDevMode ? [{ debugName: "isTablet" }] : (
    /* istanbul ignore next */
    []
  ));
  isDesktop = computed(() => ["lg", "xl"].includes(this.screenSize()), ...ngDevMode ? [{ debugName: "isDesktop" }] : (
    /* istanbul ignore next */
    []
  ));
  // Computed flags for responsive behavior
  shouldAutoCollapseTree = computed(() => this.isTablet(), ...ngDevMode ? [{ debugName: "shouldAutoCollapseTree" }] : (
    /* istanbul ignore next */
    []
  ));
  shouldUseDrawerForDetails = computed(() => this.isTablet() || this.isMobile(), ...ngDevMode ? [{ debugName: "shouldUseDrawerForDetails" }] : (
    /* istanbul ignore next */
    []
  ));
  detailsDrawerVisible = signal(false, ...ngDevMode ? [{ debugName: "detailsDrawerVisible" }] : (
    /* istanbul ignore next */
    []
  ));
  // Track manual tree toggle to prevent auto-collapse override
  userManuallySizedTree = false;
  detailsComponent = signal(null, ...ngDevMode ? [{ debugName: "detailsComponent" }] : (
    /* istanbul ignore next */
    []
  ));
  detailsInputs = signal({}, ...ngDevMode ? [{ debugName: "detailsInputs" }] : (
    /* istanbul ignore next */
    []
  ));
  hasMoreData = this.containerState.hasMoreData;
  loadingMore = this.containerState.loadingMore;
  selectedTreeNode = this.treeEventHandler.selectedTreeNode;
  scrollToNodeKey = this.scrollManagement.scrollToNodeKey;
  scrollToCenterNodeKey = this.scrollManagement.scrollToCenterNodeKey;
  // Node search loading state
  searchingForNode = this.searchNavigation.searchingForNode;
  searchingNodeKey = this.searchNavigation.searchingNodeKey;
  // Search-related computed values
  searchResults = this.adminSearchService.results;
  isSearching = this.adminSearchService.isSearching;
  hasSearchQuery = this.adminSearchService.hasQuery;
  // Computed loading state for tree (combines general loading + node search loading)
  treeLoading = computed(() => this.isLoading() || this.searchingForNode(), ...ngDevMode ? [{ debugName: "treeLoading" }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * Computed property that builds the hierarchy tree including "Load More" nodes.
   * Now enhanced with search highlighting and expansion.
   */
  hierarchyTree = this.treeManagementService.hierarchyTree;
  activeGridComponent;
  detailsLoading = signal(false, ...ngDevMode ? [{ debugName: "detailsLoading" }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * Returns the CSS class for the main container based on details panel state
   */
  hostClass = computed(() => {
    return this.detailsService.showPanel() ? "has-details" : "";
  }, ...ngDevMode ? [{ debugName: "hostClass" }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * Returns the CSS class for the main splitter
   */
  mainSplitterClass = computed(() => {
    return "main-layout";
  }, ...ngDevMode ? [{ debugName: "mainSplitterClass" }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * Returns the CSS class for the details panel based on its visibility
   */
  detailsPanelClass = computed(() => {
    return this.detailsService.showPanel() ? "details-visible-state" : "details-hidden-state";
  }, ...ngDevMode ? [{ debugName: "detailsPanelClass" }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * Signal to determine if the nested router outlet content should be scrollable.
   * Defaults to false (for grid pages).
   */
  nestedContentScrollable = this.containerState.nestedContentScrollable;
  faAngleLeft = faAngleLeft;
  faAngleRight = faAngleRight;
  // Collapsible tree state
  treeCollapsed = this.containerState.treeCollapsed;
  // Flag to prevent infinite loops when programmatically changing selected client
  isNavigatingToClient = false;
  initialRouteProcessed = false;
  // Flag to track when component is fully initialized and ready for runtime client changes
  componentFullyInitialized = false;
  // Only show tree if user has access level above client
  showTreePanel = computed(() => {
    return this.authDataService.shouldShowTreeNavigation();
  }, ...ngDevMode ? [{ debugName: "showTreePanel" }] : (
    /* istanbul ignore next */
    []
  ));
  toggleTreePanel() {
    this.userManuallySizedTree = true;
    this.containerState.toggleTreePanel();
  }
  constructor() {
    const shouldShowTree = this.authDataService.shouldShowTreeNavigation();
    if (shouldShowTree) {
      this.scrollManagement.initialize({
        selectedTreeNode: () => this.selectedTreeNode(),
        setSelectedTreeNode: (node) => this.treeEventHandler.setTreeSelection(node)
      });
      effect(() => {
        const shouldToggle = this.treeEventHandler.toggleTreePanel();
        if (shouldToggle) {
          this.toggleTreePanel();
          this.treeEventHandler.toggleTreePanel.set(false);
        }
      });
      effect(() => {
        const errorData = this.treeEventHandler.errorMessage();
        if (errorData) {
          this.handleError(errorData.error, errorData.message);
          this.treeEventHandler.errorMessage.set(null);
        }
      });
      effect(() => {
        const shouldAutoCollapse = this.shouldAutoCollapseTree();
        const isCurrentlyCollapsed = this.treeCollapsed();
        if (shouldAutoCollapse && !isCurrentlyCollapsed && !this.userManuallySizedTree) {
          this.toggleTreePanel();
        }
        if (!shouldAutoCollapse) {
          this.userManuallySizedTree = false;
        }
      });
      let previousRouteKey = void 0;
      let previousRouteType = void 0;
      effect(() => {
        if (!this.initialRouteProcessed)
          return;
        const { keyToSelect, typeToSelect } = this.routeHandler.getEntityInfoFromRoute();
        const currentRouteKey = isDefined(keyToSelect) ? keyToSelect : void 0;
        const currentRouteType = isDefined(typeToSelect) ? typeToSelect : void 0;
        if (currentRouteKey === previousRouteKey && currentRouteType === previousRouteType) {
          return;
        }
        previousRouteKey = currentRouteKey;
        previousRouteType = currentRouteType;
        if (isDefined(keyToSelect) && isDefined(typeToSelect)) {
          if (typeToSelect === ADMIN_TREE.NODE_TYPES.CLIENT) {
            return;
          }
          const currentSelected = this.selectedTreeNode();
          if (currentSelected?.key === keyToSelect && currentSelected?.type === typeToSelect) {
            return;
          }
          this.treeEventHandler.setEventSource(EventSource.SYSTEM);
          this.scrollManagement.suppressScrollToClient.set(false);
          void Promise.resolve().then(async () => {
            setTimeout(async () => {
              await this.handleRouteBasedSelection(keyToSelect, typeToSelect);
            }, 100);
          });
        }
      });
      let previousCollapsed = this.treeCollapsed();
      effect(() => {
        const collapsed = this.treeCollapsed();
        if (previousCollapsed && !collapsed) {
          this.scrollManagement.suppressScrollToClient.set(false);
          const selectedNode = this.selectedTreeNode();
          if (isDefined(selectedNode?.key)) {
            this.scrollManagement.scrollToCenterNode(selectedNode.key);
          } else {
            const selClient = this.adminState.selectedClient();
            if (isDefined(selClient?.id) && this.treeEventHandler.shouldAllowAutoScroll()) {
              this.scrollManagement.scrollToClient(selClient.id);
            }
          }
        }
        previousCollapsed = collapsed;
      });
    }
    let previousClientId = void 0;
    effect(() => {
      const selectedClient = this.clientDataService.getSelectedClientSignal()();
      const currentClientId = selectedClient?.id;
      if (!this.initialRouteProcessed || currentClientId === previousClientId) {
        previousClientId = currentClientId;
        return;
      }
      previousClientId = currentClientId;
      if (!this.componentFullyInitialized) {
        if (isDefined(selectedClient?.id)) {
          this.updateTreeSelectionForCurrentClient(selectedClient).catch(() => {
          });
        }
        return;
      }
      if (isDefined(selectedClient?.id) && !this.isNavigatingToClient) {
        const currentUrl = this.router.url;
        const expectedClientPath = `/admin/client/${selectedClient.id}`;
        if (currentUrl.startsWith(expectedClientPath)) {
          this.updateTreeSelectionForCurrentClient(selectedClient).catch(() => {
          });
          return;
        }
        this.treeEventHandler.setEventSource(EventSource.SYSTEM);
        this.scrollManagement.suppressScrollToClient.set(false);
        void Promise.resolve().then(async () => {
          await this.handleSelectedClientChange(selectedClient);
        });
      }
    });
    effect(() => {
      const shouldUseDrawer = this.shouldUseDrawerForDetails();
      const isPanelOpen = this.detailsService.showPanel();
      if (shouldUseDrawer && isPanelOpen) {
        this.detailsDrawerVisible.set(true);
      }
      if (!shouldUseDrawer && this.detailsDrawerVisible()) {
        this.detailsDrawerVisible.set(false);
      }
    });
    this.setupBreakpointObserver();
  }
  /**
   * Updates tree selection when we're already on the correct route but selection is wrong
   */
  async updateTreeSelectionForCurrentClient(selectedClient) {
    if (!isDefined(selectedClient.id))
      return;
    try {
      this.adminState.setSelectedClient(selectedClient);
      const searchResult = await this.adminSearchService.prepareClientForTreeSelectionWithOwner(selectedClient);
      if (searchResult) {
        const treeNodes = this.hierarchyTree();
        const nodeToSelect = this.treeNodeService.findNodeByKey(treeNodes, selectedClient.id);
        if (nodeToSelect) {
          this.treeEventHandler.setTreeSelection(nodeToSelect);
          if (!this.treeCollapsed() && this.treeEventHandler.shouldAllowAutoScroll()) {
            this.scrollManagement.scrollToClient(selectedClient.id);
          }
        }
      }
    } catch (error) {
      console.warn("Error updating tree selection for current client:", error);
    }
  }
  ngOnInit() {
    try {
      const shouldShowTree = this.authDataService.shouldShowTreeNavigation();
      if (shouldShowTree) {
        this.treeEventHandler.initialize({
          getHierarchyTree: () => this.hierarchyTree(),
          findNodeByKeyRecursive: (nodes, key) => this.treeNodeService.findNodeByKey(nodes, key)
        });
        this.routeHandler.initializeRouteHandling({
          selectNodeFromRouteOrState: () => void this.selectNodeFromRouteOrState(),
          ensureSuperAdminNodeExpansion: () => this.ensureSuperAdminNodeExpansion(),
          onChangeDetection: () => {
            this.cdr.detectChanges();
            this.initialRouteProcessed = true;
            this.componentFullyInitialized = true;
          },
          destroyRef: this.destroyRef
        });
        this.treeRefreshService.initialize();
      } else {
        this.initialRouteProcessed = true;
        this.componentFullyInitialized = true;
        this.cdr.detectChanges();
      }
    } catch (error) {
      const isVersionMismatch = this.adminTreeErrorHandler.handleAdminTreeError(error, "component initialization");
      if (!isVersionMismatch) {
        console.error("Admin container initialization error:", error);
        throw error;
      }
    }
  }
  ngOnDestroy() {
    this.treeEventHandler.cleanup();
    this.adminState.clearAllState();
  }
  /**
   * Setup breakpoint observer to detect screen size changes
   */
  setupBreakpointObserver() {
    this.breakpointObserver.observe([
      Breakpoints.XSmall,
      Breakpoints.Small,
      Breakpoints.Medium,
      Breakpoints.Large,
      Breakpoints.XLarge
    ]).pipe(takeUntilDestroyed()).subscribe((result) => {
      if (result.breakpoints[Breakpoints.XLarge]) {
        this.screenSize.set("xl");
      } else if (result.breakpoints[Breakpoints.Large]) {
        this.screenSize.set("lg");
      } else if (result.breakpoints[Breakpoints.Medium]) {
        this.screenSize.set("md");
      } else if (result.breakpoints[Breakpoints.Small]) {
        this.screenSize.set("sm");
      } else if (result.breakpoints[Breakpoints.XSmall]) {
        this.screenSize.set("xs");
      }
    });
  }
  /**
   * Handles when a node is not found in the current tree data
   */
  onNodeNotFoundInData(event) {
    this.treeEventHandler.onNodeNotFoundInData(event);
  }
  // --- Tree Building Logic with Pagination ---
  /**
   * Ensures the super admin node is expanded by default when using modern schema
   */
  ensureSuperAdminNodeExpansion() {
    const isSuperAdmin = this.authDataService.isSuperAdmin();
    const usesModernSchema = this.apiSchemaService.usesModernSchema();
    const distributor = this.adminState.vendorDistributor();
    if (isSuperAdmin && usesModernSchema && isDefined(distributor)) {
      const superAdminExpandedNodes = this.adminState.expandedNodes()["super-admin"] ?? /* @__PURE__ */ new Set();
      if (!superAdminExpandedNodes.has("super-admin-root")) {
        this.adminState.setDistributorNodeExpanded("super-admin", "super-admin-root", true);
      }
    }
  }
  async navigateToNode(nodeType, nodeKey) {
    return this.navigationService.navigateToNode(nodeType, nodeKey);
  }
  handleError(error, message) {
    console.error(message, error);
    this.messageService.add({
      severity: "error",
      summary: "Error",
      detail: `${message}: ${error instanceof Error ? error.message : "Unknown error"}`
    });
  }
  /**
   * Navigates to the selected client from clientDataService and selects it in the tree
   * This method handles the home button functionality
   */
  async navigateToSelectedClient() {
    const selectedClient = this.clientDataService.getSelectedClient();
    if (!selectedClient || !isDefined(selectedClient.id)) {
      this.messageService.add({
        severity: "warn",
        summary: "Warning",
        detail: "No client selected to navigate to"
      });
      return;
    }
    try {
      this.treeEventHandler.setEventSource(EventSource.SYSTEM);
      this.scrollManagement.suppressScrollToClient.set(false);
      if (isNullOrUndefined(selectedClient.owner?.id)) {
        const fullClient = await this.adminTreeService.fetchClient(selectedClient.id);
        if (isDefined(fullClient)) {
          await this.handleSelectedClientChange(fullClient);
        } else {
          console.warn("Could not load full client data for navigation");
          await this.navigateToNode(ADMIN_TREE.NODE_TYPES.CLIENT, selectedClient.id);
          this.adminState.setSelectedClient(selectedClient);
        }
      } else {
        await this.handleSelectedClientChange(selectedClient);
      }
    } catch (error) {
      this.handleError(error, "Error navigating to selected client");
    }
  }
  /**
   * Handles automatic navigation when the selected client changes
   * This prevents infinite loops by setting a flag during navigation
   * Uses the client's owner information for efficient hierarchy loading
   */
  async handleSelectedClientChange(selectedClient) {
    if (this.isNavigatingToClient || !isDefined(selectedClient.id)) {
      return;
    }
    try {
      this.isNavigatingToClient = true;
      this.adminState.setSelectedClient(selectedClient);
      await this.navigateToNode(ADMIN_TREE.NODE_TYPES.CLIENT, selectedClient.id);
      if (this.treeCollapsed()) {
        try {
          await this.adminSearchService.prepareClientForTreeSelectionWithOwner(selectedClient);
          const treeNodes = this.hierarchyTree();
          const nodeToSelect = this.treeNodeService.findNodeByKey(treeNodes, selectedClient.id);
          if (nodeToSelect) {
            this.treeEventHandler.setTreeSelection(nodeToSelect);
          }
        } catch (error) {
          console.warn("Could not prepare tree data while collapsed:", error);
        }
        return;
      }
      const searchResult = await this.adminSearchService.prepareClientForTreeSelectionWithOwner(selectedClient);
      if (searchResult) {
        const treeNodes = this.hierarchyTree();
        const nodeToSelect = this.treeNodeService.findNodeByKey(treeNodes, selectedClient.id);
        if (nodeToSelect) {
          this.treeEventHandler.setTreeSelection(nodeToSelect);
        } else {
          console.warn("Could not find tree node for client after preparation");
        }
        this.scrollManagement.scrollToClient(selectedClient.id);
      } else {
        console.warn("Could not prepare client for tree selection");
      }
    } catch (error) {
      this.handleError(error, "Error handling selected client change");
    } finally {
      this.isNavigatingToClient = false;
    }
  }
  /**
   * Handles node expansion
   */
  async onNodeExpand(node) {
    this.scrollManagement.temporarilySuppressScrollToClient(0);
    this.treeEventHandler.setEventSource(EventSource.USER);
    try {
      await this.treeEventHandler.onNodeExpand(node);
      this.scrollToExpandedNode(node);
    } catch (error) {
      this.handleError(error, "Error expanding node");
    }
  }
  async onGroupExpand(groupNode) {
    this.scrollManagement.temporarilySuppressScrollToClient(0);
    this.treeEventHandler.setEventSource(EventSource.USER);
    try {
      await this.treeEventHandler.onNodeExpand(groupNode);
      this.scrollToExpandedNode(groupNode);
    } catch (error) {
      this.handleError(error, "Error expanding group");
    }
  }
  onNodeCollapse(node) {
    this.scrollManagement.temporarilySuppressScrollToClient(0);
    this.treeEventHandler.setEventSource(EventSource.USER);
    this.treeEventHandler.onNodeCollapse(node);
  }
  async onNodeSelect(node) {
    this.scrollManagement.temporarilySuppressScrollToClient(0);
    this.treeEventHandler.setEventSource(EventSource.USER);
    return this.treeEventHandler.onNodeSelect(node);
  }
  onNodeUnselect(node) {
    this.treeEventHandler.onNodeUnselect(node);
  }
  findSelectedNode() {
    const selectedNode = this.selectedTreeNode();
    if (selectedNode)
      return selectedNode;
    const { keyToSelect, typeToSelect } = this.getEntityInfoFromRoute();
    if (!isDefined(keyToSelect)) {
      const selectedClient = this.adminState.selectedClient();
      if (isNullOrUndefined(selectedClient))
        return null;
      const nodes2 = this.hierarchyTree();
      return this.treeNodeService.findNodeByKey(nodes2, selectedClient.id);
    }
    const nodes = this.hierarchyTree();
    if (isDefined(typeToSelect)) {
      return this.treeNodeService.findNodeByKey(nodes, keyToSelect, typeToSelect);
    } else {
      return this.treeNodeService.findNodeByKey(nodes, keyToSelect);
    }
  }
  // Capture the activated router outlet component
  onRouterOutletActivate(component) {
    this.activeGridComponent = component;
  }
  /**
   * Scrolls to center an expanded node to show its children
   */
  scrollToExpandedNode(node) {
    if (isDefined(node.key)) {
      this.scrollManagement.scrollToCenterNode(node.key);
    }
  }
  /**
   * Gets entity information from route parameters
   */
  getEntityInfoFromRoute() {
    return this.routeHandler.getEntityInfoFromRoute();
  }
  /**
   * Selects a node in the tree based on the current route parameters.
   * This method updates the `selectedTreeNode` signal and loads missing data if needed.
   */
  async selectNodeFromRouteOrState() {
    const { keyToSelect, typeToSelect } = this.getEntityInfoFromRoute();
    const treeNodes = this.hierarchyTree();
    if (isNullOrUndefined(keyToSelect)) {
      this.treeEventHandler.setTreeSelection(null);
      return;
    }
    const nodeToSelect = this.findNodeToSelect(treeNodes, keyToSelect, typeToSelect);
    if (nodeToSelect) {
      this.treeEventHandler.setTreeSelection(nodeToSelect);
      this.updateStateBasedOnNodeType(nodeToSelect);
      this.scrollManagement.handleNodeSelectionScroll(keyToSelect);
      if (typeToSelect === ADMIN_TREE.NODE_TYPES.CLIENT) {
        await this.updateClientStateFromRoute(keyToSelect);
      }
    } else if (isDefined(keyToSelect) && isDefined(typeToSelect)) {
      await this.handleRouteBasedSelection(keyToSelect, typeToSelect);
    }
  }
  /**
   * Finds the correct node to select based on key and optional type
   */
  findNodeToSelect(treeNodes, key, type) {
    if (isDefined(type)) {
      const nodeByType = this.treeNodeService.findNodeByKey(treeNodes, key, type);
      if (nodeByType)
        return nodeByType;
    }
    return this.treeNodeService.findNodeByKey(treeNodes, key);
  }
  /**
   * Handles search query changes
   */
  onSearchQueryChange(query) {
    this.treeEventHandler.onSearchQueryChange(query);
  }
  /**
   * Handles search result selection
   */
  async onSearchResultSelect(result) {
    this.scrollManagement.suppressScrollToClient.set(false);
    return this.treeEventHandler.onSearchResultSelect(result);
  }
  /**
   * Handles search clear
   */
  onSearchClear() {
    this.scrollManagement.suppressScrollToClient.set(false);
    this.treeEventHandler.onSearchClear();
  }
  /**
   * Handles selection based on route parameters for any node type
   */
  async handleRouteBasedSelection(keyToSelect, typeToSelect) {
    const currentSelected = this.selectedTreeNode();
    const treeNodes = this.hierarchyTree();
    const nodeExistsInTree = this.findNodeToSelect(treeNodes, keyToSelect, typeToSelect);
    if (nodeExistsInTree && currentSelected?.key === keyToSelect && currentSelected?.type === typeToSelect) {
      return;
    }
    const searchResult = await this.prepareTreeDataForNodeType(keyToSelect, typeToSelect);
    if (isDefined(searchResult)) {
      await this.updateTreeSelectionFromRoute(keyToSelect, typeToSelect);
      if (!this.treeCollapsed() && typeToSelect === ADMIN_TREE.NODE_TYPES.CLIENT) {
        setTimeout(() => {
          this.scrollManagement.scrollToClient(keyToSelect);
        }, 300);
      }
    } else {
      console.warn("[Route Handler] prepareTreeDataForNodeType returned null, cannot proceed");
    }
  }
  /**
   * Prepares tree data based on node type
   */
  async prepareTreeDataForNodeType(keyToSelect, typeToSelect) {
    if (typeToSelect === ADMIN_TREE.NODE_TYPES.CLIENT) {
      return this.prepareClientTreeData(keyToSelect);
    }
    return this.prepareNonClientTreeData(typeToSelect);
  }
  /**
   * Prepares tree data for client nodes with distributor context
   */
  async prepareClientTreeData(clientId) {
    let currentClient = this.adminState.selectedClient();
    if (isDefined(clientId) && currentClient?.id !== clientId) {
      try {
        const fetchedClient = await this.adminTreeService.fetchClient(clientId);
        if (fetchedClient) {
          this.setClientAndSyncServices(fetchedClient);
          await this.setVendorContextForClient(fetchedClient);
          currentClient = fetchedClient;
        } else {
          return null;
        }
      } catch (error) {
        console.error(`[prepareClientTreeData] Failed to fetch client ${clientId}:`, error);
        return null;
      }
    }
    if (isNullOrUndefined(currentClient)) {
      return null;
    }
    if (isDefined(clientId) && currentClient.id !== clientId) {
      console.error(`[prepareClientTreeData] Client mismatch: requested ${clientId}, but currentClient is ${currentClient.id}`);
      return null;
    }
    let result = await this.adminSearchService.prepareClientForTreeSelectionWithOwner(currentClient);
    if (!isDefined(result) && isDefined(currentClient.id)) {
      result = await this.adminSearchService.prepareClientForTreeSelection(currentClient.id);
    }
    return result;
  }
  /**
   * Prepares tree data for non-client nodes
   */
  async prepareNonClientTreeData(typeToSelect) {
    try {
      const treeNodes = this.hierarchyTree();
      if (treeNodes.length > 0) {
        return { success: true };
      }
      console.warn("Tree not loaded for non-client node type:", typeToSelect);
      return null;
    } catch (error) {
      console.warn("Error checking tree state for node type:", typeToSelect, error);
      return null;
    }
  }
  /**
   * Updates tree selection based on route parameters with distributor context
   */
  async updateTreeSelectionFromRoute(keyToSelect, typeToSelect) {
    await this.searchNavigation.scheduleSelection(keyToSelect, typeToSelect ?? "client");
    if (typeToSelect === ADMIN_TREE.NODE_TYPES.CLIENT) {
      await this.updateClientStateFromRoute(keyToSelect);
    }
  }
  /**
   * Ensures adminState has full client data for the navigated-to client.
   * Gates on clientDataService (the global/login selection) to detect when we need
   * to fetch: if the globally-selected client already matches we can reuse its data;
   * otherwise we fetch to populate adminState without touching clientDataService.
   */
  async updateClientStateFromRoute(keyToSelect) {
    const currentFullClient = this.clientDataService.getSelectedClient();
    if (currentFullClient?.id === keyToSelect) {
      this.adminState.setSelectedClient(currentFullClient);
    } else {
      await this.loadAndSetClientFromRoute(keyToSelect);
    }
  }
  /**
   * Loads client data from API and updates state
   */
  async loadAndSetClientFromRoute(keyToSelect) {
    try {
      const client = await this.adminTreeService.fetchClient(keyToSelect);
      if (isDefined(client)) {
        this.setClientAndSyncServices(client);
        await this.setVendorContextForClient(client);
      }
    } catch (error) {
      console.warn("Error loading client data for selection:", error);
    }
  }
  /**
   * Sets client in admin state only.
   * clientDataService is the global selection owned by login/sidebar — tree navigation
   * must not mutate it, so we intentionally update adminState exclusively here.
   */
  setClientAndSyncServices(client) {
    this.adminState.setSelectedClient(client);
  }
  /**
   * Sets vendor distributor context for client if available
   */
  async setVendorContextForClient(client) {
    if (!client.owner?.id)
      return;
    const contextId = this.adminState.getActiveContextId();
    if (!isDefined(contextId))
      return;
    const vendor = this.adminState.getVendorById(client.owner.id, contextId);
    if (isDefined(vendor)) {
      this.adminState.setClientVendor(vendor);
    }
  }
  /**
   * Updates application state based on the selected node type
   */
  updateStateBasedOnNodeType(node) {
    if (!isDefined(node.type) || !isDefined(node.key))
      return;
    if (node.type === ADMIN_TREE.NODE_TYPES.CLIENT) {
      const clientData = node.data;
      if (isDefined(clientData)) {
        const fullClient = this.clientDataService.getSelectedClient();
        if (fullClient?.id === clientData.id) {
          this.adminState.setSelectedClient(fullClient);
        } else {
          this.adminState.setSelectedClient(clientData);
        }
      }
    }
  }
  static \u0275fac = function AdminContainerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AdminContainerComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _AdminContainerComponent, selectors: [["app-admin-container"]], features: [\u0275\u0275ProvidersFeature([
    GridConfigurationFactory,
    ADMIN_GRID_CONFIGS_PROVIDER,
    TreeTableConfigurationFactory,
    ADMIN_TREE_TABLE_CONFIGS
  ])], decls: 4, vars: 3, consts: [["treeNavigationTemplate", ""], ["mainContentTemplate", ""], [1, "flex", "h-full", "overflow-hidden", "relative", 3, "ngClass"], ["stateKey", "main-layout", 1, "h-full", "w-full", 3, "ngClass", "panels", "panelSizes", "minSizes", "defaultLayout", "responsive"], [1, "h-full", "w-full"], [1, "fixed", "inset-0", "bg-black", "bg-opacity-30", "z-40"], ["styleClass", "w-8 h-8", "strokeWidth", "2", 1, "fixed", "z-50", "top-1/2", "left-1/2", "-translate-x-1/2", "-translate-y-1/2"], [3, "toggleTreePanel", "navigateToSelectedClient", "nodeExpand", "nodeCollapse", "nodeSelect", "nodeNotFoundInData", "searchQueryChange", "searchResultSelect", "searchClear", "treeCollapsed", "hierarchyTree", "isLoading", "searchingForNode", "searchingNodeKey", "selectedTreeNode", "scrollToNodeKey", "scrollToCenterNodeKey", "searchResults", "isSearching"], [3, "routerOutletActivate", "nestedContentScrollable", "detailsPanelClass", "shouldUseDrawer"]], template: function AdminContainerComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "div", 2);
      \u0275\u0275conditionalCreate(1, AdminContainerComponent_Conditional_1_Template, 2, 0);
      \u0275\u0275conditionalCreate(2, AdminContainerComponent_Conditional_2_Template, 5, 18, "app-adaptive-layout", 3)(3, AdminContainerComponent_Conditional_3_Template, 2, 3, "div", 4);
      \u0275\u0275elementEnd();
    }
    if (rf & 2) {
      \u0275\u0275property("ngClass", ctx.hostClass());
      \u0275\u0275advance();
      \u0275\u0275conditional(ctx.isLoading() ? 1 : -1);
      \u0275\u0275advance();
      \u0275\u0275conditional(ctx.showTreePanel() ? 2 : 3);
    }
  }, dependencies: [
    AdaptiveLayoutComponent,
    CommonModule,
    NgClass,
    ProgressSpinnerModule,
    ProgressSpinner,
    TreeNavigationPanelComponent,
    AdminMainContentComponent,
    ButtonModule,
    TooltipModule
  ], styles: ["\n[_nghost-%COMP%] {\n  display: block;\n  height: 100%;\n}\n[_nghost-%COMP%]   .flex.relative[_ngcontent-%COMP%] {\n  position: relative;\n  overflow: visible;\n}\n[_nghost-%COMP%]     .p-splitter-panel {\n  transition-property: all;\n  transition-duration: 300ms;\n  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);\n}\n[_nghost-%COMP%]     app-adaptive-layout app-adaptive-layout .p-splitter {\n  border-width: 0px;\n}\n[_nghost-%COMP%]     .panel-transition {\n  transition-property: all;\n  transition-duration: 300ms;\n  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);\n}\n[_nghost-%COMP%]     p-splitter {\n  height: 100%;\n}\n[_nghost-%COMP%]   -shadowcsshost     p-splitter.flex.relative {\n  position: relative;\n  overflow: visible;\n}\n[_nghost-%COMP%]     .details-hidden-state .p-splitterpanel:nth-child(3), \n[_nghost-%COMP%]     .details-hidden-state .p-splitter-panel:nth-child(3) {\n  display: none;\n}\n[_nghost-%COMP%]     .details-hidden-state .p-splitter-gutter {\n  display: none;\n}\n[_nghost-%COMP%]     .details-hidden-state .p-splitter-panel:first-of-type {\n  width: 100%;\n  max-width: 100%;\n  flex: 1 1 0%;\n}\n[_nghost-%COMP%]     .search-highlighted {\n  background-color: rgba(255, 235, 59, 0.3) !important;\n  border-left: 3px solid var(--primary-color, #3b82f6) !important;\n}\n[_nghost-%COMP%]     .search-highlighted .p-treenode-content {\n  background-color: rgba(255, 235, 59, 0.2) !important;\n  font-weight: 600 !important;\n}\n[_nghost-%COMP%]     .search-highlighted:hover {\n  background-color: rgba(255, 235, 59, 0.4) !important;\n}\n[_nghost-%COMP%]     app-adaptive-layout {\n  height: 100%;\n  overflow: hidden;\n  width: 100%;\n  max-width: 100%;\n  box-sizing: border-box;\n}\n[_nghost-%COMP%]     app-adaptive-layout .p-splitter {\n  height: 100%;\n  overflow: hidden;\n  width: 100% !important;\n  max-width: 100% !important;\n}\n[_nghost-%COMP%]     app-adaptive-layout .p-splitterpanel {\n  overflow: hidden;\n  min-width: 0;\n  max-width: 100%;\n  box-sizing: border-box;\n}\n[_nghost-%COMP%]     app-adaptive-layout .p-splitterpanel > div {\n  height: 100%;\n  min-width: 0;\n  width: 100% !important;\n  max-width: 100% !important;\n  box-sizing: border-box;\n}\n[_nghost-%COMP%]     app-adaptive-layout .p-splitterpanel > div.overflow-auto {\n  overflow-y: auto;\n  overflow-x: hidden !important;\n}\n[_nghost-%COMP%]     app-adaptive-layout .p-splitterpanel > div.overflow-auto > * {\n  max-width: 100%;\n  box-sizing: border-box;\n}\n[_nghost-%COMP%]     app-adaptive-layout .p-splitterpanel:last-child {\n  min-width: 0;\n}\n[_nghost-%COMP%]     app-adaptive-layout .p-splitterpanel:last-child > div {\n  overflow: hidden;\n}\n[_nghost-%COMP%]     app-adaptive-layout p-accordion, \n[_nghost-%COMP%]     app-adaptive-layout .p-accordion {\n  width: 100% !important;\n  max-width: 100% !important;\n  min-width: 0 !important;\n  overflow: hidden !important;\n}\n[_nghost-%COMP%]     app-adaptive-layout .p-accordion-header, \n[_nghost-%COMP%]     app-adaptive-layout .p-accordion-content, \n[_nghost-%COMP%]     app-adaptive-layout .p-accordion-panel {\n  width: 100% !important;\n  max-width: 100% !important;\n  min-width: 0 !important;\n  box-sizing: border-box !important;\n  overflow: hidden !important;\n}\n[_nghost-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%] {\n  height: 100%;\n  overflow: hidden;\n}\n[_nghost-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%]   .p-splitter[_ngcontent-%COMP%] {\n  height: 100%;\n  overflow: hidden;\n}\n[_nghost-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%]   .p-splitter-panel[_ngcontent-%COMP%] {\n  overflow: hidden;\n  min-width: 0;\n}\n[_nghost-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%]   .p-splitter-panel[_ngcontent-%COMP%]    > div[_ngcontent-%COMP%] {\n  overflow: auto;\n  height: 100%;\n  min-width: 0;\n}\n[_nghost-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%]   .p-splitter-panel[_ngcontent-%COMP%]:last-child {\n  min-width: 0;\n}\n[_nghost-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%]   .p-splitter-panel[_ngcontent-%COMP%]:last-child    > div[_ngcontent-%COMP%] {\n  overflow: hidden;\n}\n[_nghost-%COMP%]   .h-full.flex.relative[_ngcontent-%COMP%] {\n  position: relative;\n  overflow: visible;\n}\n@media (max-width: 768px) {\n  [_nghost-%COMP%]   app-adaptive-layout[_ngcontent-%COMP%] {\n    overflow-x: hidden;\n  }\n  [_nghost-%COMP%]   .p-splitter-panel[_ngcontent-%COMP%] {\n    min-width: 0 !important;\n  }\n}\n.has-details[_ngcontent-%COMP%]     .main-layout .p-splitter-gutter {\n  z-index: 10;\n  cursor: col-resize;\n}\n  .load-more-node .p-treenode-content {\n  font-style: italic;\n  color: var(--primary-color);\n  cursor: pointer;\n}\n  .load-more-node .p-treenode-content:hover {\n  background-color: var(--surface-hover);\n}\n  .load-more-node .p-treenode-icon {\n  margin-right: 0.5rem;\n}\n/*# sourceMappingURL=admin-container.component.css.map */"], changeDetection: 0 });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AdminContainerComponent, [{
    type: Component,
    args: [{ selector: "app-admin-container", imports: [
      AdaptiveLayoutComponent,
      CommonModule,
      ProgressSpinnerModule,
      TreeNavigationPanelComponent,
      AdminMainContentComponent,
      ButtonModule,
      TooltipModule
    ], changeDetection: ChangeDetectionStrategy.OnPush, providers: [
      GridConfigurationFactory,
      ADMIN_GRID_CONFIGS_PROVIDER,
      TreeTableConfigurationFactory,
      ADMIN_TREE_TABLE_CONFIGS
    ], template: `<div class="flex h-full overflow-hidden relative" [ngClass]="hostClass()">
  <!-- Backdrop and spinner overlay when loading -->
  @if (isLoading()) {
    <div class="fixed inset-0 bg-black bg-opacity-30 z-40"></div>
    <p-progressSpinner
      class="fixed z-50 top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2"
      styleClass="w-8 h-8"
      strokeWidth="2"
    ></p-progressSpinner>
  }

  <!-- Layout with tree navigation (for users with access above client level) -->
  @if (showTreePanel()) {
    <app-adaptive-layout
      [ngClass]="mainSplitterClass()"
      [panels]="[
        {
          content: treeNavigationTemplate,
          scrollable: false,
          minSize: treeCollapsed() ? '36px' : 15,
        },
        { content: mainContentTemplate, scrollable: true, minSize: 10 },
      ]"
      [panelSizes]="treeCollapsed() ? ['36px', 100] : [25, 75]"
      [minSizes]="[treeCollapsed() ? '36px' : 15, 10]"
      [defaultLayout]="'horizontal'"
      [responsive]="true"
      stateKey="main-layout"
      class="h-full w-full"
    >
      <!-- Tree navigation panel -->
      <ng-template #treeNavigationTemplate>
        <app-tree-navigation-panel
          [treeCollapsed]="treeCollapsed"
          [hierarchyTree]="hierarchyTree"
          [isLoading]="isLoading"
          [searchingForNode]="searchingForNode"
          [searchingNodeKey]="searchingNodeKey"
          [selectedTreeNode]="selectedTreeNode"
          [scrollToNodeKey]="scrollToNodeKey"
          [scrollToCenterNodeKey]="scrollToCenterNodeKey"
          [searchResults]="searchResults"
          [isSearching]="isSearching"
          (toggleTreePanel)="toggleTreePanel()"
          (navigateToSelectedClient)="navigateToSelectedClient()"
          (nodeExpand)="onNodeExpand($event)"
          (nodeCollapse)="onNodeCollapse($event)"
          (nodeSelect)="onNodeSelect($event)"
          (nodeNotFoundInData)="onNodeNotFoundInData($event)"
          (searchQueryChange)="onSearchQueryChange($event)"
          (searchResultSelect)="onSearchResultSelect($event)"
          (searchClear)="onSearchClear()"
        ></app-tree-navigation-panel>
      </ng-template>

      <!-- Main content panel -->
      <ng-template #mainContentTemplate>
        <app-admin-main-content
          [nestedContentScrollable]="nestedContentScrollable"
          [detailsPanelClass]="detailsPanelClass"
          [shouldUseDrawer]="shouldUseDrawerForDetails"
          (routerOutletActivate)="onRouterOutletActivate($event)"
        ></app-admin-main-content>
      </ng-template>
    </app-adaptive-layout>
  } @else {
    <!-- Simple layout for client-level users (no tree navigation) -->
    <div class="h-full w-full">
      <app-admin-main-content
        [nestedContentScrollable]="nestedContentScrollable"
        [detailsPanelClass]="detailsPanelClass"
        [shouldUseDrawer]="shouldUseDrawerForDetails"
        (routerOutletActivate)="onRouterOutletActivate($event)"
      ></app-admin-main-content>
    </div>
  }
</div>
`, styles: ["/* src/app/features/administration/components/admin-container/admin-container.component.scss */\n:host {\n  display: block;\n  height: 100%;\n}\n:host :host.flex.relative {\n  position: relative;\n  overflow: visible;\n}\n:host ::ng-deep .p-splitter-panel {\n  transition-property: all;\n  transition-duration: 300ms;\n  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);\n}\n:host ::ng-deep app-adaptive-layout app-adaptive-layout .p-splitter {\n  border-width: 0px;\n}\n:host ::ng-deep .panel-transition {\n  transition-property: all;\n  transition-duration: 300ms;\n  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);\n}\n:host ::ng-deep p-splitter {\n  height: 100%;\n}\n:host :host ::ng-deep p-splitter.flex.relative {\n  position: relative;\n  overflow: visible;\n}\n:host ::ng-deep .details-hidden-state .p-splitterpanel:nth-child(3),\n:host ::ng-deep .details-hidden-state .p-splitter-panel:nth-child(3) {\n  display: none;\n}\n:host ::ng-deep .details-hidden-state .p-splitter-gutter {\n  display: none;\n}\n:host ::ng-deep .details-hidden-state .p-splitter-panel:first-of-type {\n  width: 100%;\n  max-width: 100%;\n  flex: 1 1 0%;\n}\n:host ::ng-deep .search-highlighted {\n  background-color: rgba(255, 235, 59, 0.3) !important;\n  border-left: 3px solid var(--primary-color, #3b82f6) !important;\n}\n:host ::ng-deep .search-highlighted .p-treenode-content {\n  background-color: rgba(255, 235, 59, 0.2) !important;\n  font-weight: 600 !important;\n}\n:host ::ng-deep .search-highlighted:hover {\n  background-color: rgba(255, 235, 59, 0.4) !important;\n}\n:host ::ng-deep app-adaptive-layout {\n  height: 100%;\n  overflow: hidden;\n  width: 100%;\n  max-width: 100%;\n  box-sizing: border-box;\n}\n:host ::ng-deep app-adaptive-layout .p-splitter {\n  height: 100%;\n  overflow: hidden;\n  width: 100% !important;\n  max-width: 100% !important;\n}\n:host ::ng-deep app-adaptive-layout .p-splitterpanel {\n  overflow: hidden;\n  min-width: 0;\n  max-width: 100%;\n  box-sizing: border-box;\n}\n:host ::ng-deep app-adaptive-layout .p-splitterpanel > div {\n  height: 100%;\n  min-width: 0;\n  width: 100% !important;\n  max-width: 100% !important;\n  box-sizing: border-box;\n}\n:host ::ng-deep app-adaptive-layout .p-splitterpanel > div.overflow-auto {\n  overflow-y: auto;\n  overflow-x: hidden !important;\n}\n:host ::ng-deep app-adaptive-layout .p-splitterpanel > div.overflow-auto > * {\n  max-width: 100%;\n  box-sizing: border-box;\n}\n:host ::ng-deep app-adaptive-layout .p-splitterpanel:last-child {\n  min-width: 0;\n}\n:host ::ng-deep app-adaptive-layout .p-splitterpanel:last-child > div {\n  overflow: hidden;\n}\n:host ::ng-deep app-adaptive-layout p-accordion,\n:host ::ng-deep app-adaptive-layout .p-accordion {\n  width: 100% !important;\n  max-width: 100% !important;\n  min-width: 0 !important;\n  overflow: hidden !important;\n}\n:host ::ng-deep app-adaptive-layout .p-accordion-header,\n:host ::ng-deep app-adaptive-layout .p-accordion-content,\n:host ::ng-deep app-adaptive-layout .p-accordion-panel {\n  width: 100% !important;\n  max-width: 100% !important;\n  min-width: 0 !important;\n  box-sizing: border-box !important;\n  overflow: hidden !important;\n}\n:host app-adaptive-layout app-adaptive-layout {\n  height: 100%;\n  overflow: hidden;\n}\n:host app-adaptive-layout app-adaptive-layout .p-splitter {\n  height: 100%;\n  overflow: hidden;\n}\n:host app-adaptive-layout app-adaptive-layout .p-splitter-panel {\n  overflow: hidden;\n  min-width: 0;\n}\n:host app-adaptive-layout app-adaptive-layout .p-splitter-panel > div {\n  overflow: auto;\n  height: 100%;\n  min-width: 0;\n}\n:host app-adaptive-layout app-adaptive-layout .p-splitter-panel:last-child {\n  min-width: 0;\n}\n:host app-adaptive-layout app-adaptive-layout .p-splitter-panel:last-child > div {\n  overflow: hidden;\n}\n:host .h-full.flex.relative {\n  position: relative;\n  overflow: visible;\n}\n@media (max-width: 768px) {\n  :host app-adaptive-layout {\n    overflow-x: hidden;\n  }\n  :host .p-splitter-panel {\n    min-width: 0 !important;\n  }\n}\n.has-details ::ng-deep .main-layout .p-splitter-gutter {\n  z-index: 10;\n  cursor: col-resize;\n}\n::ng-deep .load-more-node .p-treenode-content {\n  font-style: italic;\n  color: var(--primary-color);\n  cursor: pointer;\n}\n::ng-deep .load-more-node .p-treenode-content:hover {\n  background-color: var(--surface-hover);\n}\n::ng-deep .load-more-node .p-treenode-icon {\n  margin-right: 0.5rem;\n}\n/*# sourceMappingURL=admin-container.component.css.map */\n"] }]
  }], () => [], null);
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(AdminContainerComponent, { className: "AdminContainerComponent", filePath: "src/app/features/administration/components/admin-container/admin-container.component.ts", lineNumber: 92 });
})();

// src/app/features/administration/resolvers/client.resolver.ts
var clientResolver = (route, _state) => {
  const clientDataService = inject(ClientDataService);
  const messageService = inject(MessageService);
  const clientId = route.paramMap.get("clientId");
  if (!isDefined(clientId) || !isString(clientId)) {
    messageService.add({
      severity: "error",
      summary: "Error",
      detail: "Invalid client ID"
    });
    return of(null);
  }
  return clientDataService.fetchClientWithCache(clientId).pipe(map((response) => {
    if (!isDefined(response)) {
      messageService.add({
        severity: "error",
        summary: "Error",
        detail: "No client data found"
      });
      return null;
    }
    return response;
  }), catchError(() => {
    messageService.add({
      severity: "error",
      summary: "Error",
      detail: "Failed to load client data"
    });
    return of(null);
  }));
};

// src/app/features/administration/resolvers/distributor.resolver.ts
var distributorResolver = (route, _state) => {
  const accountsService = inject(AccountsService);
  const messageService = inject(MessageService);
  const distributorId = route.paramMap.get("distributorId");
  if (!isDefined(distributorId) || !isString(distributorId)) {
    messageService.add({
      severity: "error",
      summary: "Error",
      detail: "Invalid distributor ID"
    });
    return of(null);
  }
  return accountsService.getDistributor(distributorId).pipe(map((response) => {
    if (!isDefined(response)) {
      messageService.add({
        severity: "error",
        summary: "Error",
        detail: "No distributor data found"
      });
      return null;
    }
    return response;
  }), catchError(() => {
    messageService.add({
      severity: "error",
      summary: "Error",
      detail: "Failed to load distributor data"
    });
    return of(null);
  }));
};

// src/app/features/administration/resolvers/vendor.resolver.ts
var vendorResolver = (route, _state) => {
  const accountsService = inject(AccountsService);
  const messageService = inject(MessageService);
  const vendorId = route.paramMap.get("vendorId");
  if (isNullOrUndefined(vendorId)) {
    messageService.add({
      severity: "error",
      summary: "Error",
      detail: "Vendor ID not found"
    });
    return of(null);
  }
  return accountsService.getVendor(vendorId).pipe(catchError(() => {
    messageService.add({
      severity: "error",
      summary: "Error",
      detail: "Failed to load vendor data"
    });
    return of(null);
  }));
};

// src/app/core/guards/owner-tier.guard.ts
var ownerTierRank = {
  client: 0,
  vendor: 1,
  distributor: 2,
  system: 3
};
var ownerTierGuard = (route) => {
  const authDataService = inject(AuthDataService);
  const router = inject(Router);
  const requiredOwnerType = route.data["requiredOwnerType"];
  if (!requiredOwnerType) {
    return true;
  }
  const currentOwnerType = authDataService.getUserOwnerType();
  if (!currentOwnerType) {
    router.navigate(["/unauthorized"], {
      queryParams: { reason: "owner_type_missing" }
    }).catch(() => {
    });
    return false;
  }
  const allowed = ownerTierRank[currentOwnerType] >= ownerTierRank[requiredOwnerType];
  if (!allowed) {
    router.navigate(["/unauthorized"], {
      queryParams: { reason: "owner_type_restricted" }
    }).catch(() => {
    });
    return false;
  }
  return true;
};

// src/app/features/administration/administration.routes.ts
var ADMINISTRATION_ROUTES = [
  {
    path: "",
    component: AdminContainerComponent,
    children: [
      {
        path: "super-admin/:superAdminId",
        canActivate: [ownerTierGuard],
        data: { requiredOwnerType: "system" },
        loadChildren: () => import("./chunk-NWUZFCO5.js").then((m) => m.SUPER_ADMIN_ROUTES)
      },
      {
        path: "vendor/:vendorId",
        canActivate: [ownerTierGuard],
        data: { requiredOwnerType: "vendor" },
        resolve: {
          vendor: vendorResolver
        },
        loadChildren: () => import("./chunk-XN6HEUGJ.js").then((m) => m.ADMIN_VENDORS_ROUTES)
      },
      {
        path: "distributor/:distributorId",
        canActivate: [ownerTierGuard],
        data: { requiredOwnerType: "distributor" },
        resolve: {
          distributor: distributorResolver
        },
        loadChildren: () => import("./chunk-AZBGQ4L5.js").then((m) => m.ADMIN_DISTRIBUTOR_ROUTES)
      },
      {
        path: "client/:clientId",
        canActivate: [ownerTierGuard],
        data: { requiredOwnerType: "client" },
        resolve: {
          client: clientResolver
        },
        loadChildren: () => import("./chunk-7IH5KIEQ.js").then((m) => m.ADMIN_CLIENT_ROUTES)
      }
    ]
  }
];
export {
  ADMINISTRATION_ROUTES
};
//# sourceMappingURL=chunk-34CK45ZE.js.map
